# קו הבסיס של שער התנועה  ⟦C-0372 · 2026-08-30⟧

🔴 הוספת שורה לקובץ הזה היא פעולה של PM או של רוי, ⛔ לעולם לא של DEV.

⛔ **שער שהסוכן הנבדק יכול להרחיב בעצמו ⛔ אינו שער** (‏C-0366, כלל ⓐ — «השער ⛔ אינו
נפתח מבפנים»). ‏DEV **מוריד** שורה מכאן כשהממצא שלה נסגר, ו⛔ לעולם ⛔ אינו מוסיף אחת.
האכיפה: `scripts/check-motion.mjs` ⛔ מסרב לרוץ בלי המשפט שלמעלה, מילה במילה;
‏`scripts/motion-gate.test.ts` מאמת שכל שורה מצטטת ממצא **קיים** ב-`plan/60-findings.md`
ומשימה **קיימת** ב-`plan/50-tasks.md`, ושמספר השורות ⛔ אינו עולה על תקרה **נעוצה בטסט**.
⇒ הרחבת קו הבסיס דורשת **שלוש** עריכות נפרדות, וכל אחת מהן נראית בדיף ש-QA קורא.

## שני הכללים, ו⛔ לא כלל שלישי

* **ⓐ** בלוק `@keyframes` או הצהרת `transition:` הנוגעים במאפיין שאינו `transform` ואינו
  `opacity` ⇒ הדפדפן **מפריס או מצייר בכל פריים**, ⛔ ולא מריץ על הקומפוזיטור.
* **ⓑ** רוחב אחוזי מוטבע — ``style={{ width: `${x}%` }}`` — בתוך רכיב שמריץ לולאת
  `requestAnimationFrame` ⇒ פריסה **וגם** ציור בכל פריים.

## מה נמדד — הרצה חיה, C-0372, 2026-08-30T23:0xZ

**6 הפרות.** ⚠️ **⛔ לא 5, וההפרש מדוד ⛔ ולא משוער:**
נמצאו **שתי** הפרות ⓐ ⛔ שלא היו בציפייה — `arena-impact-a` ו-`arena-impact-b` ב-
`app/arcade/arcade-tokens.css` מנפישים `color`; ו-`components/StoryScreen.tsx:219`
(שהיה בציפייה) ⛔ **אינו** הפרה לפי כלל ⓑ כפי שנכתב: הקובץ ⛔ אינו מכיל
`requestAnimationFrame` ולו פעם אחת, ו-`pct` שם הוא `Math.round((index/total)*100)` —
**מדד סיפור N מתוך M**, שמשתנה בניווט ⛔ ולא בפריים. הרחבת כלל ⓑ אליו היא **הרחבה של
הכלל**, ⛔ ולכן ⛔ לא נעשתה כאן.

## השורות

`- <קובץ> · <כלל> · <מפתח> · <ממצא> · <משימה>`
⚠️ **המפתח ⛔ אינו מספר שורה.** מספר שורה זז עם כל עריכה מעליו, וקו בסיס שנעוץ במספרי
שורות מרקיב בתוך שבוע. המפתח הוא שם ה-`@keyframes` או **הביטוי** שבתוך הרוחב.

- app/arcade/arcade-tokens.css · A · arena-impact-a · F-172 · T-234
- app/arcade/arcade-tokens.css · A · arena-impact-b · F-172 · T-234
