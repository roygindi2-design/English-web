# קו הבסיס של שער רצפת הטקסט  ⟦T-236 · 2026-09-02⟧

🔴 הוספת שורה לקובץ הזה היא פעולה של PM או של רוי, ⛔ לעולם לא של DEV.

⛔ **שער שהסוכן הנבדק יכול להרחיב בעצמו ⛔ אינו שער** (‏אותו כלל בדיוק כמו
`scripts/motion-baseline.md`, C-0366 · כלל ⓐ). ‏DEV **מוריד** שורה מכאן כשהממצא שלה
נסגר, ו⛔ לעולם ⛔ אינו מוסיף אחת. האכיפה: `scripts/check-text-floor.mjs` ⛔ מסרב
לרוץ בלי המשפט שלמעלה, מילה במילה; `scripts/text-floor-gate.test.ts` מאמת שכל שורה
מצטטת ממצא **קיים** ב-`plan/60-findings.md` ומשימה **קיימת** ב-`plan/50-tasks.md`.

## ⛔ למה קובץ קפוא, ⛔ ולא חלון `SOFT_UNTIL`

`D-177` (רוי, 02/09/2026, `F-180`) אסר לחלוטין את מנגנון ה-`SOFT_UNTIL`/soft-window
אחרי שתאריך התפוגה שלו האדים את `npm run verify` לכל סוכן, בלי שקוד אחד נשבר —
«אין מאריכים תאריך, ⛔ ואין דגל תאריך חדש במקומו». ⇒ קו בסיס קפוא, בדיוק כמו
`scripts/motion-baseline.md`: הוא ⛔ אינו פג לעולם, הוא רק מתכווץ — שורה אחת בכל פעם,
כשהממצא שלה נסגר.

## הכלל — `35-design-constitution.md § א9`

⛔ **אין טקסט מתחת ל-12px בשום מסך, בשום מצב, כולל בזירה.**

* **ⓐ** `text-[<n>px]` (מחלקת Tailwind בערך שרירותי) עם `n < 12` בכל `app/**` או
  `components/**`.
* **ⓑ** כל `font-size` מתחת ל-`12px`/`0.75rem` ב-`app/globals.css` וב-
  `app/arcade/arcade-tokens.css` — שני הקבצים ש-§ א9 נוקבת בשמם.

## מה נמדד — הרצה חיה, C-0394, 2026-09-02

**3 הפרות, ⛔ לא 1.** קו הבסיס שהחוקה ציטטה (‏F-162, 28/08) ציפה ל-`text-[11px]` ×1
בלבד; סריקה חיה של `app/**` ו-`components/**` בטיק הזה מצאה **שתיים נוספות שלא היו
בציפייה** — אותו דפוס בדיוק כמו `scripts/motion-baseline.md` (‏F-172, שתי הפרות בלתי
צפויות). שלושתן `text-[11px]`/`text-[11.5px]` בתוויות משניות בזירה ובטבעת העולם —
⛔ אין אף אחת מהן ב-`app/globals.css` או ב-`app/arcade/arcade-tokens.css` (כלל ⓑ:
**אפס הפרות**). ⛔ הטיק הזה **לא נגע** בקוד המפר — הקפאה בלבד, בדיוק כמו ⓒ ב-F-172.

## השורות

`- <קובץ> · <כלל> · <מפתח> · <ממצא> · <משימה>`
⚠️ **המפתח ⛔ אינו מספר שורה.** מספר שורה זז עם כל עריכה מעליו. המפתח הוא הביטוי
`text-[<n>px]` או שם ה-`font-size` המדויק.

- components/WorldRing.tsx · A · text-[11.5px] · F-162 · T-236
