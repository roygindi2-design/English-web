# מפת הלופ — המסמך החי היחיד של שכבת ההרצה

> **⛔ זה ⛔ אינו מסמך תוכנית ו⛔ אינו ארכיון.** הוא מתאר את **מה שרץ בפועל**:
> אילו משימות מתוזמנות קיימות, מתי, על איזה מודל, לאן הן דוחפות, ומה מהן ⛔ אינו
> ניתן לשינוי מתוך הריפו. **מי שמשנה תזמון · מודל · הדלקה או כיבוי — כותב כאן
> שורה באותה עריכה** (`RULES § 0.29 ה׳` · `docs/agents/roster.json` `_why`).
>
> 🔴 **⛔ אין קובץ מקביל.** עד 09/09 חמישה מקומות חיים ציטטו את הנתיב הזה ו⛔ הקובץ
> ⛔ לא היה קיים (`F-199`) ⇒ כל סוכן שניסה לקיים את ההוראה נתקע. **הפתרון היה ליצור
> אותו, ⛔ לא לפתוח מסמך חדש ליד.** ⇒ ⛔ **אל תיצור מסמך מצב שני.**

---

## 1 · שש המשימות המתוזמנות — נמדד 2026-09-14T23:50Z ב-`list_triggers`

| סוכן | `cron` UTC | ליום | מודל | ענף התוצאה של הפלטפורמה | connectors | דלוק? |
|---|---|---|---|---|---|---|
| **DEV** | `5 0,1,2,3,5,6,7,8,9,11,12,13,14,15,17,18,19,20,21,23 * * *` | 20 | `claude-opus-5` | `claude/exciting-cray` | Supabase · Figma · Adobe · Mobbin | ⛔ כבוי |
| **PM** | `35 0,2,5,8,11,14,17,19,20 * * *` | 9 | `claude-opus-5` | `claude/focused-ride` | Supabase · Figma · Descript · Mobbin · Adobe | ⛔ כבוי |
| **QA — מסלול מלא** | `35 4,10,16,22 * * *` | 4 | `claude-sonnet-5` | `claude/dazzling-shannon` | Supabase · Figma · **Kernel** | ⛔ כבוי |
| **QA — מסלול שער** | `35 1,6,12,18,23 * * *` | 5 | `claude-haiku-4-5-20251001` | `claude/confident-noether` | ⛔ אין | ⛔ כבוי |
| **CONTENT** | `35 3,9,15,21 * * *` | 4 | `claude-sonnet-5` | `claude/elegant-allen` | Linear · Notion | ⛔ כבוי |
| **PROMOTER** | `35 7,13 * * *` | 2 | `claude-sonnet-5` | `claude/kind-meitner` | Supabase · Netlify · Figma · **Kernel** | ⛔ כבוי |

### 🕐 למה דווקא הרשת הזאת — ⛔ נמדד, ⛔ לא נבחר לנוחות  ⟦נבנה מחדש 14/09⟧

🔬 **המדד הוא זמן החזקת נעילה, ⛔ ולא אורך טיק** — כי `LOCK_HELD_BY` הוא מה שבאמת חוסם
סוכן אחר. חושב מיומן הקומיטים, זוג «lock» ⇄ «release the lock» לכל סוכן, שלושה ימים:

```
DEV       n=27   חציון 24ד     CONTENT   n= 9   חציון 32ד  (זנב ל-53)
PM        n=13   חציון 20ד     PROMOTER  n= 4   חציון 22ד
QA        n= 8   חציון 16ד
```

⇒ DEV יורה ב-**:05** ומחזיק את הנעילה בערך **:09–:33**. ⇒ **כל סוכן שאינו DEV יורה
ב-:35**, כל אחד בשעות משלו. ⛔ ארבע השעות ש-DEV מדלג עליהן — **04 · 10 · 16 · 22** —
הן אלה שאחרי טיק CONTENT, שהחזקתו של 32ד גולשת אל תוך השעה הבאה.

🔴 **ומה שהרשת הזאת מתקנת, כי זו הייתה תקלה של סשן התפעול ⛔ ולא של הסוכנים:**
הגרסה שנכתבה שעה קודם לכן העלתה את DEV לשעתי ב-:05 ו**השאירה** את QA-שער ו-PROMOTER
על :12. שניהם נחתו **בתוך** נעילת DEV ב**כל** ירייה — ⛔ לא לפעמים, **100%** — ו-Smart
Wait של תשע דקות ⛔ אינו מכסה חמש-עשרה דקות שנותרו. PROMOTER הוא **היחיד** שדוחף
ל-`main`, ⇒ הייצור כולו היה קופא בשקט. ⇒ ⛔ **⛔ אין ברשת הנוכחית ולו התנגשות מתוכננת
אחת**; ה-Smart Wait (שלושה סבבי `sleep 180`) קיים לרעש, ⛔ ולא לחפיפה.

📊 **הטבלה המלאה, 24 שעות:**
```
h  :05        :35        │ h  :05        :35
00 DEV        PM         │ 12 DEV        QA-שער
01 DEV        QA-שער     │ 13 DEV        PROMOTER
02 DEV        PM         │ 14 DEV        PM
03 DEV        CONTENT    │ 15 DEV        CONTENT
04 —          QA-מלא     │ 16 —          QA-מלא
05 DEV        PM         │ 17 DEV        PM
06 DEV        QA-שער     │ 18 DEV        QA-שער
07 DEV        PROMOTER   │ 19 DEV        PM
08 DEV        PM         │ 20 DEV        PM
09 DEV        CONTENT    │ 21 DEV        CONTENT
10 —          QA-מלא     │ 22 —          QA-מלא
11 DEV        PM         │ 23 DEV        QA-שער
```

**⛔ כולן כבויות בכוונה.** נמדד: `enabled=false` בשש, `ended_reason` ריק
ו-`suspension_reason` ריק ⇒ לפי תיעוד הכלי זהו **השהיה בידי המשתמש**, ⛔ ולא
כיבוי אוטומטי ו⛔ לא תקלה. הריצה האחרונה של כל אחת הייתה 08/09.
⛔ **⛔ ולא נגענו ב-`PAUSED_BY_HUMAN`** — הוא הבלם שכל סוכן יוצא עליו, והדלקתו
הייתה **חוסמת את ההפעלה מחדש** במקום לתעד את ההשהיה.

**ושש משימות נוספות, מדור Cowork, עדיין קיימות וכבויות** (נוצרו 05/08–06/09):
DEV · PM · QA-מלא · QA-שער · CONTENT · PROMOTER הישנות, ועוד אחת בשם
«בדוק את טיק PM ועדכן את מפת הלופ». ⛔ **אל תדליק אותן** — הן מצביעות על תצורת
Cowork שכבר ⛔ אינה בתוקף.

---

## 2 · המעבר מ-Cowork ל-Claude Code Remote — 2026-09-07T20:31–20:35Z

שש המשימות החיות **נוצרו כולן בארבע דקות** באותו ערב (`created_at`: DEV 20:31:30 ·
PM 20:32:16 · QA-מלא 20:33:52 · QA-שער 20:34:29 · CONTENT 20:34:55 ·
PROMOTER 20:35:30). ⇒ **כל שדה שהוגדר בהן הוגדר אז**, ⛔ ולא נגזר ממה שהיה קודם.
זה ההקשר לכל «מתי זה השתנה?» בקובץ הזה.

---

## 3 · ⚠️ ההצהרה על מודל ה-DEV ⛔ אינה תואמת את מה שרץ

**נמדד, שלושה מקורות:**

| מקור | מה הוא אומר |
|---|---|
| ‏`session_request.config.model` של משימת DEV | **`claude-opus-5`** |
| הפרומפט שהמשימה שולחת, שורה 3 | «🚦 **מודל: fable 5** (`claude-fable-5-1`) — ⛔ הצהרה, ⛔ לא סוד (`RULES § 0.23ט`). הדהד את השורה הזאת בדוח.» |
| ‏`docs/agents/roster.json`, שורת DEV | `"model": "claude-fable-5-1"` |

**⇒ הפער קיים מאז 2026-09-07T20:31:30Z** — רגע יצירת המשימה. ⛔ אין ראיה שהוא
«השתנה» מאוחר יותר; הוא **נולד** כך. חמשת האחרים תואמים: PM `claude-opus-5` ·
QA-מלא `claude-sonnet-5` · QA-שער `claude-haiku-4-5-20251001` ·
CONTENT `claude-sonnet-5` · PROMOTER `claude-sonnet-5`.

🔴 **⛔ ואיש ⛔ לא יישר את זה.** רוי הורה במפורש (09/09): ⛔ אל תשנה את המודל
במשימה ו⛔ אל תתקן את ההצהרה כדי שתתאים לו. **זו הכרעתו, ⛔ ולא של סוכן.**
⇒ הסוכן שקורא כאן: **דווח על הפער, ⛔ ואל תסגור אותו.**

---

## 4 · הקצב — מי קובע מה, ומה קצב הטיקים

⛔ **שני «קצבים» שונים לגמרי, ובלבול ביניהם הוא המקור לרוב השאלות.**

### ⓐ קצב הטיקים — מי יורה ומתי
נקבע **אך ורק** ב-`cron` של המשימה המתוזמנת (‏§ 1 למעלה), כלומר **בשכבת
הפלטפורמה**. ⛔ **⛔ אין לו ייצוג אכיף בריפו** — הטבלה ב-`RULES § 0.23ט` והשדה
`cronUTC` ב-`roster.json` הם **הצהרות שמשקפות אותו**, ⛔ ולא מקורו.
⇒ **שינוי תזמון = בקשה מפורשת של רוי + שורה כאן, באותה עריכה** (`§ 0.29 ה׳`).

### ⓑ קצב העבודה — איזו פרוסה של המוצר פעילה
נקבע ב-`ACTIVE_WORKSTREAM` שב-`plan/00-control.md`, והוא **של QA בלבד**:

```
QA      קובע ומזיז את ACTIVE_WORKSTREAM, ⛔ ורק כשהיא מוצתה או חסומה מבחוץ.
        «מוצתה» = ⛔ אפס שורות ⬜ בזרימה. ⛔ לא «מרגיש שסיימנו».
PM      פותח פרוסות ⛔ רק בזרימה הפעילה. חריג יחיד: 🔴 שעוצר לומד.
        רשאי להעביר את השדה ל-`general` בלבד — כיוון אחד.
DEV     מסנן את התור אליה, ⛔ ולעולם ⛔ אינו מזיז אותה.
        קורא קדימה לזרימה הבאה ברצף כשבפעילה נשארו פחות מ-3 שורות ⬜.
CONTENT ⛔ אינו נוגע בשדה. ההזמנה ב-`plan/25-content-commissions.md` היא הערוץ.
PROMOTER ⛔ אינו נוגע בשדה.
```

**נוהל המעבר** — שלוש החותמות (‏ⓐ נגיש בהקשות · ⓑ עובד על נתונים אמיתיים ·
ⓒ נשמר), שורת `plan/61-deferred.md` **באותו קומיט ולפני הזזת השדה**, ו-
`npm run build:surfaces` באותו טיק — חי ב-`docs/agents/QA.md` (‏`36 § 13.1` ·
`D-116` · `D-145`), ו-`loop:health` בדיקות 11 · 13 · 14 מודדות אותו.

### ⓒ מה מודד שהקצב באמת קורה
`npm run loop:health` **בדיקה 17** — קוראת `git log` על `origin/work/current` מול
`docs/agents/roster.json`, ומבחינה בין **ארבעה** מצבים:

```
n/m  ⛔ לא נמדד     roster חסר · הרף אינו בקלון   ⇒ ⛔ אינה נספרת בקוד היציאה, בשום תאריך
🟢   עבודה         קומיט שנגע במשהו מעבר לרגיסטרים
🟡   חי ללא עבודה  רק שורות יומן · עם הסמן המוצהר
🟠   חי ללא עבודה  ⛔ בלי הסמן — מדווח, ⛔ ואינו מפיל
🔴   שקט מוחלט     ⛔ אף קומיט בתחילית הסוכן      ⇒ **המצב היחיד שמפיל**
```
⛔ **הסיווג הוא לפי הדיף, ⛔ לא לפי נוסח הקומיט** — טיק שנגע אך ורק
ב-`plan/00-control.md` · `plan/archive/**` · `docs/plan-*.md` הוא טיק ללא עבודה,
בלי קשר למילים שנכתבו בכותרת.
🔴 **וכשכל הסוכנים הדלוקים שותקים בבת אחת** בדיקה 17 אומרת את זה במפורש —
זו **תקלה אחת ברמת הלופ**, ⛔ ולא חמש תקלות נפרדות.

---

## 5 · שכבת הפלטפורמה — ⛔ אינה ניתנת לביצוע מהריפו

⛔ **⛔ אף אחת מהשורות כאן ⛔ אינה קובץ.** כולן ב-claude.ai ⇒ Claude Code ⇒
הסביבה או ה-Routines. תיעוד: `https://code.claude.com/docs/en/claude-code-on-the-web`
⇒ **סוכן שמזהה צורך בשורה כזאת כותב פריט ב-`plan/03-for-roy.md` ו⛔ ממשיך לעבוד.**

| # | מה | מצב נמדד 09/09 |
|---|---|---|
| ① | **מדיניות רשת** — יציאה ל-`*.netlify.app` מהסביבה שבה רצות המשימות | ⛔ חסום. `CONNECT tunnel failed, 403` · הפרוקסי: `connect_rejected · policy denial`. **מדיניות, ⛔ לא תקלה** (15 מדידות) ⇒ בדיקת העשן יכולה לרשום רק «⛔ לא נמדד» |
| ② | **הרשאת הדחיפה של PROMOTER ל-`main`** | `auto_mode_allow` ו-`auto_mode_environment` **ריקים בשש המשימות** ⇒ ההרשאה ⛔ אינה מוגדרת בשכבה הזאת |
| ③ | **`allowed_push_branches`** | ⛔ **אין שדה כזה בתצורה שנמדדה.** מה שקיים הוא `outcomes…branches` — **ענף אחד** לכל משימה (‏§ 1), ⛔ ולא רשימת היתר ⇒ **הכלל נאכף בקלון**, בשער בעלות-הענף שב-`scripts/hooks/pre-push` |

🔬 **ולמה ③ ⛔ אינו חוסם:** שער בעלות-הענף רץ **לפני** מוצא החירום `SKIP_VERIFY`,
ודוחה `refs/heads/main` לכל זהות שאינה `promoter-agent` ו-`refs/heads/dev` לכל
מי שאינו QA. **שתי שכבות של אותו כלל** — הריפו תופס תאונה בקלון, השרת היה תופס
אותה ברשת. ⇒ אחת מהן קיימת ועובדת.

⚠️ **ושישה ענפי `claude/*` נטושים** — ענף התוצאה של כל משימה (‏§ 1) ⛔ אינו
`work/current`, ⇒ כל ריצה שמשאירה קומיט שם משאירה אותו **מחוץ למסלול המיזוג**.
זה מה ש-`loop:health` **בדיקה 18** סופרת (`F-207`), והיעד שלה הוא **0 נטושים**.

---

## 5.5 · 🧊 מה ⛔ לא בוצע בכוונה, ו⛔ למה — כדי ש⛔ לא ייבדק שוב מאפס

### ⛔ חילוץ פרומבל משותף ל-`docs/agents/_preamble.md` — ⛔ **⛔ לא בוצע (09/09)**

**התוכנית הניחה** שפרק ה-git wrapper, בלוק שני-הראנטיימים והסבר `GIT_ASKPASS`
משוכפלים **מילה במילה בחמשת הפרומפטים**, ושהחילוץ שווה **~7KB × 25 טיקים ביום**.

🔬 **נמדד 09/09, ⛔ והנחת היסוד ⛔ לא התקיימה:**
```
זהות מוחלטת בחמשת הקבצים   →   4 שורות · 157 תווים
זהות בארבעה (בלי PROMOTER)  →   6 שורות · 393 תווים
שורות לא-ריקות בפרק לכל קובץ:  DEV 87 · QA 103 · PM 44 · CONTENT 33 · PROMOTER 8
גודל הפרק:                     DEV 8,311 · QA 8,002 · PM 5,039 · CONTENT 3,934 · PROMOTER 495
sha256 של הפרק:                חמישה שונים
```
⇒ ⛔ **⛔ אין «בלוק משותף» לחלץ.** הבעיה האמיתית ⛔ אינה כפילות אלא **סחף**: חמישה
עותקים שהתפצלו זה מזה לאורך זמן. חילוץ 393 תווים ⛔ אינו מצדיק שכתוב של עשרות טענות
`toContain` ב-`scripts/agent-prompts.test.ts`, ותנאי העצירה שהתוכנית עצמה כתבה
(«אם צעד 2 רחב מדי — עצור») מתקיים **לפני** שורת הקוד הראשונה.

⇒ **מה שיהפוך את זה לכדאי, אם וכאשר:** ⓐ **קודם** לאחד את חמשת הווריאנטים לנוסח
קנוני אחד — שינוי שמשנה מה ארבעה סוכנים קוראים, ⇒ הכרעה של רוי ⛔ ולא ניקוי; ⓑ **ואז**
החילוץ הוא מכני וזול. ⛔ **בסדר ההפוך זה שכתוב טענות מהזיכרון**, וזה בדיוק מה שהתוכנית
אסרה.

## 6 · יומן השינויים בשכבת ההרצה

⛔ **שורה אחת לכל שינוי, החדש למעלה. ⛔ בלי פרוזה.**

| מתי (UTC) | מה השתנה | מי הורה | ראיה |
|---|---|---|---|
| 2026-09-09 | **סבב תשתית 2** על `ops/loop-round-2` — 8 שלבים מתוך 9. שלב 9 ⛔ לא בוצע (§ 5.5) | רוי | הפירוט בגוף המסמך |
| 2026-09-09 | **`roster.json` DEV ⇒ `claude-opus-5`** — ⛔ הופך הוראה קודמת של רוי, באישורו המפורש | רוי | `config.model` של המשימה הוא `claude-opus-5` מאז 2026-09-07T20:31:30Z |
| 2026-09-09 | **`SKIP_VERIFY` מפסיק להיות עקיפה מוחלטת** (`T-275` · `F-191`) | רוי | שער הציטוטים + `plan-hygiene`/`measure-plan-tables` רצים גם תחתיו |
| 2026-09-09 | **`npm run verify:fast`** — שבע פקודות, 40 שניות, ⛔ לעולם לא «ירוק» | רוי | נגזר מ-`package.json`, תת-קבוצה ממש של התשע |
| 2026-09-09 | **שער הנעילה** ב-`pre-push` — `LOCK_HELD_BY` מול `git config user.name`. כל הקודם זוכה, ורק דיף רגיסטר עובר תחת נעילה זרה | רוי | `git grep LOCK_HELD_BY -- scripts/` ⇒ **0** לפני · 6 טענות ב-`pre-push-gates.test.ts` אחרי |
| 2026-09-09 | **`docs/agents/CRITIC.md` ⇢ `docs/agents/QA.md`**, ו-`NEXT_AGENT: QA`. תחיליות הקומיט נשארות **שתיהן** | רוי | `§ 0.4` כבר הכריע «`QA` הוא השם» ב-08/09; הקובץ היה המקום האחרון |
| 2026-09-09 | **בדיקה 3.5** ב-`loop:health` — שורה סגורה בטבלת «פתוח» של רוי | רוי | 53 מתוך 71 שורות היו סגורות; 18 באמת פתוחות |
| 2026-09-09 | **`WORKSTREAM_ENDING`** ב-`00-control.md` — PM כותב ב-≤5 שורות ⬜, QA קורא | רוי | ⛔ לא היה שום אות בין השניים |
| 2026-09-09 | **טבלת «מדד המקצועיות»** ב-`plan/archive/control-log.md` — PROMOTER, ⛔ לא חוסמת | רוי | תשעה מספרים, כולם מיוצרים בידי משהו שכבר רץ |
| 2026-09-09 | הקובץ הזה נוצר בנתיב שחמישה מקומות חיים כבר ציטטו (`F-199`) | רוי | `grep -rn LOOP-ARCHITECTURE` ⇒ `RULES:1147` · `roster.json:2` · `40-decisions:2529,3208` · `50-tasks:315` |
| 2026-09-09 09:02–09:05 | שש המשימות כובו בזו אחר זו, בהפרש ~30 שניות | רוי (השהיה מכוונת) | `enabled=false` · `ended_reason` ריק · `suspension_reason` ריק |
| 2026-09-07 20:31–20:35 | שש משימות **חדשות** נוצרו — המעבר ל-CCR. שש הישנות נותרו כבויות | רוי | `created_at` של השש |
| 2026-09-07 | `PROMOTER` מ-`0 23 * * *` ל-`5 0,12 * * *` — פעמיים ביום | רוי | `cron_expression` הנמדד · `roster.json` `_whyPromoterIs30` |
| 2026-09-05 14:49 | `CONTENT` מ-`48 */6` ל-`15 3,7,13,19` — המרווח מ-DEV גדל מ-18 ל-45 דקות | רוי (`D-190`) | `T-256` |
