/**
 * PURE. ⛔ אפס React, DOM, שעון, env ו-I/O.
 *
 * ⟦NEW 15/09 · `C-0623` · `T-361`⟧ המחרוזת של הסט. ⛔ היא יושבת כאן ו⛔ לא ברכיב
 * מאותה סיבה שכל מחרוזת אחרת יושבת ב-`lib/core`: מחרוזת שנראית ללומד — ואפילו
 * ל**קורא מסך בלבד** — היא תוכן, ותוכן ⛔ אינו נכתב בתוך JSX.
 */

/**
 * ⛔ **תיאור הסט לקורא מסך, ⛔ ולא כותרת.** ה-SVG כולו `aria-hidden`, ⇒ המחרוזת הזאת
 * ⛔ אינה נקראת בפועל — היא ה-`<title>` שהופך את ה-SVG לניתן-לזיהוי בכלי פיתוח
 * ובבדיקות, ⛔ בלי להוסיף רעש למי שמנווט במקלדת. ⛔ ואין בה מידע שהלומד צריך:
 * כל מה שמשנה לקרב — השעון, החיים, המאנה, המילה — חי בטקסט אמיתי מחוץ לסט.
 */
export const ARENA_SCENE_HE = 'זירת הקרב — תפאורה';
