# 05 · המחלקות — היעדים הפתוחים, ⛔ ולא היסטוריה

> ⟦נוצר 09/09/2026 · דרישת רוי⟧ ⛔ **הקובץ הזה הוא **נטו**.** הוא מחזיק את מה שפתוח
> **עכשיו**, ⛔ ולא את מה שהיה. ⇒ **יעד שהושג ⛔ אינו עובר לכאן עם ✅ — הוא נמחק.**
> הראיה חיה ב-`git log` ובשורות ה-`T` שנסגרו.
>
> **⛔ מי כותב: PM בלבד.** DEV · QA · CONTENT · PROMOTER — **קוראים**.
> **⛔ תקרה: 4,096 בתים.** `loop:health` בדיקה 19 — נקרא ~15 פעם ביום.
>
> **מה ⛔ אינו כאן, ואיפה כן:**
> ```
> 15 האחרונות שבוצעו   ⇒ docs/plan-open.md § «✅ נסגרו לאחרונה»
> המחלקה הפעילה        ⇒ ACTIVE_WORKSTREAM · המתקרבת ⇒ WORKSTREAM_ENDING (00-control)
> השורות עצמן          ⇒ plan/50-tasks.md · אינדקס ב-docs/plan-open.md
> מחלקה שנחתמה         ⇒ plan/archive/departments-archive.md (npm run archive)
> ```

## ▶️ המחלקה בעבודה

**`general`** ⟨C-0576⟧ — ל-`msgs` ⛔ אין שורה כשירה ל-DEV. המוקד על החוצי-מערכת
עד ש-QA יזיז אותו (`§ 0.23 ז׳`). ‏`PREV_WORKSTREAM: msgs`.
⚠️ **C-0608: ⬜=0 בפתיחת הטיק — הפעם החמישית ב-36 שעות** (`F-252`). ⇒ שתי שורות נפתחו: `T-348`·`T-349`.

## המחלקות והיעדים הפתוחים

| מחלקה | מה הלומד מקבל שם | יעדים פתוחים |
|---|---|---|
| `story` | סיפור ברמתו, הקשה פותחת תרגום, סוף מסכם | ⛔ אין ⬜. חסומות עד שהמוקד יחזור: `T-207`·`T-208`·`T-209`. `נוחות`: `T-232`·`T-269` |
| `cards` | חפיסות, סינון מילים, חזרה מרווחת | ① מלאכת הכרטיס (`T-325`). ⟨הכשל בדפדפן ⇒ `T-329` 🟣 C-0608; קורא-מסך ⇒ `T-322` 🟣⟩. ⛔ אינה חתומה — ⓑⓒ ב-env |
| `arena` | קרב ארקייד, בחירת דמות, תוצאות | ⛔ אין ⬜. ⛔ אינה חתומה — ⓑⓒ **חסומות** ב-env. בסיס הדמות (11 שכבות) ⇒ `61-deferred` (`D-241`) |
| `studies` | לימודים כמכולת מסלולים, מיקום נשמר | ① **`T-247`** — נתיב המודולים בתוך מסלול (`D-238`: סף 400 בוטל). ⛔ תחזית קצב ⛔ אינה נבנית (`D-237`) |
| `msgs` | ראה למעלה | ⛔ אין יעד ל-DEV. ⬜ יחידה `T-193` (CONTENT); ההרכבה (`39 § 7`) חסומה ב-`R-026` |
| `amirnet` | תרגול שלושת סוגי השאלות ברמה נבחרת, וסימולציה מקצה לקצה | ① CEFR למסד: `T-270`ⓑ 🟣 (הטבלה חיה), ⓒ ⇒ `T-323`·`T-324`. מנוע הסימולציה (`41 § 8`-3) **גמור** (`D-231`·`T-316`). 🔴 **ומעבר ל-①, ⛔ אין מה לפתוח:** `41 § 9.2`·`§ 9.4` ⇒ פריטים 71·73 של רוי · `rc`=0 (`F-235`ⓑ, CONTENT) |
| `loop` · `base` · `general` | תשתית — ⛔ מחוץ לרצף `36 § 13` | 🆕 **יעד `general`, C-0608: «דרך אחת החוצה מכל כשל, ונצבעת».** ⛔ יעד ה-RTL **גמור** — שלושת מסכי הזירה נמדדו חי (‏`T-337`…`T-346` ✅). היעד החדש הוא אותו כלל בשלושה מופעים, ⛔ ולא שלושה באגים: ① יציאת הסיפור (`T-318`·`D-228`ⓐ ✅) · ② כשל חפיסה **מלא** (`T-329` 🟣 C-0608, ‏`≤736` נמדד ב-3 רוחבים) · ③ כשל חפיסה **חלקי** ⇒ `T-349` — היום הפקד ב-`top=783.5` על מסך 780. ועוד `T-348`: הגדר הכפולה במסך הסיכום (`D-246`·`F-253`), המופע השלישי והאחרון של `T-342`/`T-346`. `base`: התנעה קרה 5.6ש (`T-327`·`F-240`). `loop`: תא היומן = 36% מתקרת `00-control` ⇒ `T-340` |

⚠️ **הטבלה ⛔ אינה נכתבת מחדש בכל טיק.** PM נוגע **רק** במחלקה שהשתנתה.
