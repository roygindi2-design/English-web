<!-- חלק מ-plan/. הבעלות והחוקים מוגדרים ב-plan/00-control.md. אל תערוך קובץ שאינו שלך. -->

## 6. ממצאי הביקורת  ⟦OWNER: Critic כותב · Dev מסמן טופל⟧

**חומרה:** 🔴 `CRITICAL` (שבור/מסוכן/תוכן שגוי) · 🟠 `HIGH` (חריגה מהארכיטקטורה) · 🟡 `MEDIUM` (איכות) · ⚪ `LOW` (ליטוש)

| # | חומרה | קובץ:שורה | הממצא | תרחיש הכשל המדויק | תיקון מוצע | סטטוס | סבב |
|---|---|---|---|---|---|---|---|
| Q-001 | 🟡 MEDIUM | `docs/SETUP.md` נספח ב-2 · `lib/core/auth.ts:signupOutcome` | **שאלה פתוחה ל-PM — נפתחה על ידי Dev לפי הוראה מפורשת בתוכנית ה-UX של T-002** ("אם Supabase לא מאפשר את ההפרדה הזו בהגדרה פשוטה — Dev לא מאלתר, אלא פותח שאלה"). התוכנית דורשת "אימות אימייל מופעל, ומשתמש לא מאומת רשאי להיכנס וללמוד". **Supabase לא מציע את הצירוף הזה כהגדרה.** `Confirm email = ON` ⇐ אין session עד לאישור, כלומר המשתמש **לא** יכול להיכנס. `Confirm email = OFF` ⇐ יש session מיד, אבל **לא נשלח מייל אימות כלל**. אין מתג שלישי | לומד נרשם בטלפון, סוגר את האפליקציה כדי לפתוח את המייל, וחוזר — אם ON הוא לא יכול ללמוד עד שיאשר; אם OFF כתובת המייל של כל המשתמשים לעולם אינה מאומתת, ואין דרך לשחזר סיסמה בביטחון | **Dev לא הכריע.** הקוד תומך בשתי ההגדרות ומדווח איזו מהן פעילה (`outcome`), כך שהחלטת ה-PM היא שינוי הגדרה ב-Supabase ולא שינוי קוד. **נדרשת הכרעת PM:** ⓐ ON — חיכוך בכניסה, כתובות מאומתות; ⓑ OFF — כניסה מיידית, אימות נדחה לשלב שעולה כסף/שולח דואר (כפי שהתוכנית התכוונה), בדרך של `resend` יזום | 🔓 פתוח — שאלה ל-PM | 0 |

### 6.1 ממצאים שנדחו במודע
| # | הממצא | מי דחה | הנימוק |
|---|---|---|---|
| — | — | — | — |

---
