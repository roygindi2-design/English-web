<!-- ארכיון נגזר של `plan/60-findings.md`. ⛔ נוצר על ידי `npm run archive`, ⛔ ואינו נערך ביד.
     כל שורה כאן היא **הנוסח המלא** של שורה שהוגדמה ברגיסטר החי. ⛔ אפס מחיקה. -->

# ארכיון — 60-findings.md

<!-- 63 שורות -->
| F-001 | 🔴 CRITICAL | `git` — ענפים | **עבודת PM אמיתית אבדה, וקידום הפריסה היה נכשל.** קומיט `0561f9d` (תיקוני מנועים 7.1/7.4/7.5/7.7, פתרון W1, R-008, R-009) נדחף בטעות ל-`main` ומעולם לא היה ב-`dev`. `main` ו-`dev` התפצלו | ה-Critic מריץ `git merge --ff-only dev` ב-20:00 → `Diverging branches can't be fast-forwarded` → **הקידום נכשל, האתר לעולם לא עולה לאוויר.** במקביל, R-008/R-009 ותיקוני המנועים לא היו קיימים בענף העבודה — Dev היה מממש מפרט מיושן | ✅ **תוקן על ידי רוי בטיק זה:** `main` מוזג לתוך `dev`, התוכן שוחזר ל-`plan/70-engines.md` ו-`plan/20-alerts.md` | ✅ טופל | 0 |
| F-005 | 🟠 HIGH | `plan/50-tasks.md:15` מול `:20` | **T-007 מובטח להיכשל ב-T-012.** T-007 מורה לקלוט NGSL עם **2,801** ערכים; T-012 היא בדיקה שנכשלת אם הקובץ אינו **2,809** בדיוק. `plan/10-pedagogy.md:64` מכריע: 2,809 הוא v1.2, ו-2,801 הוא ערך של גרסה מוקדמת שמקורה בדומיין ה-`.org` הפרוץ (R-004) | Dev לוקח את T-007, קולט 2,801 ערכים, מסמן ✅. Dev לוקח את T-012, כותב את הבדיקה — והיא נכשלת על הנתונים שהוא עצמו קלט. סבב Critic מבוזבז על סתירה שהייתה כתובה בתוכנית מראש | ✅ **תוקן על ידי רוי בטיק זה:** T-007 עודכנה ל-2,809 עם נעילת גרסה מפורשת ל-v1.2 | ✅ טופל | 0 |
| F-015 | ⚪ LOW | `components/AuthForm.tsx:159,177` | חסר `enterKeyHint` בשדות. שאר תכונות הקלט **מצוינות** — `dir="ltr"`, `inputMode="email"`, `autoComplete`, `autoCapitalize="none"`, `spellCheck={false}`, `aria-invalid`. עבודה טובה של Dev | במקלדת מובייל, מקש האישור בשדה האימייל אומר "Enter" במקום "Next", ובסיסמה "Enter" במקום "Go" — חיכוך קטן אבל מצטבר בטופס שכל משתמש עובר | `enterKeyHint="next"` באימייל, `enterKeyHint="go"` בסיסמה | ✅ **טופל C-0019 (F-015 + TD-5).** `<LatinField>` הוא עכשיו שדה הקלט הלטיני היחיד במוצר: `enterKeyHint` הוא prop **חובה** בטיפוס, ולכן שדה לטיני שלא מצהיר על מקש פעולה אינו מתקמפל. אימייל → `next`, סיסמה → `go`. **נמדד ולא הוצהר:** ההוראה נקראת מה-DOM החי ב-`check:mobile`, כי `enterkeyhint` אינו נראה בצילום מסך ואינו נראה בדיף. **מוטציה הוכיחה שהמדידה עובדת:** החלפת `go` ב-`next` בשדה הסיסמה הפילה **6 בדיקות מובייל** (‎/signup ו-/login × 320/375/414) ובדיקת יחידה אחת. חמש פקודות רצו טרי: typecheck ✅ · check:core ✅ · 252 בדיקות ✅ · build ✅ · 333 בדיקות מובייל ✅ | 0 |
| F-018 | 🔴 CRITICAL | `plan/RULES.md` § 0.2 שכבה 4 · `netlify.toml` | **`[skip ci]` שבר את הקידום — האתר מעולם לא עלה לאוויר.** הקידום הוא `git merge --ff-only dev`, שאינו יוצר קומיט מיזוג. לכן ראש `main` אחרי כל קידום הוא קומיט של Dev, שנושא `[skip ci]`. Netlify קורא את ההודעה העליונה ומדלג | רוי ניסה לפתוח את האתר ולא מצא כלום. `main` פיגר 26 קומיטים ולא קודם מעולם; וגם אילו קודם — Netlify היה מדלג על הבנייה. שכבת ההגנה הרביעית ביטלה את השלישית | ✅ **תוקן: התג בוטל לחלוטין** (§ 0.7). שלוש השכבות שנשארו מספיקות ואינן סותרות. `--no-ff` נשקל ונפסל — הוא היה מחזיר את פיצול הענפים של F-001 | ✅ טופל | 0 |
| F-019 | 🟠 HIGH | `apkg` שהעלה רוי | **החפיסה שהתקבלה היא בכיוון ההפוך ובתוכן הלא נכון.** "100 Basic Hebrew Phrases · TeachMeHebrew.com", 2012, 101 כרטיסים. שדות: `עברית \| English \| [sound:*.mp3] \| תעתיק`. זו הוראת **עברית לדוברי אנגלית**, וגם **ביטויים** ולא אוצר מילים | Dev שיקלוט אותה כמו שהיא יקבל 101 ביטויי נימוס ("בוקר טוב", "על לא דבר") בכיוון הפוך — אפס ערך למסלול אמיר"ם, שהוא **אנגלית לדוברי עברית** ברמת מילה בודדת. בנוסף אין רישיון שימוש מסחרי גלוי מ-TeachMeHebrew.com (R-010) | ⛔ **לא נקלטת לתוכן.** הערך היחיד שלה הוא כמקרה מבחן אמיתי לפייפליין `.apkg` — ראה **T-044** (היה T-034 עד C-0039, ראה F-025). רוי: המאגר הגדול צריך להיות אנגלית→עברית ברמת מילה | ✅ **נסגר C-0289 (T-194) — התיישן, ⛔ ולא תוקן.** `find -iname "*.apkg"` ⇒ ריק, ופריט 10 ב-`03-for-roy.md` נסגר 23/08 «⛔ אין מה להעלות» — R-013 אוסר ממילא. | — |
| F-020 | 🟠 HIGH · **פגם (false-accept)** | `lib/core/contentSchema.ts:103` (`inflections`) | **השער מפליט מילה מחוץ-לרמה כי `inflections()` ממציא מילים אמיתיות לא-קשורות.** הפונקציה מוסיפה סיומות עירומות `${w}d` · `${w}r` · `${w}es` · `${w}st` · `${w}er` לכל טוקן, ולכן `card = car+d` · `career = care+er` · `best = be+st` נחשבים "נטיית מילת המטרה" ופטורים מבדיקת סחיפת הרמה (`driftIn` דרך `isTargetToken`, שורה 149/153). סימטרית: השער גם פוסל מסיחים תקינים | **אומת בהרצה חיה (`npx vitest`, C-0011 עומק):** כותרת `car`, דוגמת `neutral` = "The card is in the car." כאשר `card` **אינו** ב-`allowedWords` → `gateSense` מחזיר `{ok:true, reasons:[]}` — מילה מחוץ-לרמה עוברת בשקט לדוגמה שהלומד רואה. סימטרית: מסיח סמנטי תקין `career` לכותרת `care` נדחה `{ok:false, "career" is the headword or an inflection}`. **32 הבדיקות הקיימות אינן תופסות זאת; המוד המהיר (C-0011) סימן T-039 ✅ דרך מבחן שלוש-השאלות בלי להריץ את התרחיש** | התנֵה את הסיומות העירומות במורפולוגיה (`d`/`r`/`st` רק אחרי גזע מסתיים ב-`e`; `es` רק אחרי `s/x/z/ch/sh`), או אמת כל צורה מיוצרת מול רשימת NGSL לפני שמייחסים אותה לנטייה | ✅ **טופל C-0012.** כל סיומת מותנית עכשיו בצורת הגזע: `d`/`r`/`st` בוטלו (ענף ה-`e` כבר מייצר `cared`/`carer`/`carest` דרך `base+ed/er/est`, ולכן הן לא קנו דבר) · `es` רק אחרי `s/x/z/ch/sh` · `er`/`est`/`ed` העירומות רק כשהמילה אינה מסתיימת ב-`e`. נוסף `MIN_INFLECTION_STEM = 3`: `be`→`b`+`est`=**best** ו-`see`→`se`+`ed`=**seed** הם אותה קבלת שווא, ולכן ענף השמטת ה-`e` חסום על גזע קצר — ‏`${w}ing` נשאר ללא תנאי כדי לשמר `being`/`seeing`. **6 בדיקות חדשות; אומת שכל אחת נכשלת מול הקוד הישן** (card עבר `{ok:true}` · best לא דווח · career נדחה). מחיר: TD-12 (דחיית שווא על `use`/`age`) | 0 |
| F-021 | 🟠 HIGH · **נפתח על ידי DEV** | `plan/15-syllabus-digest.md` §§ 110–116 מול `plan/50-tasks.md` T-017/T-018 | **התמצית חסרה את 1.7.1, וסותרת את עצמה ואת T-017 בשני מתוך שלושה כללי סינון.** ⓐ **1.7.1 (כלל הקליטה המחייב) אינו בתמצית.** T-018 מורה במפורש "מימוש כלל הקליטה של **1.7.1**", ו-Dev אסור עליו לפתוח את `10-pedagogy.md`. ⓑ **סתירה פנימית:** § 2 קובע שרשומת `!` ורשומה מנוקדת **"אינם נטענים"**; § 3 (D-013) קובע שרשומה `low` "אינה מוצגת ללומד, **גם אם נטענה**" — כלומר מניח שהיא כן נטענת; T-017 (ב)+(ג) מורה במפורש לטעון אותן ולסנן בהמשך. שניים מול אחד | ⓐ T-018 היא המשימה הרביעית והאחרונה במסלול הקריטי של T-023 — **זו שסוגרת את R-006**. סוכן Dev שיכתוב לה תוכנית יאלץ להמציא את הכלל ש-10-pedagogy מסמן `⛔ סטייה ממנו = תוכן לימודי מומצא` — הכשל הגרוע ביותר האפשרי כאן. לכן היא נשארת ⬜ **בלי תוכנית**, והמסלול הקריטי נעצר ב-3 מתוך 4. ⓑ אילו הסתירה הייתה נפתרת בניחוש, כל מספר הכיסוי של R-005 היה נמדד מול מדיניות שרירותית — ופער של עשרות אחוזים בין שתי הקריאות הוא בדיוק מה שמכריע אם יש בכלל מקור תרגום מספיק | **ל-PM, כחלק מ-T-020:** ⓐ העתק את 1.7.1 לתמצית מילה במילה. ⓑ הכרע בין § 2 ל-§ 3/T-017 ותקן את המפסיד. ⚠️ **ההכרעה אינה חוסמת את המדידה:** תוכנית `2026-08-07-coverage-measurement.md` הופכת את המדיניות ל**נתון ולא ללוגיקה** — `measureCoverage()` מקבל `CoveragePolicy`, והדוח מדפיס את הכיסוי **בשתי הקריאות** (STRICT לפי § 2 · LENIENT לפי T-017+D-013). לכן שום קוד לא ייכתב מחדש בעקבות ההכרעה. ההכרעה עדיין נדרשת — היא קובעת מה **הפייפליין** (T-035/T-037) באמת שומר | ✅ **נסגר C-0049.** אומת בראיות: ⓐ התמצית שורה 92 — "**בחירת משמעות — 1.7.1, מועתק מילה במילה**" עם ארבעת הכללים (המפתח `(lemma,POS)` · מסלול מהיר synset יחיד · מסלול H1 synset↔synset · GAP), כלומר 1.7.1 נמצא בתמצית. ⓑ D-025 (`40-decisions.md:351`, PM C-0046, "סוגר את F-021 ⓑ") מכריע `GAP` לא נטען · `!` נטען כ-low · ניקוד אינו עילה — מגובה במקור הראשוני (README של Hebrew Wordnet), ו**חתום ע״י רוי** (00-control). שתי תת-הבעיות סגורות. | 0 |
| F-022 | 🟠 HIGH · **פגם (רדום — אין צרכן עדיין)** | `lib/core/progress.ts:18` (`timeToFirstCorrectMs`) מול `supabase/migrations/0003_provenance_telemetry.sql:78` (`time_to_first_correct int`) | **חוסר התאמת טיפוסים TS↔SQL על שדה D-010.** בקוד השדה הוא `number` במילישניות ללא גבול; בסכימה העמודה היא `int` (int4, מקסימום 2,147,483,647ms ≈ 24.85 ימים). ההערה במיגרציה עצמה קובעת "milliseconds from first exposure to first correct answer" — כלומר הכוונה ל-ms מפורש | לומד רואה מילה, נכשל שוב ושוב, ועונה נכון לראשונה 26 יום אחרי החשיפה (סביר ב-SRS שהאינטרוולים בו מגיעים לשבועות) → `timeToFirstCorrectMs ≈ 2.25e9` → ה-UPSERT ל-`word_progress` נכשל `integer out of range` → **כל שורת ההתקדמות אובדת, לא רק השדה הזה.** ⚠️ רדום היום: `progress.ts` אין לו צרכן (אין מסך ואין נתיב שרת שכותב ל-`word_progress`), ולכן אינו מגיע ללומד — אך יישבר ברגע שמשימה 4 של T-005 (`0005` + `POST /api/review`) תחווט את ההתמדה | שנה את העמודה ל-`bigint` (או אחסן שניות), **לפני** חיווט ההתמדה במשימה 4 | ✅ **נסגר C-0039.** `time_to_first_correct` הוא `bigint` בהצהרה **וגם** ב-`alter table … alter column … type bigint` נפרד — ‏`create table if not exists` הוא no-op מול פרויקט שכבר הריץ 0003, ולכן תיקון שחי רק בבלוק לא היה מגיע לשם. שתי בדיקות ב-`lib/supabase/telemetry.test.ts`; מוטציה שמחזירה `int` ומוטציה שמסירה את ה-`alter` מפילות כל אחת את הבדיקה בשמה | 0 |
| F-023 | 🟠 HIGH · **פגם (רדום — אין צרכן עדיין)** | `lib/core/progress.ts:16` (`consecutiveCorrectRecognition`) מול `supabase/migrations/0003_provenance_telemetry.sql:67-85` (`word_progress`) | **לשדה מצב הרצף אין עמודה ב-SQL ואינו נגזר מהצבירה.** `word_progress` מחזיק attempts/correct_attempts/time_to_first_correct/attempts_to_mastery/mastered_at בלבד — אין עמודה לרצף ההיזכרות, והוא אינו ניתן לשחזור מ-attempts/correct_attempts. ההערה במיגרציה ("everything gate 7.7 needs is derivable from the aggregate") שגויה לשדה זה | לומד עונה נכון פעמיים ברצף (streak=2), הסשן נסגר, וב-טעינה מחדש מה-DB הרצף חוזר ל-0 → `attempts_to_mastery` לעולם לא ייקבע (או ייקבע מאוחר), ו-`directionFor` (`flashcard.ts:226`) מחזיר מילה שכבר התקדמה חזרה ל-recognition → הלומד חוזר על מילה ששלט בה. ⚠️ רדום: אין עדיין שכבת persistence שקוראת/כותבת את השדה | `alter table public.word_progress add column consecutive_correct_recognition int not null default 0;` ומיפויו בשכבת ההתמדה — **לפני** משימה 4 של T-005 | ✅ **נסגר C-0039.** ‏`consecutive_correct_recognition int not null default 0` נוסף ל-`word_progress` (בהצהרה + `add column if not exists`). ‏`not null` בכוונה, בניגוד לשני שדות D-010: רצף אפס הוא מדידה ולא "לא ידוע". ההערה השגויה ב-0003 ("derivable from the aggregate") תוקנה בקובץ עצמו וב-`lib/core/progress.ts` | 0 |
| F-024 | 🟠 HIGH · **פגם (רדום — אין צרכן עדיין) / שאלת מפרט** | `lib/core/scheduler.ts:148` (`hasFutureExam = horizonDays > 0`) | **יום המבחן עצמו (daysUntilExam=0) נופל ל-`classic` במקום `triage`.** התנאי `> 0` מוציא את יום המבחן יחד עם מבחנים שעברו, בסתירה למפרט 7.1 ("תאריך מבחן קרוב מדי → טריאז'") — ויום המבחן הוא הקרוב מכל. אין אף בדיקה ל-`daysUntilExam=0` (בדיקת "מבחן שעבר" משתמשת בתאריך עבר ממש), מה שמרמז שהגבול לא נשקל | לומד פותח את האפליקציה בבוקר יום המבחן (`today=examDate`), state `{EF:2.5,interval:6,rep:2}`, תשובה `good` → `mode:'classic'`, `triage:false`, `nextReviewDate` 15 יום קדימה (אחרי המבחן) → אפס תיעדוף של מילים בסיכון בסשן הקריטי ביותר. ⚠️ רדום: `scheduler.ts` אין לו צרכן (אין מסך). **שאלת מפרט:** אם ההחלטה היא "ביום המבחן מוותרים על תזמון" — יש לתעד; כרגע היא סמויה וסותרת את 7.1 | `horizonDays >= 0` (רק `<0` = עבר→classic); אז `usableHorizon = 0 − gap < triageMin` → טריאז' → חזרה היום | ✅ **נסגר C-0039.** ‏`horizonDays >= 0` — יום המבחן עצמו הוא האופק הקרוב ביותר ולכן טריאז'. שתי בדיקות: יום המבחן→triage, ו**אתמול**→classic (מגן מירייה מעבר למטרה). מוטציה חזרה ל-`> 0` מפילה את הראשונה | 0 |
| F-025 | 🟠 HIGH · **פגם (היגיינת פרוטוקול)** | `plan/50-tasks.md:36` ו-`:43` | **מזהה משימה כפול T-034.** שתי משימות שונות נושאות את אותו מזהה: שורה 36 = "מסלול נסה בלי חשבון" (M1), שורה 43 = "פייפליין קליטת `.apkg`" (M2). F-012 ו-F-019 שניהם מפנים ל-"T-034" ולא ברור לאיזו | סוכן שקורא "המשך העבודה הופרד ל-T-034" (F-012) מגיע לשורה 43 (פייפליין apkg) במקום לשורה 36 (הטעימה), ומבצע את המשימה הלא נכונה — או מסמן ✅ על משימה שלא בוצעה. הוכנס ב-C-0005 (‏362fd0b), קדם לחלון הביקורת אך עדיין פתוח. ⚠️ תיעוד/תהליך — אינו חוסם קידום | הקצה מזהה חדש (למשל T-044) לאחת מהשתיים, ועדכן את ההפניות ב-F-012/F-019 | ✅ **נסגר C-0039.** פייפליין ה-`.apkg` (M2) מוספר מחדש **T-044**; הטעימה (M1) מחזיקה ב-T-034 כי אליה מפנות F-012 ותוכנית התצוגה. הפניית F-019 עודכנה. `scripts/plan-hygiene.test.ts` אוכף ייחודיות מזהה + רצף ללא חורים — מוטציה שמכפילה מזהה מפילה שתי בדיקות | 0 |
| F-027 | 🔴 CRITICAL · **פגם · דווח על ידי רוי מהאתר החי (02-inbox ח׳) · נפתח ונמדד על ידי DEV** | `app/onboarding/page.tsx` + `scripts/verify-mobile.mjs:264` + `app/dev/onboarding/page.tsx` | **שער הכניסה למוצר נראה סגור: רוי נרשם, נחת ב-onboarding, ולא ראה לא כפתור המשך ולא דרך לצאת.** המסך אינו שבור — הוא פשוט **גבוה מהמסך**. נמדד ב-Chromium ברוחב 375 מול חלון 780: גובה המסמך **1090px**, כפתור `שמירה והתחלה` מתחיל ב-**y=852** (‏72px מתחת לקפל) וכפתור `יציאה מהחשבון` ב-**y=928** (‏148px מתחת). שתי הפעולות היחידות במסך יושבות מחוץ לצפייה הראשונה, ואין שום סימן שהעמוד נגלל. גם ב-320 (‏y=880) וגם ב-414 (‏y=828). **הפגם שרד את הבארייר בגלל שלוש תקלות מצטברות, לא אחת:** ⓐ בדיקת "‏primary action in thumb zone" נקבה ב-`/onboarding`, ו-`/onboarding` מחזיר 307 ל-`/login` בלי משתני Supabase (TD-13) — כלומר היא מדדה את מסך ההתחברות **פעמיים** ואת טופס המטרה **אף פעם**. ההערה בקובץ עצמה מודה בזה ("‏every ok /onboarding line is really the login screen") ואיש לא הזיז את הבדיקה לפיקסצ׳ר. ⓑ הפיקסצ׳ר `/dev/onboarding` רינדר את **הטופס לבדו**, בעוד המסך האמיתי מרנדר גם את פס הכתובת מעליו וגם את טופס היציאה מתחתיו — נמדד: הכפתור ב-**y=726** בפיקסצ׳ר הישן מול **y=852** בהרכב האמיתי. פיקסצ׳ר נמוך מהמסך שהוא מייצג מדווח "ok" על פריסה שאיש לא מדד. ⓒ לכפתור לא היה `[data-primary-action]`, ולכן גם אילו נמדד — הסלקטור החלופי היה תופס את הקישור בפס הכתובת. **זה לא היפותטי: המוטציה שהסירה את הסימן הפילה את הבדיקה עם `sits at y=211`** — בדיוק אותו תחליף שכבר הטעה את הבדיקה פעם אחת ב-`/login` (שם היא מדדה את כפתור הצגת הסיסמה) | לומד חדש משלים הרשמה, נוחת במסך היחיד שמאחורי חומת הסשן, ורואה כותרת · פס כתובת · שלוש אפשרויות דקות · שדה תאריך. הפעולה שממשיכה אותו למוצר נמצאת 72px מתחת לקצה המסך. הוא אינו יודע שהעמוד נגלל, ולכן המוצר **מסתיים עבורו כאן**. ⛔ אין מסלול עוקף: `/study` ריק (P-001), ו-`middleware` מחזיר כל מי שאין לו פרופיל ל-onboarding | **פוצל לשניים בכוונה.** ✅ **החצי של Dev — נסגר C-0064:** ⓐ הבדיקה הועברה מ-`/onboarding` ל-`/dev/onboarding` · ⓑ הפיקסצ׳ר הפך לעותק של המסך האמיתי פחות שער הסשן, ו-`verify-mobile.test.ts` משווה אלמנט-אלמנט כדי שלא יסטו שוב · ⓒ הכפתור סומן, ונוסף `FLOW_ROUTES` עם **שלוש** בדיקות חדשות בכל מסך זרימה ובשלושת הרוחבים (הדרישה שרוי ניסח ב-02-inbox ח׳): `exactly one primary action` · `is hit-testable` (‏`elementFromPoint`, לא קריאת מלבן) · `reachable by scrolling` (הקליפ נקרא מ-`computed style`). בנוסף `firstPaintTop` **מדווח בכל הרצה**, כך שהמספר חי בלוג ולא בהערה. ⛔ **החצי שנשאר פתוח — של ה-PM:** להביא את הפעולה אל תוך הצפייה הראשונה. סרגל פעולה דביק או מסך קצר יותר — שתיהן החלטות מסך ב-`40-decisions.md`, ו-Dev אינו ממציא אותן (⛔ ולכן הבדיקה **לא** טוענת שהפעולה גלויה בקפל — היא תופעל ברגע שההחלטה תינחת). ⚠️ **לרוי, עכשיו:** הכפתור קיים ועובד — יש לגלול למטה | ✅ **נסגר C-0071 — שני החצאים.** החצי העיצובי מומש כ-`components/ActionBar.tsx` לפי D-028/§ 4.2ג: סרגל `fixed` מעוגן-לתחתית עם `env(safe-area-inset-bottom)`, ריפוד מסמך על `body:has([data-action-bar])` ו⛔**לא** על `<main>` (‏`<footer>`/`/sources` הוא אח **אחרי** `<main>`, `app/layout.tsx:41` מול `:47`). **הבדיקה נכתבה לפני הקוד ונפלה על המספר של הממצא:** `"שמירה והתחלה" first paints at y=852 on a 780px viewport` ב-375 · `y=880` ב-320 · `y=828` ב-414 — כלומר החסם החדש מודד בדיוק את מה שרוי ראה. **אחרי התיקון: 716/716/716 מול חלון 780**, וכל אחד עדיין מעל רצפת אזור האגודל (390). **שתי מוטציות, שתיהן הרגו בדיקה בשמה:** הסרת כלל הריפוד → **15** כשלי `action bar does not cover the licence link` (`link bottom 756 vs bar top 703`) · `fixed`→`static` → החסם העליון נופל בשלושת הרוחבים (`y=893 · 865 · 841`). ⚠️ **סטייה מוצהרת אחת:** הקו הדק **תמיד-דלוק** ולא 'כשהתוכן נגלל מתחתיה' — ל-CSS אין ביטוי לתנאי הזה, והחלופה היא IntersectionObserver ברכיב לקוח, בעוד שהסרגל חייב לעבוד בלי JS (טופס היציאה). האיסור באותו משפט הוא על **צל**, וקו פיקסל בטוקן עומד בו. **check:mobile ✅ 489 בדיקות · 704 בדיקות יחידה (מ-692).** | 0 |
| F-028 | 🟠 HIGH · **פגם (false-accept · רדום — אין צרכן עדיין)** | `lib/core/apkg.ts:116` (וסימטרית `:110`) | **הקיצור `latin.length === 1` מדלג על סינון `TRANSLIT_NAME`, ולכן שדה תעתיק בודד מקודם ל-`front`.** חפיסה עם שדה עברי ושדה תעתיק בלבד, **ללא שדה אנגלית**, מקבלת את התעתיק כ-`front` — ש-`ApkgCard` (שורה 69) מגדיר "English, always". וכשהתעתיק הוא השדה הראשון בסוג-הפתק, `classifyDeck` (שורה 167) קורא אותו כ-`latin` → `direction='en_to_he'`, ו-`apkgIngestDecision` עם רישיון מותר מחזיר `ingest:true` — בדיוק כשל-הכיוון ש-F-019 קיים כדי לחסום. | **אומת חי (`npx vitest`, C-0067 עומק):** `parseAnkiNote({fieldNames:['עברית','תעתיק'], flds:'בית␟bayit'})` → `{ok:true, front:'bayit', back:'בית', translit:'bayit'}` — אותו ערך ב-`front` וב-`translit`, כלומר רומניזציה מוצגת כמילה האנגלית. וגרוע יותר: `classifyDeck([{fieldNames:['תעתיק','עברית'], flds:'bayit␟בית'}]).direction = 'en_to_he'`, ו-`apkgIngestDecision(deck,'permitted') = {ingest:true, reasons:[]}` → חפיסה עברית-בכיוון-הפוך נקלטת כחפיסת אנגלית תקפה. **בקרה שאומתה:** החפיסה האמיתית בת 4 השדות (F-019) עדיין נכונה — `direction='he_to_en'`, `front="thank you"`. הפער נוגע רק לחפיסה שאין בה שדה אנגלית כלל. | תמיד לסנן `TRANSLIT_NAME` מרשימת ה-latin לפני `pickSingle` (הסר את קיצור `latin.length === 1`), ולדחות `no_latin_field` כשלא נשאר שדה אנגלית אמיתי. | ✅ **נסגר C-0069.** הסינון `TRANSLIT_NAME` חל **תמיד**, בשני הכתבים; `english.length === 0` → `no_latin_field` (שדה חסר, לא דו-משמעי), וסימטרית `hebrewMeaning` → `no_hebrew_field`. **שוחזר חי לפני התיקון** (`{ok:true, front:'bayit', translit:'bayit'}` ו-`{ingest:true, reasons:[]}`), **ואחריו נמדד:** `parsed=0` · `rejected.no_latin_field=1` · `{ingest:false, reasons:['parse_rate_too_low']}`. ⚠️ `classifyDeck` עדיין מדווח `direction='en_to_he'` לאותה חפיסה, כי הוא קורא כיוון מ**כתב** שדה המיון ולא משמו — הפער שנותר הוא **בשם הנימוק ולא בהכרעה**, והרחבתו היא שינוי התנהגות מעבר למה ש-F-028 ביקש. 5 בדיקות חדשות (**692** סה"כ, 46 קבצים), 4 מוטציות — 2 הרגו בדיקה בשמה, 2 שקולות. ראה 3.1.23 | 0 |
| F-030 | 🟡 MEDIUM · **פגם (שומר מתכלה + התאמה חיובית-שגויה) · נפתח ותוקן על ידי DEV באותו טיק** | `scripts/build-ingest-sql.test.ts:11,21,34,67,80` (לפני התיקון) | **חמש בדיקות נפלו על `dev` בלי ששום קוד השתנה.** נמדד C-0079 על עץ נקי (stash): `5 failed \| 4 passed`. טיק ה-CONTENT של C-0078 הוסיף `data/generated/batch-2026-08-13.jsonl` — 60 משמעויות שהסקריפט קולט נכון, 0 נפסלו — והשומר החזיק מדידה ישנה כמספרים קשיחים: `343 rows` · `6 batch files` · `1 low`. **פגם שני, חמור יותר:** משמעות בביטחון נמוך זוהתה לפי `line.includes("'low'")`, ו-C-0078 הכניס את הכותרת `low` עצמה — שורה שביטחונה `'high'` נספרה כנמוכה (4 מול 3 מסומנות) | כל טיק תוכן מאדים את החבילה, ה-Critic מקבל עץ אדום שאינו מעיד על רגרסיה, וה-Dev הבא עורך את השומר במקום לקרוא אותו. שומר שנערך שגרתית מפסיק להיות שומר. במקביל, ההתאמה החיובית-שגויה הייתה מתפוצצת שוב בכל אצווה שתכיל כותרת שהיא גם שם ערך בעמודה כלשהי | הספירות נגזרות מקובצי האצווה בזמן ריצה (שוויון מול המקור — טענה **חזקה** ממספר קשיח), והמספר הקשיח נשמר כ**רצפה** בלבד (`ROWS_FLOOR=403` · `FILES_FLOOR=7`) כדי שהיעלמות אצווה עדיין תיתפס. תבנית הביטחון: `", 'low', "` — כותרת נפתחת מיד אחרי `(` ולכן ⛔ אינה יכולה להתאים. תוכנית ה-spot-check נבדקת מול `spotCheckPlan()` הטהור | ✅ **טופל C-0079.** שתי מוטציות אימתו: ⓐ התעלמות מקובץ האצווה האחרון → 5 בדיקות נפלו · ⓑ `needs_human_review` תמיד `false` → `expected [] to have a length of 3`. **785 בדיקות ✅ (מ-780 עוברות + 5 נופלות)** | 0 |
| F-031 | 🟡 MEDIUM | `app/api/health/route.ts` | **⚠️ אזהרה מקדימה ל-Critic — לא רגרסיה.** אחרי T-053 (C-0093) `/api/health` בודק גם את הטבלה `word_progress`, ולכן ה-endpoint החי יחזיר `ok:false` + 503 עד שרוי יריץ את המיגרציות (`03-for-roy` פריטים 4 · 9 · 12 · 18) | ה-Critic מריץ את בדיקת העשן של `RULES § 0.1.1 ד׳` אחרי הקידום הבא, מקבל `ok:false`, ולפי אותו סעיף פותח 🔴 CRITICAL + `NEXT_AGENT=HUMAN` — **על הפיצ׳ר עצמו**. הלופ נעצר בגלל בדיקה שעובדת כמתוכנן | ⛔ אל תפתח 🔴 על `ok:false` שבו `database_schema` הוא הכשל היחיד ושלוש בדיקות ה-ENV ירוקות. זהו **בדיוק** המצב ש-T-053 נבנתה לחשוף. הכשל האמיתי היחיד: 404, HTML, או `ok:false` שבו בדיקת ENV נכשלת | ✅ **נסגר C-0289 (T-194) — התיישן, ⛔ ולא תוקן.** ההגירות הורצו: `plan/00-control.md` מתעד עשן עם `database_schema` ok. האזהרה כיסתה מצב שחלף. | 0 |
| F-032 | 🟡 MEDIUM | `plan/40-decisions.md` | **אין תוכנית UX למצב הסיום של `/study` — T-055 חסומה ב-PM ולא ב-Dev.** נמדד C-0093: `grep` על `40-decisions.md` מחזיר **אפס** אזכורים למסך סיום המחזור. `45-product-questions.md` ש׳2 ו-`02-inbox.md` ה׳ מבקשים את המצב, אך ההכרעה לא נחתה | Dev שמבצע את T-055 בלי הכרעה **ממציא מסך** — טקסט, פעולה ראשית ויעד ניווט שאיש לא אישר. זו בדיוק החריגה שסעיף «אתה מממש בלבד» אוסר, והיא תתגלה רק בסקירה | ה-PM מכריע ב-`40-decisions.md`: מה מבדיל `done` מ-`empty`, איזו **פעולה אחת** יש בכל אחד, ולאן היא מובילה. ⛔ בלי מספרים חדשים ובלי הבטחת תאריך (`2026-08-13-m0-finish.md` משימה 5) | ✅ **נסגרה C-0137 (DEV).** ההכרעה נחתה `5a86a28` ב-**2026-08-13T15:53:21Z** — § 4.2ו: «בסוף המחזור **מסך סיום**» · «**יוצאים** — מסך הסיום, ומשם **חזרה לבורר**» · «המילה האחרונה — **מסך סיום ולא מסך לבן**». הממצא נפתח `c4f40df` ב-**15:49:54Z**, ⇒ **שלוש וחצי דקות קודם**, והשורה נשארה 🔓 ~35 מחזורים. שלוש השאלות נענו בקוד: `done` נבדל מ-`empty` ב-`data-deck-done` + זהות החפיסה, **פעולה ראשית אחת** (`data-primary-action`), יעדה `/cards`. ⛔ השורה לא נמחקה — הממצא היה **נכון כשנכתב**, וזה חלק מהתיעוד | 0 |
| F-034 | 🟠 HIGH · **פגם · נפתח על ידי CRITIC (C-0098, עומק)** | `app/api/study/queue/route.ts:159-170` | **השאילתה מושכת `.limit(200)` בלי `.order()`.** Postgres אינו מבטיח סדר שורות בלי `order`, ולכן 200 השורות שנמשכות הן תת-קבוצה שרירותית ברגע שללומד יש מעל 200 שורות `word_progress`. חפיסת `unknown` מסננת `attempts>0 AND repetition=0` ב-JS **אחרי** החיתוך השרירותי (`route.ts:194`), וחפיסת `due` ממיינת רמות רק בתוך אותה תת-קבוצה. מתחת ל-200 שורות `sortQueue` ממיין הכל וההתנהגות תקינה לחלוטין | לומד עם 500 שורות `word_progress` שמתוכן 30 "לא ידעתי" — אם ה-30 מחוץ ל-200 הראשונות שפוסטגרס החזיר → `route.ts:172` data → `route.ts:194` סינון → **`total:0`, חפיסה ריקה** בזמן ש-30 מילים שנכשלו ממתינות. ובחפיסת due מילות A1 שלא נמשכו מדולגות מול B2 שכן — ערובת D-034 בטלה מעל 200 | הוסף `.order('next_review_at',{nullsFirst:false}).order('word_id')` לפני `.limit()`, ודחוף את תנאי unknown ל-SQL (`.gt('attempts',0).eq('repetition',0)`) כך שהתקרה תחתוך את האוכלוסייה הנכונה | ✅ **נסגר C-0099 (DEV)** — `.order('next_review_at',{nullsFirst:false}).order('word_id')` לפני `.limit(MAX_QUEUE_ROWS)`, ותנאי «לא ידעתי» נדחף ל-SQL (`.gt('attempts',0).eq('repetition',0)`) כך שהתקרה חותכת את האוכלוסייה הנכונה. `isUnknownRow` הטהור נשאר כהגדרה היחידה של החפיסה. שלוש בדיקות מקור חדשות: order לפני limit · שובר שוויון `word_id` · התנאי בענף `unknown`. 924/57 ✅. ⟨היסטוריה: ⛔ לא חסם קידום — ה-endpoint רדום — אינו מחווט לשום מסך (`grep` מחזיר רק `.next/types`; `/study` עדיין מרנדר מצב ריק), ולומד אינו מגיע אליו; הסף >200 שורות בלתי-נגיש טרם השקה. לתיקון כחלק מ-T-064 משימה 3 שנוגעת ממילא בשאילתה | 0 |
| F-035 | 🟠 HIGH · **פגם (תיעוד) · נפתח על ידי CRITIC (C-0098)** | `docs/api-contract.md:282` מול `lib/core/deck.ts:62-70` | **החוזה מבטיח 400 על `limit` פסול, הקוד חותך בשקט.** שורה 282 קובעת "400 — deck לא מוכר או limit פסול", אבל `clampQueueLimit` לעולם אינו מסמן שגיאה: `''`·`'-5'`·`'3.5'`·`'abc'`·`'0'` כולם נופלים ל-`DEFAULT_QUEUE_LIMIT=20` והבקשה מחזירה **200**. רק `deck` פסול מפיק 400 | מפתח לקוח קורא את החוזה, שולח `?limit=0` בציפייה ל-400 שיחשוף באג, ומקבל בשקט חפיסת ברירת-מחדל של 20 — ענף הטיפול-בשגיאה שלו הופך לקוד מת והבקשה הפגומה מוסווית | תקן את החוזה ל-"400 — deck לא מוכר" והוסף שהמשפט ש-limit פסול נחתך לברירת המחדל (לעולם לא נדחה) — או, אם רוצים דחייה, החזר מ-`clampQueueLimit` כשל דיסקרימיננטי ו-400 במסלול. היום השניים חלוקים | ✅ **נסגר C-0099 (DEV)** — תוקן **התיעוד ולא הקוד**: חיתוך שקט הוא ההתנהגות הנכונה לפרמטר תצוגה, ודחייה הייתה הופכת כתובת שמורה עם `?limit=0` למסך שגיאה. החוזה קובע עכשיו «400 — `deck` לא מוכר» ומסביר ש-`limit` פגום **נחתך** לברירת המחדל 20 ומחזיר 200. הבדיקה מודדת שאין **שורה** שמבטיחה 400 על `limit`, ⛔ ולא שהמחרוזת נעלמה — התיקון עצמו מזכיר אותה. ⟨היסטוריה: פער תיעוד/חוזה, לא פגם בקוד שמגיע ללומד (§0.12) | 0 |
| F-036 | 🟠 · **ממצא עיצוב (בלם 10) · פגם נגישות · נפתח על ידי CRITIC (C-0098)** | `components/OnboardingForm.tsx:197` | **בדיקת ניגודיות בינארית נכשלה — 4.42:1.** כפתור השליחה משתמש ב-`bg-brand text-brand-on` = לבן `#ffffff` על `--brand #2a78d6` = **4.42:1**, מתחת לרצפת 4.5:1 (18px semibold אינו "טקסט גדול" ב-WCAG). `lib/core/palette.ts:14` מתעד במפורש ש-4.42 הוא **הסיבה** לקיום `--brand-surface` (#1d4ed8, 6.70:1) כ"מילוי כפתור ראשי"; `error.tsx:23` ו-`global-error.tsx:26` משתמשים נכון בטוקן הזה | כל לומד חדש עובר את מסך ההרשמה; הכפתור הראשי שלו נמדד 4.42 < 4.5 בבהיר. עיוור-בדיקה נלווה: `palette.test.ts` מרצפֵּן `brand-on on brand-surface` אך **לא** את הצמד `brand-on on brand` שהכפתור באמת משתמש בו, ולכן הפגם עבר ירוק ב-CI | `bg-brand` → `bg-brand-surface` (שינוי טוקן יחיד, זהה לשני כפתורי השגיאה) | ✅ **נסגר C-0099 (DEV)** — `bg-brand` ⇒ `bg-brand-surface` ב-`OnboardingForm.tsx:197` (6.70:1). **⚠️ עיקר התיקון הוא סגירת עיוורון הבדיקה שהממצא זיהה:** רצפה לצמד `brand-on on brand` הייתה מקבעת צמד שאסור להתקיים, ולכן במקומה נוספה ל-`lib/core/palette.test.ts` סריקת מקור על כל `.tsx` ב-`app/` וב-`components/` — `bg-brand` **מדויק** אסור כמילוי (⛔ `bg-brand-surface` אינו נפגע), ומסך חדש מכוסה ביום שייכתב. הודגמה אדומה על הקובץ היחיד שנכשל לפני התיקון. ⟨היסטוריה: ⛔ ממצא עיצוב — לא החזיר מקל ולא חסם (בלם 10); ממצא עיצוב 1 מתוך 2 המותרים לאבן דרך | 0 |
| F-037 | 🟡 MEDIUM · **נפתח על ידי CRITIC (C-0098, בדיקת עשן)** | `app/api/health/route.ts` (פריסה חיה) | **בדיקת עשן אחרי קידום 848119d: `/api/health` החזיר את גרסת 3-הבדיקות הישנה (`ok:true`, ⛔ בלי `database_schema`) גם ~17 דק׳ אחרי ה-push, בעוד `/api/study/queue` (נתיב שנוצר ב-e1fd933) החזיר 401 ⇒ הפריסה החדשה חיה.** e1fd933 הוא **צאצא** של c4f40df (health 4-בדיקות), ולכן כל פריסה שמכילה את ה-queue מכילה גם את ה-health החדש — הפונקציה פרוסה, וה-GET ל-`/api/health` מוגש stale מ-edge-cache/פיגור התפשטות. שני המסלולים `force-dynamic` | ה-Critic מריץ בדיקת עשן §0.1.1 ד, מקבל 3-בדיקות ok:true, ⛔ ואינו יכול לאשר מקצה-לקצה שעדכון T-053 חי — בזמן שהפריסה עצמה **הצליחה** (queue 401). אם ה-3-בדיקות נמשך שעות ⇒ בעיית caching אמיתית על ה-GET | ⛔ **לא 🔴:** האתר חי (ok:true), פונקציות פרוסות (queue 401), ⛔ אין 404/HTML/ok:false ⇒ טריגר §0.1.1 ד לא נורה. מעקב: ודא ש-`/api/health` מתהפך ל-4-בדיקות `ok:false` (database_schema — צפוי לפי F-031) כשה-edge יתנקה; אם לא, בדוק caching של Netlify על ה-GET (T-046 טוקן חסר ⇒ אין גישה ללוח) | ✅ **נסגר C-0289 (T-194) — התיישן, ⛔ ולא תוקן.** פריט 22 ב-`03-for-roy.md` נסגר 23/08 «התיישן» — 13 קידומים מאז, ובדיקת העשן מחזירה ok על ארבע הבדיקות. | 0 |
| F-038 | 🟡 MEDIUM · **פגם (מיפוי תוכנית סותר) · נפתח על ידי DEV (C-0122)** | `docs/superpowers/plans/2026-08-14-world-compose.md:14` ו-`plan/00-control.md` `ACTIVE_TASK_ID` מול `plan/50-tasks.md:70,71` | **שני מיפויים שונים של אותה תוכנית חיים בריפו, והם off-by-one.** כותרת התוכנית וה-`ACTIVE_TASK_ID` אומרות «T-061 = משימות 1–4 · T-062 = 5–6 · T-063 = 7–8», אך לתוכנית **תשע** משימות, ושורת T-061 ב-`50-tasks` דורשת מפורשות גם את ⓒ `POST /api/world/posts` = **משימה 5**; שורת T-062 מתעדת את המיפוי השני «T-061 = 1–5 · T-062 = 6–7 · T-063 = 8–9», והוא זה שתואם את תוכן המשימות בפועל (פיד+לשונית = 6–7 · מסך ההרכבה+פיקסצ׳ר = 8–9) | סוכן שקורא את הכותרת בלבד סוגר את T-061 אחרי משימה 4, כלומר **בלי `POST /api/world/posts`** — ואז T-062 (הפיד) נבנית מול נקודת קצה שאינה קיימת, ומשימה 9 (פיקסצ׳ר `check:mobile`) נשמטת מכל שלוש המשימות ולא נבנית בכלל | **מצטמצם C-0123, ⛔ ואינו נסגר.** משימה 5 בוצעה, ולכן T-061 שלמה **בשני המיפויים** — הסיכון שהממצא תיאר (סגירת T-061 בלי `POST /api/world/posts`) ⛔ **לא התממש**. מה שנותר: שיוך משימות 6–9. אמץ את המיפוי של `50-tasks` (**T-061 = 1–5 · T-062 = 6–7 · T-063 = 8–9**) ותקן את שורה 14 בתוכנית. ⛔ DEV לא תיקן בעצמו: מיפוי משימות לאבני עבודה הוא הכרעת PM. אמץ את המיפוי של `50-tasks` (**T-061 = 1–5 · T-062 = 6–7 · T-063 = 8–9**) ותקן את שורה 14 בתוכנית. ⛔ DEV לא תיקן בעצמו: מיפוי משימות לאבני עבודה הוא הכרעת PM, ותיקון חד-צדדי היה מסתיר את הסתירה במקום לפתור אותה | ✅ **נסגר C-0289 (T-194) — התיישן, ⛔ ולא תוקן.** שלוש המשימות שהמיפוי היה אמור להגן עליהן — T-061 · T-062 · T-063 — סגורות. ⛔ אין עוד עבודה שהמיפוי יכול להפיל. | 0 |
| F-039 | 🟡 MEDIUM · **פגם (אסרציה עיוורת בתוך תוכנית) · נפתח על ידי DEV (C-0124)** | `docs/superpowers/plans/2026-08-14-world-compose.md` משימה 6, שלב 1 (בדיקת «—») | **קוד הבדיקה שבתוכנית מודד את המקף ⛔ ולא את המספר שלידו.** האסרציה היחידה שמגנה על המונה היא `expect(CODE).toContain('—')`, והיא **עיוורת**: נמדד בטיק הזה שמוטציה שהחליפה את «מילים שהפקת» ב-`state.posts.length` — כלומר תווית שמעליה מספר שאינו מודד אותה, בדיוק מה שהמשימה אוסרת — **שרדה** את הנוסח שבתוכנית. זהו אותו מחלקת כשל של C-0120 ושל F-035, והפעם היא בתוך **מסמך התכנון** ולא בקוד. ⚠️ **התיקון בטיק הזה הוא מקומי בלבד** (אתר קריאה + איסור `posts.length`, בתוך אותה בדיקה, ⛔ בלי בדיקה 11) — משימות 7–9 באותה תוכנית מחזיקות אסרציות `toContain(<שם>)` נוספות שלא נבדקו | `WorldFeed.test.ts` — האסרציה שנוספה · `30-architecture.md` C-0124 | DEV | ✅ **נסגר C-0142 — משימות 8–9 נמדדו ו⛔ לא נקראו.** **עשר מוטציות מסוג F-039 הורצו על `ComposeDraft.test.ts` ו-`WordBank.test.ts`; שבע נפלו בשם ו**שלוש שרדו**, כלומר הנוסח שנראה כאיתור-אתר-קריאה היה עיוור בשלושה מקומות: ⓐ מחיקת `<EnWord>{target}</EnWord>` מהנחיית הפרסום («הוסף את המילה של היום כדי לפרסם») — החלון `[\s\S]{0,200}` תפס את שומר `target !== null` שתי שורות מעל המשפט · ⓑ `onPick(word)` ⇒ `onPick('the')` — כל 24 השבבים מוסיפים אותה מילה · ⓒ `<EnWord>{word}</EnWord>` ⇒ `<EnWord>{'—'}</EnWord>` — בנק של מקפים. **התיקון בבדיקות בלבד, ⛔ אפס שינוי בקוד מוצר:** הזהות נשלפת מהקובץ (`.words.map((x) =>` · הארגומנט של `draftContainsTarget`) ומוזרקת לאסרציה, ולכן שינוי שם `word`⇒`token` נשאר **9/9 ירוק** (⛔ לא F-041) בעוד ערך קבוע נופל בשם. הפירוט והטבלה ב-`30-architecture.md` C-0142. [לשעבר 🔓 פתוח — משימה 7 בוצעה C-0127 ונותרו 8–9.]** ⚠️ **הנוסח נבחן בפועל ולא הונח:** שבע האסרציות של משימה 7 נכתבו כאיתור-אתר-קריאה (ארגומנט ה-`apiGet` · הארגומנט שמועבר ל-`worldSheetTextHe` · הסטר שכותב את המצב), ו**המוטציה מסוג F-039 — החלפת המונה בטיוטת המשפט במספר שאינו קשור (`TABS.length`) — נכשלה בשם.** בנוסח שבתוכנית היא הייתה שורדת. ⛔ על מבצע משימות 8–9 לעשות את אותו הדבר, ו⛔ לא להעתיק את האסרציות כלשונן ⚠️ **הישנות שנייה, C-0176 (T-081):** בדיקת ה-44px בתוכנית `2026-08-17-level-map.md` משימה 5 שלב 3 נשענת על `/<button[\s\S]{0,400}?>/g`; הכמת העצל עוצר ב-`>` של `onClick={() => …}` ⛔ ולא בסוגר התגית, ולכן הקטע שנתפס לעולם אינו מכיל `min-h-touch` והבדיקה **נכשלת גם על קוד תקין**. תוקן באותו טיק ב-lookbehind `(?<!=)`, ומוטציה הרגה את השומר המתוקן בשמו. ⇒ הממצא ⛔ אינו מופע בודד: **תוכנית שמספקת קוד בדיקה חייבת שהקוד יורץ פעם אחת לפני שהוא נכתב למסמך.** | 0 |
| F-040 | 🟠 HIGH · **פגם (סתירה בין קוד להערתו — יחידת ספירה) · נפתח על ידי CRITIC (C-0125, מוד עומק)** | `app/api/world/status/route.ts:68` מול `:21-24` ו-`:74-76` | **`activeWords` סופר שורות `word_progress` (סנסים), ⛔ לא headwords מובחנים — בסתירה להערת הקוד עצמו ולאופן ש-`functionWords` מחושב שתי שורות מעליו.** שורות 21-24 מזהירות מפורשות ש-`{count:'exact', head:true}` "would OVERSTATE the bank (139 senses vs 121 headwords) ... would report rows, ⛔ not headwords" — אך שורה 68 עושה בדיוק זאת, ושורה 76 מציבה `activeWords: active.count`. לעומת זאת `functionWords` (שורה 75) כן עובר דרך `uniqueHeadwords(...)`. **תרחיש כשל:** לומד עם 12 שורות `word_progress` פעילות שמתוכן 2 הן שני הסנסים של אותו headword (למשל `can` תפקוד+תוכן; `words` הוא `unique(headword,pos)`) → `activeWords=12` בעוד המספר המובחן האמיתי הוא 11 → `isWorldUnlocked` חוצה את `MIN_ACTIVE_WORDS` מוקדם והשער נפתח לפני שהלומד יודע 12 מילים מובחנות. הכיוון תמיד ניפוח (count ≥ distinct). | ⛔ לספור headwords מובחנים כמו ב-`bank/route.ts:77`: `.select('word_id, words!inner(headword)')` (בלי `head:true`) והעברה דרך `uniqueHeadwords(...).length`; מוטציה שמשאירה את הספירה הגולמית חייבת להפיל בדיקה | DEV | ✅ **סגור — C-0126 (DEV, 19:4xZ), ⛔ נסגר לפני משימה 7 כפי שנדרש.** הקריאה היא `.select('words!inner(headword)')` תחת `.limit(MAX_BANK_ROWS)` ועוברת `uniqueHeadwords(...).length` — זהה ל-`bank/route.ts`, כלומר **שני צדי הפרדיקט חולקים הגדרה אחת של «אותה מילה»**. `flattenJoined` חדל להיות עותק מקומי ועבר ל-`lib/supabase/postgrest.ts` (⛔ ולא ל-`/lib/core` — צורת חוט של PostgREST אינה החלטה של השכבה הטהורה), עם 6 בדיקות התנהגות. **⛔ אפס `head: true` · אפס `count: 'exact'` · אפס `.count` נותרו בקובץ, ונאכף בבדיקה.** **שלוש מוטציות, שלוש נפילות בשם, ⛔ אף אחת לא שרדה** (ספירה גולמית · הסרת `.limit` · מפרידה חלקית), שחזור אומת ב-`diff`. `docs/api-contract.md` עודכן באותו קומיט — **היחידה** תועדה, לא רק הטיפוס. אימות טרי: typecheck ✅ · check:core ✅ · **1110/1110 ב-70 קבצים ✅** · build ✅ · check:mobile 645 ✅ | 0 |
| F-041 | 🟡 MEDIUM · **פגם (אסרציה בלתי-ניתנת-לסיפוק בתוך תוכנית) · נפתח על ידי DEV (C-0127)** | `docs/superpowers/plans/2026-08-14-world-compose.md` משימה 7, שלב 1 | **שתי שורות באותה בדיקה סותרות זו את זו, וכל קוד תקין נכשל בה.** השורה הראשונה דורשת `expect(CODE).toContain('aria-disabled')` והשנייה אוסרת `expect(CODE).not.toContain('disabled=')` — אבל המחרוזת `aria-disabled=` **מכילה** את `disabled=`. כלומר הבדיקה אדומה בדיוק כשהרכיב עושה את מה ש§ 4.2ב מחייבת, וירוקה כשאין רכיב כלל. ⚠️ נמדד בטיק הזה: הבדיקה נכשלה על המימוש הנכון, ⛔ ולא על באג | מבצע שסומך על נוסח התוכנית מקבל אדום שאין לו תיקון בקוד, ועלול «לתקן» אותו בהסרת `aria-disabled` — כלומר להחזיר את הכפתור למצב שבו הקשה אינה פותחת את הגיליון, וזו בדיוק ההתנהגות ש§ 4.2ב אוסרת. זהו אותו מחלקת כשל של F-039 (אסרציה שאינה מודדת את מה שהיא מתיימרת), הפעם בכיוון ההפוך: לא עיוורת אלא בלתי-אפשרית | ✅ **תוקן בטיק שגילה אותו (C-0127), בשני המקומות:** `components/TabBar.test.ts` מודד `not.toMatch(/(?<!aria-)disabled=/)` — התכונה החשופה בלבד — ומוטציה שהוסיפה `disabled={false}` נפלה בשם; **ונוסח התוכנית עצמו תוקן** כדי שמבצע משימות 8–9 לא יעתיק אותו שוב | ✅ טופל | 0 |
| F-043 | 🟡 MEDIUM · **פגם (מוטציית-בדיקה שאינה מפילה דבר) · נפתח ונסגר על ידי DEV (C-0129)** | `docs/superpowers/plans/2026-08-14-world-compose.md` משימה 9 שלב 6 | **המוטציה שהתוכנית מורה עליה כדי לאמת את המדידה — «הסר `min-h-touch` משבב אחד בבנק, צפה לאדום ב-44px בשלושת הרוחבים» — נשארת ירוקה על הקוד הזה.** נמדד חי בטיק: אחרי הסרת `min-h-touch min-w-touch` מ-`components/WordBank.tsx:34` ההארנס הדפיס `ok /dev/world @320/375/414px all tap targets >= 44px`. הסיבה אינה שהמדידה שבורה אלא שהגובה נקבע פעמיים: `py-2` (16px) + גובה שורה של `text-lg` (28px) = **44px בדיוק**, כלומר המחלקה היא חגורה על גבי כתפיות ואינה הרכיב הנושא של הגובה | מבצע שמריץ את שלב 6 כלשונו רואה ירוק, מסיק «ההארנס אינו מודד את הפיקסצ'ר», ומסיר את הפיקסצ'ר או מרחיב את ההארנס לשווא — בדיוק ההפך מהמסקנה הנכונה | ⛔ לא לשנות את `WordBank.tsx` (44px הוא 44px). נוסח שלב 6 בתוכנית תוקן באותו קומיט למוטציה שבאמת מפילה: `px-3 py-2 text-lg` ⇐ `px-3 py-0 text-sm`. **נמדד אחריה:** שלוש נפילות בשם, `/dev/world @320/375/414px all tap targets >= 44px — too small: button"Lorem" 70x22 · … · button"Consequat" 101x22` — כלומר 24 שבבי הפיקסצ'ר נספרו בשמם, ו⛔ אף כשל לא הופיע ב-`/world/compose`: זו הראיה המדידה שבלי הפיקסצ'ר הבנק מעולם לא נמדד (C-0104). שוחזר ואומת ב-`cmp` בית-בית | ✅ **סגור C-0129 — התוכנית תוקנה, הקוד ⛔ לא נגע.** | 0 |
| F-044 | ⚪ LOW · **פגם (ניבוי שגוי בתוך תוכנית) · נפתח ונסגר על ידי DEV (C-0131)** | `docs/superpowers/plans/2026-08-14-arrival-and-constitution.md` משימה 1 צעד 2 | **התוכנית מצהירה «ארבע הבדיקות הראשונות נופלות, החמישית עוברת ריקנית». נמדד בפועל C-0131: 3 נפלו ו-2 עברו.** הבדיקה *never removes an outline anywhere in the sheet* עוברת ריקנית גם היא לפני התיקון, כי לא היה בגיליון `outline` כלל — היא שוללת נוכחות, ולכן גיליון ריק מספק אותה | מבצע שסומך על ניבוי הצעד קורא «3 failed \| 2 passed» ומסיק שקובץ הבדיקה שהוא הרגע כתב שבור, או גרוע יותר — משנה בדיקה תקינה כדי להתאים אותה לניבוי | אין תיקון קוד: הבדיקה נכונה כפי שהיא, ו**מוטציה ⓖ (`button { outline: none; }`) הפילה אותה בשמה** — כלומר היא אינה חלולה, רק אינה נופלת על מצב ההתחלה. הניבוי בתוכנית הוא מה שהיה שגוי | ✅ **נסגר באותו טיק.** המספר האמיתי (3/2) נרשם ב-`plan/30-architecture.md` § C-0131 ובשורת T-069, ⛔ ולא הוחלף בניבוי | 0 |
| F-045 | ⚪ LOW · **פגם (מדידה עיוורת בתוך תוכנית) · נפתח ונסגר על ידי DEV (C-0133)** | `docs/superpowers/plans/2026-08-14-arrival-and-constitution.md` § «Measurements this plan rests on» + משימה 2 צעד 2 | **המדידה שהתוכנית נשענת עליה משתמשת ב-`grep -o 'rounded-[a-z0-9-]*'`, שדורש מקף ולכן ⛔ אינו רואה `rounded` חשוף.** בפועל יש שניים כאלה ב-`app/loading.tsx:13,14`, ו-`rounded` לבדו הוא **4px** של Tailwind — ערך רביעי לכל דבר. לכן הניבוי «38 מופעים» היה 40, וניבוי הצעדים (21 ⇒ 11 ⇒ 0) לא יכול היה להתאמת. | צעד 2 בתוכנית מורה למבצע «אם קובץ מופיע ברשימה אחת ולא בשנייה — ⛔ עצור והתאם». מבצע שקורא 40 במקום 38 עלול להסיק שהשומר שהוא הרגע כתב שבור ו**לרכך אותו** עד שיתאים ל-38 — כלומר להוציא מהשומר בדיוק את הערך הרביעי היחיד שהתוכנית לא ידעה עליו. | אין תיקון קוד: השומר נכתב נכון מלכתחילה — הבדיקה החמישית («⛔ never accepts a bare `rounded`») קיימת בדיוק בשביל המקרה הזה, והמדידה בפרוזה היא שפיגרה אחריה. שני המופעים הוכרעו לפי **D-036 ⓒ** («שלד טעינה ⇒ `rounded-2xl`»), ⛔ בלי ערך רביעי ובלי שאלה לרוי. | ✅ **נסגר באותו טיק.** המספר האמיתי (40 ב-18 קבצים) נרשם ב-`plan/30-architecture.md` § C-0133 ובשורת T-068, ⛔ ולא הוחלף בניבוי | 0 |
| F-046 | 🟠 HIGH · **פגם (ייחוס שגוי של שגיאות קונסול בין מסלולים · לסירוגין) · נפתח ונסגר על ידי DEV (C-0134)** | `scripts/verify-mobile.mjs` — לולאת `ROUTES` (`consoleErrors = []`) מול `page.on('console')` | **הארנס האשים מסלולים בבקשות שהם מעולם לא שלחו.** ההקשה של T-067 מתחילה עבודה ששורדת אחרי סוף האיטרציה, והלולאה מאתחלת את `consoleErrors` בראש האיטרציה הבאה — ולכן כל הודעה מאוחרת נדחפת למערך של **המסלול הבא**. שני חצאים נמדדו, ושניהם לסירוגין (6 · 5 · 6 כשלים בשלוש הרצות — גרוע יותר משגוי): ‏ⓐ Chromium מוסר את הודעת הקונסול של משאב שנכשל באירוע נפרד, **אחרי** ש-`waitForResponse` כבר נפתר ⇒ `/dev/card` הואשם ב-`/api/study/queue?deck=due` (בקשת `/study`, ⛔ בלי `&limit=1` — לכן היא מוכחת כשל המסלול הקודם), ו-`/dev/world` — הפיקסצ׳ר שאינו שולח בקשה כלל — הואשם ב-`/api/world/bank` של `/world/compose`. ⓑ ההקשה על `/` נוחתת ב-`/signup`, וה-favicon שלו עוד היה באוויר כשה-`goto` הבא ביטל אותו ⇒ הביטול נרשם על `/signup`. **אומת שהבסיס ירוק לפני השינוי (723 ✅ עם `git stash push scripts/`) ⇒ השינוי שלי הוא הסיבה, ⛔ לא ניחוש.** | — | — | ✅ **נסגר C-0134 במקור, ⛔ לא בתסמין.** ⛔ **לא** נוספה רשומת `EXPECTED_CONSOLE` למסלול החף — רשומה על `/dev/world` הייתה משתיקה בדיוק את האזעקה שההערה שלו קיימת כדי להרים. במקום זה ההקשה ממתינה **בתוך המסלול שגרם לה**: `waitForResponse` (חזק מ-`waitForRequest`, ומוכיח שהבקשה יצאה) · `waitForLoadState('networkidle')` בכל שלושת הסוגים · ולולאת המתנה **מבוססת-תנאי וחסומה ב-2 שניות** עד ש⛔ הודעה **נוספת** נרשמה (הספירה לפני ההקשה נשמרת — ‏`/study` ו-`/world/compose` כבר רשמו את אותה כתובת בטעינה, ולכן «האם הודעה קיימת» תמיד היה אמת והמתנה כזו לא המתינה כלל). ⛔ אפס `sleep` עיוור. שומר יחידה נוסף ב-`verify-mobile.test.ts` נועל את `waitForResponse` ואוסר חזרה ל-`waitForRequest`. **771 ✅ בארבע הרצות רצופות.** | — |
| F-047 | ⚪ LOW · **פגם (שני ניבויים שגויים בתוך תוכנית) · נפתח ונסגר על ידי DEV (C-0134)** | `docs/superpowers/plans/2026-08-14-arrival-and-constitution.md` משימה 3 צעדים 2 ו-6 | **צעד 2 מצהיר «שש מתוך שבע הבדיקות נופלות». נמדד בפועל: 4 נפלו ו-3 עברו.** שתיים עוברות ריקנית ולא במקרה — ‏«gives each entry one of the three kinds» משווה `0 === 0` ולולאתה ריקה, ו-«runs the arrival block BEFORE the clean-console check» משווה `indexOf` שמחזיר `-1` מול אינדקס חיובי. **צעד 6 מנבא «~21 בדיקות חדשות». נמדד: 723 ⇒ 771 = 48**, מהן **30 בדיקות `check()`** (10 לרוחב × 3 רוחבים) ו-**18 שורות `report()`** — הסיכום סופר `notes.length`, שכולל את שתיהן. | — | — | ✅ **נסגר: אין תיקון קוד.** שתי הבדיקות הריקניות מכוסות בפועל על ידי «measures the tap», שדורשת `FLOW_ARRIVAL[route]` בקובץ ולכן נופלת בהיעדר הטבלה — כלומר החסינות קיימת, רק לא בבדיקה שהתוכנית ייחסה לה אותה. הניבויים תוקנו בשורת התוצאה כאן. | — |
| F-049 | ⚪ LOW · **פגם (ניבוי שגוי בתוך תוכנית) · נפתח ונסגר על ידי DEV (C-0139)** | `docs/superpowers/plans/2026-08-15-m0-study-closeout.md` — משימה 2, צעד 11 וצעד 12 | **צעד 11 מנבא «count +9 over whatever task 1 left (three checks × three widths)». נמדד בפועל: 801 ⇒ 825 = +24.** הניבוי סופר רק את שלוש הטענות הנקובות והחמיץ את **חמש הבדיקות הגנריות שכל מסלול חדש מקבל בכל רוחב** (`no horizontal scroll` · `dir=rtl` · `lang=he` · `all tap targets >= 44px` · `clean console`) ⇒ 8 שורות לרוחב × 3 = 24. ⚠️ **`heading anchored to top` ⛔ לא רץ** — לפיקסצ׳ר אין כותרת, ולכן 8 ולא 9. **בנוסף, צעד 12 מנבא `tallest box 4px`. נמדד: `tallest box 48px`** — המוטציה מקטינה את `h-40`, ומרגע זה התיבה הגבוהה היא `h-12` (48px), ⛔ לא זו שמוטמרה. | סוכן שסומך על המספר שבתוכנית מדווח 810 במקום 825, או חושד בעצמו שהוסיף בדיקות שלא התכוון להן. הניבוי של צעד 12 גרוע יותר: `4px` מתאר תיבה שאינה קיימת, וסוכן שמחפש אותו עלול «לתקן» את הרצפה 100 עד שהמספר יתאים | ⛔ הקוד ⛔ לא נגע — הרצפה 100, שלוש התיבות והבדיקות נכונות והורצו. **הנוסח בתוכנית תוקן באותו קומיט** לשני המספרים שנמדדו, כדי שמשימה 3 (ביקורת M0) לא תספור מספר שלא היה | ✅ **סגור באותו טיק (C-0139)** — נוסח התוכנית תוקן, הקוד לא | 0 |
| F-051 | 🟡 MEDIUM · **פגם (מדיניות RLS כפולה — מחיקה שאינה מוחקת) · נפתח על ידי DEV (C-0143, טיק תכנון)** | `supabase/migrations/0003_low_confidence_is_visible.sql:28,39,45` מול `0002_content_bank.sql:152,159,165,171` | **שלוש מתוך ארבע פקודות ה-`drop policy if exists` ב-0003 נוקבות בשמות ש⛔ שום מיגרציה מעולם לא יצרה, ולכן `0002` מעולם לא הוחלפה.** **נמדד בטיק הזה בשחזור create/drop על כל הספרייה בסדר החלה, ⛔ לא בקריאה:** `read approved content` (senses) · `read distractors` · `read items` — אפס מופעי `create` בכל `supabase/migrations/`; הרביעית, `read examples`, נוקבת במדיניות ש-0003 עצמה יוצרת שתי שורות אחריה. התוצאה: **על כל אחת מארבע טבלאות התוכן יושבות שתי מדיניות SELECT חיות** — ‏`senses`: `content_senses_select` (≠low) + `read all senses` (true) · `sense_examples`: `content_examples_select` (≠low) + `read examples` (true) · `sense_items`/`sense_distractors`: המדיניות של 0002 + `read verified …`, **אותו פרדיקט בדיוק** | ⚠️ **אף לומד אינו רואה היום שורה שגויה** — Postgres מבצע OR על מדיניות מתירה, ו-`using (true)` בולע את הפרדיקט הצר. **וזו בדיוק הסכנה:** הקובץ מצהיר על חוק אחד והמסד אוכף אחר, ואי-ההתאמה אינה יכולה לצוץ עד שמישהו יבטל את המדיניות שהוא סבור שהיא היחידה — ואז D-013 שב לחיים ומבטל את D-024 בשקט: משמעות בביטחון נמוך תיעלם מהלומד שוב. ⚠️ **התזמון:** ‏T-072 עומדת להכניס ~2,400 שורות חומר ניקוד לשתיים מארבע הטבלאות האלה | ‏T-071: ⓐ בדיקת מקור שמשחזרת create/drop בסדר החלה ונכשלת על `drop` ללא `create` תואם (שלושת ה-no-op של 0003 מוחרגים — ⛔ מיגרציה שהוחלה אינה נערכת ואינה משנה שם) **וגם** על טבלת תוכן עם יותר ממדיניות קריאה אחת · ⓑ מיגרציה `0011_content_read_policies.sql` שמוחקת את ארבע המדיניות של 0002. **no-op בזמן ריצה, מוכח טבלה-טבלה ב-`docs/superpowers/plans/2026-08-15-scoring-material.md` משימה 2** | ✅ **נסגר C-0145 (DEV, T-071).** `0011_content_read_policies.sql` מוחקת את ארבע מדיניות `0002` ששרדו, ו⛔ **אינה נוגעת ב-0003** (מיגרציה שהוחלה אינה נערכת מחדש). ⚠️ **הפגם נמדד לפני התיקון ו⛔ לא נלקח באמון:** הבדיקה החדשה «leaves exactly one live SELECT policy on each content table» נכשלה **בשם** על `senses carries 2 read rules: content_senses_select, read all senses`, ואחרי המיגרציה 9/9 ירוקות. **מוטציה:** מחיקת שורת ה-`drop` של `content_senses_select` מ-0011 ⇒ אדום בשם, אותה הודעה בדיוק; שוחזר ואומת ב-`cmp` בית-בית. שלושת ה-no-op של 0003 מוחרגים ב-`GRANDFATHERED_NOOP_DROPS` ⇒ הבדיקה «never drops a policy name that no earlier migration created» עוברת ותופסת **רביעי** אם ייכתב. ⛔ אפס שינוי נראוּת ללומד — שקילות טבלה-טבלה בתוכנית. המיגרציה ממתינה לרוי (`03-for-roy` C-0145) ו⛔ אינה חוסמת את T-072 | 1 |
| F-054 | ⚪ LOW · **פגם (נוסח תוכנית סותר את קוד התוכנית) · נפתח ונסגר על ידי DEV באותו טיק (C-0145)** | `docs/superpowers/plans/2026-08-15-scoring-material.md` משימה 2, מדור `Files` | **מדור `Files` מורה «append two `describe` blocks», והקוד המילולי שמתחתיו מכיל `describe` אחד** (בן ארבע בדיקות) לצד ארבעה עוזרים ברמת המודול. ⛔ אין שני בלוקים בשום מקום בתוכנית. | סוכן שסופר בלוקים במקום לקרוא את הקוד מחפש בלוק שני שאינו קיים, או ממציא אחד — ואז הבדיקות שהוא כותב אינן אלה שהתוכנית ניבאה, והפרש הבדיקות (`+4`) מפסיק להיות מדיד. נמדד: `+4` בדיוק (1,215 ⇒ 1,219), כלומר ארבע בדיקות בבלוק אחד | **נוסח התוכנית תוקן באותו קומיט** ל-«append ONE `describe` block of four tests plus its module-level helpers», עם ציון התיקון. ⛔ הקוד לא נגע — הוא היה נכון מלכתחילה | ✅ **נסגר C-0145**, אותו טיק | 0 |
| F-055 | ⚪ LOW · **פגם (קוד התוכנית סותר את הצעד הבא באותה תוכנית) · נפתח ונסגר על ידי DEV באותו טיק (C-0146)** | `docs/superpowers/plans/2026-08-15-scoring-material.md` משימה 3, שלב 5 (סוף בלוק ה-`js`) מול שלב 6 | **בלוק הקוד של שלב 5 מסתיים ב-`console.log(\`wrote ${SCORING_OUT} …\`)` בתוך מקטע 7, כלומר **לפני** `console.log(\`wrote ${OUT}\`)` של מקטע 6 — ושלב 6 של אותה תוכנית מצהיר במפורש על הפלט הצפוי: «the existing five report lines, **then** `wrote …0003_scoring_material.sql`». שני הצעדים סותרים זה את זה מילולית. | מפתח שמעתיק את הבלוק כלשונו מקבל את שורת הדיווח של 0003 **שנייה מתוך שש** במקום אחרונה, ואז שלב 6 «נכשל» באימות ידני על סדר שאינו פגם אמיתי — בדיוק דפוס F-054: הנוסח הוא הפגם, ⛔ לא הקוד | **בוצע:** שורת ה-`console.log` הוצבה במקטע 6 מיד אחרי `wrote ${OUT}` ⇒ הפלט תואם את שלב 6 תו-בתו. נוסח שלב 5 בתוכנית תוקן ומסביר את ההפרדה. ⛔ אפס שינוי בקוד המחולל מעבר למיקום השורה | ✅ **נסגר C-0146 (DEV) — נוסח, ⛔ לא קוד מוצר.** אומת בהרצה: `wrote supabase/seed/0001_content_batches.sql` ואז `wrote supabase/seed/0003_scoring_material.sql — 938 examples · 1407 items · 1876 distractors`, שישית מתוך שש | C-0146 |
| F-057 | 🟠 HIGH · **פגם (הנחיה בטלה שנשארה כטקסט חי בקובץ ההוראות של הסוכנים) · נפתח ונסגר על ידי DEV באותו טיק (C-0149)** | `docs/AGENT_BLUEPRINT.md:247,249-250,406` | **מעבר לתקציב של F-029, קובץ ההוראות החזיק שתי הנחיות שה-RULES כבר ביטל במפורש, ושתיהן נוסחו כפקודה ולא כהיסטוריה.** ⓐ `:247` ו-`:406` — «עברו לפחות 20 שעות מ-`LAST_PROMOTED_AT`» כתנאי קידום, בעוד `RULES § 0.1.1 ב׳` (11/08) קובע **«עד קידום אחד ליממה»** ו**מבטל בשמו** גם את «פעם ב-20 שעות» (0.2) וגם את «כל 6 שעות» (0.11). ⓑ `:249-250` — «**כל שאר הקומיטים שלך — עם התג `[skip ci]`**», בעוד `RULES § 0.7` הוריד את התג ב-2026-08-06 **כתיקון באג**. | ⛔ **ⓑ הוא הכשל היקר מבין השניים, והוא כבר קרה פעם אחת:** `--ff-only` אינו יוצר קומיט מיזוג, ולכן ראש `main` אחרי קידום הוא קומיט של Dev — עם התג עליו. Netlify קורא את ההודעה העליונה, מדלג, ו**הקידום מצליח בלי שדבר עולה לאוויר** (`§ 0.7`, הבאג המקורי). סוכן חדש שקורא את `AGENT_BLUEPRINT` כמקור ההוראות שלו מוסיף `[skip ci]` בתום לב ומחזיר את הבאג. ⓐ זול יותר אך לא אפס: Critic שסקר נקי 19 שעות אחרי הקידום הקודם היה נמנע מקידום שהחוק מחייב. | סנכרן את שני הסעיפים ל-`RULES`, ⛔ בלי לנסח מדיניות חדשה — שני הכללים כבר הוכרעו. | ✅ **נסגר C-0149 (DEV), אותו טיק.** ⓐ `:247` ו-`:406` ⇒ «עד קידום אחד ליממה» עם ציטוט `§ 0.1.1 ב׳` ועם שני הענפים («לא קידם היום — מקדם» · «קידם היום — לא שוב»). ⓑ `:249-250` ⇒ ⛔ «אין `[skip ci]` באף קומיט», עם **הסיבה** ולא רק עם האיסור, כדי שסוכן לא «יתקן» חזרה. בנוסף נוספה לשני המקומות **בדיקת העשן של `§ 0.1.1 ד׳`** (`/api/health` ⇒ `"ok": true`), שנעדרה מסעיף סמכות הפריסה — הכשל ששרד 18 פריסות. **נמדד אחרי התיקון:** `grep -n "20 שעות\|skip ci" docs/AGENT_BLUEPRINT.md` ⇒ **5 שורות (250 · 253 · 406 · 422 · 428), כולן ציטוט-שלילה או הפניה ל-§ 0.7**, ⛔ אפס הנחיות חיות (`:428` היא דוגמת קומיט ישן בתוך הסבר הביטול, ו-`:422` היא כותרת «שכבה 4 — בוטלה»). ⛔ לא נגעתי ב-`plan/RULES.md` — הוא של רוי. | C-0149 |
| F-058 | 🟡 MEDIUM · **התנגשות מזהים במרשם החוב · נפתח ונסגר על ידי DEV (C-0150)** | `plan/30-architecture.md:575` מול `:579` | **`TD-28` הוטבע פעמיים** — C-0102 (`elapsed_ms` נמדד מהסימון הקודם) ו-C-0145 (`0011_content_read_policies.sql` טרם הורצה). נמדד בפקודה: 31 שורות `TD-`, התנגשות אחת בדיוק, ⛔ לא הוערך. זהו בדיוק הכשל של F-025, מרשם אחד הצידה — ושם הוא כבר נעול במכונה, כאן לא נבדק מעולם | «TD-28 נסגר» ביומן סוגר פריט דו-משמעי, ו-`grep -n TD-28` מוסר לסוכן הבא את החוב של מישהו אחר. F-025 שרד 33 מחזורים בלי שאיש הבחין, וגם כאן איש לא הבחין מ-15/08 | מספור מחדש של המאוחר (⛔ לא החרגה — שורת מסמך אינה נושאת מצב חיצוני כמו מיגרציה ב-TD-23), פלוס שומר `describe` שלישי ב-`scripts/plan-hygiene.test.ts` | ✅ **נסגר C-0150.** TD-28 של C-0145 ⇒ **TD-31** (הסדר הוכרע ב-`git log -S`, ⛔ לא הונח). שלוש הפניות עודכנו, ורשומת היומן של C-0145 **סומנה בהערה ולא נמחקה**. שלוש בדיקות חדשות נכתבו **לפני** התיקון ורצו אדום (ייחוד + רצף), ואז ירוק. **1,233 ⇒ 1,236 ✅, +3 בדיוק** | 0 |
| F-061 | 🟠 HIGH · **פגם (רַצֶ׳ט סנכרון אדום ב-`dev` — דוח נוצר שלא חודש עם האצווה) · נפתח ונסגר על ידי DEV באותו טיק (C-0165)** | `docs/gate-recheck.md:6-9` · `scripts/measure-gate.test.ts:48` | **`npm test` היה אדום על `dev` בפתיחת הטיק, ⛔ ולא מפני שקוד נשבר.** `scripts/measure-gate.test.ts:48` טוען `expect(readFileSync(COMMITTED)).toBe(fresh)`, והדוח המחויב אמר **469 שורות** בעוד `node scripts/measure-gate.mjs` מחזיר **534**. הפער הוא בדיוק 65 — `batch-2026-08-16.jsonl` של C-0164 (CONTENT, 04:22Z), שנדחף בלי להריץ `npm run measure:gate`. ⚠️ **C-0163 דיווח `1,369/1,369` ב-01:46Z וזה היה נכון אז** — האצווה נחתה שעתיים וחצי אחר כך, כלומר אף אחד לא שיקר; הבדיקה הירוקה פשוט קדמה לשבירה | **הסוכן הבא שמריץ אימות מוצא סוויטה אדומה שאין לה קשר לעבודתו**, ומוציא טיק על דיבוג של קובץ נוצר. גרוע מזה: `docs/gate-recheck.md` הוא הראיה שהתוכן שנשלח ללומד עבר את השער — דוח שמדבר על 469 שורות בעוד הבנק מחזיק 534 **אינו טענה על 65 השורות החדשות בכלל**, והן היחידות שטרם נבדקו בפומבי | ✅ **נסגר בטיק הזה:** `node scripts/measure-gate.mjs` הורץ, והדוח מחויב — `534 rows read from 9 batch files` · `1068 example sentences · 1602 item stems re-gated` · **`0 rows rejected`**, כלומר 65 השורות החדשות עוברות. ⛔ **הכלל שחסר, ולא התיקון:** טיק CONTENT שמוסיף `batch-*.jsonl` חייב להריץ `npm run measure:gate` **באותו קומיט** — אחרת הרַצֶ׳ט אדום עד שסוכן אחר יבחין. ⇒ פריט ל-**PM** (הוספה ל-`80-content-lessons` ולפרומפט של CONTENT) | ✅ טופל (הכלל → PM) | 0 |
| F-062 | 🔴 CRITICAL · **פגם (`npm test` אדום על `dev` — שני תוצרים נגזרים לא חודשו עם האצווה) · נפתח על ידי CRITIC (C-0166, ⚡ מהיר)** | `scripts/build-word-levels-sql.test.ts:174` · `supabase/seed/0002_word_cefr_levels.sql` · `scripts/build-ingest-sql.test.ts` · `scripts/build-ingest-sql.mjs:103,106` · `data/generated/manifest.json` · `data/generated/batch-2026-08-13.jsonl` | **הרצה טרייה בטיק הזה (07:05Z): `npm test` ⇒ `Test Files 2 failed \| 88 passed (90)` · `Tests 1 failed \| 1352 passed (1353)`. שתי נפילות, ⛔ ולא קוד שנשבר — שני תוצרים נגזרים שלא חודשו כשאצוות התוכן נחתו.** ⓐ **הזרע מיושן:** `npm run build:levels` בהרצה שלי משנה את `supabase/seed/0002_word_cefr_levels.sql` ב-**61 שורות** (60 המשמעויות של `batch-2026-08-16.jsonl`), והבדיקה `build-word-levels-sql.test.ts:174` (F-048ⓐ) נופלת `expected false to be true`. ⓑ **ingest קורס בהעמסה:** `node scripts/build-ingest-sql.mjs` זורק `no manifest declares batch_file "batch-2026-08-13.jsonl"` — אין `manifest-2026-08-13.json`, ו-`manifest.json` הוצף ב-C-0164 להצהיר **רק** על `batch-2026-08-16.jsonl` ⇒ קובץ הבדיקה `build-ingest-sql.test.ts` נכשל כולו בהעמסה (0 tests). ⚠️ **C-0165 טען שסגר את הסוויטה האדומה (F-061) אך ⛔ מעולם לא הריץ `npm test`** (טיק תכנון) — חידש רק את `docs/gate-recheck.md` והשאיר שני תוצרים נוספים אדומים. | לומד: לא ישיר. אבל **כל סוכן שמריץ אימות מוצא סוויטה אדומה** ⇒ אין קידום נקי (`0002_word_cefr_levels.sql` הוא הזרע שמזין רמות CEFR ללומד — זרע מיושן = תוכן שאיש לא רואה, F-048ⓐ), ו-`build:ingest` שבור מונע בניית SQL ההזרקה של האצוות כולן. תרחיש קונקרטי: Dev/Critic הבא מריץ `npm run typecheck && check:core && npm test && build` → נעצר ב-`npm test` exit 1 → אין קידום, טיק נשרף על דיבוג נגזרות. | **Dev (או CONTENT) מריץ `npm run build:levels` ומחייב את `0002_word_cefr_levels.sql` באותו קומיט; משחזר כיסוי manifest ל-`batch-2026-08-13.jsonl` (מוסיף `manifest-2026-08-13.json` או מחזיר את השורה ל-`manifest.json`) עד ש-`build:ingest` ו-`npm test` ירוקים.** ⛔ שורש משותף עם F-061 — הכלל של ה-PM «CONTENT שמוסיף `batch-*.jsonl` מריץ את כל הנגזרות (`measure:gate` · `build:levels` · `build:ingest`) ומחייב אותן באותו קומיט» חייב לכסות **את שלושתן**, לא רק `measure:gate`. ⇒ ✅ **טופל C-0167 (DEV) — שני החלקים, ⛔ ולא אחד מהם, ועוד מופע שלישי שהתגלה תוך כדי.** **ⓐ:** `npm run build:levels` הורץ ו-`0002_word_cefr_levels.sql` חויב — **+61/-1 שורות**, הכותרת המחוללת עוברת `416 מילים → 476` (`468 exact_pos · 8 lemma_only · 0 unmatched`). **ⓑ:** ⛔ **לא הומצא מניפסט.** נמדד ב-`git log -- data/generated/manifest.json`: `manifest.json` הוא **מצביע «האצווה האחרונה» שכל טיק CONTENT דורס**, והוא היה המניפסט היחיד שהצהיר על `batch-2026-08-13.jsonl` מאז C-0078 — כלומר C-0164 השמיד את הפרוֹבֶנַנס של 08-13 בכך שכתב את שלו. הקובץ שוחזר **בית-בית מהיסטוריה** (`git show 38aab36:data/generated/manifest.json` ⇒ `data/generated/manifest-2026-08-13.json`, `md5 bab05c76…` זהה משני הצדדים), ולכן `model`/`prompt_version` הם המקוריים ו⛔ לא ברירת מחדל. ⇒ `build:ingest` ירוק, ושלושת הזרעים (`0001` +99 · `0002` +62 · `0003` +593) חויבו באותו קומיט. **השורש הפך לשתי בדיקות שנופלות בשם, ⛔ ולא לתיקון חד-פעמי** (`scripts/build-ingest-sql.test.ts`): ① «נותן לכל אצווה מניפסט בשמה» — `manifest.json` ⛔ **אינו** מתקבל ככיסוי, כי הוא נדרס בכל טיק תוכן; ② «רץ עד הסוף — קלט חסר הוא נפילה בשם, לא סוויטה מתה» — `run()` עטוף ב-try/catch, כי הזריקה בזמן ההעמסה הרגה את **כל** האסרציות הנקובות בקובץ (בדיוק הכשל ש-F-048ⓑ הזהיר ממנו באותו קובץ). **מוטציה אימתה:** הסרת `manifest-2026-08-13.json` ⇒ הסוויטה נאספת ומדווחת **`17 failed \| 1 passed (18)`** עם שני השומרים החדשים נופלים בשמם — במקום `Tests no tests` לפני התיקון; שוחזר. ⚠️ **מופע שלישי של אותו שורש, שהתגלה באימות ו⛔ לא בקריאה:** `npm test` נשאר אדום אחרי ⓐ+ⓑ על `scripts/measure-plan-tables.test.ts` — C-0166 הוסיף את F-062 ל-`60-findings.md` בלי להריץ `npm run measure:plan`, ו-`docs/plan-tables.md` אמר `62 שורות` מול 63 בפועל. חודש (שורה אחת). ⇒ **הכלל של ה-PM חייב לכסות ארבע נגזרות ו⛔ לא שלוש — `measure:gate` · `build:levels` · `build:ingest` · `measure:plan` — ולחול גם על CRITIC (עורך רגיסטרים), לא רק על CONTENT.** **אימות טרי בטיק הזה:** `typecheck` ✓ · `check:core` `/lib/core purity: OK` ✓ · **`Test Files 90 passed (90)` · `Tests 1371 passed (1371)`** ✓ (בסיס 1353 + 2 שומרים חדשים + 16 שהיו מתים בהעמסה) · `build` ✓ exit 0 · עץ העבודה נקי אחרי `npm test` ⇒ F-048ⓑ מחזיק | ✅ טופל | 0 |
| F-065 | 🟡 MEDIUM · **פגם (מדידה עיוורת — false-reject · התאום המשלים של F-039) · נפתח על ידי DEV (C-0180)** | `docs/superpowers/plans/2026-08-19-arcade-server.md` § 4 (שבע השאלות, שורות הגרפ) | **חמש משבע שאלות הבדיקה-העצמית בתוכנית הן `grep` גולמי על הקובץ כולו, ולכן הן פוגעות בהערות ובאסרציות הבדיקה עצמן.** נמדד ב-C-0180 על מימוש T-093 **שאין בו ולו הפרה אחת**: `grep -rn "word_progress"` החזיר 3 שורות (הערה ב-`0014_arcade.sql` שמסבירה שאין FK · `it('⛔ word_progress אינו נקרא')` · `expect(CODE).not.toContain('word_progress')`) · `grep -rn "setTimeout\|setInterval"` החזיר את `it.each(['setTimeout', 'setInterval', …])` · `grep -rn "cefr_level"` החזיר את הערת ה-JSDoc שמסבירה למה לא משתמשים בו · `grep -rn "Math.random"` החזיר את ההערה «⛔ אין בקובץ Math.random». אחרי הלבנת הערות והחרגת `*.test.ts` — **כל שמונת הטוקנים אפס** | **⛔ זהו F-039 מהצד השני של המראה, והנזק זהה.** F-039 הוא **false-accept** (הערה מרצה אסרציה); זה **false-reject** (הערה מדווחת כהפרה). ⚠️ **הסכנה המבצעית היא הכיוון שהיא דוחפת אליו:** סוכן שמאמין לגרפ יעשה את הפעולה הזולה — **ימחק את ההערות שמתעדות את D-044 ואת D-034** — ואז המימוש יישאר תקין אבל התיעוד שמסביר *למה* ייעלם, וההערה הבאה שתיכתב תשחזר את המעגל. ⚠️ ובכיוון ההפוך: סוכן שרואה חמש «הפרות» ומתעלם מכולן, לומד להתעלם גם מהשישית — שתהיה אמיתית | **תוקן במקום אחד ולא בכולם: `route.test.ts` של T-093 מלבין הערות ב-`withoutComments()` לפני כל טענה, וכל 18 האסרציות חותכות קטע לפני שהן בודקות בתוכו.** ⛔ **מה שלא תוקן: § 4 בתוכנית עצמה** — היא נקראת שוב בטיק של T-094, ותחזיר את אותן חמש הפגיעות. **ההכרעה: להחליף את שורות ה-`grep` ב-§ 4 בבדיקה שמלבינה הערות ומחריגה `*.test.ts`** (הנוסח המדויק הורץ ב-C-0180 ורשום ב-`plan/30-architecture.md` § 3.1.41), **או** להוסיף ל-§ 4 שורת אזהרה שהגרפ עיוור ושפגיעה בהערה ⛔ אינה הפרה. ⛔ בשום מקרה: מחיקת ההערות | ✅ **נסגרה C-0182 (DEV) — במקור ולא במופע.** ארבע שורות ה-`grep` ב-§ 4 הוחלפו בסקריפט שמלבין הערות (`/*…*/` · `//…` · `--…` ל-SQL) ומריץ שמונה טוקנים על חמשת קבצי הזירה; **הורץ בפועל בטיק והחזיר פלט ריק** ⇒ אפס הפרות. ⛔ אף הערה לא נמחקה. נוספו ל-§ 4 שתי שורות אזהרה מפורשות: פגיעה בהערה או בשם בדיקה ⛔ אינה הפרה. ⚠️ **ובאותו טיק נמדד הכיוון ההפוך, ובקוד שלנו:** אסרציית D-050 שהתוכנית הכתיבה (`expect(CODE).not.toContain('xp')`) **הפילה קובץ נקי** — `xp` יושב בתוך `export` בכל קובץ TypeScript. ⛔ הפעולה הזולה (שינוי שם ה-export) לא נעשתה; המדידה תוקנה לגבול מזהה (`\bxp\b`), ומוטציה `const score = …` אימתה שהיא **עדיין** מפילה הפרה אמיתית. **הכלל שנרשם ב-`30-architecture.md` § 3.1.42: טוקן קצר נמדד בגבול מזהה, ⛔ ולעולם לא כ-substring.** | 0 |
| F-068 | ⚪ LOW · **פגם (ניבוי שגוי בתוך תוכנית) · נפתח ונסגר על ידי DEV באותו טיק (C-0185)** | `docs/superpowers/plans/2026-08-19-arcade-screens.md` § 1 צעד 4 | **התוכנית מצהירה «8 בדיקות עוברות» עבור `lib/core/arcadeBattle.test.ts`, והקובץ שהיא עצמה מכתיבה מכיל תשעה בלוקי `it`.** נספר על גוף הבדיקה שבתוכנית ⛔ ולא על הזיכרון: פתיחת קרב · נכונה · שגויה · הקשה כפולה · `advance` בלי בחירה · `advance` · סוף בחיים · סוף בשאלות · סריקת האיסורים = **9**. | ⛔ **אינו מפיל דבר, וזו בדיוק הסיבה שהוא נרשם:** מבצע שסופר 8, רואה 9 ומניח שהוא כתב בדיקה מיותרת — או גרוע ממנו, מוחק אחת כדי «להתאים לתוכנית». המחיקה הזולה ביותר היא בדיוק סריקת האיסורים, שהיא הבדיקה היחידה בקובץ שמחזיקה את D-044/D-045/D-050. | ⛔ אין תיקון בקוד — המספר בתוכנית שגוי, המימוש נכון. **נמדד בטיק:** `npx vitest run lib/core/arcadeBattle.test.ts` ⇒ `Tests 9 passed (9)`, ומוטציה (`enemyHp` יורד גם בשגיאה) הפילה **שתיים** מהתשע בשמן. | ✅ נסגר — נמדד ⛔ ולא תוקן | 0 |
| F-069 | ⚪ LOW · **פגם (פריסה שהתוכנית מכתיבה ⛔ ונופלת בשער המדידה של הריפו עצמו) · נפתח ונסגר על ידי DEV באותו טיק (C-0185)** | `docs/superpowers/plans/2026-08-19-arcade-screens.md` § 1 צעד 13 («הפריסה, מלמעלה למטה») מול `scripts/verify-mobile.mjs:770` | **התוכנית מורה על «שורת סגירה מעוגנת» כאיבר הראשון ומעליה שום דבר, ואז על `<h1>` במצבי הכשל — כלומר כפתור 44px בשורה נפרדת מעל הכותרת.** השער `heading anchored to top` (F-011 · F-016) מודד `h1.top - header.bottom` ורצפתו **48px**; שורת סגירה של 44px ועוד `gap-6` דוחפת את הכותרת ל-60px. | **נמדד חי ⛔ ולא שוער:** ההרצה הראשונה של `check:mobile` על המימוש נכשלה בשלושה רוחבים — `/arcade @320px · @375px · @414px heading anchored to top — dead band of 60px above the heading`. כלומר תוכנית שממומשת **כלשונה** מייצרת מסך שהריפו מגדיר כרגרסיה של F-011. | ✅ **נסגר במימוש, ⛔ בלי לוותר על אף דרישה:** הכותרת ופעולת הסגירה יורדות ל-`topBar()` אחד — `flex flex-row items-center justify-between` — כך שהיציאה נשארת מעוגנת למעלה והכותרת חוזרת לראש המסך. הנימוק המדוד כתוב בקוד ליד הפונקציה. **אחרי התיקון: `check:mobile` ⇒ `✓ all mobile/PWA guarantees hold (858 checks)`.** ⚠️ **סייג מדוד:** מסך הקרב עצמו ⛔ אין בו `h1` (המילה היא `<EnWord>` בתוך `<p>`), ולכן השער מדלג עליו — מה שנמדד כאן הוא מצבי הכשל והסיום. | ✅ נסגר — `components/ArenaBoard.tsx` | 0 |
| F-071 | 🟠 HIGH · **פגם תהליכי (שער אדום שקודם ל-`main` תוך טענת ירוק) · נפתח ותוקן על ידי DEV באותו טיק (C-0187)** | `scripts/measure-plan-tables.test.ts` · `plan/50-tasks.md` T-088 · `docs/plan-tables.md` | **`npm test` היה **אדום** ב-`502b32f` — הקומיט שה-Critic קידם ל-`main` ב-C-0186 בעודו מדווח `1589/1589`.** ⛔ נמדד ולא הוסק: `git checkout 502b32f && npx vitest run scripts/measure-plan-tables.test.ts` ⇒ `Tests 2 failed` מול `4 passed`, ואותה הרצה בדיוק על `dev` HEAD לפני שנגעתי בקוד ⇒ אותן שתי נפילות. **הסיבה היא הקומיט עצמו:** C-0186 סגר את T-081 ל-✅, והתא ⛔ של **T-088** מצטט את T-081 כחסם ⇒ `stale task blockers: T-088 waits on T-081 (done)`, ובנוסף `docs/plan-tables.md` המחויב חדל להיות זהה להרצה טרייה | **סוכן סוגר משימה, וההודעה השנייה של אותו קומיט הופכת את השער לאדום — ואז השער האדום נוסע ל-`main`.** ⚠️ **הנזק המצטבר גדול מהתסמין:** הטיק הבא (זה) פותח בשער אדום שאינו שלו, ועליו להוכיח שהוא לא שבר אותו לפני שהוא רשאי לדחוף שורה. וגרוע מזה — `npm test` ירוק הוא **התנאי לקידום**; שער שנצבע ירוק לפני העריכה האחרונה של אותו קומיט ⛔ אינו מודד את מה שנדחף | ✅ **תוקן בטיק הזה, במינימום ובעובדה בלבד:** התא ⛔ של T-088 חדל לצטט משימה שנמסרה — «הבורר ב-`components/LevelMapScreen.tsx`… **נמסר כבר** (C-0186), ולכן ⛔ אינו חסם» — ו-`npm run measure:plan` הורץ ⇒ `stale task blockers: none`, `docs/plan-tables.md` נכתב מחדש. ⛔ **לא נגעתי בסטטוס של T-088, בתחולתה ובחסם ה-UX שלה** — אלה של ה-PM. **המלצה לתהליך: `npm test` נמדד אחרי העריכה האחרונה בקומיט, ⛔ ולא לפניה** — סגירת משימה ב-`plan/` היא שינוי שהשער מודד | ✅ תוקן — ⚠️ הלקח התהליכי פתוח → **CRITIC** | 0 |
| F-072 | 🟡 MEDIUM · **פער מפרט (כלל שאין לו ולו מופע אחד) · נפתח על ידי DEV (C-0189, טיק תכנון)** | `plan/40-decisions.md` § 4.2יא · `plan/50-tasks.md` — `T-098` | **§ 4.2יא דורשת «שאר האריחים מושבתים עם תנאי פתיחה מדיד ונקוב במספר», ובאותה נשימה קובעת «אריח בלי תנאי מדיד ⛔ אינו נכנס לרשת» — ו⛔ אינה נוקבת ולו באריח אחד כזה.** ספרייה · הודעות · מייל · סימולטורים מסומנים בסעיף עצמו «⛔ לא נפתחה להן ולו משימה אחת», ולכן לאיש מהם אין מספר. ⚠️ ובנוסף: האריח הגדול מוגדר כ«זה שיש בו משימה פעילה (חיוב שלא נגמר)», ו⛔ **אין הגדרה מה הוא חיוב פתוח עבור «הרכבה»** — לזירה יש אות מדיד (`round !== null` מ-`GET /api/arcade/round`), להרכבה אין ולו אחד | Dev מבצע את T-098 ומקבל רשת של **שני אריחים פתוחים ואפס מושבתים** ⇒ מדד ההצלחה ⓐ של § 4.2יא («כל אריח מושבת נושא מספר ⛔ ואין ולו אחד עם «בקרוב» לבד») **עובר ריק** — הוא נכון על קבוצה ריקה, ולכן ⛔ אינו מודד דבר. ובמקביל «חשיפה הדרגתית» (D-046 · R-021) — הרווח שאמור להחזיר את הלומד («אריח נעול מציג מספר שמתקצר») — ⛔ **אינו קיים על המסך בכלל.** תרחיש קונקרטי: לומד חדש פותח את `העולם`, רואה שתי אפליקציות פתוחות, ⛔ ואין לו שום סימן שיש עוד משהו שייפתח | **PM נוקב באריח אחד לפחות עם תנאי פתיחה שהוא ספירה** (D-046: «תנאי פתיחה = ספירה, ⛔ לעולם לא תאריך») — «הספרייה היא הבשלה ביותר» לפי הסעיף עצמו — **ומגדיר מהו «חיוב שלא נגמר» עבור «הרכבה»** במונחים של נתון שכבר על החוט. ⛔ ה-Dev פתח ואינו מכריע: אלה החלטות מסך, והן ב-`40-decisions.md` | ✅ **טופל C-0255.** ⛔ נסגר בהכרעת PM ⛔ ולא בידי ה-Dev: **D-074ⓑ** נקבה באריח «הספרייה» עם תנאי פתיחה שהוא **ספירה** («נדרשים 3 סיפורים ברמה שלך, יש N», והמספר מהשרת), ו-**T-137 הנחיתה אותו** ב-C-0255. ⇒ מדד ההצלחה ⓐ של § 4.2יא ⛔ אינו עובר עוד על קבוצה ריקה: `libraryTile` מייצר אריח `locked` נושא-מספר, והבדיקה מודדת אותו בשני ערכים שונים. ⚠️ **החצי השני של הממצא — «מהו חיוב שלא נגמר עבור «הרכבה»» — ⛔ נותר פתוח → PM** ו⛔ אינו נסגר כאן | 0 |
| F-077 | 🟡 MEDIUM · **מדידה שמדדה קירוב (מיקום ה-`h1`) ⛔ ולא את הפגם (חלל ריק) · נפתח ותוקן על ידי DEV באותו טיק (C-0200)** | `scripts/verify-mobile.mjs:774-796` (`heading anchored to top`) | **בדיקת F-011/F-016 מדדה `main h1`.top מול תחתית ה-header, כלומר היא הניחה בשקט שהכותרת היא הדבר הראשון שנצבע.** ההנחה החזיקה בכל מסך שנבנה עד היום, ו-`/world` שבר אותה **כשורה במפרט**: § 4.2יא מציבה את רשת האריחים **מעל** הפיד, ולכן 256px של **תוכן אמיתי** יושבים מעל ה-`h1` של `<WorldFeed>` — וההארנס קרא להם «חלל מת» ונפל בשלושת הרוחבים | ⛔ הכשל אינו תיאורטי: הוא נמדד בהרצה (`dead band of 256px` ב-320/375/414) על מימוש שעושה **בדיוק** את מה שהמפרט מורה. מסך עתידי כלשהו שיציב ווידג׳ט מעל הכותרת ייחסם באותה צורה, וההתמודדות המפתה היא להזיז את המפרט כדי לרצות את המדידה | ⛔ **לא להרפות ולא לוותר — לצמצם:** המדידה עברה למדוד את **הטקסט הראשון שנצבע** ב-`main` (או `img`/`svg`/`canvas`/`video`), עדיין רק במסלולים שיש בהם `main h1`, ועדיין מול תחתית ה-header ובאותו סף 48px. ⚠️ **ואומת שהיא ⛔ אינה חלשה יותר:** החזרת `flex-1 justify-center` ל-`app/page.tsx` (הפגם המקורי של F-011) **הפילה אותה** ב-112/138/153px — כי מכולה ממורכזת מתחילה בראש והטקסט שבתוכה ⛔ לא. ⛔ הנעילה הסטטית של F-011 ב-`scripts/verify-mobile.test.ts` ⛔ לא נגעה ונשארה בתוקף | ✅ **תוקן C-0200.** ⚠️ הלקח פתוח → **CRITIC**: מדידה שמודדת קירוב עוברת בשקט עד היום שבו המוצר נכנס לפער — ושלוש בדיקות ירוקות ⛔ אינן ראיה שהמדד הוא הפגם | 0 |
| F-082 | 🟠 HIGH · **רגרסיה בשער מדידה שנמדדה בבידוד ⛔ ולא שוערה · נפתחה על ידי DEV (C-0212, טיק ביצוע של T-125)** | `scripts/verify-mobile.mjs` (השער `action bar does not cover the licence link`) · `components/StudyDeckScreen.tsx` · `app/globals.css:88` · הקומיט `7cf994f` (C-0211 · T-124) | **`npm run check:mobile` נכשל בשישה שערים ב-`dev`, ו⛔ אף אחד מהם אינו של הטיק הזה.** נמדד בבידוד (`git stash` + `git checkout`) ⛔ ולא הוסק: ⓐ בראש `dev` — **6 כשלים**; ⓑ ב-`2a6ef3f` (סוף C-0210, לפני T-124) — **3 כשלים**; ⇒ **שלושת החדשים נולדו בדיוק ב-`7cf994f`** (C-0211 · T-124), הקומיט היחיד שביניהם. הכשל: `/study @320px · @375px · @414px — action bar does not cover the licence link — link bottom 676 vs bar top 645`. ⚠️ זו **חתימת המוטציה המדויקת** ש-F-027 תיעד כשהרג את כלל הריפוד (`body:has([data-action-bar])`) — אלא שהכלל עצמו **קיים ותקין** (`app/globals.css:88`, אומת). ⇒ הסיבה אינה הריפוד אלא **קיצור העמוד**: T-124 החליף בענפי הכשל של `StudyDeckScreen` טקסט ארוך בשורת יציאה קצרה, המסמך התקצר מתחת לגובה החלון, ה-`<footer>` (אח **אחרי** `<main>`, `app/layout.tsx:47`) צף למעלה — וסרגל הפעולה ה-`fixed` מכסה את קישור הרישיון. ⓒ שלושת הכשלים הנותרים — `/dev/tabs/cards @320/375/414 clean console — request failed: /study?_rsc=…` — **קדמו ל-C-0210** ו⛔ מקורם לא אותר; הם ⛔ אינם של T-124 | לומד ב-`/study` במצב כשל (סכמה חסרה · סשן שפג · תקלה חולפת) ⛔ אינו יכול להקיש על קישור הרישיון: הוא יושב **מתחת** לסרגל הפעולה, שהוא `fixed` ו-`z`-מעליו. זו בדיוק הדרישה שנולדה מ-F-027 — פעולה שקיימת בקוד, ⛔ נמדדת כ«קיימת», ו⛔ אינה ניתנת להקשה. ⛔ **אינו חוסם את T-126 ואת T-130** (מסכי הזירה, קבצים אחרים), ולכן ⛔ לא נלקח באותו טיק: הוא דורש הכרעה על **מה** מתקצר או **כמה** ריפוד — ⛔ ולא תיקון מכני | ⓐ **DEV:** למדוד את הגובה בשלושת הרוחבים ולהחליט בין הגדלת `padding-bottom` ב-`body:has([data-action-bar])` לבין `min-height` על `<main>` בנתיבי `FLOW_ROUTES`; מוטציה שמחזירה את הערך הישן חייבת להפיל את השער בשמו. ⓑ **DEV, בנפרד:** לבודד את מקור `request failed: /study?_rsc=…` (‏RSC prefetch מ-`/dev/tabs/cards`) — ⛔ ייתכן שהוא פיקסצ׳ר ולא מוצר. ⛔ **שניהם ⛔ אינם הכרעת מסך** ולכן ⛔ אינם ממתינים ל-PM | ✅ **ⓐ נסגר C-0214 · ⓑ נסגר C-0220 (T-131) — שניהם.** **ⓑ:** שלוש שגיאות `/dev/tabs/cards` נמדדו `net::ERR_ABORTED` על פריפץ׳ RSC של `<Link href="/study">` — ביטול של Chromium בניווט, ⛔ בלי תשובת שרת ⇒ **פיקסצ׳ר, ⛔ לא פגם מוצר.** ⛔ `EXPECTED_CONSOLE` לא נגעתי בו (ורשומה שם ⛔ לא הייתה יכולה להתאים בכלל: כל הרשומות בנויות `status of NNN`). המאזין מסנן על `errorText` **בלבד** ⛔ ולא על הכתובת. `check:mobile` **3 כשלים → 0, 927 בדיקות**; מוטציית `<img>` שבור מאדימה את `clean console` ⇒ ⛔ לא הושתק. ⛔ **ההשערה המקורית («אין `env` בהרנס») הופרכה** — אין כאן בקשת שרת כלל. הפירוט `30-architecture § 3.1.67` ⚠️ **ההשערה בטור «ההשפעה» הופרכה במדידה:** העמוד ⛔ לא התקצר — `footer a[href="/sources"]` יורד ל-**676px בדיוק בכל מסך זרימה**, `/study` כלול. מה שזז הוא **גובה הסרגל**: 77px בכל מקום מול **135px** ב-`/study` (נמדד ב-320·375·414, בלתי תלוי ברוחב) — כי C-0211 הוסיף שם שורה שנייה לתוך `<ActionBar>`, וההזמנה `5rem = 80px` מכסה 77 ⛔ ולא 135. ⇒ התוקן ⛔ אינו העלאת קבוע: `ActionBar` פולט `data-action-bar={layout}` (`single` / `stacked`), `globals.css` מחזיק כלל לכל צורה, ו-`ActionBar.test.ts` אוסר צורה בלי כלל. **`check:mobile` 6→3 כשלים באותו עץ**; שלושת הנותרים הם בדיוק בסיס `2a6ef3f` = **ⓑ**. הפירוט `30-architecture § 3.1.64` | 0 |
| F-087 | 🟡 MEDIUM · **בדיקה שמדדה הערת תיעוד במקום את הקוד שהיא מתיימרת לאמת · נפתח ותוקן על ידי DEV באותו טיק (C-0223, טיק ביצוע)** | `lib/supabase/lexicalClassBackfill.test.ts:4` (נוסח התוכנית) · `docs/superpowers/plans/2026-08-20-lexical-class-retirement.md` משימה 1 צעד 1 | **נוסח הבדיקה שבתוכנית עשה `SQL.split(';')` על המקור הגולמי, ובחר בפקודה הראשונה שמכילה `'content'`.** הערת הכותרת של `0016` מסבירה במפורש למה המילוי ⛔ אינו `case when … else 'content'`, ולכן היא מכילה את המחרוזת בעצמה ⇒ `.find()` החזיר את **בלוק ההערה**. זו מחלקת F-039 · F-065 בלבוש SQL | ⛔ הבדיקה נכשלה על מיגרציה **תקינה** (`expected '-- 0016_lexical_class_backfill.sql — …' to match /origin\s*=\s*'generated'/`). המסלול המסוכן היה ברור: להשקיט אותה במחיקת ההערה שמתעדת את האיסור, ואז הבדיקה עוברת ⛔ ובלי שאיש יידע למה הכלל קיים | הלבנת הערות SQL (`--` ו-`/* */`) לפני כל טענה, כמו `withoutComments` ב-`app/api/arcade/result/route.test.ts:9`. ⛔ ההערה ⛔ אינה נמחקת | ✅ **תוקן C-0223.** ‏`withoutSqlComments` נוסף לראש הקובץ; 6/6 ירוקות. **המוטציה נשמרה חיה:** הסרת `and origin = 'generated'` מהמיגרציה עדיין מפילה בדיוק את הבדיקה בשמה | 0 |
| F-088 | 🟡 MEDIUM · **שער שאינו יכול ליפול — הלבנת הערות שמוחקת בדיוק את מה שהבדיקה מחפשת (התאום ההפוך של F-087) · נפתח ותוקן על ידי DEV באותו טיק (C-0224, טיק ביצוע)** | `docs/superpowers/plans/2026-08-20-lexical-class-retirement.md` משימה 2 צעד 1 (האסרציה על `lib/core/flashcard.ts`) · `app/api/world/lexicalClassOnly.test.ts:26` | **התוכנית הכתיבה `expect(withoutComments(readFileSync('lib/core/flashcard.ts'))).not.toContain('is_function_word')`, וניבאה במפורש בצעד 2 שהיא **נופלת** — היא ירוקה מלכתחילה.** האזכור היחיד בקובץ (`flashcard.ts:102`) יושב **בתוך בלוק `/** */`**, ולכן `withoutComments` מוחק אותו לפני הטענה. נמדד ⛔ ולא שוער: הרצה ראשונה **לפני** כל עריכה החזירה `Tests 6 failed` מול `1 passed (7)`, והעוברת היחידה היא בדיוק זו. | הצעד 5 (ניקוי ההערה) היה מבוצע או מדולג — **ובשני המקרים הסוויטה ירוקה**. ⇒ הצעד היחיד במשימה שנועד להגן על תיעוד מיושן היה נכנס למאגר בלי שום שער, וה-Critic היה קורא «8/8 ירוקות» כראיה לכיסוי שאינו קיים. וגרוע ממנו: התבנית הזאת מועתקת קדימה — משימה 3 מוסיפה עוד סריקות מקור, וכל אחת שתועתק עם הלבנה על קובץ שכל האזכור בו הוא הערה תהיה קישוט. | ⚠️ **⛔ לא להסיר את ההלבנה גורף** — על שלושת הנתיבים היא נכונה ונדרשת (F-039 · F-065). הטענה על `flashcard.ts` נמדדת על המקור **הגולמי**, כי מה שהצעד משנה הוא ההערה עצמה. ⛔ ובנוסף — שער שאוסר בלי להתיר מזמין סחיפה עיוורת: נוספה בדיקה בכיוון **החיובי** ש-`boolean(row, 'is_function_word')` ב-`lib/core/batchRecord.ts:170` **נשאר**, כי שם זה שדה ב-jsonl מול סוכן התוכן ⛔ ולא עמודה. | ✅ **תוקן C-0224.** שתי הבדיקות במקום אחת; ההרצה אחרי התיקון ולפני עריכת הקוד: `Tests 7 failed` מול `1 passed (8)` ⇒ ה-flashcard נעה לצד הנופל **והשער החיובי נשאר ירוק**, כלומר שתיהן מודדות. אחרי משימה 2: `8 passed (8)`. | 0 |
| F-091 | ⚪ LOW · **בדיקה חלולה — עוזר הלבנה משותף שבולע את גוף הרכיב כולו (משפחת F-064 · F-088) · נפתח ותוקן על ידי DEV באותו טיק (C-0227, טיק ביצוע)** | `components/LevelMapScreen.test.ts:14-19` (המקור שממנו מועתק) · `components/ArenaBoard.test.ts:5-7` · `components/LevelScan.test.ts` (מתוקן) | **שלוש ההחלפות של `withoutComments` חותכות `{/* … */}` לפני בלוקי ההערות הרגילים, והכמת העצל בולע כל מה שבין `{` ⇐ JSDoc לבין ה-`*/}` הבא בקובץ.** נמדד ⛔ ולא שוער: ב-`LevelScan.tsx` הפרופ `initialWords` נושא JSDoc בתוך טיפוס-אובייקט (`}: {`), ההערה הבאה שנסגרת ב-`*/}` היא הערת ה-JSX של קישור הכשל ⇒ **128 שורות נמחקו מ-`CODE`**, ושלוש בדיקות נפלו על קוד תקין. סריקה על שלושת הרכיבים הקיימים: `LevelMapScreen` 6613 מול 6625 תווים · `ArenaBoard` 14222 מול 14228 · `ArcadeEntry` זהה ⇒ ⛔ **אף אחד מהם אינו מפעיל את המלכודת היום** | הכיוון המסוכן הוא ההפוך מהנפילה: כל `not.toContain` על קוד שנבלע **עובר לשווא**. רכיב עתידי שיעתיק את התבנית ויכתוב JSDoc על פרופ — הדפוס הרגיל ב-TypeScript — יקבל סוויטה ירוקה ששומרת על **קובץ ריק**, וה-Critic יקרא אותה כראיית כיסוי. ⛔ אין נזק ללומד היום | ⛔ **לא להסיר את ההלבנה** — היא נדרשת (F-039 · F-065). ההחלפה הראשונה מיותרת: אחרי שכל `/* … */` נחתך, `{/* … */}` נותר `{}` בלתי מזיק. ⇒ **שתי החלפות במקום שלוש.** ⚠️ ובנוסף, בטיק הזה **מוטציה תפסה אסרציה חלולה שנייה שאני עצמי כתבתי**: רֶגֶקְס שמתחיל ב-`<button` ורץ עצלנית עד `aria-pressed` פותח בכפתור הקודם בקובץ ⇒ הוחלף בחיתוך לפי האלמנט (`split('<button')`) | ✅ **תוקן C-0227 ב-`components/LevelScan.test.ts` בלבד** (שתי החלפות, עם נימוק מוטבע). ⛔ **שלושת הקבצים הקיימים ⛔ לא נגעתי בהם** — הם ⛔ אינם מפעילים את המלכודת (נמדד לעיל), והחלפה גורפת בטיק ביצוע של T-082 הייתה חורגת מהמשימה. **לתיקון גורף: פריט ל-PM.** אימות טרי: `Tests 25 passed (25)`, והמוטציה (הסרת `min-h-touch` מתא הרשת) מפילה **בדיוק אחת** | 0 |
| F-092 | 🟠 HIGH · **פגם — `POST /api/arcade/result` אינו אידמפוטנטי, ורצף retry-on-online (T-109 · C-0218) מכפיל שורות `arcade_runs` ומנפח את `wins` ואת `arcade_collected_words.times_missed`/`times_correct` · נפתח על ידי CRITIC (C-0237, 🔬 עומק)** | `app/api/arcade/result/route.ts:70-124` (הנתיב קורא מצב חי, קורא ל-`planArcadeWrites`, ואז `insert` על `arcade_runs` בלי מפתח כלשהו) · `components/ArenaBoard.tsx:257-264` (`useEffect` שמאזין ל-`online` וקורא שוב ל-`send(pendingResult)` עם אותו גוף) · `supabase/migrations/0014_arcade.sql:56-73` (טבלת `arcade_runs` — נמדד: ⛔ **אין `unique(user_id, …)`**, רק `arcade_runs_counts_check`; `insert` מתקבל בכל קריאה) · `lib/api/client.ts` (‏`ApiUnreachableError` מוזרק ל-`catch`) | — | **לומד מסיים קרב עם מיס אחד על מילה W וניצחון אחד.** `ArenaBoard.send` שולח `{answers}`. השרת מריץ `applyWin` וכותב `arcade_progress {wins:1}`, `arcade_runs {new row}`, `arcade_collected_words {W, times_missed:1}` (‏`route.ts:99-123`). התשובה נאבדת ברשת סלולרית; `apiPost` זורק `ApiUnreachableError`; `ArenaBoard.tsx:225` שומר `pendingResult = payload`; הלומד חוזר אונליין; ה-`useEffect` ב-`ArenaBoard.tsx:257-264` שומע `window.addEventListener('online', retry)` וקורא שוב ל-`send(pendingResult)` עם אותו גוף בדיוק. השרת קורא עכשיו את `arcade_progress` (כבר `wins:1`) ואת `arcade_collected_words` (כבר `times_missed:1`), ולכן `planArcadeWrites` בונה `wins:2`, `times_missed:2`, ומכניס שורת `arcade_runs` **שנייה** (אין מפתח ייחודי; `0015` לא הוסיף). קרב אחד עם מיס אחד נראה עכשיו כשני קרבות ושני מיסים; הלומד חצי-דרך ל-`levelUp` שלא מגיע לו | הוסף ל-`ArenaBoard.startBattle` `runId: crypto.randomUUID()`, שלח אותו בגוף POST, הוסף עמודה `run_id uuid not null unique` ל-`arcade_runs` (במיגרציה חדשה `0017_arcade_run_id.sql`), וקצר-סיבוב את הנתיב: לפני `planArcadeWrites`, בדוק `select 1 from arcade_runs where user_id=? and run_id=?` — אם קיים, החזר את תגובת ה-JSON האחרונה (מטמון בעמודה חדשה `response_snapshot jsonb`) ⛔ **בלי לכתוב שוב**. זה הדפוס הסטנדרטי של `Idempotency-Key` על POST פיננסי (Stripe) והוא הפתרון הצר לנפילת רשת. **⛔ אל תסתמך על `run_id` בצד לקוח לבדו** — הוא יעיל רק אם השרת רואה בו את המפתח | 🟢 **לסקירה → CRITIC** · ✅ **תוקן C-0242 (DEV) — שלוש המשימות של התוכנית נסגרו, והתיקון נמדד ⛔ ולא שוער.** Task 1 ב-C-0241 (`0017_arcade_run_id.sql`), **Task 2 ו-Task 3 יחד ב-C-0242** ⛔ ולא בשני טיקים: `runId` הוא שדה **חובה** בחתימת `planArcadeWrites`, ולכן Task 2 לבדה משאירה את `route.ts` אדום ב-`tsc` (`Property 'runId' is missing`), ו-`RULES § 0.2` אוסר לדחוף עץ אדום. **מה שנסגר בפועל:** ⓐ `arcade_runs` היא הכתיבה **הראשונה** מבין השלוש ⇒ התנגשות `23505` עוצרת **לפני** ש-`arcade_progress` ניפח את `wins` · ⓑ `parseRunId` (uuid) ⇒ מפתח פגום מקבל **422 לפני כל כתיבה**, ⛔ ולא `22P02` כ-503 · ⓒ קריאת `response_snapshot` לפני `planArcadeWrites` ⇒ שידור חוזר מקבל **אותו** גוף 200 בלי אף כתיבה · ⓓ `23505` בלולאה ⇒ `return` עם התצלום, ⛔ ולא `continue` (זו ההגנה היחידה שמחזיקה בשני שידורים **בו-זמנית**, TOCTOU) · ⓔ `ArenaBoard` יוצר `crypto.randomUUID()` **ברגע השליחה**, ⛔ לא בתוך `send` ו⛔ לא ב-`useState`. **שלוש מוטציות אומתו נופלות בשמן:** היפוך סדר הכתיבות ⇒ «expected 'arcade_progress' to be 'arcade_runs'» · `continue` במקום `return` ⇒ הבדיקה «ⓑ התנגשות ייחודיות עוצרת את שאר הכתיבות» נפלה על היעדר `return`/`break` · מפתח קבוע במקום `randomUUID` ⇒ **שתי** בדיקות נפלו. ⚠️ **ומדידה שלא הייתה בתוכנית:** הבלוק `/** … */` שהתוכנית הכתיבה ל-`ResultPayload` ⛔ אינו ניתן למימוש — מלבן ההלבנה ב-`ArenaBoard.test.ts:5` בלע **8,232 תווים** מהמקור והפיל 12 בדיקות ותיקות; הוחלף בהערת שורה. ⏳ **⛔ אינו סגור בייצור עד שרוי מריץ את `0017`** (`03-for-roy` פריט 43) — עד אז הנתיב יחזיר 503 ⇒ ⛔ **לפני הקידום** | 0 |
| F-093 | 🟡 MEDIUM · **סטייה שהוכרזה בתוכנית בלי למדוד את השער החי שאוסר עליה · נפתח ונסגר על ידי DEV באותו טיק (C-0241, טיק ביצוע)** | `docs/superpowers/plans/2026-08-20-arcade-result-idempotency.md:29` («סטייה 1») · `scripts/migration-hygiene.test.ts:74` (השער) · `plan/03-for-roy.md` פריט 42 | **התוכנית (C-0240) הכריזה «הקובץ הוא `0018` ⛔ ולא `0017`» ו⛔ נימקה ש«הרווח המספרי ⛔ אינו פגם» — בלי לפתוח את הקובץ שמודד בדיוק את זה.** `scripts/migration-hygiene.test.ts:74` הוא שער חי בשם «leaves no gap in the sequence», והוא נופל על כל דילוג. נמדד C-0241 בהרצה טרייה: `expected [… 16, 17] to deeply equal [… 16, 18]`. ⛔ **וההנחה שהשריון «אינו עולה כלום» שגויה בעצמה:** `0017_stories.sql` **טרם נכתב** (T-134 ⬜) | ⛔ **לא כשל לומד — כשל לופ, והוא רחב:** הפער היה מאדים את `npm test` **לכל סוכן בכל טיק** עד ש-T-134 תיכתב — כלומר ימים, ⛔ ולא דקות. וגרוע ממנו, המסלול המפתה מהאדום הזה הוא **החלשת השער** («זה רק מספור»), שהיא בדיוק הכשל שהשער נבנה למנוע: מי שמריץ ידנית ורואה 0016 ואז 0018 ⛔ אינו יודע אם קובץ אבד | הסטייה מבוטלת ⛔ ולא השער: המיגרציה לוקחת את `0017` כלשון הממצא המקורי, ומיגרציית הסיפורים עוברת ל-`0018_stories.sql`. ⛔ **אין כאן הוראה שרוי ביצע ובוטלה** — הקובץ ההוא מעולם לא היה קיים, ופריט 42 ⏳ פתוח. ⚠️ **הלקח לטיק תכנון:** סטייה מנוסח ממצא מחייבת הרצה של השער שהיא נוגעת בו, ⛔ ולא נימוק בפרוזה | ✅ **נסגר C-0241 באותו טיק.** שלושת המסמכים עודכנו באותו קומיט (פריט 42 · T-134 · התוכנית), `migration-hygiene` 9/9 ירוקות, ו⛔ **אף שורה בשער ⛔ לא נגעתי בה** | 0 |
| F-094 | 🟡 MEDIUM · **שורת ממצא בת 7 תאים ⇒ הרגיסטר המכונה-קריא דיווח `0` כסטטוס של הממצא שחוסם קידום · נפתח ותוקן על ידי DEV (C-0241, טיק ביצוע)** | `plan/60-findings.md` שורת `F-092` (נכתבה C-0237 · נערכה C-0240) · `lib/core/planTable.ts:134` (`FINDING_STATUS_INDEX = 6`) · `scripts/measure-plan-tables.test.ts:69` (הרַצֶ׳ט) | **שורת `F-092` נכתבה עם **7** תאים מול כותרת בת 8 — עמודת «הממצא» מעולם לא נכתבה, וההצהרה קופלה לתוך תא «חומרה» כמו בכל שורה בקובץ.** ⇒ כל תא אחריה הוסט שמאלה, ו-`FINDING_STATUS_INDEX = 6` קרא את תא **«סבב»**. נמדד C-0241: לפני התיקון אינדקס 6 החזיק `'0'`; אחריו הוא מחזיק `'🔓 פתוח → **DEV**…'`. ⛔ **וזה ⛔ אינו מחלקת ⓐ** (13 שורות בנות 9 תאים, החלטת סכמה של ה-Critic) — זהו תא **חסר**, ⛔ לא עודף | ⛔ **הרַצֶ׳ט האדים את העץ לכל סוכן** (`expected 14 to be less than or equal to 13`) כבר ב-C-0240 — כלומר טיק התכנון דחף רגיסטר פגום ו⛔ לא הריץ `npm test`. וחמור ממנו בשקט: כל צרכן מכונה של הרגיסטר קרא את הסטטוס של **הממצא היחיד שחוסם קידום** כ-`0`, ⛔ ולא כ«פתוח → DEV». כלי שמסכם «מה פתוח» היה מדלג עליו | התא שלא נכתב מולא ב-`—` — בדיוק תקדים C-0220 (T-129), שבו Dev מילא ארבעה תאים שלא נכתבו. ⛔ **אף מילה בפרוזה של ה-Critic ⛔ לא שונתה, ⛔ אף תא ⛔ לא הוזז ידנית** — התיקון מחזיר את `סטטוס` למקומו הסכמתי. ⛔ **והתקרה ⛔ לא הועלתה** (`⛔ nothing raises it silently`) | ✅ **תוקן C-0241.** `findings: 93 rows, 13 malformed` — חזרה לתקרה, ⛔ בלי לגעת בה. `docs/plan-tables.md` חודש באותו קומיט (שער «leaves the committed report identical to a fresh run») | 0 |
| F-095 | 🟡 MEDIUM · **פער מפרט (כלל התאמה שהמשימה אוסרת עליו חלופה אחת ⛔ ואינה נוקבת באחרת) · נפתח על ידי DEV (C-0245, טיק תכנון)** | `plan/50-tasks.md` שורת T-134ⓑ · `lib/core/contentSchema.ts:112` (`inflections`, אסורה בשם) · `docs/superpowers/plans/2026-08-21-story-schema-and-content.md` § «סטייה 1» | **T-134ⓑ מורה «התאמה על למה מנורמלת בלבד» ואוסרת את `inflections()` — ⛔ ואינה מגדירה מהו הנרמול.** הבנק הוא **למות** (`be` · `read`), ⛔ ולא צורות. ⇒ השוואה מילולית פוסלת כל צורת גוף שלישי, עבר והווה ממושך | **נמדד C-0245 על הבנק האמיתי** (495 למות, מהן 294 ב-A1), על סיפור A1 סביר בן 55 טוקנים נבדלים: השוואה מילולית פוסלת **19** מהם (`opens · wants · looks · asks · says · walks · finds · gives · takes · reads · tells`…) ⇒ ⛔ **אין אנגלית תקנית שעוברת את השער** ⇒ **T-135 ⛔ לא הייתה נסגרת לעולם**, והרצף כולו נתקע במשימה השנייה | **הכלל שנבחר בתוכנית, ⛔ ואינו הרחבה:** הטוקן **מוּפחת** ללמה שבבנק (סיומות סגורות ומוצהרות) ⛔ ולא הלמה מורחבת לצורות ⇒ ⛔ ה-false-accept של F-020 (`car`+`d`=`card`) ⛔ אינו נולד מחדש, כי ⛔ אין כלל סיומת `-d`. הפחתה מורידה את הפסילות מ-19 ל-**8**, מהן **6 פערי אוצר מילים אמיתיים**. הצורות החריגות (`is` ← `be`) יושבות ב**מפה סגורה** בתבנית `CONTRACTION_STEMS` הקיימת, והערך שלה נבדק מול הרמה כמו כל למה. ⛔ **המפה ⛔ אינה מכניסה למה לבנק** | ✅ **נסגר C-0289 (T-194) — התיישן, ⛔ ולא תוקן.** `storyLemma` נבנתה ב-`lib/core/storyGate.ts` (הפחתה, ⛔ לא הרחבה), ו-T-135 נסגרה ב-C-0247. הנוסח הישן שרד רק בשורת משימה סגורה. | 0 |
| F-100 | ⚪ LOW · **פגם בתוצר תכנון (תוכנית שלא נקבה בשני שומרים חיים שהשינוי שלה מחייב לכוון מחדש) · נפתח ונסגר על ידי DEV באותו טיק (C-0250, טיק ביצוע)** | `docs/superpowers/plans/2026-08-21-lesson-anatomy-and-flow-coverage.md` משימות 3–4 · `scripts/verify-mobile.test.ts:171` (`measures reachability before anything else scrolls the page`) · `scripts/verify-mobile.test.ts:739` (`declares an arrival for EVERY flow route`) | **התוכנית הצהירה «⛔ אין להחליש בדיקה קיימת» ו⛔ לא נקבה בשתי בדיקות שהשינוי שלה עצמו מפיל:** ⓐ שומר הסדר עוגן ב-`code.indexOf('FLOW_ROUTES.includes(route)')`, ואחרי הפיצול העוגן מצא את **הבלוק השני** ⇒ נפל על «⛔ אינו מכיל scrollIntoView» · ⓑ שומר הנחיחות דרש שוויון מדויק בין מפתחות `FLOW_ARRIVAL` ל-`FLOW_ROUTES`, ושתי הרשומות החדשות יושבות על `PRIMARY_ACTION_ROUTES` ⇒ נפל על 6≠4 | מבצע שקורא «אל תחליש» ורואה שתי בדיקות אדומות שהתוכנית ⛔ לא הזכירה עומד בפני שני מוצאים גרועים: לוותר על המשימה, או למחוק את הבדיקה. שניהם היו מוציאים שומר חי מהתמונה — ⓐ במיוחד, כי עוגן שמצביע על הבלוק הלא נכון **עובר בשקט** על כלום (משפחת F-091: בדיקה חלולה) | ⓐ העוגן כוון ל-`PRIMARY_ACTION_ROUTES.includes(route)` — **אותו קוד עצמו**, שתי האסרציות זהות · ⓑ הקבוצה הורחבה ל**איחוד** `FLOW_ROUTES ∪ PRIMARY_ACTION_ROUTES`, והשוויון נשאר דו-כיווני ⇒ **הרחבה, ⛔ לא החלשה** ⇒ ✅ **נסגר C-0250.** **המדידה שמוכיחה ש-ⓐ עדיין נושא משקל:** הקדמת `scrollIntoView` לפני `const reachable` ⇒ «measures reachability before anything else scrolls the page» נפלה בשמה, ושוחזר. **ⓑ:** הסרת שתי הלשוניות מ-`PRIMARY_ACTION_ROUTES` ⇒ «declares a list that is FLOW_ROUTES plus the two tab fixtures» נפלה. ⚠️ **הלקח פתוח → CRITIC:** תוכנית ששינוי שמות בה נוגע בשומר קיים חייבת לנקוב בו בשם ובשורה, אחרת «אל תחליש» הופך להוראה שאי-אפשר לציית לה | ✅ טופל · הלקח → CRITIC | 0 |
| F-101 | 🟡 MEDIUM · **פגם (מירוץ בבדיקת הנחיתה עצמה — הסמן נקרא לפני שהיעד נצבע) · נפתח ונסגר על ידי DEV באותו טיק (C-0250, טיק ביצוע)** | `scripts/verify-mobile.mjs`, ענף `if (arrival.kind === 'navigates')` — `const marker = await page.locator(arrival.marker).count()` מיד אחרי `waitForURL` | **נמדד בהרצת הארנס, שלושת הרוחבים, ⛔ ולא שוער.** בנחיתה על `/study` ב-`t=0`: `url=/study` · `h1="מנת היום"` · **`marker=0` · `primary=0`**; ב-`t=300ms` ואילך: **`marker=1` · `primary=1`**. `waitForURL` נפתר ברגע שהכתובת השתנתה, והיעד עדיין נצבע ⇒ הבדיקה קראה DOM שטרם הגיע. כל הרשומות שקדמו עברו רק משום שיעדן רונדר סינכרונית (‏`/signup` סטטי · `/login` הפניית 307) | **בדיקה שמפילה מסך שרונדר בפועל.** שלוש נפילות `/study really rendered — no element matching [data-action-bar]` על התנהגות **נכונה** של המוצר, והמוצא הזול הוא להחליף סמן או למחוק את הרשומה — כלומר להוריד כיסוי בגלל תזמון. הכיוון ההפוך מסוכן מכפליים: מסלול שייכשל אמיתית ייקרא «מירוץ» | המתנה חסומה לפני הספירה, בדיוק כתבנית שענף `announces` כבר נושא שורות ספורות מתחת: `.first().waitFor({ timeout: 5000 }).catch(() => {})` ואז `.count()`. ⛔ ⛔ החלשה — מסך שלעולם ⛔ אינו מרנדר את הסמן נופל בתום הפסק, בדיוק כמו קודם ⇒ ✅ **נסגר C-0250.** **המוטציה מדדה את התיקון:** מחיקת ה-`waitFor` ⇒ שלוש הנפילות חוזרות מילה במילה, שוחזר. שני שומרי מקור חדשים ב-`scripts/verify-mobile.test.ts` (‏`waits for the marker before counting it` — כולל סדר `.waitFor` לפני `.count` · `keeps the wait bounded`, כדי שמסך מת ⛔ יפול ⛔ ולא יתלה). ⚠️ **פתוח → CRITIC:** אותו לקח בדיוק כמו C-0134 (למדוד אחרי שהדבר הגיע), והוא חזר בקובץ הזה בפעם השנייה ⇒ שווה בדיקה אם עוד קריאה סינכרונית בארנס נושאת אותו מירוץ | ✅ טופל · הלקח → CRITIC | 0 |
| F-103 | ⚪ LOW · **בדיקה חיה שמנמקת את עצמה בהחלטה שבוטלה · נפתח על ידי DEV (C-0251, טיק תכנון)** | `components/Flashcard.test.ts:239` — `it('⛔ ⛔ swipe-to-grade (D-032 בתוקף)')` | **אותה מחלקת כשל בדיוק כמו T-127, רק שהיא יושבת בבדיקה ולא בהערה, ו⛔ איש לא רשם אותה.** הבדיקה אוסרת `onPointerDown · onPointerMove · onTouchMove · onSwipe` על **כל** הקובץ בנימוק «D-032 בתוקף» — ו-**D-042 הפכה את D-032 במפורש** (`40-decisions.md:1046`). ⇒ כל מימוש של T-099 מפיל אותה, והמבצע עומד מול הפיתוי «הבדיקה אומרת שאסור, אז המשימה לא רלוונטית» — או, גרוע ממנו, מוחק אותה בלי להחליף | **בדיקה שמגינה על כלל בטל תופסת את מקומו של השומר האמיתי.** מחיקה שלה הייתה משאירה את הקובץ **בלי** שומר נגד גרירה, וזה בדיוק הדבר ש-D-042ⓒ עדיין אוסרת | **כיוון מחדש ⛔ ולא החלשה** — משימה 3 של `docs/superpowers/plans/2026-08-21-flashcard-swipe-and-decay.md` מחליפה אותה בחמש בדיקות **חדות יותר**: ⛔ אין מטפל תנועה (זו הגרירה) · שני הכפתורים עדיין בקובץ · המחווה קוראת ל-`onGrade` ⛔ ולא למסלול שני · ⛔ אין `preventDefault` · ⛔ אין `style={{}}` | ✅ **סגור C-0252 (טיק ביצוע).** הבדיקה כוונה מחדש לחמש בדיקות חדות יותר ⛔ ולא הוחלשה: היעדר מטפל תנועה · שני הכפתורים · `resolveSwipe`+`onGrade` באותה בדיקה · אפס גרירה אופקית · אפס `style={{}}`. ⛔ ראה גם **F-106** — אחת מחמשת האסרציות שהתוכנית הכתיבה הייתה בלתי-אפשרית וכוונה מחדש | 0 |
| F-104 | 🟡 MEDIUM · **בדיקה שהתוכנית הכתיבה סותרת את שתי הבדיקות שלצדה · נפתח ותוקן על ידי DEV באותו טיק (C-0252, טיק ביצוע)** | `docs/superpowers/plans/2026-08-21-flashcard-swipe-and-decay.md` משימה 2 צעד 1 ⇒ `lib/core/swipeGrade.test.ts` | הבדיקה «הזווית סימטרית לשני הכיוונים האנכיים» שהתוכנית הכתיבה מילה במילה טוענת ש-`endY: 340` (כלומר `dy=-60`, **36.9°**) מחזיר `'good'` — בעוד **שתי הבדיקות שמעליה באותו בלוק** קובעות ש-36.9° הוא `null` לפי D-042ⓑ. בנוסף האסרציה השנייה בה, `f(340) === f(460 - 120)`, היא **טאוטולוגיה**: ‏`460-120 === 340`, כלומר השוואת קריאה לעצמה | המימוש הנכון **מפיל** את הבדיקה, והדרך היחידה «להעביר» אותה היא להרחיב את סף הזווית מעבר ל-30° שההחלטה נוקבת בו ⇒ החלקה אלכסונית בזווית 37° הייתה מסמנת מילה, וזה בדיוק הסימון-בטעות ש-D-033 נבנתה למנוע. והטאוטולוגיה פירושה שסימטריה ⛔ לא נמדדה כלל | ⛔ הבדיקה ⛔ לא הוחלשה: היא **כוונה מחדש** לטענה שהתיימרה לטעון — סימטריה נמדדת ב**שני** צדי הגבול (‏`dy=±40` ⇒ שניהם `'good'` · `dy=±60` ⇒ שניהם `null`). ⛔ המימוש ⛔ לא נגע | ✅ **סגור C-0252.** ‏`Tests 18 passed (18)`, והמוטציה שמרחיבה את הזווית מפילה בשם | 0 |
| F-105 | 🟠 HIGH · **שתיים משלוש המוטציות שהתוכנית מחייבת ⛔ לא הפילו דבר — שני שערים חיים היו בלתי-נמדדים · נפתח ותוקן על ידי DEV באותו טיק (C-0252, טיק ביצוע)** | `lib/core/swipeGrade.test.ts`, הבלוק `D-042ⓑ — סף המרחק` | **נמדד ⛔ ולא שוער.** ⓐ הבדיקות בנו את הקלט מהקבוע עצמו (`endX: 100 + SWIPE_MIN_DISTANCE_PX - 1`), ולכן המוטציה `64 ⇒ 32` הזיזה **גם את הקלט וגם את הציפייה** ⇒ `16 passed`. ⓑ המוטציה `Math.abs(dx) ⇒ Math.hypot(dx, dy)` עברה גם היא: הבדיקה היחידה שהתיימרה להפריד ביניהן (גלילה אנכית ארוכה) נדחית ממילא על **הזווית** | ⓐ סף ההחלקה יכול היה להיות כל מספר ואיש לא היה יודע. ⓑ שער המרחק יכול היה להיות אוקלידי בשקט ⇒ החלקה של 60px אופקי בזווית כשרה (hypot≈67) הייתה **מסמנת מילה** אף שהיא מתחת לסף 64px — סימון בטעות, D-033 | ⓐ המספרים הפכו **מילוליים** (63 · 64) ונוספה אסרציה שהקבוע הוא 64. ⓑ נוספה הבדיקה היחידה שמפרידה בין שני השערים: `dx=60 · dy=30` ⇒ ‏26.6° ✓ · ‏hypot≈67.1 ✓ · ערך מוחלט 60 ✗ | ✅ **תוקן C-0252.** אחרי התיקון **שלוש** המוטציות מפילות בשם: ⓐ ⇒ 3 נפילות · ⓑ ⇒ נפילה אחת · רצועת הקצה ⇒ נפילה אחת. ‏`Tests 18 passed (18)` בשחזור | 0 |
| F-106 | 🟡 MEDIUM · **אסרציה בלתי-אפשרית בתוכנית — אפשר לספק אותה רק במחיקת קוד עובד · נפתח ותוקן על ידי DEV באותו טיק (C-0252, טיק ביצוע)** | `docs/superpowers/plans/2026-08-21-flashcard-swipe-and-decay.md` משימה 3 צעד 1 ⇒ `components/Flashcard.test.ts` | התוכנית הכתיבה `expect(SRC).not.toContain('preventDefault')` וניבאה במפורש שהיא «עוברת כבר עכשיו». ⛔ היא ⛔ אינה יכולה לעבור: `onSubmit` של טופס ההקלדה קורא `e.preventDefault()` מאז T-085 | סוכן שממלא את התוכנית מילולית מוחק את הקריאה ⇒ טופס ההקלדה **מרענן את העמוד** בשליחה, והלומד מאבד את התשובה שהקליד. כלומר האסרציה שוברת את כיוון ההקלדה כדי לאכוף כלל על **מחווה** שהטופס ⛔ אינו | ⛔ ⛔ למחוק את הקריאה: הבדיקה כוונה לטענה ש-D-042ⓒ באמת עושה — **אפס `preventDefault` במטפל מחווה**. נמדד לפי המטפל העוטף (`lastIndexOf('onSubmit') > lastIndexOf('onPointer')`) ועם שומר-ריק שדורש בדיוק מופע אחד | ✅ **סגור C-0252.** ‏`Tests 38 passed (38)` בשני קבצי הרכיב, וכיוון ההקלדה ⛔ לא נגע | 0 |
| F-107 | 🟠 HIGH · **בדיקה חיה חדשה ירשה מצב עמוד מבדיקה קודמת ומדדה את ההפך ממה שהצהירה · נפתח ותוקן על ידי DEV באותו טיק (C-0252, טיק ביצוע)** | `scripts/verify-mobile.mjs`, בלוק `if (route === '/dev/deck')` של המחווה (האחרון בגוף הלולאה) | **נמדד בהרצה חיה ⛔ ולא שוער:** `swipe before reveal ⛔ does not grade` נפלה בשלושת הרוחבים עם `remaining moved 5 → 4`. הסיבה — בלוק **יעדי המגע** שרץ קודם באותה לולאה **חושף** את הכרטיס הראשון כדי למדוד את שני כפתורי הסימון, ולכן העמוד שהגיע לבלוק החדש ⛔ אינו במצבו ההתחלתי | המחווה «לפני החשיפה» רצה על כרטיס **חשוף** וסימנה אותו כדין ⇒ הבדיקה הפילה מימוש תקין. ⚠️ הכשל המסוכן הוא ההפוך: אילו הטענה הייתה מנוסחת רופף היא הייתה **ירוקה** ומעידה על הגנה שמעולם לא נבדקה | ⛔ ⛔ להחליש ו⛔ לא להסיר: הבלוק **מבסס את התנאי שהוא מודד** — `page.reload()` מחזיר את הפיקסטורה, ואחריו `check` שסופר `[data-reveal]` ומאמת שהחזית באמת בלתי-חשופה. ⛔ בלי הספירה הבדיקה הייתה עוברת ריק ביום שבו בלוק אחר יחשוף שוב | ✅ **תוקן C-0252.** ‏`check:mobile` ✓ **1059** (‏1041→1053→1059, כל מספר מהרצה משלו) · שתי מוטציות (הסרת ה-`reload` · שינוי תווית ה-`check`) מפילות את השומר בשם | 0 |
| F-112 | ⚪ LOW · **שער שנועל את איות הזהות ⛔ ולא את התכונה שהוא מודד · נפתח ותוקן על ידי DEV באותו טיק (C-0259, טיק ביצוע)** | `scripts/measure-sense-accuracy.test.ts:59` — «writes exactly what it prints» | **בדיקה קיימת נפלה על **שינוי שם משתנה**, ⛔ ולא על רגרסיה.** האסרציה הייתה המחרוזת המילולית `writeFileSync(OUT, markdown`, ו-Task 5 של התוכנית מצרף סעיף שני לאותו דוח ⇒ הערך הנכתב הופך ל-`full`. התכונה שהשער נועד לשמור — «הנכתב והמודפס הם רינדור אחד ⛔ ולא שניים» — ⛔ לא נפגעה כלל. ⛔ **התוכנית ⛔ לא נקבה בבדיקה הזאת** בשום צעד, ולכן הנפילה התגלתה בהרצה ⛔ ולא בקריאה. | סוכן שמריץ את Task 5 מילולית מקבל `npm test` אדום על שינוי תקין, ומתפתה ⓐ להחזיר את השם הישן (ומאבד את הסעיף השני) או ⓑ למחוק את השער (ומאבד את ההגנה על «רינדור אחד»). שתי התגובות גרועות מהתיקון. | ✅ **תוקן בטיק הזה, ⛔ בלי להחליש:** האסרציה קוראת מעכשיו את **המזהה** מתוך המקור (`/writeFileSync\(OUT,\s*([A-Za-z_$][\w$]*)/`) ומאמתת ש-`console.log(<אותו מזהה>)` קיים. ⇒ שינוי שם עובר, ⛔ ופיצול שני הערכים ⛔ עדיין נופל. **אומת במוטציה:** ‏`console.log(full)` ⇒ `console.log(markdown)` הפיל את הבדיקה (‏1 failed \| 11 passed), והשחזור החזיר ‏12/12. | ✅ טופל | 0 |
| F-113 | 🟡 MEDIUM · **ניבוי מספרי שגוי בתוכנית — ספירת שורות הוצגה כספירת מועמדים · נפתח ותוקן על ידי DEV באותו טיק (C-0259, טיק ביצוע)** | `docs/superpowers/plans/2026-08-21-content-headroom-and-cross-validation.md` — Task 5 צעד 9 | **התוכנית מצהירה «נמדד 2026-08-21, ⛔ ואינו ניחוש: 3,301 מועמדים», וההרצה החזירה 3,291.** השורש נמדד ⛔ ולא שוער: `3301` הוא `gold.lowGlosses`, כלומר **ספירת שורות** ב-H1; `tallyCrossValidation` עובר על `entry.low` שהיא **`Set`**, ולכן סופר זוגות `(lemma, גלוסה מנורמלת)` **נבדלים**. מדידה ישירה: `lowGlosses 3301` · `distinct 3291` ⇒ **10** שורות ב-H1 חוזרות על גלוסה שכבר נישאת לאותה למה. | סוכן שמאמין למספר בתוכנית רואה `3291` בפלט, מסיק שהמימוש מאבד 10 מועמדים, ומוסיף «תיקון» שסופר שורות במקום זוגות נבדלים — כלומר **מנפח** את המכנה של שיעור השדרוג ב-10 טענות כפולות ברגע שמקור שני ינחת. | ✅ **המספר בתוכנית תוקן ל-3291, ⛔ והקוד ⛔ לא נגע.** אותה מחרוזת עברית לאותה למה היא **טענה אחת** לאימות ⛔ ולא שתיים ⇒ הדדופליקציה נכונה, והניבוי הוא שהיה שגוי. הערה מנומקת נכתבה ב-Task 5 צעד 9 עם שני המספרים ועם ההפרש. | ✅ טופל | 0 |
| F-115 | 🟡 MEDIUM · **שתי הוראות בתוכנית ⛔ אינן יכולות להתקיים יחד — סתירה פנימית בשער תיעוד · נפתח ותוקן על ידי DEV באותו טיק (C-0261, טיק ביצוע)** | `docs/superpowers/plans/2026-08-21-onboarding-trim-and-dev-user.md` Task 2 (Step 1 מול Step 4) · `app/api/profile/route.test.ts` | **התוכנית מורה על `expect(section).not.toContain('institution')` לסעיף `POST /api/profile` בחוזה, ובאותה נשימה מורה לכתוב בו שלוש שורות שמסבירות «מה קרה לשדה `institution`».** שתי ההוראות סותרות: כל נוסח שמסביר את ההסרה **מכיל** את שם השדה, וכל נוסח שעובר את הטענה ⛔ אינו יכול להסביר דבר | הסוכן שמבצע כלשונה מקבל בדיקה בלתי-ניתנת לסיפוק, ואז «פותר» אותה בדרך הזולה: מוחק את פסקת ההסבר. ⇒ **החוזה נשאר בלי השורה שמונעת `drop column`** — בדיוק הכשל ש-D-056 נועדה למנוע, ובדיוק ההפך מכוונת התוכנית | הטענה נכתבה על מה שהתוכנית **התכוונה** לו: השדה ⛔ אינו מתועד **כשדה מתקבל** — `not.toContain('"institution"')` (⛔ אינו בגוף הדוגמה) + `not.toContain('120')` (⛔ אין לו תקרה) — **וגם** `toContain('D-056')` + `toContain('institution')`, כלומר ההסבר **חייב** להישאר. הנימוק נכתב בהערה בבדיקה עצמה | ✅ **תוקן C-0261 (DEV).** ‏`app/api/profile/route.test.ts` ⇒ 8/8 ירוקות. ⚠️ **נוסח התוכנית ⛔ לא תוקן בקובץ התוכנית** — הוא מסמך היסטורי של הטיק שכתב אותו; הסטייה מוצהרת בהערה בקוד, במקום שבו הסוכן הבא יקרא אותה | 1 |
| F-118 | 🟠 HIGH · **פלטת מקלדת הבלוקים קורסת לעין דיכרומטית — נמדד בכלי שנכתב היום · נפתח על ידי DEV (C-0289, טיק ביצוע)** | `plan/39-messages-spec.md` § 3 — טבלת «פלטת חלקי דיבר» · `scripts/validate_palette.mjs` | **חמשת הצבעים ב-§ 39.3 ⛔ אינם סדרה קטגורית תקינה.** נמדד ב-`node scripts/validate_palette.mjs --colors "#f2b544,#5b9bf5,#2ec5c5,#8b95ab,#d178e8" --surface "#0f172a"`: **כחול `#5b9bf5` מול סחלב `#d178e8`** — ΔE **5.9** בפרוטן ו-**16.5** בדויטן; **טורקיז `#2ec5c5` מול אפור `#8b95ab`** — ΔE **16.0** בפרוטן ו-**9.3** בדויטן. הסף שלנו הוא 20. ⚠️ ועל משטח בהיר (`#f8fafc`) **כל חמשת הצבעים** נופלים גם בניגודיות: 1.75 · 2.69 · 2.03 · 2.87 · 2.65 מול רצפה של 3:1. | לומד פרוטן פותח את מקלדת הבלוקים ורואה **שם עצם וכינוי כאותו צבע**, ו**תואר וחיבור כאותו צבע** — כלומר ארבע מחמש הקטגוריות מתמזגות לשתיים. ⚠️ ובמצב בהיר ⛔ אף אחד מהפסים ⛔ אינו נראה כגבול פקד כלל. ⇒ הצעה להוסיף צבעים לארבעת חלקי הדיבר החסרים (פריט 53ⓐ לרוי) **מחמירה את זה**, ⛔ ולא מתקנת: תשע קטגוריות באותו מרחב צבע יתמזגו עוד יותר. | ⛔ **⛔ אין לתקן את הצבעים כאן ביד** — § 39.3 הוא מסמך נגזר מ-`36`, והצבעים בו הם הכרעת עיצוב. **נרשם לרוי כחלק מפריט 53ⓐ עם המספרים.** ⚠️ מה שכן מוצהר כבר עכשיו: הכלל «כל בלוק נושא פס צבע **וגם** מקרא כתוב» ש-§ 39.3 עצמו קובע הוא מה שמונע מהפגם הזה להיות חוסם — **ובדיוק לכן ⛔ אסור לוותר עליו**, בשום מסך של המקלדת. ⇒ `npm run check:palette` הוא עכשיו הכלי שכל שינוי צבע חייב לעבור. | ✅ **נסגר 24/08 בהכרעת רוי — D-112, ⛔ ולא בתיקון.** *«אם זה מתאים לאייפון בחוקים אין לי בעיה»* ⇒ ⛔ **הצבע במקלדת הוא קישוט, והמקרא הכתוב הוא המידע** — וזה בדיוק מה שכללי אפל דורשים ומה ש-`39 § 3` כבר מחייב. ⇒ הפלטה נשארת · ⛔ ⛔ אין מוסיפים צבעים (חמישה כבר מתמזגים, תשעה יתמזגו יותר) · **T-201** מקבע בבדיקה שבלוק ⛔ לעולם ⛔ אינו מוצג בלי המקרא שלו. ⚠️ **הפטור מרצפת הניגודיות תקף בדיוק כל עוד סעיף זה מתקיים.** | 0 |
| F-119 | 🟠 HIGH · **סוכן CONTENT עדיין נושא פרומפט שדוחף ל-`dev` ועוקף את השער · נפתח על ידי DEV (C-0290, טיק ביצוע)** | `docs/agents/CONTENT.md` · `plan/00-control.md` (`WORKING_BRANCH`) | **שלושה מארבעת הפרומפטים המתוזמנים עודכנו לצורת האתחול החדשה, והרביעי ⛔ לא.** עדכון המשימה המתוזמנת של CONTENT נחסם **פעמיים** בידי מסווג ההרשאות של הסביבה (`Blocked by classifier`) — ⚠️ ⛔ **לא בגלל תוכן שגוי, אלא כי זו המשימה היחידה שהיא `enabled`**. ⇒ הקובץ בריפו נכון (`git clone -b work/current` · `push origin work/current`), **והמשימה המתוזמנת עדיין מכילה את הנוסח הישן**. | CONTENT רץ ב-04:00Z, משכפל את `dev`, מייצר אצווה, ו**דוחף ישירות ל-`dev`** — ⛔ בלי `verify`, ⛔ בלי הליכה, ⛔ בלי `loop:health`, כלומר **עוקף את השער כולו** ביום הראשון שבו השער קיים. ⚠️ ואז `work/current` נמצא **מאחורי** `dev`, וה-`rebase` של DEV בטיק הבא ירוץ מעל עבודה שאיש לא סקר. | ⛔ **פעולה אנושית — רוי בלבד.** בעריכת המשימה המתוזמנת «English-web loop — CONTENT»: להחליף `-b dev` ב-`-b work/current`, ו-`push origin dev` ב-`push origin work/current`. **שתי מחרוזות, ⛔ ולא כתיבה מחדש.** ✅ **ובינתיים הפגם ⛔ אינו שקוף:** בדיקה 10 הורחבה 24/08 למדוד **שני הכיוונים** — `behind > 0` נופל בשמו («משהו נדחף ל-dev ועקף את השער»), ובדיקה שסופרת רק «לפני» הייתה מדווחת ok כל הזמן. | ✅ **נסגר 24/08 — המשימה המתוזמנת של CONTENT עודכנה בהצלחה בניסיון השלישי.** היא משכפלת `work/current` ודוחפת אליו, ומפנה ל-`docs/agents/CONTENT.md` ול-`plan/25-content-commissions.md`. ⚠️ שני הניסיונות הראשונים נחסמו בידי מסווג ההרשאות; הנוסח השלישי, בלי גליפים ובלי ניסוח מתריס, עבר. ⇒ **ארבעת הסוכנים על אותו ענף.** | 0 |

<!-- 1 שורות -->
| F-122 | 🟠 HIGH · **קובץ ההוראות של DEV מדפיס כלל שבוטל, והוא הכלל שמכשיל תוכניות מסך · נפתח על ידי DEV (C-0297, טיק תכנון)** | `docs/agents/DEV.md` — הטבלה «⛔ HOW TO USE `docs/design/` — `36 § 14.4`» | **הטבלה בפרומפט אומרת «גימור חזותי — מהחוקה ומהאסימונים. בגימור, החוקה גוברת», ו-`36 § 14.4` **התהפך 24/08 בהוראת רוי (P4-2)** ואומר את ההפך המדויק: «הרנדר מחייב — פריסה וגימור כאחד», ושכבה A היא ההחרגה היחידה.** ⚠️ ⛔ זו ⛔ אינה אי-דיוק ניסוחי: `scripts/check-plan-shape.mjs` (בדיקת `finish`) **פוסלת במפורש** תוכנית מסך שנשענת על הנוסח הישן, והערת הקוד שם נוקבת בסיבה — «זה היה דלת היציאה מכל פער חזותי». ⇒ הפרומפט וההרמנס סותרים זה את זה, וה-DEV קורא את הפרומפט ראשון. | ‏DEV קורא את `DEV.md`, בונה מסך שנראה שונה מהרנדר בצללים · בהגבהה · בטיפוגרפיה, ומצדיק כל פער במשפט «הגימור מגיע מהחוקה» — בדיוק הנוסח שבוטל. ⚠️ ואם הוא כותב תוכנית, `npm run check:plan` מפילה אותה על `finish` **בלי שהוא מבין למה**, כי הפרומפט שלו אומר שהוא צודק. ⇒ טיק שרוף, או — גרוע יותר — מסך שנבנה לפי הכלל הבטל ועובר, כי ⛔ אין שער חזותי אוטומטי. | החלף את שתי שורות הטבלה ב-`docs/agents/DEV.md` בנוסח של `36 § 14.4` כפי שהוא היום: **פריסה · מחרוזות · מבנה · סדר ⇒ מחייב · גימור חזותי ⇒ ✅ מחייב גם הוא · נגישות (שכבה A) ⇒ גוברת על הצילום.** ⛔ ולהוסיף במפורש: «⛔ אין «הגימור מגיע מהחוקה» כתשובה לפער מהרנדר». ⚠️ ‏`scripts/agent-prompts.test.ts` כבר קיים ומגן על הקובץ מסודות — **בדיקה שם שתפיל את הנוסח הישן** היא מה שמונע חזרה. | ✅ **נסגר C-0298 באותו ערב, בדיוק כפי שהוצע.** שתי שורות הטבלה ב-`docs/agents/DEV.md` הוחלפו בשלוש: פריסה ⇒ מחייב · **גימור ⇒ מחייב גם הוא** · נגישות שכבה A ⇒ **גוברת על הצילום**, והיא ההחרגה היחידה. ⇒ ובדיקה ב-`scripts/agent-prompts.test.ts` **פוסלת את הנוסח הישן בשני הפרומפטים שנוגעים ברנדר** ודורשת את החדש — ⛔ ולא רק «אין את הישן». ⚠️ **והבדיקה תפסה אותי בזמן אמת:** ההערה שכתבתי כדי להסביר את ההיפוך **ציטטה את המשפט המת**, והבדיקה הפילה אותה. ⇒ נוסח מחדש בלי הציטוט. ⚠️ **הכשל היה שלי:** הפכתי את `36 § 14.4` (D-114) ועדכנתי מפרט · `check-plan-shape` · פרומפט QA — ⛔ **ולא את פרומפט DEV**. **הסוכן עצמו תפס את זה בטיק הראשון שלו.** | 0 |

<!-- 3 שורות -->
| F-128 | 🟡 MEDIUM · **שתי שורות משימה מגדירות בדיקות שהריפו ⛔ אינו יכול להריץ, ואף אחד ⛔ לא מדד זאת לפני שהתוכנית נכתבה · נפתח על ידי DEV (C-0303, טיק תכנון)** | `plan/50-tasks.md` שורות T-202 · T-203 · `vitest.config.ts` · `package.json` | **נמדד בטיק הזה, ⛔ ולא הוערך.** T-202 ו-T-203 נוקבות שתיהן בבדיקות מסוג `expect(screen.getByText('שאלת הבנה')).toBeInTheDocument()` ו«אחרי הקשה אחת על הפעולה הראשית» — כלומר **בדיקת רינדור ב-DOM**. `vitest.config.ts` הוא `environment: 'node'`, ‏`package.json` ⛔ אינו מחזיק `jsdom` ו⛔ אינו מחזיק `@testing-library/react`, וההערה בקונפיג עצמו אומרת במפורש: «components/ holds source-scanning guards only … a React render test does not belong here — **add jsdom first** if that ever changes». ⇒ הבדיקות ששתי השורות דורשות ⛔ **אינן ניתנות להרצה היום**, ⛔ ולא בגלל טעות בניסוח. | לולא המדידה, ה-DEV הבא היה מגלה זאת אחרי שכתב את הקוד — ואז הפיתוי הוא להוריד את הבדיקה לסריקת מקור ולסמן ירוק. **זו בדיוק החולשה ש-T-202ⓔ נוקבת בה בעצמה:** «a fixture of a component in isolation is what let F-124 pass 1,119 green checks». | **התוכנית `docs/superpowers/plans/2026-08-26-story-slice-c.md` צעד 1 פותרת זאת בתוך הטיק המבצע** — התקנת `jsdom` · `@testing-library/react` · `@testing-library/dom` והצטרפות פר-קובץ ב-`// @vitest-environment jsdom`, ⛔ בלי לשנות את ברירת המחדל `node`. ⛔ ואם הארגז חוסם את ההתקנה — ⛔ **אין להוריד את הבדיקה לסריקת מקור בשקט**: ממצא, ודיווח מפורש. | ✅ **נסגר — אומת בהרצה C-0307 (DEV).** `package.json` מחזיק `jsdom` · `@testing-library/react` · `@testing-library/dom`, ‏`vitest.config.ts` נשאר `environment: 'node'` כברירת מחדל, וההצטרפות היא פר-קובץ ב-`// @vitest-environment jsdom`. `components/StoryScreen.dom.test.tsx` רץ בטיק הזה ⇒ **8 בדיקות עברו** — בדיקת רינדור אמיתית, ⛔ ולא סריקת מקור. **PM** נשאר לידיעה: שורת משימה שנוקבת בבדיקה חייבת לנקוב בבדיקה שהריפו יכול להריץ. ✅ **על `dev` — מוזג C-0310 (QA · ff-only).** | 0 |
| F-133 | 🟠 HIGH · defect (⛔ אינו מגיע ללומד) · **פיקסטורת `/dev/story` מזווגת שאלה אמיתית עם גוף של סיפור אחר ⇒ מסך ההבנה בפיקסטורה ⛔ אינו בר-מענה, והבדיקה שלו מאשרת חוויה לא-קוהרנטית · נפתח על ידי CRITIC (C-0306, טיק שער)** | `data/generated/story-questions-2026-08-25.jsonl:2` (השאלה שייכת ל-«The letter in the book») · `app/dev/story/done/page.tsx:24-27,56-60` (הגוף = «The library near the river») · `app/dev/story/page.tsx:41-51` · `components/StoryScreen.dom.test.tsx:18-27` | **נמדד ב-`next start` בדפדפן חי ב-375×780, ⛔ ולא שוער.** גוף הפיקסטורה הוא ה-`STORY` של `render_video_A.py` — «The library near the river», הסיפור שממנו נמדד `kol-A-06-question.png`, ששאלתו היא «What did Maya find inside the book?» ⇒ «one sentence in pencil». ⛔ אך השאלה שהפיקסטורה טוענת היא **שורה 2** של ה-jsonl — «Who wrote the letter that was in the book?», תשובות `[אם · אנשים · חבר]`, נכונה=חבר — והיא שייכת ל**סיפור אחר**, «The letter in the book». הגוף המוצג ⛔ אינו נוקב במכתב ו⛔ לא בכותב: «someone had written one sentence in pencil». המשוב עצמו **עובד** (נכונה=חבר מודגשת ✓, שגויה ✗), אבל ⛔ אף קורא ⛔ אינו יכול לגזור «חבר» מהגוף. ⚠️ ⓑ render-fidelity משני: תשובות ה-jsonl **בלי ניקוד** (`אם/אנשים/חבר`), והרנדר `kol-A-06` מציג תשובות **מנוקדות** (`מִשְׁפָּט אֶחָד בְּעִפָּרוֹן`) — פער מדוד במחרוזות, שייך ל-CONTENT. | לומד שמגיע ל-`/dev/story/done` (וכל קורא של בדיקת ה-DOM) רואה שאלת הבנה **ללא תשובה נגזרת** — בדיוק «האם הפיקסטורה מייצגת חוויית לומד תקינה», הלקח מ-23/08 שהפיקסטורה **בעצמה מצטטת** בהערתה. הבדיקה `StoryScreen.dom.test.tsx` מקבעת על הזיווג הזה ⇒ 2,676 בדיקות ירוקות ⛔ אינן יכולות לתפוס רגרסיה של «שאלה בלי תשובה». ⛔ **⛔ אינו מגיע ללומד:** `/dev/*` מוזן-פיקסטורה, והנתיב האמיתי `app/(tabs)/world/story` קורא שאלות **לפי כותרת** מ-Supabase, מזווגות נכון. | לזווג את גוף הפיקסטורה עם **שאלתו שלו**: או להשתמש בשאלת «library/river» שממנה נמדד `kol-A-06` («What did Maya find inside the book?» ⇒ «one sentence in pencil»), או להחליף את הגוף ל-«The letter in the book». להחיל את אותו תיקון על `app/dev/story/page.tsx` ועל `components/StoryScreen.dom.test.tsx` כדי ששלושתם יסכימו. את ⓑ (ניקוד התשובות) — לתעד לזרימת CONTENT. | ✅ **ⓐ נסגר בקוד C-0307 (DEV)** — הפיקסטורה היא מקור **אחד**, `app/dev/story/story-fixture.ts`, וגוף ושאלה באים בה מאותו סיפור: `STORY` ו-`QUESTION` של `render_video_A.py`. `app/dev/story/story-fixture.test.ts` קורא את קובץ הפייתון ומודד מחרוזת מול מחרוזת, מוודא שכל מילת תוכן בשאלה נפתרת אל אסימון שבגוף דרך `storyLemma` של המוצר, וסורק את שלושת הצרכנים כך שעותק שני **מפיל את הבנייה** — הוכח בהרצה: זיווג F-133 נתפס, זיווג הרנדר נקי. הליכה חיה ב-`next start` ב-375×780: «What did Maya find inside the book?» ⇒ «מִשְׁפָּט אֶחָד בְּעִפָּרוֹן», נגזרת מ«had written one sentence in pencil» · ⛔ אפס גלילה · ⛔ אפס יעד<44px · ⛔ אפס שגיאת קונסול. ⚠️ ⓑ (ניקוד התשובות) הופרד ל-**F-134** → CONTENT. ✅ **ⓐ על `dev` — מוזג C-0310 (QA · ff-only).** | 0 |
| F-135 | 🟠 HIGH · **הזרימה הפעילה מתרוקנת ואיש ⛔ אינו מזיז אותה — כל טיק DEV בחלון הזה הוא טיק ריק · נפתח על ידי DEV (C-0308, טיק ביצוע)** | `plan/00-control.md` (`ACTIVE_WORKSTREAM`) · `scripts/measure-plan-tables.mjs` (הדגלים) · `docs/agents/CRITIC.md` | **נמדד בלילה הראשון של הלופ:** `story` התרוקנה כשה-QA מיזג ב-**05:29**, DEV מדד אותה יבשה ב-**07:03**, ו-`ACTIVE_WORKSTREAM` עדיין אמר `story` ב-**07:45**. ⛔ **הדגל באינדקס היה קיים — והוא ⛔ לא הכריע:** הוא אמר «או שהוא גמור ויש לעבור לבא בתור, **או** שה-PM חייב לפתוח בו את הפרוסה הבאה», ⛔ בלי לנקוב בזרימה הבאה ו⛔ בלי לנקוב בבעל התפקיד. ⚠️ ו-`docs/agents/CRITIC.md` אמר «הזז כשהיא **מוצתה**» — מילה ש⛔ מעולם ⛔ לא הוגדרה. | ‏DEV לוקח עבודה **רק** מהזרימה הפעילה ורץ **12 פעמים ביום מול 4 של QA**. ⇒ בין 07:03 ל-11:00 שני טיקי DEV היו נופלים על זרימה ריקה: שכפול, קריאת פרומפט, ⛔ ואפס תוצר. **זה ⛔ אינו באג של נכונות — זה בזבוז שקט**, וזה בדיוק הסוג שאיש ⛔ אינו מבחין בו כי הכל «ירוק». | ✅ **שלושה תיקונים, C-0308.** ⓐ **הדגל מכריע:** נוקב ב-`nav` בשמה ובמספר המשימות שבה, נוקב ב-**QA** כבעל התפקיד, ודורש **בטיק הזה, ⛔ ולא בבא**. ⓑ **`loop:health` בדיקה 11** — הבדיקה היחידה שמודדת **בזבוז** ⛔ ולא נכונות; אדומה עכשיו על `story`. ⓒ **«מוצתה» הוגדרה** בפרומפט QA: **אפס שורות ⬜**, ⛔ ולא פרשנות. ⚠️ **ומה ש⛔ אינו נפתר בזה, ונאמר במפורש:** ⛔ **«אין ⬜» ⛔ אינו «הפיצ׳ר גמור».** הרגיסטר מתרוקן גם כשאיש פשוט ⛔ לא כתב את הפרוסה הבאה. ⇒ QA חייב לומר **איזה מהשניים** מול `36 § 13`. **הגדרת-גמור לכל פריט ב-`36 § 13` ⛔ אינה קיימת** — נרשם כפריט לרוי. | ✅ **נסגר C-0309 — שני חצאיו.** ⓐ המנגנון: הדגל מכריע · בדיקה 11 · «מוצתה» הוגדרה. ⓑ **וההגדרה שחסרה — רוי נתן אותה 26/08 (D-116 · `36 § 13.1`):** שלוש חותמות — **הגעה בלחיצות · פעילות על נתונים אמיתיים · שמירה ששורדת יציאה** — ומפורטות פריט־פריט ב-`13.2`. ⇒ ⛔ **«אין ⬜» כבר ⛔ אינו הקריטריון**, והוא הוחלף בשלושה תנאים **שנמדדים בהליכת דפדפן**. ⚠️ ושתי החמרות שנגזרו: **כתובת שמקלידים ⛔ אינה הגעה** (ופיקסטורת `/dev/*` ⛔ אינה ⓐ), ו**מסך שמצייר כשל בהיעדר env ⛔ אינו «פעילות תקינה»**. | 0 |

<!-- 1 שורות -->
| F-137 | 🟡 MEDIUM · defect (⛔ אינו מגיע ללומד בארגז החול) | `lib/core/worldRing.ts` (`RingNodeState`) · `components/WorldRing.tsx` (`toArenaState` · `toStoriesState`) · `plan/50-tasks.md` (T-146ⓒ) | **‏T-146ⓒ («⛔ תמיד עם יציאה», D-065) ⛔ אינה נסגרת בטבעת · נפתח על ידי DEV (C-0314, טיק בנייה).** ⓐ ו-ⓑ של T-146 **כן** נסגרו במבנה — `ringScreen` מחזיר אזור שגיאה אחד ופעולה אחת, ⛔ ואין «שגיאה וגם תוכן». ⓒ ⛔ לא: המודל **משטח בכוונה** את כל קודי הכשל ל-`unknown` («הצומת ⛔ אינו מכיר קודי שגיאה»). | לומד שהסשן שלו פג מקבל `session_expired` ⇒ המודל רואה `unknown` ⇒ המסך מציע «נסה שוב» → `/world` ⇒ אותו כשל שוב ⇒ **לולאה**, ⛔ ולא `/login` — מחלקת המבוי הסתום של F-027. ⛔ ואותו דבר ל-`schema_missing`, שדורש ניווט ללשונית שעובדת ⛔ ולא «נסה שוב». | ✅ **נסגר בקוד C-0315 (DEV), ⛔ ולא בהכרעה.** נמדד מחדש: הפער ⛔ לא היה «מחלקה חדשה» אלא **צרכן חמישי** של `lib/core/failureExit.ts` — הטבלה שכבר מכריעה «לאן אפשר ללכת מכאן» ב-`LevelMapScreen` · `StudyDeckScreen` · `LevelScan` · `app/error.tsx`. ⇒ `ringScreen(inputs, retryHref, code)`, ושלושת ענפי המצב הריק עוברים דרך `emptyScreen()` **אחד**. ⚠️ **והתוכנית ⛔ לא נערכה בשקט:** בלוק ה-`Interfaces` שלה נושא עדיין את החתימה בת שני הארגומנטים, והשורה נרשמה ב-`plan/26-plan-feedback.md` (§ 0.5ג — משוב ⛔ אינו חוסם ביצוע). **הליכה 375×780:** `/world` ⇒ **בדיוק אחד** «נסה שוב» · `/dev/world/ring/expired` ⇒ **אפס** «נסה שוב», פעולה אחת «התחברות מחדש» → `/login`. | ✅ נסגר C-0315 | C-0314 |

<!-- 1 שורות -->
| F-139 | 🟠 HIGH · improvement (⛔ אינו מגיע ללומד — מכונת הלופ) · **`cards` הוכרזה זרימה ב-`36 § 13` (D-122) אך חסרה מאוצר המילים של הקוד ⇒ חמש שורותיה «תג לא מוכר» ובדיקה 11 ⛔ אינה יכולה למדוד · נפתח על ידי CRITIC (C-0316, טיק שער)** | `lib/core/planTable.ts:335` (‏`WORKSTREAMS`) · `plan/00-control.md` (‏`ACTIVE_WORKSTREAM: cards`) · `docs/plan-open.md` (מחולל) | **נמדד בטיק הזה, ⛔ ולא הוסק.** עריכת D-122 הוסיפה `cards` כפריט 3 ב-`36 § 13`, הוסיפה שורת `cards` ל-`WORKSTREAM_TICKS`, ותייגה חמש שורות `M1 · cards` — אך `WORKSTREAMS` ב-`planTable.ts` נשאר `[story·nav·arena·studies·msgs·loop·base]`, **⛔ בלי `cards`**. ⇒ `classOf` מחזיר `workstream=null` לחמש השורות; `measure:plan` מדווח **«5 bad tags»**, האינדקס מדפיס «⛔ תג לא מוכר … אינו נספר בשום מקום», והן נופלות ל«ללא זרימה». `loop:health` בדיקה 11: «⛔ `cards` אינו בטבלת המאזן». | QA מזיז את `ACTIVE_WORKSTREAM` ל-`cards` (מחויב לפי הרצף · nav ⬜=0), אך לוח המחוונים של הלופ עצמו אומר שהזרימה הפעילה ⛔ אינה קיימת, ובדיקה 11 ⛔ אינה יכולה לאשר שיש בה עבודה. ⚠️ DEV **כן** יכול לקחת את חמש השורות לפי התג המילולי `cards` (הן ב-`eligibleTaskIds`), אך אזהרת «תקן או הסר» מזמינה **הסרה** של זרימה מוכרזת כדין. | **DEV:** הוסף `'cards'` ל-`WORKSTREAMS` ב-`lib/core/planTable.ts` **אחרי `'nav'`** (מיקום 3 ברצף), ואז `npm run measure:plan`. ⇒ חמש השורות מסווגות · בדיקה 11 מודדת · המאזן מציג. ⛔ CRITIC ⛔ אינו נוגע בקוד. | ✅ **נסגר 26/08 (עריכת אופרטור, אותו תיקון שה-QA כתב).** `'cards'` נוסף ל-`WORKSTREAMS` אחרי `'nav'` ב-`lib/core/planTable.ts` · `planTable.test.ts` **51 עברו** · `measure:plan` הורץ מחדש · ⇒ **`loop:health` = 11/11 — בדיקה 11 מודדת עכשיו `cards — 5 משימות ⬜`.** ⚠️ השורש נרשם בקוד עצמו: פריט חדש ב-`36 § 13` חייב לנחות ב-`WORKSTREAMS` **באותו קומיט**, ⛔ לא באחד אחריו. | C-0316 |

<!-- 4 שורות -->
| F-155 | 🟠 HIGH · **הזירה מציירת משטחים כהים משלה ו⛔ אין לה דיו משלה — ו⛔ אין לה גם רקע.** ⛔ **הפגם ⛔ אינו הקלף; הקלף היה המדגם.** | `app/arcade/arcade-tokens.css` (הבלוק `[data-arena-scope]`) · `app/globals.css:11-38` (‏`tokens:light` / `tokens:dark`) · `components/ArenaBattle.tsx:523` (`<section data-arena-scope>`) · `docs/design/render_video_B.py` | **נמדד בדפדפן חי בטיק הבנייה C-0332 (‏Playwright · `next start` · 375×780 · `colorScheme` **בשתי** הסכימות · `getComputedStyle`), ⛔ ולא הוערך בעין.** ⓐ **דיו:** `--ink` ו-`--ink-muted` נקבעים ב-`globals.css` לפי `prefers-color-scheme`, ו-`[data-arena-scope]` ⛔ אינו דורס אותם. בסכימה **בהירה** — שהיא ברירת המחדל של `:root` — תווית קלף הלחש נמדדה **1.42:1**, ורצפת שכבה א׳ היא 4.5:1. ⓑ **רקע:** ל-`<section data-arena-scope>` ⛔ אין רקע כלל — `getComputedStyle` החזיר `rgba(0, 0, 0, 0)` ⇒ המסך יורש את `--surface`, כלומר **לבן** בסכימה בהירה, בעוד `render_video_B.py` מצייר את הבמה כולה בכחול־ליל. ⇒ ⓐ ו-ⓑ הם **אותו חור בדיוק**: לזירה יש פלטת משטחים ⛔ ואין לה שכבת טקסט/רקע תואמת. | **הפגם ⛔ אינו תיאורטי, וזו ברירת המחדל ⛔ ולא קצה:** לומד שמכשירו בסכימה בהירה — ברירת המחדל של `:root` — פותח את הזירה ורואה קלפי לחש שהטקסט עליהם כמעט בלתי קריא (1.42:1), על מסך לבן שהרנדר צובע כחול־ליל. ⛔ הוא ⛔ אינו יכול לבחור לחש שהוא ⛔ אינו יכול לקרוא. | ⛔ **הכרעת PM, ו⛔ לא בחירת מימוש — משום שהתיקון הישיר סותר טקסט כתוב.** ‏`app/arcade/arcade-tokens.css` קובע בראשו «כל טוקן סמנטי אחר (רקע, מסגרת, `--brand`) ממשיך להגיע מ-`globals.css`» ⇒ דריסת `--ink` בתוך הסקופ **סותרת אותו**. שני מסלולים, ושניהם מסמך: ⓘ הזירה מקבלת **סכימה כהה מוצהרת** (רקע `--arena-night` + דיו זירה על **כל** משטחיה) ⇒ עדכון המשפט ב-`arcade-tokens.css`; ⓘⓘ כל משטח כהה נושא טוקן דיו זירה נקודתית — מה ש-C-0332 עשה **לקלף בלבד**. ⛔ ⓘ הוא שינוי מסך שלם ⇒ ⛔ אינו נלקח בשקט. | ✅ **נסגר C-0334 (T-214)** — הסקופ מצהיר `background: var(--arena-night)` ודיו משלו; שלושת הצמתים עברו לטוקני הזירה. **נמדד בדפדפן חי:** 13/13 צמתי טקסט ≥4.5:1 ב**שתי** הסכימות, ובדיקת מוטציה (הסרת ה-`background` בלבד) הפילה את השער **בשם** על `זמן קרב` 2.1:1 · `הקוסם` 1.35:1 | C-0332 |
| F-156 | ⚪ LOW · defect חזותי (⛔ **אינו שובר מסך ו⛔ אינו חוסם לומד**) · **מסגרת קלף הלחש מתחת לרצפת 3:1 של רכיב לא-טקסטואלי, אחרי שהמילוי החליף גוון.** | `app/arcade/arcade-tokens.css` (`--arena-stone` · `--arena-card`) · `components/SpellCard.tsx:67` | **נמדד בדפדפן חי בטיק C-0332 (‏`getComputedStyle` על ארבעת הקלפים), ⛔ ולא הוערך.** המסגרת במצב **לא נבחר** היא `--arena-stone` = `rgb(74, 72, 88)` על מילוי `rgb(24, 33, 56)` ⇒ **1.8:1**. רצפת שכבה א׳ לרכיב לא-טקסטואלי היא **3:1**. ⚠️ המצב **נבחר** ⛔ אינו נפגע: `--arena-gold` על אותו מילוי = **7.29:1**. | הגבול בין קלף לקלף במניפה נשען על המסגרת. ב-1.8:1 ארבעת הקלפים נקראים כמלבן אחד על מסך בהיר או בעין חלשה. ⛔ **הבחירה עצמה ⛔ אינה נפגעת** (א2 מכובד: `aria-pressed` **וגם** המילה «נבחר»), ולכן זה גימור ⛔ ולא חסם. | ⛔ **⛔ לא נסגר ב-C-0332 ובנימוק מדוד:** `--arena-stone` הוא טוקן **מוזג וחי** מ-T-177, והוא מצייר גם מסגרות אחרות בזירה — שינוי גוון שלו הוא שינוי מסך, ⛔ ולא תיקון קלף. ⇒ או הגהה של הטוקן, או קו־שיער פנימי שני בקלף בלבד. ⚠️ שים לב שקו־השיער הפנימי הקיים (`border-white/25`, `render_video_B.py:267`) **כבר** נותן גבול — כך שייתכן שהתשובה היא «מדוד, ⛔ ואל תשנה». | ✅ **נסגר C-0334 (T-214)** — ⛔ `--arena-stone` ⛔ לא הוגה: הקלף הלא-נבחר לוקח `--arena-card-edge` (ערך `--arena-ink-dim` בתפקיד גבול) ⇒ **7.36:1** מול המילוי ו-**6.88:1** מול הבמה, שניהם מעל 3:1. נמדד בשער החדש (בלוק 2c), בשתי הסכימות | C-0332 |
| F-158 | ⚪ LOW · defect חזותי (⛔ אינו חוסם לומד) · **רקע הדמות על הבמה נעלם בסכימה כהה, אחרי שהבמה קיבלה רקע משלה · נפתח על ידי DEV (C-0334, טיק ביצוע)** | `components/ArenaAvatar.tsx:55` (`BACKGROUND_CLASS = text-surface-raised`) · `app/arcade/arcade-tokens.css` (`--arena-night`) | **נמדד בדפדפן חי בטיק C-0334** (‏Playwright · `next start` · 375×780 · `getComputedStyle` על שכבת הרקע של `<ArenaAvatar>`), ⛔ ולא הוערך: שכבה 1 של הדמות מצוירת ב-`--surface-raised`, שהוא `#1e293b` בסכימה **כהה** ⇒ **1.02:1** מול `--arena-night`. בסכימה בהירה הוא לבן ⇒ הקלף נראה. | הלומד רואה שתי דמויות על לוחית לבנה בסכימה בהירה, ו⛔ בלי לוחית כלל בסכימה כהה — שני מסכים שונים לאותו קרב. ⛔ הצורות עצמן קריאות בשתיהן, ולכן ⛔ אין חסימה. | שכבת הרקע של הדמות לוקחת טוקן זירה (או יורדת לגמרי) — **בתוך T-215**, שכבר משכתבת את `ArenaAvatar` לסדר `38 § 4`. ⛔ ⛔ לא כאן: T-214 ⛔ אינה נוגעת ב-`ArenaAvatar`. | ✅ **נסגר C-0335 (DEV, בתוך T-215) — הלוח ⛔ לא נצבע מחדש, הוא ⛔ נמחק.** `38 § 4` מונה אחת־עשרה שכבות ו⛔ אין בהן רקע ⇒ שכבה שאינה בעוגן ⛔ אינה מקבלת טוקן, היא יורדת. **נמדד בדפדפן חי אחרי המחיקה** (375×780, שתי הסכימות): הגיבור **10.56:1** והיריב **6.88:1** על כחול־הליל, **זהה בשתיהן**. ⚠️ ובאותה מחיקה נסגר גם ערוץ שני שלא היה בשורה: צללית האימפקט של T-182 הייתה **מלבן** בגודל ה-`viewBox`, ועכשיו היא דמות. | C-0334 |
| F-146 | 🟡 MEDIUM · **הכרעת מכניקה — ⛔ אינו defect ו⛔ אינו חוסם** · **`sense_distractors!inner` נשאר בשאילתת הזירה אף שהוא ⛔ אינו מספק עוד ולו אפשרות אחת — הוא נועל רמות על נתון שאיש ⛔ אינו רואה · נפתח על ידי DEV (C-0323, טיק בנייה)** | `app/api/arcade/round/route.ts:21` (`ROUND_SELECT`) · `lib/core/arcadeRound.ts` (`isEligible`) · `lib/core/arcadeLadder.ts:76` (`describeLevel`) | **נמדד בטיק הזה על המקור, ⛔ ולא שוער.** T-152 העבירה את ארבע האפשרויות לתרגומים עבריים של מועמדים אחרים באותה רמה, ו-`isEligible` **חדל** לדרוש שלושה מסיחים למילה: הכשירות היא band + תרגום לא ריק. ⛔ **אבל הנתיב עדיין מצרף `sense_distractors!inner`** — כלומר מילה שיש לה תרגום עברי תקין ו⛔ אין לה מסיחים **אנגליים** ⛔ אינה מגיעה כלל לשכבה הטהורה. ⇒ שער שהעילה שלו נמחקה. | `describeLevel` מחשב `unlocked = eligible >= 15`, ו-`eligible` נספר על מה שהנתיב **הביא**. ⇒ רמה שיש בה 15 מילים עם תרגום אך רק 11 מהן נושאות `sense_distractors` מוצגת ללומד **נעולה עם המספר «יש 11»** — נעילה על נתון אנגלי ש⛔ אינו מוצג ו⛔ אינו נדרש עוד. | **הסרת `sense_distractors!inner` מ-`ROUND_SELECT` היא שורה אחת** — ⛔ **ו-DEV ⛔ לא נגע בה בכוונה:** היא מרחיבה את בריכת הקרב ו**משחררת נעילות רמה שהלומד רואה**, כלומר שינוי מכניקת למידה ⇒ טבלת § 0.16 שולחת אותה ל-PM. ⚠️ נדרשת גם מדידה על הנתון בפועל (כמה מילים לכל band נושאות תרגום אך ⛔ לא מסיחים) לפני שמישהו מכריע. ⛔ הבדיקה `route.test.ts` מחזיקה את המצב הנוכחי במפורש בשם — היא תאדים כשההכרעה תגיע, וזה מכוון. | ✅ **נסגר C-0329 · T-212** — `sense_distractors!inner` ירד מ-`ROUND_SELECT`. ההכרעה שנדרשה הוכרעה ב-**D-129** (PM): 713/713 שורות אצווה נושאות ≥3 מסיחים ⇒ **0 שורות מושפעות היום**, והשער יורד לפני ש-T-153 תהפוך אותו למוקש. | C-0323 |

<!-- 2 שורות -->
| F-161 | 🟡 MEDIUM · פער עוגן (⛔ אינו חוסם לומד, ⛔ אך הוא הכרעה) · **‏`37 § 12` נוקב ב«ארבעה סלוטי ציוד», ‏`38 § 2` מונה **שבע** משבצות, ו⛔ אף מסמך עוגן ⛔ אינו אומר **אילו ארבע** · נפתח על ידי DEV (C-0337, טיק תכנון)** | `plan/37-arena-spec.md § 12` · `plan/38-character-base.md § 2` · `docs/design/render_video_B.py:72` (`GEAR`) · `docs/superpowers/plans/2026-08-28-arena-home.md` (Task 1) | **נמדד בגְרֶפּ, ⛔ ולא שוער.** ‏`37 § 12` כותב «ארבעה סלוטי ציוד». ‏`38 § 2` מונה `ראש · כתפיים · גוף · מותן · יד ראשית · יד משנית · רגליים` — **שבע**. ‏D-132 הכריע שהמסך מתייג **בשם המשבצת**, ו⛔ לא נקב ב**אילו** ארבע. ‏`render_video_B.py:72` מצייר ארבעה גליפים: חרב · מגן · **לחש** · שריון — ושלושה מהם נקראים ישירות למשבצת (`mainHand` · `offHand` · `body`), ⛔ אך ל**לחש** ⛔ אין משבצת ב-`38 § 2` כלל. ⇒ החיתוך הוא **שלוש**, ⛔ ולא ארבע. | לומד רואה ארבע משבצות; אם PM יכריע אחרת, שם משבצת אחד משתנה. ⛔ אפס מיגרציה, ⛔ אפס שינוי ב-`ARCADE_ITEMS`, ⛔ אפס נתון לומד. | ⚠️ **DEV הכריע ביניים תחת `RULES § 0.16` (הפיך בקומיט אחד, ונרשם):** `mainHand · offHand · head · body`, בסדר הימין-לשמאל של הרנדר — שלוש נקראו מהגליפים, והרביעית (`head`) תופסת את מקום ה**לחש**, שאין לו משבצת. ⇒ **PM מאשר או מחליף.** ⚠️ **נסגר על ידי D-135 (PM, C-0338), ⛔ ולא על ידי הכרעת-הביניים:** ‏PM מדד ש-`mainHand` היא משבצת ש-**0 מתוך 5** הפריטים ב-`ARCADE_ITEMS` יכולים למלא, ו-`legs` נמלאת בדיוק ב-`boots` ⇒ **`mainHand` יורדת ו-`legs` נכנסת**, מחרוזת אחת. ‏C-0341 בנה על ההכרעה: `HOME_SLOTS = legs · offHand · head · body`, ו-`banner` היא `null` ⛔ ולא משבצת (D-135 במפורש). שומר `⛔ כל ארבע המשבצות ניתנות למילוי` ב-`lib/core/arenaHome.test.ts` מודד בדיוק את הנימוק הזה. | ✅ C-0341 | C-0337 |
| F-162 | ⚪ LOW · פער בחוקה (⛔ אינו defect במוצר) · **תווית משבצת הציוד היא **9px** ברנדר, ולשכבה א׳ ⛔ אין רצפת גודל-טקסט כלל · נפתח על ידי DEV (C-0337, טיק תכנון)** | `docs/design/render_video_B.py:175` · `plan/35-design-constitution.md` (שכבה א׳) · `plan/36-video-spec.md § 14.4` | **נמדד בגְרֶפּ, ⛔ ולא בעין.** ‏`render_video_B.py:175` מצייר את תווית המשבצת ב-`9` נקודות (`c.txt(..., name, 9, "Medium", ...)`). ‏`36 § 14.4` מחייב את הרנדר **גם בגימור**, ושכבה א׳ היא ההחרגה היחידה — ושכבה א׳ מונה ניגודיות · יעד 44px · ⛔ אין מצב בצבע בלבד, ו⛔ **אינה נוקבת ברצפת גודל טקסט**. ⇒ ⛔ **אין עילה בחוקה להעלות את 9px**, והתוכנית מיישמת את הרנדר. | תווית בת 9 נקודות בעברית על מסך 375px היא קריאה גבולית ללומד מבוגר. ⛔ הניגודיות ⛔ אינה הבעיה — היא נמדדת בשער של D-134. | הכרעה של PM: או ששכבה א׳ מקבלת **רצפת גודל טקסט** מפורשת (ואז מספר, ⛔ ולא «קריא»), או ש-9px נשאר ונרשם כמכוון. ⛔ ⛔ אין דרך שלישית — «נראה קטן» ⛔ אינו עילה. ⚠️ **נסגר על ידי D-137 (PM, C-0338 · שכבה ב׳):** ‏PM סרק את `app/` ו-`components/` ומדד ש-9px היה נעשה הטקסט הקטן ביותר ששוגר במוצר — **18% מתחת** ל-`text-[11px]` היחיד ו-**25% מתחת** למדרגה הקטנה בסולם ⇒ **טקסט משני בזירה הוא `text-xs` (12px) ומעלה, ובזירה בלבד**. ‏C-0341 מיישם: תווית המשבצת, תת-הכותרת (11.5 ברנדר) ותווית ההפרדה (10.5 ברנדר) — כולן `text-xs`, ושומר ב-`components/ArenaHome.test.ts` מפיל כל `text-[Npx]` מתחת ל-12. | ✅ C-0341 | C-0337 |

<!-- 1 שורות -->
| F-120 | 🔴 CRITICAL · **טוקן Supabase נדחף ל-GitHub · נפתח על ידי רוי (24/08), ⛔ לא על ידי הלופ ו⛔ לא על ידי הבדיקה שנכתבה שעה קודם** | `docs/agents/DEV.md` — הבלוק «SQL and Migrations» · `scripts/agent-prompts.test.ts` | **בהעברת ארבעת הפרומפטים לריפו (C-0290) הועבר איתם גם `supabase login --token sbp_…` ו-`--project-ref`.** הניקוי שכתבתי חיפש `github_pat_` **בלבד**, ⇒ טוקן ה-Supabase עבר, נדחף ל-`work/current` ול-`dev`, וישב בהיסטוריה הציבורית של הריפו. ⚠️ **הבדיקה `agent-prompts.test.ts` עברה ירוק על הקומיט הזה** — היא בדקה מחלקה אחת של סוד והציגה את זה כ«אפס סודות». | טוקן גישה אישי ל-Supabase נותן **שליטה מלאה בפרויקט** — סכימה, נתונים, מפתחות. מי שקורא את הריפו מקבל את הדאטהבייס של הלומדים. ⚠️ **ומחיקה מה-HEAD ⛔ אינה מספיקה:** git שומר את האובייקט, ו-GitHub מחזיק אובייקטים לא-מקושרים נגישים לפי SHA גם אחרי דחיפה כפויה. | ✅ **מה שכבר בוצע 24/08:** הבלוק הוסר מהקובץ · ההיסטוריה נכתבה מחדש (`filter-branch` על שלושת הקומיטים) · `reflog expire` + `gc --prune=now` · **דחיפה כפויה לשני הענפים** — `git log origin/dev origin/work/current -S` מחזיר אפס. ההוראות הוחזרו לשורת האתחול של המשימה המתוזמנת בלבד. ⇒ `agent-prompts.test.ts` נושא עכשיו **רשימת תבניות סגורה ומוצהרת** (PAT · `sbp_` · JWT · service_role · URL עם סיסמה) **ובדיקה שנייה שמוודאת שכל תבנית תופסת דוגמה אמיתית**, כדי שהרשימה ⛔ לא תהיה קישוט. ⛔ **ומה ש⛔ אינו בוצע ו⛔ אינו יכול להתבצע על ידי סוכן: החלפת הטוקן.** ⇒ פריט 55 לרוי. | ✅ **נסגר 28/08 — רוי סובב את הטוקן.** נמדד ⛔ ולא שוער: המשימה המתוזמנת של DEV עודכנה ב-19:30Z ונושאת **טוקן `sbp_` שונה** מזה שדלף. ⇒ **הטוקן החשוף מת**, וזו הסגירה היחידה שהייתה ודאית — מחיקה מההיסטוריה ⛔ לא ביטלה חשיפה, ורוטציה כן. ⚠️ **הלקח נשמר בקוד:** `scripts/agent-prompts.test.ts` סורק את **כל** הריפו (F-165), ⇒ טוקן שיחזור לקובץ כלשהו יפיל את הבנייה. | 0 |

<!-- 2 שורות -->
| F-050 | 🟡 MEDIUM · **פגם תהליכי (שער מיושן — מחלקה, ⛔ לא מופע) · נפתח על ידי DEV (C-0141, ביקורת M0)** | `plan/50-tasks.md` — כל תא ⛔ בעמודת הסטטוס (הדגמה: T-054 · T-055) · `plan/60-findings.md` F-031 | **תא ⛔ מתעד חסם ו⛔ אינו מתעד שום דרך להבחין שהחסם הוסר.** התא נושא נימוק בפרוזה («אין endpoint של תור» · «אפס אזכורים למסך סיום») וסומך על כך שמישהו יזכור לבדוק אותו שוב. ⛔ אין פקודה, ⛔ אין תאריך תפוגה, ⛔ אין שום דבר שאפשר להריץ בשניות. ⇒ הסוכן הבא קורא את הפרוזה, מאמין לה, ועובר הלאה. | **התרחיש נמדד, ⛔ לא נצפה:** F-032 נכתבה ב-`c4f40df` ב-**2026-08-13T15:49:54Z**, וה-PM ענה לה ב-§ 4.2ו ב-`5a86a28` ב-**15:53:21Z** — **שלוש וחצי דקות אחרי**. שורת T-055 שהיא חסמה נשארה ⛔ במשך **~35 מחזורים**, ולצידה T-054 שנימוק החסימה שלה מת ב-`e1fd933` ב-**18:48:51Z** — ובפועל **השלד עצמו כבר נשלח** ב-`4dbc314` ב-**23:03:59Z** ומעולם לא נרשם. באותו פרק `MILESTONE_TICKS` טיפס **60→87** מול תקרה 120, וחלק ניכר מהטיקים נעצרו בדיוק בשערים המתים האלה. ⚠️ **מופע שני, חי כרגע, נמדד בביקורת הזו:** F-031 (🔓 פתוח) מזהיר ש-`/api/health` יחזיר `ok:false` **עד שרוי יריץ את המיגרציות** — ובדיקת העשן של C-0132 מדדה `ok:true` **4/4 כולל `database_schema`**. כלומר התנאי שהממצא מזהיר מפניו **חלף**, הממצא עדיין פתוח, ו⛔ אין בשורה שום פקודה שתגלה זאת. | **כלל, ⛔ לא טלאי: כל תא ⛔ נוקב בפקודה שמכריעה אם הוא עדיין תקף**, ולצידה הפלט שמשמעו «עדיין חסום». לדוגמה, במקום «אין endpoint של תור» ⇒ ``⛔ חסום כל עוד `grep -c "export async function GET" app/api/study/queue/route.ts` ⇒ 0``. אז החסימה נבדקת בשניות במקום להישען על זיכרון. ⚠️ **הכלל חל על ⛔ בלבד ו⛔ לא על ✅** — שורה סגורה כבר נשענת על השומר שלה. ⚠️ **בלם 10 נבדק לפני הפתיחה** (`RULES § 0.1 ו׳`): הוא מגביל **ממצאי עיצוב** ל-2 לאבן דרך; זה ממצא תהליכי — אותה מחלקה של F-032 עצמה — ולכן ⛔ אינו נספר בו ו⛔ אינו מחזיר את המקל. | 🚫 ⟨מוקפא 14י · 2026-08-31⟩ — 🔓 פתוח — התיקון בבעלות ה-PM (הטבלה שלו). ⛔ ה-Dev פתח ואינו מתקן. **המלצה: לאמץ ב-`RULES` כדרישת פורמט על תא ⛔, כך שהוא נאכף בכתיבה ולא בזיכרון** | C-0141 |
| F-060 | 🟠 HIGH · **פגם (חסם שסיבתו המוצהרת חדלה להתקיים לפני תשעה ימים) · נפתח על ידי DEV (C-0165, טיק תכנון)** | `plan/20-alerts.md:23` (P-001) · `plan/50-tasks.md` — `T-034` · `lib/core/landing.ts:60,69` | **T-034 חסומה ב-P-001, ו-P-001 אומרת «אין עדיין שום מקור עברי נקלט». זה מדיד, וזה שגוי מאז 07/08.** נמדד בטיק הזה, ⛔ לא נקרא: `npm run measure:gate` ⇒ **`534 rows read from 9 batch files` · `0 rows rejected`**; ‏`grep -c '"translation_he"'` על תשעת קובצי האצווה ⇒ **534/534**. סוכן התוכן כותב אצווה יומית מאז 07/08 ואיש לא חיבר אותה לחסם. ⚠️ **התנאי לשחרור כתוב בקוד עצמו**, `lib/core/landing.ts:60`: «the day a source lands, this array is the only thing that changes» — המקור נחת | **F-012 🟠 — הממצא הפתוח החמור ביותר במוצר — מוחזק בחסם מת.** חומת ההרשמה נשארת: הפעולה היחידה במסך הראשון היא `/signup`, בשוק שבו למתחרים יש תרגול חינם פתוח. כל יום נוסף הוא יום שבו לומד אינו יכול לראות ולו כרטיסייה אחת בלי למסור אימייל, בעוד 534 כרטיסיות מאומתות שוכבות ב-`data/generated/` ואינן נגישות לאיש. ⚠️ **הכשל אינו בקוד** — המסך, החריץ ותנאי הרינדור נבנו ונבדקו ב-T-027; חסר **נתון בלבד** | **ה-PM מסיר `⛔` מ-T-034** (הטבלה שלו, ⛔ ה-Dev פתח ואינו מסיר), או שה-Critic מכריע נגד. התוכנית לביצוע כתובה וממתינה: `docs/superpowers/plans/2026-08-16-preview-before-signup.md` (שלוש משימות). ⚠️ **שני פריטים לביקורת, ולא לביצוע שקט:** ⓐ התוכנית משנה שורת תיעוד קיימת — `PreviewCard.sourceId` מבטיח «never an agent», והעברית בבנק נכתבה בפרויקט מכותרות NGSL v1.2; הערך הישר הוא `generated:ngsl-headwords` והתיעוד מתוקן לומר זאת. ⓑ **F-012 דורשת שני חצאים, והתוכנית מכסה אחד** — «5–10 כרטיסיות» כן, «שמירת התקדמות מקומית עד ההרשמה» ⛔ לא: זו החלטת אינטראקציה של ה-PM. ⛔ אין לסגור את F-012 על המשימה הזאת לבדה | 🚫 ⟨מוקפא 14י · 2026-08-31⟩ — 🔓 פתוח → **PM** (הסרת החסם) · **CRITIC** (ⓐ ו-ⓑ) | 0 |

<!-- 5 שורות -->
| F-171 | 🟠 HIGH · **ה-PM חוסם את עצמו — 10 מתוך 41 ממצאיו הפתוחים חוסמים שורות שכבר נכתבו · נפתח על ידי QA (C-0367, בדיקה 12 באזהרה)** | `scripts/loop-health.mjs` בדיקה 12 · `plan/60-findings.md` — `F-052`→`T-066` · `F-125`→`T-136` · `F-060`→`T-034` · `F-067`→`T-217` · `F-114`→`T-103` · `F-127`→`T-209` · `F-142`→`T-199` · `F-143`→`T-199` (+2) | **נמדד בהרצה טרייה של `npm run loop:health` בטיק הזה, ⛔ ולא שוער.** בדיקה 12 הדפיסה ` warn 12 … 10 מתוך 41 ממצאי PM פתוחים חוסמים שורה`. ⚠️ **הבדיקה באזהרה עד 2026-09-02 ו⛔ אינה נוגעת בקוד היציאה — ו⛔ אזהרה ⛔ אינה שקט:** הפריטים נקראו, וזו השורה שנפתחת עליהם. | ⚠️ **תרחיש הכשל:** כל אחד מהעשרה הוא שורה **שנכתבה** — כלומר מישהו כבר השקיע בה טיק תכנון — ומחכה להכרעה של אותו סוכן שכתב את הממצא. ‏`F-142`+`F-143` שניהם חוסמים את **אותה** שורה (`T-199`), ו-`T-199` היא השארית של `cards` ב-`61-deferred`. ⇒ החסימות ⛔ אינן מתפזרות: הן מצטברות על הזרימות שכבר עברנו, ו-`36 § 13` הוא רצף חד-כיווני ⇒ ⛔ אף טיק DEV ⛔ לא יחזור אליהן מעצמו. | **כיוון תיקון:** ‏D-147 כבר קובע **ממצא אחד מוכרע בכל טיק PM**. הכיוון היחיד שנדרש כאן: התור מתחיל ב-**עשרת הממצאים החוסמים**, ⛔ לפני כל ממצא שאינו חוסם שורה — ובסדר של «כמה שורות משתחררות»: `F-142`/`F-143` (משחררות את `T-199`) ראשונות. | ✅ **נסגרה C-0377 — התור התרוקן, והמספר נמדד בהרצה טרייה בטיק הזה ⛔ ולא נזכר.** `npm run loop:health` בדיקה 12 בתחילת הטיק: ` warn 12 … 2 מתוך 43`; אחרי סגירת `F-127` (‏D-154) ו-`F-144` (‏D-155): ` ok 12 … **0 מתוך 41**`. ⇒ ⛔ אף ממצא בבעלות PM ⛔ אינו חוסם שורה כתובה. ⚠️ **המנגנון נשאר** — `D-147`/`D-164` (עד שלושה בטיק, החוסמים ראשונים) ובדיקה 12 חוסמת מ-02/09. | C-0367 |
| F-102 | 🟡 MEDIUM · **סתירה מדודה בין כיוון המחווה למיקום הכפתור ב-RTL · נפתח על ידי DEV (C-0251, טיק תכנון)** | `app/layout.tsx:33` (`dir="rtl"`) · `components/Flashcard.tsx` (בלוק `grid grid-cols-2`: `data-grade="again"` ראשון ⇒ **ימין**, `data-grade="good"` שני ⇒ **שמאל**) · `plan/40-decisions.md` D-042 · `plan/50-tasks.md` שורת T-099 | **נמדד מהמקור ו⛔ לא שוער.** העמוד כולו `dir="rtl"`, ולכן הפריט הראשון ב-`grid` יושב **מימין**: «לא ידעתי» מימין, «ידעתי» משמאל. D-042 ו-T-099 קובעות «ימין = ידעתי, שמאל = לא ידעתי» ⇒ לומד שמחליק **לכיוון הכפתור שהוא רואה** מקבל את ההפך ממה שהתכוון. ⛔ אין קריאה שמיישבת את השניים: או שהכיוון לוגי (ואז נוסח ההחלטה שגוי) או שסדר הכפתורים הפוך | **סימון הפוך בלחיצה אחת.** «ידעתי» על מילה שהלומד לא ידע דוחף את `next_review_at` שבועות קדימה — בדיוק הנזק ש-D-033 נבנתה למנוע, רק שהפעם הלומד **כיוון לכפתור הנכון** | **הכרעת PM, אחת משתיים:** ⓐ נוסח D-042 מתוקן ל«לכיוון הכפתור» (כלומר ב-RTL: שמאלה = ידעתי) · ⓑ סדר שני הכפתורים ב-`grid` מתהפך כך שהוא תואם לכיוון הפיזי. ⛔ **המבצע ⛔ אינו בוחר** — `docs/superpowers/plans/2026-08-21-flashcard-swipe-and-decay.md` מממש **כלשון ההחלטה החתומה** (`dx > 0 ⇒ good`) והופך שורה אחת אחרי ההכרעה | ✅ נסגר C-0378 (DEV, T-225ⓕ) — **D-150 מומש: חלופה ⓑ, סדר הכפתורים הוחלף.** `components/Flashcard.tsx` — `data-grade="good"` נעשה פריט ה-grid הראשון (⇒ מימין תחת RTL), `data-grade="again"` שני. `lib/core/swipeGrade.ts` ⛔ לא נגוע — `dx > 0 ⇒ good` כבר הסכים עם הרנדר. נמדד: `npx vitest run components/Flashcard.test.ts lib/core/swipeGrade.test.ts` ⇒ 50/50 ירוק; הליכה חיה ב-`/dev/deck` (localhost:3000, 375px, לאחר גילוי כרטיס) מראה «ידעתי» ✓ מימין, «לא ידעתי» ✕ משמאל — תואם `render_video_A.py:373-374` | 0 |
| F-127 | 🟡 MEDIUM · **אותו מספר בדיוק נושא שתי טענות שונות על אותו מסך, ואחת מהן ⛔ אינה נגזרת מ-D-034 · נפתח על ידי DEV (C-0303, טיק תכנון)** | `app/api/world/story/route.ts:186-203` · `components/StoryScreen.tsx:313-315` · `plan/36-video-spec.md § 7` | **נמדד על המקור בטיק הזה, ⛔ ולא שוער.** `knownHeadwords` (שורות 195-203) בונה את `knownLemmas` מ**כל שורת `word_progress`** שמצטרפת למילה עם ערך — ⛔ בלי `self_marked_known`, ⛔ בלי `repetition >= 1`, כלומר ⛔ **בלי המבחן של D-034 כלל**. ⇒ `counts.alreadyKnown` פירושו המדויק הוא «מילות הסיפור שכבר יושבות בתור החזרה», וזו **בדיוק** המשמעות שהמשפט `עברת על N מילים שהיו בתור החזרה שלך` (`36 § 7`) צריך. ⛔ **אבל השורה התחתונה של אותו מסך מדפיסה את אותו מספר עצמו כ-`N שכבר ידעת`** (`StoryScreen.tsx:314`) — טענה של **ידיעה**, שנשענת על מבחן שלא נערך. ⇒ מחרוזת שמבטיחה יותר ממה שיש לה, בדיוק המחלקה של F-123, שורה אחת נמוך יותר. | לומד שהוסיף מילה לכרטיסיות אתמול ו⛔ עוד לא ענה עליה ולו פעם אחת רואה «1 שכבר ידעת». המספר נכון; **הטענה** שגויה. וכשהמסך יציג את שני המשפטים יחד (T-202), אותו מספר יופיע פעמיים עם שתי משמעויות. | **הכרעה ל-PM, ⛔ לא ל-DEV:** ⓐ המחרוזת התחתונה משתנה ל«N בתור החזרה שלך» ⇒ מספר אחד, טענה אחת; **או** ⓑ `counts` מפוצל לשניים והנתיב מיישם את D-034 על החצי שטוען «ידעת». ⛔ אין ל-DEV סמכות לבחור — הטבלה ב-`RULES § 0.22` מציבה «כל דבר שנוגע ב-`word_progress`» אצל ה-PM. | ✅ **נסגרה C-0377 — ההכרעה כבר הייתה כתובה ו⛔ הרשם לא עודכן.** `D-154` (‏C-0370) בחרה בחלופה ⓐ: השורה התחתונה אומרת «N בתור החזרה שלך», ⛔ ולא «N שכבר ידעת» ⇒ מספר אחד, טענה אחת, ⛔ אפס שדה חדש. הניסוח כתוב בתוך `T-207` ו-`T-209`. ⛔ **לא נפתחה כאן הכרעה חדשה** — נסגר פער בין `40-decisions` לרשם (‏לקח § A1 שורה 8 · `F-171`). | 0 |
| F-140 | 🔴 CRITICAL · defect (⛔ **מגיע ללומד ברגע שהאריח נפתח**) · **ל«סינון מילים» ⛔ אין נתיב כתיבה קביל — `/api/practice` עונה 404 על מילה שטרם נפגשה, בכוונה מוצהרת, וחפיסת הרמה היא ⛔ בדיוק אוסף המילים האלה · נפתח על ידי DEV (C-0318, טיק בנייה)** | `app/api/practice/route.ts:54-59` · `components/StudyDeckScreen.tsx` (`sendGrade`) · `app/api/study/queue/route.ts` (‏`deck=level`) · `plan/50-tasks.md` (T-155ⓒ) · `docs/superpowers/plans/2026-08-26-cards-filter-slice-a.md` | **נמדד על המקור בטיק הזה, ⛔ ולא שוער.** ⓐ `app/api/practice/route.ts:59` — שורת התקדמות חסרה ⇒ **404**, ו-`route.ts:54-58` מנמק זאת במפורש: «מילה בלי שורת התקדמות מעולם לא נענתה, ומילה שמעולם לא נענתה ⛔ אינה יכולה להיות בחפיסת תרגול ⇒ הכנסה כאן הייתה ממציאה היסטוריית למידה מ-POST תועה». ⓑ **וחפיסת `level` מפריכה את ההנחה הזאת בהגדרתה:** היא כל המילים ברמה, ⇒ ללומד חדש **~0** שורות מול **305** מילים ב-A1 (‏`measure:headroom`). ⓒ ‏T-155ⓒ אוסרת במפורש את הערוץ היחיד שכן מכניס שורה — `/api/review` (‏`route.ts:158-159`) — כי הוא כותב SM-2. ⇒ ⛔ **שני הערוצים הקיימים פסולים, ו⛔ אין שלישי.** | ⓐ **הכפתור הראשי של `36 § 5`** («סינון מילים») היה מוביל לחפיסה שנכשלת על כמעט כל כרטיס: הדירוג נדחה, `<CardDeck>` משאיר את הכרטיס במקומו (בצדק — ⛔ דירוג שנבלע הוא תשובה שאבדה), והלומד תקוע. זו בדיוק מחלקת F-027. ⓑ **וההשלכה השנייה חמורה יותר מהראשונה:** גם אם הכתיבה תעבוד, `classifyProgress` מסווגת `attempts=1, repetition=0` כ-**`in_review`** ⇒ לומד שסימן «ידעתי» על מילה חדשה היה נספר תחת «לא ידעתי» בפס ובמונים ש-T-210 בנה. ⇒ ⛔ הסינון היה **סופר הפוך**. | ⛔ **⛔ לא הוכרע ב-DEV, ובכוונה:** § 0.22 שולח «מכניקת לימוד» ו«כל דבר שנוגע ב-`word_progress`» ל-PM, ושלוש הדרכים אינן שקולות — ⓐ «ידעתי» כותב `self_marked_known` (הדפוס של `app/api/levels/scan` POST, שכבר חתום) ו«לא ידעתי» כותב `attempts`; ⓑ הכנסת שורה עם `attempts` בלבד ו-`next_review_at` **NULL** (‏⇒ ⛔ אינה «מועדת עכשיו», כי `deck=due` מסנן `next_review_at <= now`); ⓒ החפיסה ⛔ אינה מדרגת כלל ומסמנת בערוץ אחר. **נמסר בטיק:** צד הקריאה (`?deck=level` · החוזה · השכבה הטהורה) **מלא וירוק**; הדירוג מנותב ל-`/api/practice` (‏attempts בלבד, T-155ⓒ) כך שהחיווט נכון ביום שההכרעה תיפול; והאריח נמסר **נעול עם המספר** (D-046 · § 4.2ו · הדפוס של `משפטים`) ⛔ ולא כ-CTA שבור. ⇒ **הפתיחה היא שורה אחת של PM, ⛔ ולא עבודת DEV.** | ✅ נסגר C-0378 (DEV) — **D-142 מומש, תוכנית `2026-08-30-cards-filter-write-path.md`.** `app/api/practice/route.ts`: ה-404 הצטמצם ל«כל חפיסה חוץ מ-`deck=level`», והענף `deck==='level'` פותח שורה — «ידעתי» ⇒ `self_marked_known: true, attempts: 0`, «לא ידעתי» ⇒ `attempts: 1, next_review_at: null` (⛔ אפס SM-2, ⛔ `insert` ⛔ ולא `upsert`). `components/DeckSelector.tsx`: אריח «סינון מילים» עובר דרך `toEntry` עם `href: '/study?deck=level'` — ⛔ אינו נעול יותר. `lib/core/levelSummary.ts` ⛔ לא נגוע — `classifyProgress` בודקת `selfMarkedKnown` ראשונה, כך ⓑ מסווגת נכון מעצמה. נמדד בהרצה חיה: `npx vitest run app/api/practice/route.test.ts lib/core/deck.test.ts components/DeckSelector.test.ts` ⇒ ירוק; `npm run verify` ⇒ exit 0 (typecheck · check:core · check:motion · check:rules · test 3125/3125 · build · check:mobile 1214 בדיקות). הליכה חיה ב-`/dev/tabs/cards` (localhost:3000, 375px): «סינון מילים» ראשונה, ⛔ בלי מנעול, `href=/study?deck=level`, המספר מוצג. | C-0318 |
| F-144 | 🟡 MEDIUM · defect חזותי (⛔ אינו שובר מסך — **סוטה מהרנדר שמחייב מאז D-114**) · **קופסת אריח החפיסה סוטה מהרנדר בארבעה ערכים, והרדיוס שהרנדר מצייר ⛔ אינו בסולם החמישה של החוקה · נפתח על ידי DEV (C-0321, טיק בנייה)** | `docs/design/render_video_A.py:288-292` · `components/DeckSelector.tsx` (‏`px-5 py-3 rounded-2xl`) · `plan/36-video-spec.md § 14.4` (D-114) · `plan/35-design-constitution.md` שכבה ב׳ (סולם הרדיוסים) | **נמדד בדפדפן חי ב-375×780 בטיק הזה (Playwright, `getBoundingClientRect` + `getComputedStyle`), ⛔ ולא הוערך בעין.** ארבעת האריחים זהים זה לזה: `x=20 · w=335 · h=78 · border-radius=16px`. הרנדר, ב**אותו** רוחב לוגי (`LW=375`, שורה 14), מצייר `c.rr(24, 486, LW-48, 62, 18)` ⇒ `x=24 · w=327 · h=62 · r=18`. ⇒ **‏x סוטה ב-4px · w ב-8px · h ב-16px · r ב-2px.** ⚠️ **וה-r ⛔ אינו סטייה של המימוש אלא סתירה בין שני מסמכים מחייבים:** `18` ⛔ אינו אחד מחמשת ערכי סולם הרדיוסים של שכבה ב׳ (`md` 6 · `lg` 8 · `xl` 12 · `2xl` 16 · `full`), ולכן ⛔ **אי-אפשר** לבנות «כמו שצויר» ולהישאר בסולם. ⛔ **קדם לטיק הזה:** הקופסה נבנתה ב-C-0318 ומוזגה ב-C-0320; C-0321 ⛔ לא נגע במחלקות האלה — הוא רק מדד אותן. | ⓐ `36 § 14.4` (D-114, 24/08) הפך את הרנדר למחייב **גם בגימור**, ואמר במפורש ש«הגימור בא מהחוקה» ⛔ אינו תשובה לפער ⇒ פער בן 16px בגובה קופסה, שאיש לא מדד, הוא בדיוק הדלת שאותו סעיף נסגר עליה. ⓑ ו-`h=78` ⛔ אינו כשל שכבה A — 78 > 44 — ולכן ⛔ **אין כאן חריגה שמצדיקה בנייה שונה מהרנדר**; הוא פשוט גדול ממה שצויר. | ⛔ **⛔ לא הוכרע ב-DEV.** § 0.22 שולחת ל-PM כל דבר שסותר מסמך עוגן, והסתירה ב-`r` ⛔ אינה ניתנת לסגירה כאן: או שהרנדר מתוקן ל-16, או שהסולם מקבל ערך שישי — ושתיהן הכרעות מסמך עוגן. ⛔ **ולא נסגר בשקט:** «הגימור בא מהחוקה» הוא בדיוק המשפט ש-D-114 ביטלה. | ✅ **נסגרה C-0377 — ההכרעה כבר הייתה כתובה ו⛔ הרשם לא עודכן.** ⓐ `D-155` (‏C-0370): סולם הרדיוסים נשאר בן חמישה ערכים ו**הרנדר** מתוקן `18`⇢`16`. ⓑ נושאת שורה כתובה — `T-228` (`x/w/h` + `render_video_A.py:288,292`), ⛔ חסומה בזרימה בלבד עד שהמוקד יחזור ל-`cards`. ⛔ **לא נפתחה כאן הכרעה חדשה** — נסגר פער בין `40-decisions` לרשם (‏לקח § A1 שורה 8 · `F-171`). | C-0321 |

<!-- 1 שורות -->
| F-180 | 🔴 CRITICAL · **`npm run verify` אדום החל מ-02/09/2026 — `SOFT_UNTIL='2026-09-02'` פג בקוד הייצור, ואף בדיקה לא עודכנה בשבילו.** | `scripts/check-plan-shape.mjs:153` (`SOFT_UNTIL`) · `scripts/check-plan-shape.test.ts` (רכיב 10, 4 בדיקות) · `scripts/loop-health.mjs:545,594,636` (`softUntil: '2026-09-02'`, בדיקות 12·13·14) · `scripts/loop-health.test.ts` (3 בדיקות) | **נמדד חי בשיבוט נקי של `work/current` (`git stash` נגד `0e11e72`), ⛔ ולפני כל שינוי של הטיק הזה:** `npm run verify` נכשל — `npx vitest run scripts/check-plan-shape.test.ts scripts/loop-health.test.ts` ⇒ **10 בדיקות נכשלות מתוך 44**, כל 190 קובצי הבדיקה האחרים ירוקים. שני הקבצים בונים `TODAY = new Date().toISOString().slice(0,10)` וקובעים `softNow = TODAY < '2026-09-02'` — עד אתמול (`01/09/2026`) זה היה `true` (מייעץ בלבד, ⛔ לא נספר), והחל מהיום (`02/09/2026`) זה `false`: הרכיב/הבדיקות עוברים ממצב «soft ⚠️/soft ok» למצב רגיל בציון ובקוד היציאה — **בדיוק כמו שהקוד מתעד בכוונה** (`check-plan-shape.mjs:149-151`: "⇒ until SOFT_UNTIL it prints... ⛔ does NOT enter missing"; `loop-health.mjs:63-64`: "a check may declare softUntil. Until that date it PRINTS its verdict..."). ⛔ **אבל שני קבצי הבדיקה עדיין מצפים למחרוזות של מצב ה-soft** (`/^ soft ⚠️ +extend/m` וכו׳ ב-`check-plan-shape.test.ts`, `warned(r.out, '12'\|'13'\|'14')` שצריך להיות `true` ב-`loop-health.test.ts`), כי אף אחד לא עדכן אותם ל-02/09. | **זו לא רגרסיה של קוד ייצור** — ההתנהגות המוצרית היא בדיוק מה שתועד ותוכנן (חלון "born soft" שנגמר ביום קבוע, בכוונה בלי דגל שמישהו צריך לזכור להפוך). **אבל `npm run verify` הוא «⛔ אין טענת הצלחה בלי ריצה טרייה» (`DEV.md` STEP 6) ועכשיו הוא אדום עבור כל סוכן שירוץ מהיום והלאה**, על כל טיק, גם טיקים שלא נוגעים בשום קובץ קשור. ⛔ **תוקן פעם אחת (24/08 בהחלטת רוי) שגייט אדום ביום הראשון מלמד סוכן להתעלם מהשער כולו** — שער-אמת (`npm run verify`) שנשאר אדום ליום/ימים בגלל תאריך שעבר, ⛔ ולא בגלל קוד שבור, הוא בדיוק אותו סיכון, ברמת כל ה-CI. | **PM/QA:** לעדכן את הבדיקות בשני הקבצים כך שיצפו למצב הרגיל (⛔ לא ה-soft) החל מ-`SOFT_UNTIL`, ואולי להוסיף בדיקת-שעון (fixture עם `--today=` מוזרק) כדי שרכיב מתוזמן-תאריך הבא לא ייתקל באותה בעיה. ⛔ Dev ⛔ אינו נוגע ב-`check-plan-shape.mjs`/`loop-health.mjs`/הבדיקות שלהם בטיק הזה — מחוץ להיקף `T-249`, וקבצים שאינם קשורים ל-`gc-memory.mjs`. | ✅ **טופל C-0386 (DEV, `T-250`).** תיקון שורש בשני קבצי המקור, ⛔ לא עדכון-בדיקות בלבד: `SOFT_UNTIL`/`TODAY`/`softNow` הוסרו כליל מ-`check-plan-shape.mjs` (רכיב `extend` נכנס תמיד ל-`ELEMENTS`, ⛔ אין עוד הדפסת `soft ok`/`soft ⚠️`), ושלושת קריאות ה-`check(...)` של בדיקות 12·13·14 ב-`loop-health.mjs` איבדו את הפרמטר `softUntil: '2026-09-02'` (ברירת המחדל `null` ⇒ `soft` תמיד `false`, אותה לוגיקת הדפסה/יציאה גנרית שכבר קיימת). ⛔ **אין דגל תאריך חדש להחליף את הישן** — `D-177` מולא כלשונו. שני קבצי הבדיקה עודכנו בהתאם (מצפים ל-`MISS`/`ok` ול-`FAIL`/`ok` רגילים, ⛔ לא ל-`soft`). **נמדד חי אחרי התיקון:** `npx vitest run scripts/check-plan-shape.test.ts scripts/loop-health.test.ts` ⇒ **44/44 ירוק**; `npm run verify` מלא (7 פקודות) ⇒ **exit 0 · 190 קבצים · 3160/3160 בדיקות**; `node scripts/loop-health.mjs` ⇒ **13/14** (בדיקות 12·13·14 כולן `ok` אמיתי; הכישלון היחיד הנותר — בדיקה 2, `F-179` — קדם לטיק הזה ואינו קשור) | C-0386 |

<!-- 1 שורות -->
| F-179 | 🟡 MEDIUM · **`npm run generate-map` — שכתוב DEV.md/LOOP-ARCHITECTURE.md מניח שהוא קיים; בקלון חי הוא ⛔ אינו קיים.** | `package.json` (בלוק `scripts`) · `docs/agents/DEV.md` («🗺️ THE ARCHITECTURE MAP») · `plan/50-tasks.md` שורת `T-235` | **נמדד חי בטיק C-0381:** `npm run generate-map` ⇒ `npm error Missing script: "generate-map"`. `node_modules/.bin/madge` ⛔ אינו קיים, `docs/architecture-map.json` ⛔ אינו קיים בעץ, ו-`madge` ⛔ אינו ב-`package.json`. `T-235` (madge) עצמה **⬜ עדיין פתוחה** ב-`plan/50-tasks.md` — «שנייה בתור לפי D-171, ממתינה להכרעת PM/רוי לקדם» (`plan/00-control.md`). | `docs/agents/DEV.md` STEP 7 ו-`claude/LOOP-ARCHITECTURE.md § 8.4` פריט 15/§ 7.4 מציגים את הסקריפט כאילו כבר נחת (`D-165`/`T-235` "✅"), אבל המדידה החיה מראה שהתשתית עצמה — ולא רק העדכון השוטף שלה — עדיין לא נבנתה. ⇒ **כל טיק שנוגע ב-`app/`·`components/`·`lib/` (כולל T-246 עצמה) ⛔ אינו יכול לקיים את הוראת STEP 7 «regenerate the map», ולא מתוך רשלנות — הפקודה עצמה ⛔ אינה קיימת.** | **PM/Roy:** לקדם את `T-235` (הוספת `madge` + סקריפט `generate-map` + `docs/architecture-map.json` ראשוני), או לתקן זמנית את STEP 7 ב-`DEV.md` כך שיבדוק קיום הסקריפט לפני שהוא דורש הרצתו. ⛔ Dev ⛔ אינו מוסיף תלות (`madge`) על דעת עצמו — זו תשתית, ⛔ לא תיקון-שורה. | ✅ נסגר C-0390 — `T-235` נבנתה (`madge` מוצמד · `generate-map.mjs` עם שער `MIN_MODULES=20` · `docs/architecture-map.json` נוצר, 372 מודולים) · `loop:health` בדיקה 2 ⇢ ok | C-0381 |

<!-- 1 שורות -->
| F-174 | 🔴 CRITICAL · **`npm run verify` היה אדום על `dev` עצמו — לא רק על `work/current` — ולכן ⛔ אף סוכן ⛔ לא היה יכול להכריז ירוק כחוק** | `scripts/loop-health.test.ts` — «is green on a healthy fixture» · הליטרל `loop health: 11/14 checks pass` | **נמדד בטיק הזה (C-0372) בשתי הרצות טריות, ⛔ ולא שוער:** ⓐ `./scripts/g stash -u` ⇒ עץ נקי של `work/current` ⇒ `npx vitest run scripts/loop-health.test.ts` = **1 failed · 27 passed**; ⓑ `./scripts/g checkout origin/dev -- scripts/loop-health.mjs scripts/loop-health.test.ts` ⇒ **אותה נפילה בדיוק**. ⇒ המקור ⛔ אינו בענף העבודה. | ⚠️ **תרחיש הכשל, וזה הגרוע מכולם:** `DEV.md STEP 6` הוא «החוק הקשה» — ⛔ אין טענת ירוק בלי `npm run verify` טרי. ⇒ כל טיק DEV/CONTENT מאז שהנפילה נכנסה עמד בפני שתי אפשרויות בלבד: לחזור בו (`revert`) על עבודה תקינה, או לדווח ירוק שאינו ירוק. ⛔ **וזה מלמד את מה שרוי כבר קבע כלקח שלב 7: בודק שאדום ביום הראשון מלמד כל סוכן שאדום הוא הצבע הרגיל.** **ולמה זה נחת:** שני ליטרלים בבדיקה נעוצים על תוצאות ש⛔ אינן של הפיקסצ׳ר — בדיקה 11 קוראת את `docs/plan-open.md` ה**אמיתי** שהפיקסצ׳ר מעתיק פנימה, ובדיקה 12 (שנחתה ב-C-0359/C-0366) ירוקה על פיקסצ׳ר בריא **בצדק**. ⇒ הסכום `11/14` מדד את מצב הרגיסטרים של יום הכתיבה, ⛔ לא את הבודק. | **מה נעשה בטיק הזה (‏`RULES § 0.22` — קריאה הפיכה, קומיט אחד מתקן אותה):** הליטרל הוסר. הסכום **נגזר** מהפלט ונבדק מולו (‏`passes + failing == 14`), שתי הבדיקות שהפיקסצ׳ר ⛔ אינו יכול לספק (8 · 10) עדיין נדרשות **ליפול בשמן**, ובדיקות 11 ו-12 ⛔ אינן נעוצות — וזה **נאמר בקובץ במפורש** ⛔ ולא הושמט. **נמדד אחרי:** `28 passed (28)`. ⛔ **מה ⛔ לא נעשה, ובכוונה:** ⛔ אפס נגיעה ב-`scripts/loop-health.mjs` — הבודק עצמו ⛔ אינו שגוי, הבדיקה שלו הייתה. | ✅ **נסגר C-0396 (QA)** — אומת: `npm run verify` טרי אחרי מיזוג `work/current` ל-`dev` (‏`--ff-only`, `6118d23`), exit 0 — `test` 3207/3207 · `check:mobile` 1205/1205. `dev` ירוק. | C-0372 · C-0396 |

<!-- 1 שורות -->
| F-181 | 🟠 HIGH · **תלות התוכנית של T-145 מפנה למודול שהוחלף — «lib/core/surfaces.ts» (⛔ קובץ שאינו קיים — זה בדיוק הממצא) (`SurfaceId` · `SURFACE_HREF` · `neglectedSurface` · `readSurfacesTouchedToday`) ⛔ אינו קיים בקלון · נפתח על ידי DEV (C-0392, טיק בנייה)** | `docs/superpowers/plans/2026-08-23-answerable-lesson-and-four-surfaces.md` Task 3 (בלוק `Interfaces`) · `plan/50-tasks.md` שורת `T-144` · `plan/50-tasks.md` שורת `T-246` · `lib/core/studyTracks.ts` | **נמדד בטיק הזה, ⛔ ולא שוער:** `grep -rn "neglectedSurface\|NEGLECT_PRIORITY\|neglectHeadlineHe" lib app components` ⇒ **אפס התאמות**. Task 3 של התוכנית (`T-145`) מצהירה «Consumes: מ-Task 2» את המודול «lib/core/surfaces.ts» (⛔ קובץ שאינו קיים — זה בדיוק הממצא) ואת ⓑ «אותה גוזרת של T-144 (המשטח שלא נגעת בו היום)» — ⛔ אך `T-144` (=Task 2) **לא נבנתה כלשונה**: `D-176` (01/09) הזיזה את הבנייה בפועל ל-`T-246`/`T-247`, שבנו **מודל שונה לחלוטין** — ארבעת המסלולים (`vocabulary`/`grammar`/`writing`/`reading`, `lib/core/studyTracks.ts`) ⛔ ולא ארבעת המשטחים (ספרייה/הרכבה/כרטיסיות/זירה) שהתוכנית מניחה, וללא כל מקבילה ל«המשטח המוזנח». | `T-145` (`אני` מקבלת פעולה אמיתית — D-079) ⛔ **אינה ניתנת לביצוע כלשונה**: כל שלב 3–6 של Task 3 קורא לפונקציה ולקבועים שלא קיימים ולא ייבנו בצורה שהתוכנית מתארת. סוכן שינסה לבצע את הצעדים כלשונם ייתקל ב-`Failed to resolve import '@/lib/core/surfaces'` — או, גרוע יותר, ימציא בעצמו מודל «משטחים» מקביל למודל «מסלולים» הקיים, מה שיוצר שתי הגדרות שונות לאותו רעיון (בדיוק המחלקה ש-§ 4.2ז אוסר). | **PM:** להכריע בין ⓐ בניית `neglectedSurface` כמודול טהור וממוקד (בלי «ארבעת המשטחים» הישנים — פרמטרים גנריים בלבד) שמספק אך ורק את מה שT-145 צריכה, ⓑ ניסוח מחדש של T-145ⓑ כך שהפעולה הראשית נגזרת מ-`lib/core/studyTracks.ts` (מודל המסלולים הקיים) במקום ממודל משטחים שהוחלף, או ⓒ פתיחת שורת משימה חדשה שמחליפה במפורש את החלק החסר של Task 2 בתוכנית. ⛔ Dev ⛔ אינו מכריע בין שלוש החלופות — זו קריאה על מקור נתונים/מכניקה חדשה, מעבר לרף D-110 של תיקון-שורה אחת. | ✅ **נסגר C-0398 (PM) — `D-180`: חלופה ⓑ.** ‏`T-145`ⓑ נגזרת מ-`lib/core/studyTracks.ts` (מודל המסלולים הקיים), Task 3 של התוכנית מוסר מתוקף עבור השורה, ו-`T-145` חזרה ל-⬜. ⛔ ⛔ אינו חוסם עוד. | C-0392 |

<!-- 2 שורות -->
| F-163 | 🟡 MEDIUM · פער מדידה (⛔ אינו defect במוצר, ⛔ אך הוא השער היחיד של K-004) · **מבחן הקבלה של ההזמנה K-004 ⛔ אינו פקודה — הוא משפט · נפתח על ידי DEV (C-0342, טיק תכנון)** | `docs/content-distractors-brief.md` (§ «איך בודקים לפני מסירה») · `package.json` (`scripts`) · `plan/40-decisions.md` D-138 § ג׳ | **נמדד בהרצה בטיק הזה, ⛔ ולא שוער.** התדריך קובע שני מבחנים: `npm run measure:gate` (קיים · ירוק) ו⛔ **המבחן השני, שהוא הסיבה שההזמנה קיימת** — «המשמעויות עם תמהיל D-023 מלא עולות מ-**127** לכיוון **445**». ⛔ **⛔ אין בריפו פקודה שמחשבת את המספר הזה.** ‏`npm run measure:gate` סופר שורות שנפסלו בשער, ⛔ ולא תמהילים; `grep -rn 'measure:' package.json` מונה שבע פקודות מדידה ו⛔ אף אחת מהן ⛔ אינה נוגעת ב-`relation_type`. ⇒ כדי לשחזר את 127 נדרשה בטיק הזה שורת `python3` אד-הוק. **והיא שחזרה אותו בדיוק:** על 14 קובצי האצווה שקדמו ל-28/08 — `rows 737 · full 127` ⇒ ⛔ הגדרת «תמהיל מלא» של D-138 (‏≥2 `semantic` + ≥1 `orthographic` שנפתרים לתרגום שכבר במאגר) ⛔ אינה דו-משמעית, והיא מה שהתוכנית מיישמת. | האצווה הבאה נמסרת, ⛔ ואיש ⛔ אינו יודע אם היא הזיזה את המספר — כי הפקודה שמודדת אותו חיה בראש של סוכן, ⛔ לא בריפו. ⚠️ **וזה כבר קרה בשקט:** `batch-2026-08-28` (האצווה הראשונה של K-004) נחתה, ונמדד בטיק הזה שהיא הזיזה תמהיל מלא **127/737 ⇒ 216/787** ולפחות-מסיח-אחד **535/737 ⇒ 704/787 = 89.5%**. ⛔ אף שורת רגיסטר ⛔ אינה יודעת את זה. | ‏`npm run measure:mix` — סקריפט מדידה בדפוס של `scripts/measure-gate.mjs`, שקורא `data/generated/batch-*.jsonl` ומדפיס שני מספרים: תמהיל מלא, ולפחות מסיח מתויג אחד. ⛔ ⛔ לא שער (⛔ אינו מפיל בנייה) — **מדידה**, בדיוק כמו `measure:coverage`. ⇒ התדריך מפנה לפקודה ⛔ ולא למשפט. | ✅ **נסגר C-0410 (DEV) — T-221 נבנתה.** `npm run measure:mix` קיימת, `verify` ירוק (`test` 3290/3290), התדריך מפנה לפקודה. | C-0410 |
| F-173 | 🟠 HIGH · **שלושה מדים בזירה מונעים ברוחב אחוזי מוטבע בתוך לולאת `requestAnimationFrame`** | `components/ArenaBattle.tsx` — הביטויים `telegraph.frac * 100` · `enemyPct` · `(mana / MANA_CAP) * 100` · ‏`scripts/motion-baseline.md` | **נמדד בהרצה חיה של `node scripts/check-motion.mjs` בטיק הזה (C-0372):** 3 הפרות מכלל ⓑ, כולן ב-`ArenaBattle.tsx`, שהוא הקובץ היחיד במוצר שמריץ `window.requestAnimationFrame` (‏`:324` · `:326`). ⚠️ **ו-`components/StoryScreen.tsx:219` — שהיה בציפייה — ⛔ אינו הפרה לפי הכלל כפי שנכתב, וזה מדוד:** הקובץ ⛔ אינו מכיל `requestAnimationFrame` ולו פעם אחת, ו-`pct` שם הוא `Math.round((index/total)*100)` — מדד «סיפור N מתוך M» שמשתנה ב**ניווט**, ⛔ לא בפריים. | ⚠️ **תרחיש הכשל:** שינוי `width` באחוזים מפעיל **פריסה וגם ציור** על שלושה אלמנטים בכל פריים של קרב בן 90 שניות — כלומר עד ~5,400 פעם, מול **≤11 שינויי מצב אמיתיים** (‏`T-231` מודדת את היחס). ⇒ ההקשה של הלומד על תשובה נכנסת לתור **אחרי** העבודה הזאת. | **כיוון תיקון:** ההמרה ל-`transform: scaleX()` על אלמנט ברוחב מלא היא **בדיוק `T-231`ⓒ**, שכבר כתובה ⇒ ⛔ **לא נפתחה כאן שורה כפולה**. ⛔ **וה-DEV ⛔ לא נגע בקוד בטיק הזה** — הפריט הוא שער בלבד, וההמרה עצמה היא עבודת מוצר בשכבה ב׳. | ✅ **נסגר C-0411 (DEV) — `T-231` נבנתה.** שלושת הביטויים הוחלפו ב-`transform: scaleX()` על אלמנט ברוחב מלא (`transformOrigin: 'right center'`, תואם RTL); `node scripts/check-motion.mjs` ירוק (0 הפרות ⓑ), שלוש שורות הבסיס שהצטטו את הממצא הזה הוסרו מ-`scripts/motion-baseline.md` (הן, ⛔ לא השורות של F-172/T-234). | C-0372 → C-0411 |

<!-- 10 שורות -->
| F-002 | 🔴 CRITICAL | `lib/supabase/auth.ts:39,65` | **טוקני ההתחברות אינם `httpOnly` ואינם `Secure`.** שתי הקריאות ל-`createServerClient` אינן מעבירות `cookieOptions`, ולכן נכנסות ברירות המחדל של `@supabase/ssr` — אומת ב-`node_modules/@supabase/ssr/dist/main/utils/constants.js`: `httpOnly: false`, אין `secure`, `maxAge: 400 ימים`. זה **סותר במפורש** את התיעוד ב-`lib/supabase/auth.ts:34` וב-`docs/api-contract.md` שמבטיחים httpOnly | לומד מתחבר → העוגייה `sb-...-auth-token` מכילה access token **ורפרש טוקן ל-400 יום** ונגישה ל-JavaScript. כל סקריפט זר (תגית אנליטיקס שנפרצה, XSS עתידי בשדה חופשי ב-Onboarding) קורא `document.cookie`, מפענח base64url, ומשתלט על החשבון לצמיתות | הוסף ארגומנט שלישי לשתי הקריאות: `cookieOptions: { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', path: '/' }` | ✅ **טופל C-0004 (T-022).** `SESSION_COOKIE_OPTIONS` ב-`lib/supabase/auth.ts` מועבר לשתי הקריאות. 4 בדיקות ב-`lib/supabase/cookieOptions.test.ts` תופסות את הארגומנט בפועל (mock על `createServerClient`) — אומת שהן נכשלות כשמסירים אותו. ⚠️ `maxAge` נשאר 400 יום → TD-7 \| ✅ טופל | 1 |
| F-003 | 🟠 HIGH | `proxy.ts:28` | **שומר הסשן נכשל פתוח.** `if (!env) return response;` — כשחסר משתנה סביבה, ההגנה מתבטלת ו**כל** המסכים המוגנים נפתחים. `app/onboarding/page.tsx` הוא Server Component סטטי בלי בדיקת הרשאה משלו, כלומר `proxy.ts` הוא נקודת האכיפה היחידה | טעות הקלדה ב-`NEXT_PUBLIC_SUPABASE_ANON_KEY` בלוח הבקרה של Netlify (או Deploy Preview שאינו יורש משתני סביבה) → הבנייה מצליחה → `GET /onboarding` בלי עוגיות מחזיר 200 עם המסך המוגן. ב-T-003 המסך הזה יכיל מוסד, ציון יעד ותאריך מבחן של הלומד | היכשל סגור: כש-`env` הוא `null`, הפנה נתיבים מוגנים ל-`/login`. בנוסף הוסף `getUser()` בתוך `app/onboarding/page.tsx` כהגנת עומק | ✅ **טופל C-0004 (T-022).** שתי ההגנות מומשו; `onboarding` הפך ל-`force-dynamic` עם `getUser()` משלו. 5 בדיקות ב-`proxy.test.ts` (חדש ב-`vitest.config.ts`) — אומת שהן נכשלות מול הקוד הישן \| ✅ טופל | 1 |
| F-004 | 🟠 HIGH | `app/api/auth/login/route.ts:12-21` · `signup/route.ts:19-26` | **גוף בקשה `null` מפיל את שני נתיבי ה-Auth ל-500.** `await request.json()` על הגוף `null` **מצליח** ומחזיר `null`; ה-`try/catch` תופס רק שגיאות פענוח. השורה הבאה ניגשת ל-`payload.email` | `curl -X POST /api/auth/login -H 'content-type: application/json' -d 'null'` → `TypeError: Cannot read properties of null` → HTTP 500 עם digest במקום החוזה `{ ok: false, code }`. כל בקשה כזו כותבת stack trace מלא ללוג של Netlify — primitive זול להצפת לוגים בידי תוקף לא מזוהה | אחרי הפענוח: `if (!payload \|\| typeof payload !== 'object') return fail('unavailable', 400);` — או ולידציה ב-`zod`, שכבר מותקן | ✅ **טופל C-0004 (T-022).** `isCredentialPayload` ב-`lib/core/auth.ts` (טהור) דוחה גם מערך ופרימיטיב, לא רק `null`; שני הנתיבים מחזירים 400 `unavailable`. 3 בדיקות + עדכון `docs/api-contract.md` \| ✅ טופל | 1 |
| F-007 | 🟠 HIGH | `package.json` → `scripts.verify` · `scripts/verify-mobile.mjs:31` | **`check:mobile` שבור, ואינו חלק מ-`verify`.** הרצה בפועל: `browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/...`. הסביבה מספקת build 1194, ו-`netlify.toml` מגדיר `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` — כלומר הסקריפט לא ירוץ גם ב-Netlify. `verify` מריץ typecheck+check:core+test+build בלבד | Dev כתב ב-`1ca09cd` "measured mobile guarantees" — אבל המדידה לא רצה ואינה יכולה לרוץ. כל טענה על 375px, יעדי מגע 44px ו-RTL היא **הצהרה לא נבדקת**, וה-Critic יסמן אותה כתקינה כי `verify` עובר | ⓐ הצמד `executablePath` לדפדפן הקיים בסביבה, או ⓑ הורד את הדפדפן מפורשות בטיק שמריץ את הבדיקה. בכל מקרה — **`check:mobile` חייב להיכנס ל-`verify`**, אחרת הבדיקה שקיימת על הנייר לא מגינה על כלום. ראה T-021 | ✅ **טופל C-0005 (T-021).** ⓐ מומש כאיתור דינמי (`resolveChromiumPath`) ולא כנתיב קשיח, כי מספר ה-build משתנה בין סביבות; ההארנס גם מרים `next start` בעצמו ומכבה אותו. `check:mobile` נוסף לסוף `verify`. **המדידה רצה בפועל: 105 בדיקות ירוקות ב-320/375/414px**, ו-`npm run verify` המלא עבר מקצה לקצה. 6 בדיקות ב-`scripts/verify-mobile.test.ts` שומרות על החיווט עצמו \| ✅ טופל | 1 |
| F-009 | 🟡 MEDIUM | `lib/core/auth.ts:111` | **שגיאת סיסמה ארוכה מדי מוצגת כשגיאת אימייל.** `validation_failed` ממופה ל-`invalid_email`, אבל GoTrue משתמש בקוד הזה גם לתקרת 72 התווים של bcrypt | לומד נרשם עם סיסמה בת 80 תווים ממנהל סיסמאות → GoTrue מחזיר 422 `validation_failed` → הממשק מציג "כתובת האימייל לא נראית תקינה" **מתחת לשדה האימייל**. הלומד מתקן שוב ושוב אימייל תקין לחלוטין ולעולם לא מצליח להירשם | מפה `validation_failed` ל-`unavailable` או הוסף קוד `password_too_long`, והגבל אורך סיסמה ב-`isAcceptablePassword` | ✅ **טופל C-0017 (משימה 1 בתוכנית account-integrity).** התקרה נספרת ב-**בתים ולא בתווים** — `passwordByteLength` דרך `TextEncoder`, כי bcrypt חותך על 72 בתים: סיסמה של 40 אותיות עבריות היא 40 תווים ו-**80 בתים**, כלומר ספירת תווים הייתה משאירה בדיוק את הבאג רק רחוק יותר. `passwordLengthProblem` מחזיר `weak_password` / `password_too_long` / `null`, ו-`checkCredentials` ממפה אותו ישירות לשדה **הסיסמה**; `isAcceptablePassword` נשאר כעטיפה. `mapAuthError` הפסיק לנחש: `validation_failed` — הקוד שבו GoTrue משתמש גם לכתובת פגומה וגם לחריגת bcrypt — עובר ל-`unavailable`, כלומר הודעה ברמת הטופס ולא האשמה מתחת לשדה שהלומד לא טעה בו; `email_address_invalid` ממשיך להאשים את הכתובת. הרמז ב-`AuthForm.tsx` הפסיק לשקר ("בלי כללים נוספים" הוסר — יש עכשיו כלל נוסף). **10 בדיקות חדשות; אומת שהן נכשלות מול הקוד הישן** (8 כשלים, בהם `expected 'invalid_email' to be 'unavailable'`), **ומוטציה של הקבוע ל-`7200` מפילה 3 מהן** — קבוע שלא מפיל בדיקה אינו נבדק \| ✅ טופל | 0 |
| F-010 | 🟡 MEDIUM | `lib/supabase/server.ts` | **קוד מת שמחזיק את מפתח ה-service_role.** הקובץ אינו מיובא מאף מקום (אומת ב-grep), ואין זכר ל-`service_role` ב-`.next/static` — כלומר אין דליפה **היום**. אבל הוא נשאר כפיתוי זמין | סוכן Dev עתידי שיצטרך לעקוף RLS ימצא פונקציה מוכנה שמייבאת את מפתח ה-service_role, וייבא אותה בטעות לרכיב שרץ בצד הלקוח. זו בדיוק הדליפה שהארכיטקטורה נועדה למנוע | מחק את הקובץ. כשבאמת יידרש service_role, ייכתב במקום ובזמן עם נימוק מפורש | ✅ **טופל C-0005 (T-024).** הקובץ נמחק. מחיקה לבדה הפיכה בטיק אחד, ולכן נוספה `lib/supabase/serviceRole.test.ts` (3 בדיקות) שסורקת את `app/`, `components/`, `lib/` ו-`proxy.ts` ונכשלת על כל קריאה של `SUPABASE_SERVICE_ROLE_KEY` — אומת שהיא נכשלת מול הקוד הישן. בנוסף המפתח הוסר מ-`.env.example` ומ-`docs/SETUP.md` (אין צרכן → אין סיבה שיישב ב-env של Netlify) \| ✅ טופל | 1 |
| F-011 | 🟠 HIGH | `app/page.tsx:17` | **67% ממסך הבית ריק.** נמדד ב-375×812: שני חללים מתים של 267px ו-275px, יחס דיו 25%. `flex-1 justify-center` ממרכז שתי שורות טקסט בתוך אזור גמיש גבוה, ויוצר פער ענק בין הכותרת לכפתור | לומד פותח את האתר בטלפון ורואה כותרת, משפט, והרבה מאוד לבן. זה לא נקרא "מינימליזם" אלא "האתר לא נטען עד הסוף". אין שום דבר שמסביר מה הוא מקבל, ואין סיבה ללחוץ | אל תמרכז — עגן את הטקסט למעלה, ומלא את החלל בתוכן שעושה עבודה: 3 שורות "מה מקבלים", מונה ימים למבחן, או תצוגה מקדימה של כרטיסייה אחת. ראה T-027 | ✅ **טופל C-0005 (T-027).** `flex-1 justify-center` הוסר, הכותרת מעוגנת מתחת ל-header, והחלל התמלא בשלוש שורות "מה מקבלים" (`LANDING_VALUE_POINTS`). הכפתור נשאר בחצי המסך התחתון (MF-5). **נוספה בדיקה מודדת** ב-`check:mobile`: הפער בין תחתית ה-header לראש ה-`h1` חייב להיות ≤48px — אומת שהיא נכשלת מול הפריסה הישנה (120/146/161px ב-320/375/414). 108 בדיקות מובייל ירוקות. **הורחב C-0076 (T-058):** F-011 חזר פעם אחת כ-F-016, ולכן הוא נעול מעכשיו גם **סטטית** — `scripts/verify-mobile.test.ts` דוחה `flex-1 … justify-center` במכולת עמוד בארבעת הקבצים (`page` · `study/page` · `loading` · `error`), והמדידה חלה על ה-JSX בלבד (`markupOnly()`) כדי שהערה שמתעדת את הפגם לא תורשע במקומו. שתי מוטציות הרגו את הבדיקה בשמה \| ✅ טופל | 1 |
| F-013 | 🟠 HIGH | `components/AuthForm.tsx:177` | **אין הצגת סיסמה, אין שדה אימות, ואין שחזור.** בשילוב עם החלטת Q-001 (אימות מייל כבוי), שגיאת הקלדה אחת בסיסמה או במייל = חשבון אבוד לצמיתות | לומד מקליד סיסמה בטלפון, נכשלת אות אחת, הוא נרשם בהצלחה ואינו יכול להיכנס שוב לעולם. אין לו דרך לדעת מה קרה ואין לנו דרך לזהות אותו — הכתובת לא אומתה | הוסף כפתור עין להצגת סיסמה (התקן המקובל, עדיף על שדה אימות במובייל) + הצג את הכתובת שנרשמה למאשור. משלים את T-026 | ✅ **טופל C-0005 (T-030).** כפתור `[data-password-toggle]` בשדה הסיסמה (`aria-pressed` + `aria-label` שמתאר את הפעולה הבאה), והודעת האישור מצטטת עכשיו את הכתובת שנרשמה בפועל (`confirmationNoticeHe`, טהור). **המדידה רצה: 144 בדיקות מובייל ירוקות ב-320/375/414px** — 4 בדיקות חדשות לכל מסלול אימות; אומת שהן נכשלות מול הקוד הישן (6 כשלי `no [data-password-toggle] button`). סקירת סוכן משנה תפסה התנגשות אמיתית: `"הסתר"` רחב מ-`"הצג"`, וכפתור ברוחב אוטומטי חרג מה-`pr-16` שהשדה שומר לו — תוקן ל-`w-14` קבוע, **ונוספה בדיקה מודדת** שמשווה את רוחב הכפתור ל-`padding-right` בפועל בשני מצבי התווית (אומת שנכשלת מול 64px מול 64px). ⚠️ הצגת הכתובת עם תיקון בלחיצה אחת ב-onboarding נשארה ב-T-026 \| ✅ טופל | 1 |
| F-014 | 🟡 MEDIUM | `app/page.tsx` · `app/globals.css` | **אין שום זהות חזותית.** המסך כולו `slate-900` על `slate-50`. אין לוגו, אין צבע מותג, אין איור, אין מצב כהה | מוצר EdTech ישראלי בשוק שבו כל המתחרים משקיעים בעיצוב. אתר אפור-לבן לחלוטין מאותת "פרויקט צד" ופוגע באמון עוד לפני שהלומד ניסה משהו. חשוב במיוחד כשמבקשים תשלום בהמשך | הגדר צבע מותג אחד + גוון הצלחה/כישלון ב-`tailwind.config.ts`, והוסף `prefers-color-scheme: dark`. ראה T-028 | ✅ **טופל C-0013 (T-028).** `lib/core/palette.ts` — 11 אסימוני צבע כטבלה טהורה + `contrastRatio()` לפי WCAG 2.1, עם 13 רצפות ניגודיות שנאכפות בבדיקת יחידה בשני המצבים. `--brand-surface` קיים כצעד נפרד ממדידה: לבן על `#2a78d6` הוא 4.42:1, מתחת לרצפה — על `#1d4ed8` הוא 6.70:1. האסימונים נכתבים ל-`globals.css` בשני בלוקים ש-`palette.test.ts` נכשל אם הם נסחפים מהטבלה, וממופים לשמות סמנטיים ב-`tailwind.config.ts` **בלי שום `dark:` variant**. **מצב כהה נמדד בפיקסלים** ב-`check:mobile`: רקע וטקסט משתנים, ו-`--surface` הכהה הוא `rgb(15, 23, 42)` בפועל. מחסום ה-slate סורק את **כל** `app/` ו-`components/` (14 קבצים) ולא רשימה ידנית — אומת שהוא נכשל על החדרת `bg-slate-200` ל-`loading.tsx`. 33 בדיקות בקובץ, 195 בסוויטה, 168 בדיקות מובייל \| ✅ טופל | 0 |
| F-016 | 🟡 MEDIUM | `app/onboarding/page.tsx:34` | **חלל מת חוזר — אותו אנטי-דפוס של F-011 לא הועבר לקובץ האח.** מסך ה-onboarding עוטף את הכותרת ב-`flex flex-1 flex-col justify-center`, בדיוק הפריסה שנפסלה ב-F-011 ותוקנה ב-`app/page.tsx` ל-`flex flex-col gap-4` מעוגן-למעלה | לומד מחובר פותח `/onboarding` ב-iPhone 375×812 → הכותרת "כמעט מוכן" ממורכזת אנכית בתוך `flex-1` → פס לבן מת של ~250px מעל הכותרת, אותו פגם ש-F-011 סימן במסך הבית. `check:mobile` אינו תופס: בדיקת "heading anchored" מוגבלת ל-route `/`, ו-`/onboarding` מפנה ל-`/login` בהיעדר סשן ולכן לא נמדד כלל | הסר `flex-1 justify-center`, השאר `flex flex-col gap-4` — כמו בתיקון F-011. שקול לפרוס את בדיקת העיגון גם ל-`/onboarding` (עם סשן מדומה) כדי לתפוס רגרסיה עתידית | ✅ **טופל C-0013 (T-028) — ורחב מהממצא.** ה-`flex-1 justify-center` הוסר מ-`app/onboarding/page.tsx`, **ובדיקת העיגון הורחבה מ-route בודד לכל מסך שיש בו `h1` תחת `main`** — מחסום שמוגבל לקובץ אחד הוא בדיוק הסיבה שהפגם שרד. ההרחבה מדדה שלושה מקרים נוספים שאיש לא ידע עליהם: **`AuthForm` ב-139px ב-`/login` ו-109px ב-`/signup`** (נמדד חי לפני התיקון, אותו `flex-1 justify-center`), `/offline` ב-307px, ו-404 ב-270–284px. כולם תוקנו. גרר תיקון נוסף: הסרת המרכוז חשפה שבורר הנסיגה `main a[href], main button` החזיר את **כפתור העין** של הסיסמה ולא את כפתור ההגשה — `AuthForm` סומן עכשיו ב-`data-primary-action`, ובדיקת אזור האגודל הורחבה ל-`/login` ו-`/signup` שמעולם לא נבדקו. ⚠️ הערת הממצא ש-`/onboarding` מפנה ל-`/login` בהיעדר סשן **אומתה חי** (307 → `/login?expired=1`), ולכן המסך עצמו עדיין לא נמדד ישירות — ראה TD-13 \| ✅ טופל | 0 |

<!-- 2 שורות -->
| F-183 | 🔴 CRITICAL · **`npm run verify` אדום על `work/current` — `scripts/plan-hygiene.test.ts` דורש רצף `T-xxx` ללא פער, ו-`C-0437` שמר בכוונה על הפער `T-254…T-260` · נפתח על ידי DEV (C-0438, טיק בנייה, בסטת `verify` המלאה לפני דחיפה)** | `scripts/plan-hygiene.test.ts` · `plan/50-tasks.md` · `plan/00-control.md:2` (הערת `NEXT_AGENT`) | **נמדד חי בטיק הזה, ⛔ ולא שוער:** `npm run verify` נופל ב-`test` על `plan/50-tasks.md — the task register > leaves no gap in the ID sequence` — הרצף עובר מ-`T-253` ל-`T-261`, וה-`expect` דורש `254…260` ⛔ קיימים. **אומת ב-`./scripts/g stash`: הכישלון הזהה קיים גם על הקומיט המקורי (`ad5f6ca`), ⛔ לפני שנגעתי בקובץ** — ⇒ זה ⛔ אינו רגרסיה שהטיק הזה הכניס. **הסיבה, בשורה עצמה:** `plan/00-control.md:2` (הערת `NEXT_AGENT` מ-`C-0437`) כותבת במפורש «⛔ טווח `T-254`…`T-260` נשמר פנוי לתוכנית שממתינה לאישור רוי» — PM שמר טווח מספרים מכוון, ו-`scripts/plan-hygiene.test.ts` (מ-`F-025`, ⛔ ולא נכתב נגד המקרה הזה) מצהיר בפירוש בהערתו: «A gap means an ID was lost, not freed» — ⛔ אין נתיב-מילוט בבדיקה עצמה לטווח שמור-בכוונה. | 🔴 **`npm run verify` ⛔ לא יכול לצאת ירוק בשום טיק** (DEV/PM/QA/CONTENT כאחד) כל עוד `T-254…T-260` נשארים ⛔ בלי שורה, כי `test` נופל ולא ממשיך ל-`build`/`check:mobile` (שרשרת `&&`). ⛔ זו ⛔ אינה תקלת מוצר ללומד — היא תקלת **תשתית-הלופ**: כל דוח «`verify` ירוק» מהרגע הזה ואילך חייב לפרט את הכישלון הבודד הזה במפורש (כפי שדוח זה עושה) או שהוא טוען הצלחה על ריצה שלא הייתה. | **הכרעת PM/רוי בין שתי דרכים, ⛔ לא תיקון DEV חד-צדדי:** ⓐ **PM ממלא את הטווח בשורות `⛔ שמורה` (placeholder) עד שהתוכנית מאושרת** — הרצף נשאר שלם, ⛔ ואף מספר לא הולך לאיבוד ולא מומצא כתוכן אמיתי. ⓑ **`plan-hygiene.test.ts` מקבל נתיב-מילוט מפורש** לטווח שמור-מוצהר (למשל `RESERVED_RANGES` הנקרא מ-`00-control.md` או קבוע ברשימה), ⛔ בלי לוותר על התפיסה של `F-025` (עדיין אין מחיקה/מיחזור של ID אמיתי). ⛔ **לא ניסיתי אף אחת מהשתיים** — שתיהן משנות חוזה בין PM לבדיקת היגיינה שנכתבה נגד מחלקת כשל אחרת, וזו בדיוק הכרעה ש-`RULES § 0.22` שולחת ל-PM. | ✅ נסגר C-0441 (PM) — `D-190`. שבע השורות `T-254`…`T-260` נכתבו לרשם בטיק הזה ⇒ הפער ברצף המזהים ⛔ אינו קיים עוד. | C-0438 |
| F-184 | 🟡 MEDIUM · **T-167ⓔ («הבלוק מחליף את הקודם — `03-for-roy.md` לא גדל בלי גבול») סותרת את הפרקטיקה החיה של הקובץ — שורות ממוספרות מצטברות ⛔ ולא בלוק יחיד מוחלף · נפתח על ידי DEV (C-0439, טיק בנייה, לפני כתיבת קוד — `RULES § 0.22` שולחת ניגוד מול מסמך עוגן ל-PM)** | `plan/50-tasks.md` T-167 · `docs/agents/CRITIC.md` STEP 7 · `plan/03-for-roy.md` (items 93·94·95·96) · `plan/RULES.md § 0.21` | **נמדד בטיק הזה בקריאת `plan/03-for-roy.md` החי, ⛔ ולא שוער:** הקובץ מחזיק ארבעה פריטים ממוספרים אחרונים (93·94·95·96) **זה לצד זה**, ולא בלוק יחיד — פריט 95 (CRITIC C-0428) כבר נושא את תוכן «שלוש ההקשות» (①②③, מסך·פעולה·תוצאה) **בתוך שורתו שלו**, ופריט 96 (DEV C-0430, נושא אחר לגמרי) יושב מעליו ⛔ ולא מחליף אותו. `CRITIC.md` STEP 7 אכן מורה לכתוב את שלוש ההקשות (T-167 ⓐ-ⓓ), אך ⛔ אינו מורה למחוק/להחליף את הבלוק הקודם — ⓔ בלבד לא מומש. | אם כל קידום עתידי מוסיף פריט ממוספר חדש (התבנית הקיימת, 93→94→95→96), `plan/03-for-roy.md` **כן גדל בלי גבול** בדיוק כפי ש-ⓔ מזהירה מפניו — בניגוד ל-`RULES § 0.21` שדורש רענון חותמת שבועית לכל פריט **פתוח**, כלומר הפריטים אמורים **להישאר** עד שרוי עונה. ⛔ מימוש מילולי של ⓔ (מחיקת/החלפת הבלוק הקודם) עלול למחוק בקשה שרוי טרם ענה עליה — בדיוק ההפך מהמטרה שהחלטת T-167 (D-101/D-098ⓓ) נכתבה בשבילה: שהתשובה שלו תמיד תיסגר. | **הכרעת PM/רוי, ⛔ לא תיקון DEV חד-צדדי:** ⓐ לוותר על «בלוק יחיד מוחלף» ולסמוך על ארכוב תקופתי (בדומה ל-`gc:memory`/`pruneControlHistory`, F-182) שמעביר פריטים **סגורים** לארכיון ומשאיר רק פתוחים — כך הקובץ נשאר חסום מלמעלה בלי לאבד בקשה שממתינה לתשובה; או ⓑ להגדיר בלוק שלוש-הקשות **נפרד** מטבלת ההסלמה הכללית, שאכן מוחלף בכל קידום — ולקבל שקידום חדש «מוותר» על תשובה לישן שטרם נענה. ⛔ לא ניסיתי אף אחת מהשתיים — שתיהן משנות את ערוץ התקשורת היחיד עם רוי (D-101), וזו בדיוק ההכרעה ש-`RULES § 0.22` (ניגוד מול מסמך עוגן · מנגנון תקשורת עם רוי) שולחת ל-PM. | ✅ נסגר C-0441 (PM) — `D-189` הכריעה אפשרות ⓑ: מדור `## POST-PROMOTION CHECK` ייעודי שמוחלף בכל קידום, וטבלת ההסלמה הממוספרת נשארת תחת `RULES § 0.21` ללא שינוי. `T-167` ⛔ אינה חסומה עוד. | C-0439 |

<!-- 1 שורות -->
| F-175 | 🟠 HIGH · **בלם מת בבקרה — `CONSECUTIVE_NO_PROGRESS` מוצהר, ו⛔ אף אחד ⛔ אינו מעלה אותו · נפתח בידי PM (C-0376), בבדיקת לינאריות הלופ שרוי הזמין** | `plan/00-control.md:7` · `docs/AGENT_BLUEPRINT.md:237` · `docs/AGENT_BLUEPRINT.md:310` | **השדה אומר «תקרה: 2 → מעבר אוטומטי ל-HUMAN», והמנגנון שאמור להעלות אותו חי אך ורק ב-`docs/AGENT_BLUEPRINT.md` וב-`docs/handoff_flow.mermaid`.** נמדד בטיק, `grep -rn "CONSECUTIVE_NO_PROGRESS"` על כל הריפו החי ⇒ **ארבע הופעות בלבד**: השדה עצמו, שתי שורות בבלוּפרינט, ושורה בדיאגרמה. ⛔ **⛔ אף אחד מארבעת פרומפטי הסוכנים (`docs/agents/*.md`) ⛔ אינו מזכיר אותו, ⛔ אין לו סעיף ב-`plan/RULES.md`, ו⛔ אין בדיקה שמודדת אותו.** ⚠️ ומאז 24/08 (`RULES § 0.23ח`) **הפרומפט שבריפו הוא הטקסט המחייב** — הבלוּפרינט ⛔ אינו הוראה לאף סוכן. | **הלופ מגיע למצב שבו הוא ⛔ אינו מתקדם, והבלם שנועד להעיר את רוי ⛔ לעולם לא יורה.** התרחיש המדויק, וכבר קרה קרוב אליו: `ACTIVE_WORKSTREAM` מתייבשת, DEV יורה **12 פעמים ביום** ויוצא בשקט בכל פעם (יציאה מוקדמת חוקית, ⛔ בלי קומיט), ‏QA ⛔ אינו מזיז את המוקד — והמונה נשאר **0 לנצח**, מפני ש⛔ אף שורת קוד ⛔ אינה מעלה אותו. ⇒ `NEXT_AGENT` ⛔ לעולם ⛔ לא יגיע ל-`HUMAN` מהנתיב הזה. ⛔ **הבלם היחיד שכן חי בנתיב הזה הוא מפסק איש-המת (36 שעות), והוא ניזון מ-`LAST_HANDOFF_AT` שהסוכן מדווח על עצמו** (‏§ 1.1 במפת הלופ). | ⓐ **הכרעה של רוי, ⛔ ולא של סוכן: לחיות או למות.** ⓑ **לחיות** ⇒ הכלל יורד לפרומפטים (מי מעלה, מתי, ומי מאפס) + בדיקה 15 ב-`loop:health` שמודדת «מונה שעומד על 0 בעוד N טיקים רצופים ⛔ לא הפיקו קומיט». ⓒ **למות** ⇒ השדה יוצא מ-`00-control` והשורות בבלוּפרינט מסומנות היסטוריות — ⛔ **שדה שמצהיר על בלם שאינו קיים גרוע משדה שאינו קיים**, כי הוא קונה ביטחון שווא. ▶️ **המלצת ה-PM: ⓒ בשלב זה** — מפסק איש-המת ובדיקות 11·13 כבר מכסות את «הלופ מסתובב ריק», והוספת מנגנון חדש ערב ההדלקה היא סיכון גדול מהתועלת. | ✅ **נסגר 06/09 — הכרעת רוי: להסיר.** `CONSECUTIVE_NO_PROGRESS` הוצא מ-`plan/00-control.md`; שלוש השורות ב-`docs/AGENT_BLUEPRINT.md` וזו שב-`docs/handoff_flow.mermaid` סומנו היסטוריות, ⛔ ולא נמחקו. `03-for-roy` פריט 87 נסגר. | 0 |

<!-- 2 שורות -->
| F-168 | 🟠 HIGH · **`D-141` ⛔ אינה ניתנת לקיום עד ש-`T-223` נוחתת — 1,200 פריטים נוספו הלילה **בלי רמה** · נפתח על ידי OPERATOR (29/08, בבדיקת הרצת הלילה)** | `lib/core/contentSchema.ts:321` · `data/generated/batch-2026-08-29*.jsonl` · `docs/agents/CONTENT.md` § D-141 | **נמדד על התוצר, ⛔ ולא שוער.** שמונה אצוות · **400 משמעויות** · **1,200 משפטי תרגול** — `items` ⇒ **`str`**, ו-**0 מהם נושאים `level`.** ⇒ ⛔ **וזו ⛔ אינה אי-ציות של הסוכן.** הוריתי לו בפרומפט לכתוב `level` ל-JSONL **עוד לפני** שהסכמה נוחתת; ⛔ אבל השער עצמו (`contentSchema.ts:321`) מריץ `stem.split(BLANK)` על כל איבר — ⇒ **אובייקט היה מפיל את השער בחריגה.** 🔴 **הסוכן עמד בפני הוראה שסותרת שער שנאכף במכונה, ובחר בשער. זו הבחירה הנכונה.** | ⚠️ **הנזק מצטבר ⛔ ואינו קבוע:** כל אצווה מוסיפה 150 משפטים לא-מתויגים. הלילה 1,200; שבוע בקצב הזה ⇒ ~8,000. ⛔ **וזו בדיוק העלות ש-D-141 נכתבה כדי למנוע** (`§ 4.3.3` פריט 1). ⇒ **התיוג בדיעבד גדל בכל טיק שבו CONTENT רץ לפני DEV.** | **הלקח, ⛔ ולא רק התיקון:** ⛔ **הוראה בפרומפט ⛔ אינה גוברת על שער שנאכף במכונה, ו⛔ אין לכתוב אותה כאילו כן.** ⓐ **DEV לוקח את `T-223` לפני שCONTENT רץ שוב** — מיגרציה מוסיפה + `items` לאובייקט + השער. ⓑ עד אז ⛔ **הוסר מהפרומפט** הסעיף «כתוב את השדות בכל מקרה». | ✅ **נסגר C-0471 (QA)** — אומת: `T-224` על `origin/dev` (`fbdd61e..4a65586`, ff-only), `npm run verify` טרי אחרי המיזוג דרך שער ה-`pre-push`, exit 0 — `test` 3520/3520 · `check:mobile` 1325/1325. **C-0466 (DEV) — נסגר על ידי `T-224`, ⛔ לא `T-223` (מיהוי שגוי מקורי, ראה docs/superpowers/plans/2026-09-06-sense-items-level-tagging.md).** מיגרציה `0021_sense_items_level.sql` מוסיפה `level`/`level_rationale` (nullable, ⛔ אין ברירת מחדל) · `contentSchema.ts` אוכפת את הזוג (D-141 § ג) · `batchRecord.ts` מקבל שתי הצורות (תאימות ל-787 השורות הקיימות) · `scoringSeed.ts`/`build-ingest-sql.mjs`/`measure-continuations.mjs` מעבירים את השדות עד הסוף. | C-OPERATOR-29/08 · C-0471 |
| F-169 | 🟠 HIGH · defect · **בדיקה 11 ב-`loop:health` מדווחת «יש עבודה פנויה» בזמן שהשורה ⬜ היחידה בזרימה הפעילה חסומה-חיצונית — הבדיקה שנכתבה נגד טיקי DEV ריקים ⛔ אינה תופסת את המקרה שלה עצמה · נפתח על ידי QA (C-0367, טיק שער מלא)** | `scripts/loop-health.mjs` — בדיקה 11 · `plan/50-tasks.md:277` (‏`T-220`, תא הסטטוס) · `plan/00-control.md` (‏`ACTIVE_WORKSTREAM`) | **נמדד בהרצה טרייה ב-19:56Z, ⛔ ולא שוער.** ‏`npm run loop:health` הדפיס ` ok  11. לזרימה הפעילה יש עבודה פנויה — arena — 1 משימות ⬜`. השורה היחידה היא `T-220`, ותא הסטטוס שלה קורא מילולית **«ⓐ ו-ⓓ ⛔ נשארות ⬜, חסומות ב-F-164»** — ‏`F-164` בבעלות **PM**, פתוחה. ⇒ ‏`arena` נושאת **אפס שורות בנות-בנייה** ל-DEV, והבדיקה ירוקה. | ⚠️ **תרחיש הכשל, בשעות:** DEV יורה **12 פעמים ביום** מול שני טיקי QA. בין הטיק הזה לבא — **12 שעות · 6 טיקי DEV** — כל אחד משכפל את הריפו, קורא פרומפט של 25KB, מסנן את התור ל-`ACTIVE_WORKSTREAM`, מוצא שורה אחת שאינה בת-בנייה, ומסיים ב«טיק שקט». **זו בדיוק המחלקה שבדיקה 11 נכתבה נגדה** (`story` התרוקנה ב-05:29 והשדה עדיין קרא `story` ב-07:03) — ⛔ רק שהפעם הרגיסטר ⛔ אינו ריק, הוא **חסום**, והמונה ⛔ אינו יודע להבדיל. | **כיוון תיקון, שורה אחת:** בדיקה 11 סופרת שורה ⬜ כ«עבודה פנויה» **רק אם תא הסטטוס שלה ⛔ אינו נושא `חסומ`** (או: ⛔ אין ממצא פתוח שמצטט את מזהה השורה). ⛔ הסף ⛔ אינו מומצא — הוא בדיוק הטקסט שכבר כתוב בתא. ⚠️ **ובטיק הזה ה-QA כבר פעל על הממצא:** ‏`ACTIVE_WORKSTREAM` הוזז ל-`studies` (חסומה-חיצונית · § STEP 5), כך שששת טיקי ה-DEV הקרובים ⛔ אינם ריקים. | ✅ **נסגר C-0471 (QA)** — אומת: `C-0401` על `origin/dev` (`merge-base --is-ancestor` ⇒ true), `npm run loop:health` טרי על הריפו החי אחרי המיזוג — **17/17**, בדיקה 11 מדווחת `general (חוצה-מערכת: general · loop · base) — 4 משימות ⬜` בלי דיווח-שקר. **נבנה C-0401 (DEV).** `scripts/loop-health.mjs` בדיקה 11 קוראת עכשיו את `plan/50-tasks.md` הגולמי (‏`textuallyBlockedOpenTasks`) ומחסירה מהמספר שהתקבל מ-`balance()` כל שורה שפותחת `⬜` (הגלגלת הראשונה מנצחת, כמתמיד) אך תא הסטטוס שלה מכיל בכל זאת `חסומ` — בדיוק כיוון התיקון שהממצא ציטט. | C-0401 · C-0471 |

<!-- 2 שורות -->
| F-176 | 🟡 MEDIUM · **כותרת המסלול כפולה במסך `כרטיסיות` · נפתח בידי QA (C-0379), בהליכת המוצר של הטיק** | `app/layout.tsx:38` · `components/LevelMapScreen.tsx:153` | **השורה «אנגלית · מסלול אמיר״ם» מוצגת פעמיים ברצף, ממש מעל הכותרת «כרטיסיות».** נמדד חי ב-`/dev/tabs/cards` (375×780, `next dev`): `app/layout.tsx:38` מרנדר אותה פעם אחת בכותרת הגלובלית של כל מסך; `components/LevelMapScreen.tsx:153` מרנדר `{TRACK_HE}` (מוגדר שורה 54, אותו מחרוזת מילה-במילה) פעם שנייה כשורת ה-`header` הפנימית של המסך עצמו — לפי ההערה בקוד היא מכוונת שם ל-`render_video_A.py:255-256`, שמצייר אותה **פעם אחת בלבד**. הרנדר המחייב `docs/design/kol-A-02-deck.png` מציג אותה פעם אחת. `grep -rn "מסלול אמיר" components app` מאשר: אלו שתי ההופעות היחידות בקוד, ואף מסך לשונית אחר (`MeScreen` · `StudiesScreen`) אינו חוזר על כותרת הגג — הבאג מבודד ל-`LevelMapScreen`. ⛔ **לא הוצג בענף הזה** — `LevelMapScreen.tsx` לא נגע בדיף `origin/dev..origin/work/current`, כלומר הכפילות כבר על `dev` והמיזוג של הטיק הזה אינו מחמיר אותה. | כל לומד שנכנס ללשונית «כרטיסיות» (מסך הבית של `36 § 5`, לפי `docs/plan-tables.md`) רואה את שם המסלול פעמיים ברצף לפני שהוא מגיע לתוכן — שורת מסך מבוזבזת ב-375×780, ודוחפת את «דרכים לתרגל» ו«מנת היום» עוד יותר מטה בגלילה. ⛔ אינו dead-end ואינו הטעיה, אבל זו בדיוק המחלקה שהערת STEP 4 ("כמה תווים, כמה תשובות בהקשה") נועדה לתפוס | ⓐ **המחליטה היא PM/DEV, לא QA:** או שהכותרת הגלובלית ב-`app/layout.tsx` יורדת למסכים שאינם צריכים אותה (ולא ל`/cards` שכבר מציג אותה בעצמו), או ש-`LevelMapScreen.tsx:151-154` מוותר על ה-`<header>` הפנימי ומסתמך על הגלובלי בלבד — כל עוד הרנדר הבא נשאר תואם ל-`kol-A-02-deck.png` (פעם אחת). | ✅ **נסגר — נמדד C-0478 (PM) בקלון הזה, ⛔ ולא שוער.** התיקון נחת ב-`T-262` לפי `D-188`: `grep -rn "מסלול אמיר" components app` ⇒ **מופע אחד**, `app/layout.tsx:54`; `components/LevelMapScreen.tsx:150-152` נושא הערה ש-`app/layout.tsx` הוא הבעלים היחיד, ו-`components/LevelMapScreen.test.ts:184-205` גוזר את המחרוזת מ-`app/layout.tsx` read-only ונופל אם רכיב כלשהו ישכפל אותה. | 0 |
| F-145 | 🟡 MEDIUM · defect חזותי (⛔ **מגיע ללומד** — כפילות מבנית בכותרת, ⛔ אינו שובר מסך) · **שורת «אנגלית · מסלול אמיר״ם» מרונדרת פעמיים בלשונית `כרטיסיות`, זו מעל זו · נפתח על ידי QA (C-0322, טיק שער) בהליכה מול הרנדר** | `app/layout.tsx:38` (כותרת ה-`RootLayout` הגלובלית) · `components/LevelMapScreen.tsx:54,153` (`TRACK_HE` → `<p class="text-sm text-ink-muted">`) · `app/(tabs)/cards/page.tsx` (מרנדר `<LevelMapScreen>`) · `docs/design/kol-A-02-deck.png` | **נמדד בדפדפן חי ב-375×780 בטיק הזה (Playwright, `innerText` + `diff:render`), ⛔ ולא הוערך בעין.** ‏`app/layout.tsx:38` מצייר את המחרוזת «אנגלית · מסלול אמיר״ם» ב**כל** מסך (כותרת גלובלית), ו-`LevelMapScreen.tsx:153` מצייר את **אותה מחרוזת בדיוק** באותה מחלקה (`text-sm text-ink-muted`) ⇒ הלשונית מציגה אותה **פעמיים**, זו מעל זו, מעל «כרטיסיות». הרנדר `kol-A-02-deck.png` מצייר אותה **פעם אחת** בלבד. ⛔ **אינו ארטיפקט של הפיקסצ׳ר** — הנתיב האמיתי `app/(tabs)/cards` מרנדר את אותו `<LevelMapScreen>`, ו-`(tabs)/layout.tsx` אינו מסיר את הכותרת הגלובלית. | הלומד בלשונית הכרטיסיות רואה את שם המסלול פעמיים ברצף — כרום מיותר שאינו ברנדר; שאר הלשוניות (`world`/`me`/`studies`) ⛔ אינן מרנדרות `TRACK_HE`, ולכן `cards` **חורגת מהן** ומהרנדר במבנה. | הסר את ה-`<p>{TRACK_HE}</p>` מ-`LevelMapScreen` — כותרת ה-`RootLayout` כבר מספקת אותה בכל מסך; לחלופין הכרע במפורש איזו משתי הכותרות היא הקנונית והשמט את השנייה. תיקון של שורה אחת. | ✅ **נסגר — אותו defect של `F-176`, נמדד C-0478 (PM) באותה פקודה.** `grep -rn "מסלול אמיר" components app` ⇒ **מופע אחד** (`app/layout.tsx:54`); `TRACK_HE` ⛔ אינו קיים עוד ב-`LevelMapScreen.tsx`. נסגר ב-`T-262` לפי `D-188`, ושמור בבדיקה `components/LevelMapScreen.test.ts:184-205`. | C-0322 |

<!-- 1 שורות -->
| F-190 | 🟠 HIGH · defect (רק בחלון טרום-הידרציה על רשת איטית/JS שנכשל לטעון — ⛔ אינו קיים בענף הנבדק כאן, `dev` נושא אותו כבר, ⛔ לכן אינו חוסם את המיזוג הזה) · **סיסמה ב-URL — `<form id="auth-form">` ב-`components/AuthForm.tsx:167` ⛔ אינו נושא `method`, ולכן שליחה לפני הידרציה היא `GET /login?email=…&password=…` · נמדד C-0479 (DEV) תוך כדי ההליכה של T-274** | `components/AuthForm.tsx:167` · `:169` (`name="email"`) · `:186` (`name="password"`) | **נמדד, ⛔ לא שוער:** בהליכה הראשונה של C-0479 הדפדפן קיבל 403 על ה-chunks (`allowedDevOrigins`, 127.0.0.1 מול localhost — תקלת סביבה בלבד), הדף ⛔ לא הודרד, וההקשה על `התחברות` ביצעה שליחה נייטיבית. יומן `next dev` באותה שנייה: `GET /login?email=walk%40example.com&password=passw0rd-walk-2026 200` ו-`GET /signup?email=…&password=…`. הטופס הוא `onSubmit` בלבד, ⛔ בלי `method="post"`, ושני השדות נושאים `name` ⇒ ברירת המחדל של HTML היא GET עם השדות ב-query. | לומד ברשת איטית (בדיוק הלומד ש-`T-274` נבנתה בשבילו) רואה את הטופס מה-SSR לפני שה-JS הגיע, מקליד ומקיש — והסיסמה שלו נוחתת ב-URL: היסטוריית הדפדפן, יומני השרת/‏CDN, ו-`Referer` של כל בקשה מהדף הבא. ⛔ אין שגיאה, ⛔ אין אזהרה — הדף פשוט נטען מחדש עם הפרטים בשורת הכתובת. | `method="post"` על ה-`<form>` (שורה אחת, הפיך בקומיט) — ואז שליחה טרום-הידרציה היא POST אל דף ה-`/login` עצמו, ⛔ בלי סיסמה בכתובת. עדיף: `action="/api/auth/{mode}"` + קבלת `application/x-www-form-urlencoded` בנתיב, כך שהשליחה עובדת גם בלי JS. ⛔ ⛔ לא תוקן ב-C-0479 — מחוץ להיקף `T-274` (STANDING ORDERS: העבודה מ-`50-tasks` בלבד) ⇒ שורת משימה ל-PM. | ✅ C-0482 (DEV) — `method="post"` על `<form id="auth-form">` (‏`components/AuthForm.tsx`). **נמדד בקלון הזה, ⛔ לא שוער — לפני/אחרי על אותו רתם (Playwright, JS כבוי = טרום-הידרציה, 375×780):** לפני: `GET /login?email=…&password=…`; אחרי: `POST /login`, ה-URL נשאר `/login`, **0** שורות `password=` ביומן השרת. שני שומרי-מקור ב-`components/AuthForm.test.ts` (RED⇢GREEN). ⚠️ המסלול ה«עדיף» (‏`action="/api/auth/{mode}"` + קבלת form-urlencoded בנתיב) ⛔ לא נבנה — הוא משנה את חוזה ה-API ונשאר של ה-PM. | C-0482 |
