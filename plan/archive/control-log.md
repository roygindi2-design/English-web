# ארכיון יומן המחזורים של 00-control.md

> נשלף מ-`plan/00-control.md` כדי לשמור עליו מתחת לתקרת 12KB (`RULES § 0.1.2 ב׳`). קריאה בלבד.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה | תוצר |
|---|---|---|---|---|---|
| C-0168 | DEV | **HUMAN** | 2026-08-16T08:37:13Z | 🛑 **טיק עצירה — בלם א׳ עצר לפני כל עבודה, ⛔ אפס קוד ואפס `npm install`.** `MILESTONE_TICKS`=120 מול תקרת 120 (`RULES § 0.1.1 ו׳`); ההעלאה בסוף הטיק ⇒ 121 = פריצה. ⛔ סוכן אינו מאפס את המונה ואינו מעלה את התקרה — נדרשת מילה אחת מרוי. | `dev` ירוק נכון ל-C-0167 (`1371/1371`), אפס 🔴 בבעלות Dev. **פתוח מעבר לבלם:** T-034 עדיין `⛔` (התוכנית כתובה, החסם P-001 של ה-PM · F-060) · תור סקירה C-0163·C-0164·C-0167 ללא קידום מאז 01:14Z · T-043 · T-046 · R-015. `03-for-roy` פריט 26 עודכן במקום. |
| C-0167 | DEV | CRITIC | 2026-08-16T07:43:10Z | 🔧 **טיק ביצוע — F-062 🔴 נסגר, `npm test` ירוק על `dev`.** ⓐ `build:levels` ⇒ `0002` +61/-1. ⓑ ⛔ **מניפסט לא הומצא** — `manifest.json` הוא מצביע שכל טיק CONTENT דורס, והיה המניפסט היחיד של `batch-2026-08-13`; שוחזר בית-בית מ-`git show 38aab36` (`md5` זהה). ⚠️ מופע שלישי של אותו שורש נמדד באימות: `measure:plan` לא הורץ ב-C-0166 ⇒ `plan-tables` אדום. חודש. | **`90/90 קבצים · 1371/1371 בדיקות` ✓ · `typecheck` ✓ · `check:core OK` ✓ · `build` exit 0 ✓.** 3 זרעים + מניפסט משוחזר + **2 שומרים חדשים שנופלים בשם** (מוטציה: `17 failed | 1 passed` במקום `no tests`). ⇒ פריט ל-PM: ארבע נגזרות, וגם ל-CRITIC. 🔴 MILESTONE_TICKS 120/120. |

### שאריות שורות מצב שהועברו ב-C-0171

- `NEXT_AGENT` (הועבר C-0171): # --- לשעבר 🔧 **C-0169 (PM, `date -u` 14:53:02Z→2026-08-17T15:18:58Z, טיק מחקר+תכנון — ⛔ אפס קוד):** התור היה **ריק מדיד** (`measure:plan` ⇒ `eligible: none`) ו-`02-inbox` הכיל שבעה פריטים חיים של רוי מ-17/08. **נפתחו 13 משימות (T-079…T-091), מהן 11 כשירות מיד.** ⚠️ **שלושה ממצאי מחקר, שלושתם עם מקור שנקרא בפועל:** ⓐ **אין ולו סף שליטה אמפירי אחד** ברמת אוצר מילים — Webb/Sasao/Ballance 2017 כותבים שנקודת החיתוך *"appears to have been arbitrary"*, ו-Xing & Fulcher 2007 שהבסיס ל-80% *"is not clear from published sources"* ⇒ **R-017 + D-037: אין שער אחוזים, הלומד עובר בעצמו.** ⓑ **למאל"ו יש הדרכה רשמית לשלושת סוגי השאלות, ובאותו קובץ «אסור להעתיק… או ללמדה… בלא אישור בכתב»** — רחב מ-R-010 ⇒ **R-018 🔴.** ⓒ **VOA Learning English — נחלת הכלל, מסחרי מותר במפורש, ⛔ בלי share-alike, כבר מדורג A2–B1** ⇒ **D-040**, המקור הטוב ביותר שנמצא בפרויקט לפריטי קריאה (⛔ בהחרגת AP/Reuters/AFP — R-019). **שלוש תוכניות UX נכתבו:** § 4.2ז מפת הרמה · § 4.2ח תיקון הכרטיסייה · § 4.2ט שיעורי אמיר"ם. **הצעת PM שרוי לא ביקש: D-041 סריקת רמה** — 12 מילים במסך, ~4 דקות ל-315 מילים, מחליפה מבחן מיצוב שאין לו מנוע. ⚠️ **חסם מת שנמצא ותוקן באותו טיק:** ציטטתי את F-020 כחסם של T-090 — הוא **נסגר ב-C-0012**, אומת ב-`60-findings.md`; הציטוט הוסר ו**הנוסח המיושן ב-R-014 בוטל**. `measure:plan` ⇒ `stale blockers: none`. ⛔ **T-004 (מבחן מיצוב) נשארה ⛔** ו-**T-088/T-090 נפתחו כבר חסומות** — חסימה היא תוצאה מוצלחת. # --- לשעבר 🛑 **C-0168 (DEV): טיק עצירה** — בלם א׳ ב-120/120. רוי סגר את M0 ואיפס את המונה (`950eecd`). ההיסטוריה המלאה בגיט.
- `STATE` (הועבר C-0171): # --- לשעבר C-0168 (DEV): טיק עצירה, בלם א׳. # --- לשעבר C-0166 (CRITIC): npm test אדום ⇒ F-062.
- `LOCK_HELD_BY` (הועבר C-0171): # --- לשעבר: "" — C-0170 (CRITIC) שחרר ב-16:52Z. # --- לשעבר: "CRITIC" — C-0170 לקח ב-16:45Z. # --- לשעבר: "" — C-0169 (PM) שחרר ב-15:18:58Z.
- `LAST_REVIEWED_COMMIT` (הועבר C-0171): # --- לשעבר C-0154 (CRITIC, 🔬 עומק 19:03Z) סקר bc684d3..5d5785f (C-0148→C-0153) — 12 קומיטים, כלי לופ בלבד. # --- לשעבר: "bc684d3" — C-0147 סקר ac80bb4..bc684d3
- `LAST_PROMOTED_AT` (הועבר C-0171): # --- לשעבר C-0132 קידום 4164ba9..5147651 (world compose + /dev/world + focus ring). בדיקת עשן health ok:true 4/4.

# --- לשעבר 🟢 **C-0152 (DEV, `date -u` 17:36:27→17:44:00, טיק ביצוע — שלוש משימות בטיק אחד): התוכנית של C-0151 מומשה במלואה, והרגיסטרים נקראים בפקודה אחת.** ‏`lib/core/planTable.ts` טהור (`splitRow` מודע ל-`\|` · `rowShape` · `classifyStatus` לפי **הגליף הראשון** · `citedFindings` · `staleBlocks` · `eligibleTaskIds`) + `scripts/measure-plan-tables.mjs` ⇒ **`npm run measure:plan`** ⇒ `docs/plan-tables.md` מחויב + רַצֶ'ט ב-`scripts/measure-plan-tables.test.ts`. ⚠️ **המספרים של C-0151 תוקנו במדידה ⛔ ולא הועתקו:** ה-`awk` הנאיבי ספר 8/23 שורות פגומות, המפצל המודע-להברחה מדווח **6** ב-`50-tasks.md` (72 שורות) ו-**19** ב-`60-findings.md` (60 שורות) — ואלה הרצפות ברַצֶ'ט. **הכלי מצא בהרצה הראשונה את מה שנבנה למצוא:** `stale blockers: T-066 cites F-020 (closed)` — הסתירה ש-F-033 רשם ב-13/08 וחיה יומיים, עכשיו פלט של פקודה; ו-`eligible:` מחזיר תשע שורות ⬜ בשאילתה אחת במקום קריאת חמישה קבצים (F-056). **נפתח F-059** (שורה תקינה בת 8 תאים, מאומתת ב-`rowShape`) → PM + CRITIC; ⛔ **הדב לא תיקן אף שורה פגומה** — `50-tasks.md` של ה-PM, `60-findings.md` של ה-Critic, וזה F-030 מחדש. **שתי מוטציות אימתו את הבדיקות** (היפוך `STATE_GLYPHS` הפיל את `T-050`; רצפה 6→5 הפילה את הרַצֶ'ט), שתיהן שוחזרו. **אימות מלא בהודעה הזאת:** `typecheck` ✓ · `check:core` `OK` · **1,259/1,259 ב-80 קבצים** (מ-1,236/78, דלתא **+23**) · `build` יצא 0. ⚠️ **סטייה מהתוכנית, מדווחת:** צעד 4 חזה «8 עוברות» וקוד הבדיקה המילולי שבתוכנית מכיל **7** — חשבון הכותב, ⛔ לא חוסר בבדיקה; ו-`50-tasks.md` לא נגעתי בו כי ל-PM אין מזהה משימה לעבודה הזאת. **לבדיקה: `npm run measure:plan` + `docs/plan-tables.md` + F-059.**

### הועבר מ-00-control.md ב-C-0177 (תקרת 12KB, RULES § 0.1.2 ב׳)

> ✅ **חסימת הקידום שוחררה C-0092 (PM):** `main` אוחה ל-`dev` בקומיט `db71f27` — התנגשות `02-inbox.md` נפתרה (י׳/יא׳ נשמרו ושניהם טופלו באותו טיק). **אומת בהרצה: `git merge-base --is-ancestor origin/main dev` ⇒ exit 0** ⇒ `git merge --ff-only dev` יעבור אצל ה-Critic, וכ-27 מחזורים כולל תיקון F-027 יכולים לעלות לאוויר. ⚠️ **החצי שנשאר פתוח לרוי (פריט 17): ⛔ אין לערוך את `main` ישירות דרך עורך GitHub** — אחרת אותה חסימה תחזור. ⚠️ ‏`PROMOTIONS_THIS_MONTH=6` מול main — ייתכן 6 קידומים שנכשלו בשקט, לבדיקת רוי.

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · **F-028 🟠 (apkg false-accept, רדום → DEV, C-0067)** · **F-029 🟡 (README תקציב מיושן, C-0067)** · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום) · F-028 (C-0069) · F-027 (C-0071 — שני החצאים) · **F-026 (C-0086 — D-029 מצוטטת, הקוד נבדק ולא הונח)**.**

### הועבר מ-`00-control.md` בידי C-0205 (DEV) — תקרת 12KB, `RULES § 0.1.2 ב׳`

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה | תוצר |
|---|---|---|---|---|---|
| C-0202 | **DEV** | — | ‏(⛔ לא נרשמה) | ⚠️ **הרשומה הזאת נכתבה בידי C-0201 ⛔ ולא בידי הסוכן שביצע** — ראה F-079. C-0202 דחף `765b83f` (מיגרציות `0003a`/`0003b`, T-119…T-121) **בזמן ש-`LOCK_HELD_BY: DEV` של C-0201 היה פעיל 15 דקות**, ו⛔ לא עדכן `CYCLE_ID`, `NEXT_AGENT`, `MILESTONE_TICKS` ולא כתב שורת יומן. | ⛔ **התוכן שלו לא נגע ולא נמחק** — הקומיט שרד את ה-rebase במלואו, וטבלת המשימות שלו (‏121 שורות, 0 פגומות) עברה לצד שלי. ⚠️ **הקומיט הזה ⛔ לא נסקר ולא אומת בידי C-0201** — הוא בתור ה-Critic כמו כל דבר אחר. |
| C-0203 | **DEV** | CRITIC | 2026-08-19T18:53:54Z | 📐 **טיק ביצוע — Task 3 מתוך ארבע בתוכנית `2026-08-19-world-home.md` (T-105).** ⚠️ **התוכנית עדיין פתוחה** ⇒ הטיק הבא של ה-Dev הוא ביצוע של Task 4 (T-106), האחרונה בה. | **הכרטיס «מה שכתבת» חי בראש `/world`, ⛔ והוא אינו כותב דבר.** הבדיקה נכתבה **ראשונה** ואומת **בהרצה אמיתית** שהיא נופלת (`ENOENT … components/RecallCard.tsx` · `no tests`) ⛔ ולא הונח שתיפול. `components/RecallCard.tsx` הוא שומר-מקור בן 12 בדיקות: ⛔ אפס `apiPost`, ⛔ אפס עמודת מנוע חזרות (D-051) — **מדד ⓐ של § 4.2יב** · ⛔ אפס `setTimeout`/`setInterval` (D-049 · S15) · ⛔ אפס ניקוד, מטבע ורצף (D-050) · ⛔ אפס hex גולמי, אפס `h-screen`, אפס `justify-center` (F-011 · F-016) · כל `duration-*` ≤300ms. מילת היעד היא **מסגרת בגובה השורה** ⛔ ולא `border-b`; ארבע אפשרויות **שתיים-ושתיים** ב-`grid-cols-2`, כל אחת `min-h-touch`; שגוי ⇒ המילה הנכונה נוחתת **בכל מקרה** והשורה אומרת «המילה הייתה X» — ⛔ אין ניסיון שני, ⛔ אין «נסה שוב» על אפשרות, ⛔ אפס עונש. **שתי מוטציות הרגו בדיקה בשמה:** `void apiPost('/api/review', {})` ⇒ «⛔ הכרטיס אינו כותב דבר» נפלה · `border-b` במסגרת ⇒ «מסגרת ⛔ ולא קו תחתון דק» נפלה. שתיהן שוחזרו, ואומת ש-12/12 חזרו ירוקות ושהקובץ ⛔ אינו מכיל עוד לא `apiPost` ולא `border-b` בקוד. ⚠️ **שלוש סטיות מוצהרות, כולן מדידה ⛔ ולא טעם, והנימוק המלא ב-`plan/30-architecture.md`:** ⓐ המשפט נעטף ב-`<EnWord>` אחד שבתוכו שני `<EnText>` והמסגרת — שתי ריצות מבודדות שכנות בפסקה עברית מסודרות **מימין לשמאל**, כלומר חצי המשפט שאחרי היעד היה נדפס לפני זה שלפניו · ⓑ `&#8203;` שהתוכנית מכתיבה בצעד 3.2 **מפיל את בדיקת ה-hex שהתוכנית מכתיבה בצעד 3.3** ⇒ אותו תו נכתב כתו ממשי (זו הפעם השנייה ברצף, אחרי `export`/`xp` ב-C-0201, שאיסור מחרוזת גורף מפיל מחרוזת שאינה הפגם) · ⓒ `robots` ⛔ לא הוכרז בעמוד הפיקסטורה — נמדד ש⛔ **אף עמוד** `app/dev/*` אינו מכריז אותו, ו-`app/dev/world/layout.tsx` כבר עוטף את המסלול. ⚠️ **F-080 🟡 נפתחה, ⛔ ולא נסגרה כאן:** שורת T-105 דורשת «מושבת **עם המספר**» (D-046), ו-`GET /api/world/recall` עונה `{ok:true, card:null}` **בלי ולו מונה אחד** ⇒ שלושת המצבים (אפס משפטים · אחד מהיום · אין מילת יעד) נראים ללקוח **זהים**, ומספר על המסך היה מומצא ⇒ ל-PM. **אימות טרי בטיק הזה, ⛔ לא «עבר קודם»:** `typecheck` 0 ✅ · `check:core` `OK` ✅ · `npm test` **1773/1773 ב-113 קבצים** ✅ (‏+16 מ-1757) · `build` 0 ✅ (‏`○ /dev/world/recall` בפלט) · `check:mobile` **891 בדיקות** ✅ (‏+15 מ-876), והפיקסטורה `/dev/world/recall` מופיעה בפלט **בלי** שורת קונסול מותרת — והשקט הזה הוא מה שמוכיח שהמדידה אינה על מסך הכשל · `measure:plan` `tasks: 121 rows, 0 malformed`. `docs/api-contract.md` עודכן **באותו קומיט** (שורת «הצרכן טרם קיים» חדלה להיות נכונה). ⛔ לא נגעתי ב-`40-decisions`, ב-`35-design-constitution`, ב-`10-pedagogy`, ב-`AppGrid.tsx` (צעד 3.7), ב-`WorldFeed.tsx`, ב-`/world/compose` וב-`main`. |

> ⬇️ הועבר מ-`plan/00-control.md` ב-C-0210 (DEV) — תקרת 12KB (`RULES § 0.1.2 ב׳`). ⛔ לא נמחק.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה | תוצר |
|---|---|---|---|---|---|
| C-0207 | **PM** | DEV | 2026-08-19T21:20:29Z | 🔍 **ביקורת בנייה מלאה בהוראה חיה של רוי, עם שלושה סוכני משנה במקביל** (מסע הלומד · תוכנית מול קוד · רגיסטרי ממצאים). ⛔ **וכל טענה אומתה על ידי בקריאה חוזרת של הקובץ עצמו** — סוכן משנה אינו מקור. | **⛔ הבנייה בריאה, וזה נמדד ולא צוטט:** `npm ci` ⇒ `typecheck` 0 · `check:core` OK · **`1792/1792` ב-114 קבצים** · `build` 0. ⇒ ⛔ **הבעיה היא מוצר.** 🔴 **ארבעה חסמי לומד (§ 4.11 · D-063…D-066 · T-122…T-125):** ⓐ `proxy.ts:59-60` זורק **כל** לומד חוזר ל-`/onboarding`, שבודק session ⛔ ולא `onboarded_at` — וכל קישור «חזרה לדף הבית» מחזיר לשם (זהו **TD-25**, רשום מזמן ומעולם לא הפך למשימה) ⓑ מאגר ריק ⇒ `DeckSelector.tsx:115-118` משבית את **שלושת** האריחים ⇒ `/study` בלתי-נגיש ⇒ הלומד ⛔ לעולם אינו רואה את `StudyEmptyState` שנבנה בשבילו ⓒ שלושה מסכי כשל בלי יציאה, ואחד מהם «נסה שוב» שלעולם לא יצליח ⓓ `העולם` נעול על `MIN_FUNCTION_WORDS=100` שהלומד ⛔ אינו יכול להזיז ו⛔ אינו רואה ⇒ **כל T-098·T-104·T-105·T-106 שנסגרו אינן נראות לאיש.** ⚠️ **ופגם שנמדד בהרצה ⛔ ולא הוסק: סף הניצחון בזירה הוא 83% ⛔ ולא 67%** — בריכה של 12 עוברת שער של 12 ומחזירה 12 שאלות מול 10 חיים (D-067 · T-126). **D-068:** ארבעה פיצ׳רים שהתוכנית מבטיחה (הכרטיס כיעד מגע · החלקה · יציאה מ-`/study` · הבמה) יושבים ⬜ בתור ⛔ ולא נלקחו ⇒ **D-069: P0 קודם לרצף.** ⚠️ ⛔ **ווידוי שנרשם:** שורת T-128 שכתבתי הכניסה צינור גולמי ל-`50-tasks` ⇒ `1 malformed` — נתפס כי הרצתי `measure:plan` **לפני** הקומיט ותוקן. זו בדיוק מחלקת F-063. הפלט הסופי: `129 rows, 0 malformed`. |

> ⬇️ הועבר מ-`plan/00-control.md` ב-C-0211 (DEV) — תקרת 12KB (`RULES § 0.1.2 ב׳`). ⛔ לא נמחק.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה | תוצר |
|---|---|---|---|---|---|
| C-0208 | **PM** | DEV | 2026-08-19T21:23:47Z | 🧾 **רוי תיקן אותי, והוא צדק.** אמרתי «מהירות ⛔ אינה תוצאה» — נכון מכנית ⛔ אבל חצי תשובה. הוא הצביע על הלולאה שכן קיימת: **מי שממהר טועה, וטעות שורפת קליע.** | **D-070 + T-130.** ⛔ **ולמה זה לא עבד — נמדד ⛔ ולא הוסק:** `ArenaBoard.tsx:405-425` מציג את חיי היריב ⛔ **ואינו מציג את התחמושת שנותרה**, בעוד `BattleState.index` כבר קיים (`arcadeBattle.ts:28`) ⛔ ואינו מגיע למסך. ⇒ הלולאה של רוי **נכונה בקוד ובלתי-נראית בממשק** — הלומד ⛔ אינו יכול לראות שהוא מבזבז קליעים. ⇒ שני המספרים באותה שורת מצב, כי המתח הוא **היחס ביניהם**. ⛔ אין שעון, ⛔ אין ספירה לאחור, ⛔ אין מכפיל מהירות, ⛔ אין ניקוד. **וההפרדה שנקבעה:** מהירות **נחווית** בקרב הרגיל (מילים לא-ידועות ⇒ Nation אוסר שעון) · **מכנית** אך ורק בסיבוב השטף (D-049 · D-062). ⛔ סדר העבודה ללא שינוי — **P0 קודם (D-069)**. |
| C-0209 | **DEV** | DEV | 2026-08-19T21:47:11Z | 📝 **טיק תכנון, ⛔ ולא ביצוע** — ⛔ לא נגעתי בקוד. לארבעת חסמי הלומד (T-122…T-125) ⛔ לא הייתה תוכנית ב-`docs/superpowers/plans/`, וזה בדיוק מה שמייצר טיקי ביצוע תקועים וסבבי אימות חוזרים. | **`docs/superpowers/plans/2026-08-19-learner-unblock.md`** — ארבע משימות במסמך אחד. ⚠️ **ההכרעה שנמדדה בקוד ⛔ ולא הונחה:** שלושת החסמים הראשונים הם החלטה שמפוזרת בין שכבת Edge לשכבת React, ואף אחת משתיהן ⛔ אינה נבדקת בריפו הזה (`vitest` בסביבת `node`, jsdom נעדר בכוונה) ⇒ **כל החלטה יורדת ל-`lib/core` כפונקציה טהורה עם בדיקת יחידה אמיתית**, והשכבות רק קוראות לה ונבדקות בשומר-מקור. ארבעה מודולים חדשים: `entryRoute` · `deckTiles` · `failureExit` · `worldGate`. ⚠️ **ושתי הפתעות שנמדדו בקריאת הקוד:** ⓐ `unknown` (קריאת `onboarded_at` שנכשלה) חייב לשלוח ל-`/studies` ⛔ ולא ל-`/onboarding` — `/studies` מטפל ב-`exam_date` חסר במפורש ונושא סרגל לשוניות, ו-`/onboarding` הוא מלכודת בלי סרגל ⇒ נפילה לשם היא אותו באג מסיבה אחרת. ⓑ T-125 מחייב **שינוי חוזה**: הספים `MIN_FUNCTION_WORDS`/`MIN_ACTIVE_WORDS` חיים ב-`route.ts` ו⛔ אינם על החוט, בעוד `TabBar.tsx:77` מחזיק העתק עברי שקט של `12` ⇒ הם עולים לתשובה, ו-`docs/api-contract.md` באותו קומיט. |

### C-0210 — הועבר מ-`plan/00-control.md` בידי C-0212 (DEV) ב-2026-08-20T00:51:25Z, ⛔ לא נמחק (`RULES § 0.1.2 ב׳` — הקובץ עמד על 14.5KB מול תקרת 12KB)

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה | תוצר |
|---|---|---|---|---|---|
| C-0210 | **DEV** | CRITIC | 2026-08-19T22:44:28Z | 📐 **טיק ביצוע** על תוכנית `2026-08-19-learner-unblock.md` — **שני חסמי P0 הראשונים נסגרו ואומתו בהרצה טרייה**, ⛔ ולא נטענו. `typecheck` 0 · `check:core` OK · **`1822/1822` ב-117 קבצים** (עלה מ-1792/116) · `build` 0. | **T-122 (D-063 · TD-25):** ההחלטה ירדה ל-`lib/core/entryRoute.ts` כפונקציה טהורה עם 12 בדיקות אמיתיות, ו**שני** המקומות אוכפים אותה — `proxy.ts` **וגם** `app/onboarding/page.tsx` (ניתוב לבדו משאיר את הכתובת הישירה פתוחה). ⚠️ **`unknown` שולח ל-`/studies` ⛔ ולא לטופס:** קריאת `onboarded_at` שנכשלה אינה עדות שהלומד חדש, וזריקה לטופס על סמך כשל היא אותו באג מסיבה אחרת. ⛔ **והקריאה למאגר צרה** — `needsOnboardingState` מגביל אותה לארבעה נתיבים, ולכן איסור התוכנית על שאילתה בכל בקשה נשמר, וזו בדיוק ההכרעה ש-**TD-25 המתין לה מאז C-0073** ⇒ נסגר. **T-123 (D-064):** `lib/core/deckTiles.ts` + מצב ריק ברמת המסך ב-`DeckSelector`. ⚠️ **שתי הכרעות שנמדדו ⛔ ולא הונחו:** ⓐ `[].every(...)` הוא `true` ב-JS ⇒ רשימה ריקה **אינה** «מסך מת», אחרת מסך שטרם בנה אריחים היה מציג מצב ריק על לא כלום ⓑ `data-primary-action` הועבר מאריח `due` ללא תנאי ל-`due && enabled`, אחרת `check:mobile` היה סופר **שתי** פעולות ראשיות במצב הריק. הסטייה המוצהרת (מצב ריק מוצג גם בכשל קריאה) נרשמה ב-`30-architecture § 3.1.61` באותו קומיט. ⚠️ **וממצא מדידה שאינו שלי ו⛔ לא נגעתי בו:** `npm run measure:plan` מדווח `findings: 83 rows, **19 malformed**` — אומת ב-`git stash` שהוא **קיים לפני** הטיק הזה ואינו נובע ממנו. זו מחלקת F-063 → **ל-PM**. `tasks: 130 rows, 0 malformed`. |


<!-- הועבר מ-00-control.md ע"י C-0214 (DEV) ב-2026-08-20T01:52:24Z — תקרת 12KB, RULES § 0.1.2 ב׳. ⛔ לא נמחק. -->
| C-0211 | **DEV** | CRITIC | 2026-08-19T23:46:48Z | 📐 **טיק ביצוע** על תוכנית `2026-08-19-learner-unblock.md` — **חסם P0 השלישי נסגר ואומת בהרצה טרייה**, ⛔ ולא נטען. `typecheck` 0 · `check:core` OK · **`1842/1842` ב-119 קבצים** (עלה מ-1822/117) · `build` 0. | **T-124 (D-065):** «לאן אפשר ללכת מכאן» ירדה ל-`lib/core/failureExit.ts` כטבלה בת שלוש שורות עם 7 בדיקות אמיתיות, ושלושת המסכים קוראים לה — `LevelMapScreen` · `StudyDeckScreen` · `app/error.tsx`. ⚠️ **ההכרעה המרכזית: שני שלישים מהמצבים ⛔ אינם רשאים להציע «נסה שוב»** — `schema_missing` כי מיגרציה שלא רצה ⛔ לא תרוץ מפני שלחצו על כפתור, ו-`session_expired` כי טעינה חוזרת עם סשן מת מחזירה 401 שוב. זה בדיוק הכפתור שענף ברירת המחדל ב-`StudyDeckScreen` הציע להם עד היום (ⓒ במשימה) ⇒ נוסף לו ענף `schema_missing` משלו. ⚠️ **ושלוש סטיות מנוסח התוכנית שנכפו במדידה ⛔ ולא נבחרו, כולן ב-`30-architecture § 3.1.62`:** ⓐ ב-`LevelMapScreen` קישור היציאה ⛔ **אינו** נושא `data-primary-action` — `<DeckSelector>` שם מרונדר **ללא תנאי** ונושא אותו מאז T-123, ולכן הסימון שהתוכנית מכתיבה היה יוצר **שניים** באותו מסך מול דרישת `check:mobile` לאחד (F-027) ⓑ ⛔ בלי `justify-center` בקובץ ההוא — `LevelMapScreen.test.ts` אוסר את המחרוזת בקובץ **כולו** (F-011 · F-016), והחלשת השומר כדי להתאים לקטע הייתה מוחקת בדיקה שנמדדה ⓒ ענף התקלה החולפת ב-`StudyDeckScreen` קיבל **יציאה בנוסף ל«נסה שוב»** — הכרעה 4 של התוכנית עצמה מחייבת זאת וקטע הצעד שלה שכח אותה; היציאה משנית במכוון ו⛔ אינה מסומנת, כי `/study` הוא `FLOW_ROUTE`. **⛔ ואסרציה קיימת תוקנה ⛔ ולא נמחקה:** הדרישה `toContain('/login')` ב-`StudyDeckScreen.test.ts` הייתה מחזירה את הכפילות שהמשימה מחקה — היעד נעול עכשיו בבדיקת היחידה. `docs/api-contract.md` עודכן **באותו קומיט** בשני הצרכנים (תקדים C-0127). |


<!-- הועבר מ-00-control.md ע"י C-0214 (DEV) ב-2026-08-20T01:52:46Z — תקרת 12KB, RULES § 0.1.2 ב׳. ⛔ לא נמחק. -->
| C-0212 | **DEV** | CRITIC | 2026-08-20T00:51:25Z | 📐 **טיק ביצוע** — **חסם P0 הרביעי והאחרון נסגר ואומת בהרצה טרייה**, ⛔ ולא נטען. `typecheck` 0 · `check:core` OK · **`1857/1857` ב-120 קבצים** (עלה מ-1842/119) · `build` 0. ⇒ תוכנית `2026-08-19-learner-unblock.md` **מוצתה**. | **T-125 (D-066):** המשפט של לשונית «העולם» ירד ל-`lib/core/worldGate.ts` עם 9 בדיקות התנהגות. ⚠️ **ההכרעה המרכזית: הספים עולים על החוט ⛔ ואינם מועתקים לצרכן.** עד היום `12` היה כתוב בעברית בתוך מחרוזת ב-`TabBar.tsx:77-78` — העתק שקט של מספר שהשרת מחזיק — ואילו הלשונית נעולה על **שניים** (`functionWords ≥ 100` **וגם** `activeWords ≥ 12`) ⇒ במאגר ריק הלומד קיבל יעד שלעולם ⛔ לא היה פותח אותה. המסלול בונה עכשיו אובייקט `thresholds` **אחד** ומעביר אותו **גם** ל-`isWorldUnlocked` **וגם** בגוף התשובה ⇒ מה שפתח את הדלת הוא מה שמסופר עליה. `MIN_FUNCTION_WORDS = 100` **לא זז** — מדיניות מוצר (D-031). ⚠️ **שתי סטיות מוצהרות מנוסח התוכנית, שתיהן ב-`30-architecture § 3.1.63`:** ⓐ ⛔ בלי `?? 0` ו-`?.` בענף ה-else — `worldStatus` שם כבר מצומצם ל-non-null, ולכן ברירות המחדל שהתוכנית מכתיבה הן קוד מת, וה-`?? 0` בפרט הוא בדיוק המשפט שהתוכנית עצמה מכנה «שגוי» ⓑ `WorldGateThresholds` הוא **שם נרדף** ל-`WorldThresholds` הקיים ⛔ ולא הצהרה שנייה: שני הצרכנים מקבלים את אותו אובייקט, והצהרה נפרדת הייתה מתירה להם להיפרד בשקט. **⛔ ותוספת שנכפתה מקריאה בקוד:** `apiGet` **ממיר טיפוס ו⛔ אינו מאמת** ⇒ גוף `ok:true` חסר-סף היה מדפיס «כשיהיו לך undefined מילים פעילות» — הפגם שהמשימה סוגרת, בדלת אחרת; `Number.isFinite` על ארבעת המספרים לפני ה-setter. **⛔ ושתי אסרציות קיימות תוקנו ⛔ ולא נמחקו** — שתיהן נעלו את המחרוזת בתוך הרכיב, והמחרוזת עברה ל-`lib/core`; מה שהן מגינות עליו (F-039) נשמר. **שלוש מוטציות על הרכיב — שלוש נפילות בשם, ⛔ אף אחת לא שרדה.** `docs/api-contract.md` עודכן **באותו קומיט**. ⚠️ **וממצא שאינו שלי, נמדד בבידוד ⛔ ולא הוסק: F-082 🟠 → DEV.** `check:mobile` נכשל ב-**6** שערים בראש `dev` מול **3** ב-`2a6ef3f` ⇒ שלושת החדשים נולדו ב-`7cf994f` (C-0211 · T-124): `/study` בשלושת הרוחבים — `action bar does not cover the licence link`. ⛔ כלל הריפוד תקין; העמוד **התקצר**. ⛔ אינו חוסם ו⛔ אינו בארבע פקודות האימות. |
