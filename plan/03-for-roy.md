<!--
plan/03-for-roy.md — מקום אחד לכל מה שנדרש מרוי.
כותבים: כל ארבעת הסוכנים. סוגר: מי שפתח, אחרי שראה שבוצע.
⛔ סוכן אינו סוגר פריט של סוכן אחר.
תקרה: 20 שורות פתוחות. עבר? הישן עובר ל-plan/archive/.
-->

# נדרש מרוי

⛔ **הכלל החשוב ביותר: כתיבה לכאן אינה עצירה.**
סוכן שצריך משהו מרוי כותב שורה כאן **וממשיך לעבוד** על משימה אחרת.
רוי ניגש לכאן אחת לכמה ימים — לופ שממתין לו הוא לופ מבוזבז.

## פתוח

| # | מי ביקש | מתי | מה נדרש מרוי | למה זה חשוב | חוסם? |
|---|---|---|---|---|---|
| 1 | רוי (הקמה) | 2026-08-12 | לאשר או להפוך את הכרעת D-025 — רשומת `!` מ-Hebrew Wordnet מוצגת כ-low בהיעדר חלופה, ולא מושמטת | תואם ל-D-024. הפיכה משנה את מדיניות התרגום לכל המאגר | לא |
| 2 | רוי (הקמה) | 2026-08-12 | לבחור שם למוצר. מועמדים: קול / לשון / דור | השם נכנס לחוקת העיצוב ולכל מסך. שינוי מאוחר יקר | לא |
| 6 | PM (C-0048) | 2026-08-12 | **להכריע מהו תקציב הקרדיטים החודשי בפועל, ולתקן את `RULES.md` בהתאם.** דיווחת ב-inbox "1000 קרדיטים"; `RULES § 0.1.1 א׳` קובע **300 לחודש ⇐ ~20 פריסות**. `RULES.md` אינו קובץ של סוכן — רק אתה מתקן אותו | כל כלכלת הקידום נגזרת מהמספר: אם 1000 נכון, התקציב הוא ~66 פריסות והלופ חונק את עצמו ללא צורך; אם 300 נכון, החשבון עלול להישרף שוב | לא |
| 4 | רוי (הקמה) | 2026-08-12 | להריץ את המיגרציה `0005_review_state.sql` ב-Supabase | TD-19: עד שתורץ, `POST /api/review` מחזיר 503 בפרודקשן | לא |
| 5 | DEV (C-0047) | 2026-08-12 | **שני קבצים ל-`data/`, והם היחידים שחסרים כדי שמספר הדיוק של T-018 יהיה קיים.** ⓐ `wordnet-sense-index.tsv` — 5 עמודות מופרדות טאב: `lemma⇥pos⇥synset_id⇥sense_number⇥tag_count` (מקור: WordNet 3.1 `index.sense` + `cntlist`, המרה מקומית). ⓑ `h1-hebrew-wordnet-synsets.tsv` — 3 עמודות: `synset_id⇥english⇥hebrew`, כלומר אותו Hebrew Wordnet **עם** מזהי synset, וסימוני `GAP`/`!` כלשונם. הפורמט המלא נוסף ל-`data/README.md` | **נמדד ולא משוער:** הקובץ שיש בריפו הוא 2 עמודות בלבד (17,564 שורות, כולן 2 עמודות). בלי ⓐ אין לכלל 4 של 1.7.1 אות שני, ובלי ⓑ כלל 3 יורד מ-synset ל-lemma. הקוד ייבנה ויוכח מול פיקסצ׳רים בלי להמתין; המספר עצמו יופיע ביום שהקבצים ינחתו | לא — הלופ ממשיך |
| 8 | DEV (C-0057) | 2026-08-12 | **להכריע (יחד עם ה-PM) איזו משתי העמודות שורדת: `words.is_function_word` או `words.lexical_class`.** נמדד: `is_function_word` קיים מ-`0002_content_bank.sql:53` והוא `boolean not null default false` — כלומר **אינו יכול לומר "לא ידוע"**, וכל שורה שאיש לא סיווג נקראת "מילת אוצר". `lexical_class text null` (נחת ב-`0006`) מבטא את שלוש התשובות. שתי אפשרויות: ⓐ למחוק את `is_function_word` ולהשאיר את `lexical_class` כמקור יחיד; ⓑ להשאיר את הבוליאני כקנוני ולא להשתמש ב-`lexical_class` | ⛔ **מחיקת עמודה הרסנית ואינה סמכות Dev.** עד ההכרעה, המגבלה `words_lexical_class_agrees` ב-`0006` אוסרת על השתיים להחזיק שתי תשובות בו-זמנית — הדאטהבייס תקין, אבל שני שדות מתארים עובדה אחת, וזה חוב שגדל עם כל צרכן חדש | לא — הלופ ממשיך |
| 7 | CONTENT (C-0053) | 2026-08-12 | **לא נדרשת פעולה — זו הודעה שמשנה את פריט 5 ואת T-043.** `newgeneralservicelist.com` **ענה בריצה הזו** (‏`/s/NGSL_12_stats.csv` ⟵ 302 ⟵ static1.squarespace.com). ‏TD-17 ("הדומיין חסום ב-egress") נכון היה ב-08/07 ואינו נכון היום. ⚠️ **הסתירה שנשארה פתוחה:** הקובץ הקנוני מדווח **2,800 שורות נתונים**, בעוד `15-syllabus-digest` נועל **2,809 בדיוק** ו-T-012 נכשלת על כל סטייה — והמראה ב-GitHub שממנו נבנו חמש האצוות הקודמות דיווח 2,818. שלושה מספרים, שלושה מקורות. | ‏NGSL הוא היחיד מארבעת הקבצים החסרים שסוכן יכול להביא בעצמו, ולכן אפשר להסיר אותו מרשימת ההעלאות הידניות. **אך לא לפני שההפרש 2,800/2,809/2,818 מוכרע** — בחירת המילים באצווה הזו (דירוגים 195–260) אינה מושפעת ממנו, אבל `allowed-words` וכל טענת כיסוי של R-005 כן. | לא |
| 9 | DEV (C-0060) | 2026-08-12 | **להריץ את `supabase/seed/0001_content_batches.sql` ב-Supabase — זהו הצעד היחיד שמעביר 343 משמעויות מהריפו אל הלומד.** הקובץ נוצר ב-C-0060, נבדק ב-9 בדיקות, וכל 343 השורות עברו את השער פעם שנייה (0 נדחו). הוא מוחל בדיוק כמו מיגרציה, בטרנזקציה אחת, וניתן להרצה חוזרת. ⚠️ **לפניו יש להריץ את 0003 · 0005 · 0006 · 0007** שטרם הורצו (TD-19 · TD-20 · TD-22). ⛔ **עד שתריץ, בדאטהבייס יש 0 שורות תוכן** — האתר החי אינו מלמד דבר. | TD-24. אינו חוסם את הלופ: הפיתוח ממשיך מול הקובץ | לא |
| 10 | DEV (C-0063) | 2026-08-12 | **לשלוח לריפו את קובץ ה-`.apkg` שהעלית (F-019), תחת `data/decks/`.** נמדד היום: חיפוש `apkg`/`.anki2` בכל הריפו מחזיר **0 תוצאות** — הקובץ מעולם לא נכנס לגיט, רק תואר בממצא | ‏T-044 (פייפליין `.apkg`) נבנה לפי התוכנית `2026-08-12-outside-sources.md` מול פיקסצ׳רים שמשחזרים את ה**צורה** שתועדה ב-F-019 (4 שדות · עברית בשדה המיון · `[sound:*.mp3]` · תעתיק), ו⛔לא מול 101 השורות האמיתיות. בלי הקובץ אי אפשר לסגור את F-019, ואסור שייכתב בשום מקום שהחפיסה "נקראה". ⚠️ **התוכן שלה לא ייקלט בכל מקרה** — `apkgIngestDecision` מסרב לה על שני נימוקים בלתי תלויים: כיוון הפוך (עברית לדוברי אנגלית) והיעדר רישיון מסחרי גלוי מ-TeachMeHebrew.com (R-010). הערך היחיד של הקובץ הוא כמקרה מבחן אמיתי לפרסר | לא |

## נסגר

| # | מי ביקש | מתי נסגר | מה בוצע |
|---|---|---|---|
| — | רוי | 2026-08-11 | T-043 חלקית: `h1-hebrew-wordnet.tsv` · `cefrj-vocabulary-profile-1.5.csv` · `octanove-vocabulary-profile-c1c2-1.0.csv` הועלו ל-`data/`. נותרים: NGSL · H2 · H3 · H4 |
| 3 | PM (C-0046) | 2026-08-12 | ✅ **רוי אישר ידנית ב-`02-inbox.md`:** `Branch deploys = None` · `Deploy Previews = Off` · Production = `main`. נרשם ב-R-015. ⚠️ זהו אישור אנושי לנקודת זמן, לא אימות אוטומטי — הוא פג בביקורת השבועית הבאה, ו-T-046 (טוקן) עדיין הדרך היחידה להפוך אותו למדיד |
