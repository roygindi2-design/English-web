<!-- חלק מ-plan/. ⟦OWNER: DEV כותב · PM מתקן ומסמן · CRITIC מאמת⟧ -->

## 26. משוב על תוכניות — הנתיב מ-DEV אל ה-PM  ⟦נפתח C-0287, הכרעת רוי 24/08⟧

**מה רוי ביקש:** *«אני רוצה שהסוכן Dev יקבל את התוכניות בצורה מסוימת, ואם יש דברים
חסרים שיעדכן את ה-PM שיכניס את הפרטים החסרים בתוכניות הבאות, ככה הם ילמדו לעבוד ביחד.»*

**למה זה קובץ ולא הערה בקומיט:** עד היום הכיוון `PM → DEV` היה מנוסח (משימה ב-`50-tasks`,
תוכנית ב-`docs/superpowers/plans/`), והכיוון ההפוך — **לא היה קיים בכלל**. ‏Dev שקיבל
תוכנית חסרה עשה אחד משניים: המציא את החסר (וזה בדיוק השער שדרכו זחילת תחולה חוזרת,
`RULES § 0.16`), או פתח פריט לרוי. שניהם שגויים. זה הנתיב השלישי.

⛔ **הכלל שהופך את זה למנגנון ולא לתלונה: המשוב הוא מדידה, לא דעת.**
```
npm run check:plan docs/superpowers/plans/<file>.md
```
הפקודה מודדת את התוכנית מול **שבעה עד תשעה** רכיבים ומדפיסה **שורה מוכנה להדבקה** כאן.
❌ «התוכנית לא הייתה ברורה» — ⛔ אינו ניתן לפעולה, וה-PM אינו יכול להשתפר מולו.
✅ «`interfaces` חסר, ו-3 מתוך 14 צעדים בלי כתובת קובץ» — פגם עם כתובת.

⚠️ **הוא מודד צורה, ⛔ לעולם לא נכונות.** תוכנית יכולה לשאת את כל תשעת הרכיבים ולהיות
שגויה לגמרי; לזה יש Critic ובדיקות. מה שהשער תופס הוא המחלקה שבה **Dev נאלץ להמציא
משהו שה-PM היה אמור להכריע**.

### הרכיבים הנמדדים

| מפתח | מה נדרש | למה |
|---|---|---|
| `tasks` | מזהי `T-XXX` שהתוכנית מכסה | בלי מזהה אין מה לסמן כשנסגר, והתוכנית הופכת יתומה |
| `files` | מדור מבנה קבצים | ‏Dev יודע מראש מה נוצר ומה נערך, ⛔ לא מגלה תוך כדי |
| `interfaces` | חתימות מדויקות | חתימה שלא נכתבה היא חתימה ש-Dev ימציא וה-Critic יפסול |
| `tests` | **קוד** בדיקה, ⛔ לא תיאור | `test-driven-development` מחייב בדיקה נכשלת לפני מימוש |
| `steps` | ‏3+ צעדי `- [ ]` | תוכנית בלי צעדים אינה ניתנת לחידוש אחרי הפסקה |
| `addressed` | כל צעד נוקב בקובץ או בפקודה | צעד בלי כתובת הוא צעד ש-Dev מנחש |
| `verify` | צעד סיום שמריץ את השער | `RULES § 0.1.1 ח׳` — השער רץ אחרון |
| `render` ⚠️ | שם קובץ הרנדר ב-`docs/design/` | תוכנית מסך בלבד. `36 § 14.4` — הפריסה מחייבת |
| `finish` ⚠️ | «הגימור מהחוקה, ⛔ לא מהצילום» | תוכנית מסך בלבד. בלי המשפט הזה Dev מעתיק פגם רנדר |

⚠️ שני האחרונים נוספים **רק** כשהתוכנית נוגעת ב-UI, ונדרשים **רק מ-23/08 ואילך** —
`36 § 14.4` לא היה קיים לפני כן, ולכן תוכנית ישנה שחסרה אותם ⛔ אינה פגם.

### הפרוטוקול

```
DEV   → מריץ check:plan על התוכנית שהוא עומד לבצע, ⛔ לפני שהוא כותב שורת קוד.
        חסר משהו? ⓐ מדביק את השורה שהפקודה הדפיסה לטבלה למטה
                  ⓑ ⛔ ממשיך לבצע — ⛔ אינו נעצר ואינו ממתין ל-PM
                  ⓒ מה שהוא נאלץ להשלים בעצמו נרשם כשורת מרחב (RULES § 0.16)
PM    → בטיק הבא: מתקן את התוכנית אם היא עדיין בעבודה, ומסמן ✅.
        ⛔ החובה האמיתית: שהתוכנית הבאה לא תחזור על אותו חסר.
CRITIC→ שורה שחוזרת פעמיים מאותו סוג = ממצא 🟡 נגד ה-PM, ⛔ לא נגד התוכנית.
```

⛔ **המשוב אינו חוסם.** תוכנית שאינה מושלמת מבוצעת בכל זאת — עצירה תהפוך את ה-PM
לתור פעם שלישית, וזו בדיוק הבעיה ש-D-110 פתרה.

### הטבלה

| מחזור | תוכנית | מה חסר | ההשלכה | סטטוס |
|---|---|---|---|---|
| C-0287 | `2026-08-21-content-headroom-and-cross-validation.md` | `files` · `verify` | ‏4/7 — התוכנית הפעילה ביותר בתור התוכן ואין בה מדור מבנה קבצים | ✅ **נסגרה C-0302 (PM).** ⛔ אין מה לקדם: `files` הוא כבר **§ A1 שורה 4** ו-`verify` הוא **§ A1 שורה 3**, ושניהם נכתבו לפני שהשורה הזאת נפתחה. ⇒ הלקח קיים, והשורה הייתה כפילות. |
| C-0299 | `2026-08-25-story-slice-a.md` | ⛔ **⛔ לא חוסר צורה — 9/9 בשער.** קוד ה-`storyPick.ts` שהתוכנית מכתיבה ⛔ אינו עובר `npm run typecheck`: `ordered[at]` תחת `noUncheckedIndexedAccess` הוא `StoryCandidate \| undefined` ⇒ שלוש שגיאות TS2322/TS18048. | ה-Dev נאלץ לשנות קוד שהתוכנית מכתיבה מילה במילה — בדיוק המחלקה שהקובץ הזה קיים בשבילה. ⇒ **בקשה ל-PM: קוד בתוכנית עובר `tsc` לפני שהוא נכתב לתוכנית.** תוקן בטיק ‏(`if (candidate === undefined) continue`), ⛔ והביצוע ⛔ לא נעצר (`RULES § 0.5ג`). ⚠️ **ופער שני, קטן ומעשי:** בלוק `Interfaces` מגדיר `ContextTapPayload = { wordId }` (camelCase), וצעד 10 של משימה 4 מתאר את החוט כ-`{ "word_id": ... }`. נבחר camelCase — זו המוסכמה של `apiPatch('/api/arcade/collected', { wordId, ... })` — ונרשם ב-`docs/api-contract.md`. | ✅ **נסגרה C-0317 (PM) — הדפוס קודם ל-`27-pm-lessons § A1` שורה 9.** ‏C-0299 + C-0314 הם **שני מופעים** של אותו פער — בלוק `Interfaces` שלא הורץ — וזה בדיוק סף הקידום ב-§ A2. ⇒ הכלל הוא עכשיו שורה שנקראת בכל טיק, ⛔ ולא בקשה בטבלה שאיש אינו קורא. **וכובד בפועל בטיק הזה:** בלוקי ה-`Interfaces` של `2026-08-26-cards-filter-slice-a.md` עברו `tsc --strict --noUncheckedIndexedAccess` ⇒ **exit 0** לפני שנכתבו. |
| C-0314 | `2026-08-26-nav-ring-slice-a.md` | ⛔ **⛔ לא חוסר צורה — 9/9 בשער.** קוד ה-`ringScreen` שבבלוק `Interfaces` מעביר את ארבעת הקלטים **כמות שהם** ובודק `anyOpen` בלבד. שתי תוצאות נמדדו על הקוד עצמו: ⓐ קלט `unknown` **שורד אל צומת** ברגע שצומת אחר פתוח ⇒ המסך נאלץ לצייר לו «—» — המחרוזת ש-T-148ⓑ אוסרת **בשם**, ו«שגיאה **וגם** תוכן» ש-T-146ⓑ אוסרת. ⓑ ‏`כתיבה חופשית` ו`אוצר מילים` הם `open` **קבוע** (‏`worldApps.ts`) ⇒ `anyOpen` **אמת תמיד** ⇒ ענף `!anyOpen` הוא **קוד מת ביום שנולד**, והמצב הריק שהתוכנית מתיימרת לספק ⛔ אינו נגיש — מחלקת F-074. | התוכנית מצהירה ש-T-146 ו-T-148 «נסגרות במבנה ⛔ ולא כטלאי», והקוד שהיא מכתיבה ⛔ אינו סוגר אף אחת מהן. ⇒ **בקשה ל-PM: כשתוכנית טוענת שהיא סוגרת שורה פתוחה, הקוד שלה נבדק מול **תרחיש הכשל** של אותה שורה, ⛔ ולא רק מול הטיפוסים.** תוקן בטיק — ענף שלישי ב-`ringScreen`: ⛔ ולו קלט חי אחד `unknown` ⇒ המסך כולו ריק, ו-`unknown` ⛔ אינו נגיש על צומת של טבעת מצוירת. ⛔ הביצוע ⛔ לא נעצר (`RULES § 0.5ג`). | ✅ **נסגרה C-0317 (PM) — הדפוס קודם ל-`27-pm-lessons § A1` שורה 9.** ‏C-0299 + C-0314 הם **שני מופעים** של אותו פער — בלוק `Interfaces` שלא הורץ — וזה בדיוק סף הקידום ב-§ A2. ⇒ הכלל הוא עכשיו שורה שנקראת בכל טיק, ⛔ ולא בקשה בטבלה שאיש אינו קורא. **וכובד בפועל בטיק הזה:** בלוקי ה-`Interfaces` של `2026-08-26-cards-filter-slice-a.md` עברו `tsc --strict --noUncheckedIndexedAccess` ⇒ **exit 0** לפני שנכתבו. |
| C-0315 | `2026-08-26-nav-ring-slice-a.md` | בלוק ה-`Interfaces` נושא `ringScreen(inputs, retryHref)` — **שני ארגומנטים**, ו⛔ אין בו ולו רמז לקוד הכשל. התוכנית ⛔ אינה טוענת לסגור את T-146ⓒ, ולכן זה ⛔ **אינו** מופע שלישי של הדפוס שמעליו — אבל השורה שהתוכנית **כן** נוקבת בה (T-146) ⛔ אינה יכולה להיסגר בחתימה הזאת. | החתימה בתוכנית ⛔ אינה מה שיושב בריפו אחרי C-0315 (`ringScreen(inputs, retryHref, code)`), ו-DEV שיחזור לתוכנית ויעתיק ממנה יפיל `typecheck`. ⇒ **בקשה ל-PM: לרענן את הבלוק, או לסמן אותו במפורש כ«תמונת מצב של הפרוסה» ⛔ ולא כחוזה חי.** ⛔ הביצוע ⛔ לא נעצר (`RULES § 0.5ג`) — הרישום הזה **אחרי** הקומיט, ⛔ ולא לפניו. | ✅ **נסגרה C-0317 (PM) — הדפוס קודם ל-`27-pm-lessons § A1` שורה 9.** ‏C-0299 + C-0314 הם **שני מופעים** של אותו פער — בלוק `Interfaces` שלא הורץ — וזה בדיוק סף הקידום ב-§ A2. ⇒ הכלל הוא עכשיו שורה שנקראת בכל טיק, ⛔ ולא בקשה בטבלה שאיש אינו קורא. **וכובד בפועל בטיק הזה:** בלוקי ה-`Interfaces` של `2026-08-26-cards-filter-slice-a.md` עברו `tsc --strict --noUncheckedIndexedAccess` ⇒ **exit 0** לפני שנכתבו. |

| C-0318 | `2026-08-26-cards-filter-slice-a.md` | ⛔ **⛔ לא חוסר צורה — 9/9 בשער.** שני נתיבי קבצים בתוכנית ⛔ אינם קיימים בריפו. ⓐ מדור `File Structure` ובדיקת המוטציה של T-211 נוקבים ב-`lib/proxy.ts`; הקובץ יושב ב-**שורש הריפו** כ-`proxy.ts` (‏Next 16 מחייב זאת — ראה ההערה בראש הקובץ). ⓑ שלושת צעדי T-157 מטילים את הגרירה על `components/CardDeck.tsx`; מטפלי המחווה יושבים ב-`components/Flashcard.tsx` (`onPointerDown`/`onPointerUp` · `resolveSwipe`), ו-`CardDeck.tsx` ⛔ אינו מכיל ולו מטפל אחד. | בדיקת המוטציה כפי שנכתבה (`readFileSync('lib/proxy.ts')`) **זורקת ENOENT** ⛔ ואינה נכשלת בשם — כלומר הבדיקה ששומרת על `PROTECTED_SCREENS` הייתה מדווחת כשל על הסיבה הלא נכונה. ⇒ **בקשה ל-PM: כל נתיב קובץ בתוכנית עובר `test -f` לפני שהוא נכתב לתוכנית** — אותה מחלקה בדיוק של «בלוק `Interfaces` שלא הורץ» (‏§ A1 שורה 9), פשוט על נתיבים במקום על טיפוסים. תוקן בטיק: הבדיקה סורקת `proxy.ts` (T-211), והגרירה מומשה ב-`Flashcard.tsx` (T-157) — שניהם נמדדו בדפדפן ועובדים. ⛔ הביצוע ⛔ לא נעצר (`RULES § 0.5ג`). | ✅ **נסגר C-0324 (PM).** הפער הועלה ל-`27-pm-lessons § A1` כ**שורה 10**: כל נתיב בתוכנית עובר `test -f` לפני שהוא נכתב. הופעל מיד — 16 הנתיבים של `2026-08-27-arena-slice-a-battle-clock.md` נבדקו בטיק, וטבלת § 4 שם מסמנת כל אחד `EXISTS`/`MISSING` ו**נוצר**/**נערך**. |
| C-0318 | `2026-08-26-cards-filter-slice-a.md` | 🔴 **פער מהותי, ⛔ ולא נתיב:** התוכנית מגדירה את `deck=level` מקצה לקצה **בצד הקריאה בלבד**. ⛔ אין בה ולו צעד אחד על **הכתיבה** — ומה קורה כשהלומד מדרג מילה מ«סינון מילים» ⛔ אינו מוגדר באף החלטה חתומה. נמדד: `app/api/practice/route.ts:59` עונה **404** על מילה בלי שורת `word_progress`, **בכוונה מוצהרת** («מילה שמעולם לא נענתה ⛔ אינה יכולה להיות בחפיסת תרגול»), ואילו חפיסת הרמה היא **בדיוק** אוסף המילים שהלומד טרם פגש: A1 = 305 מילים, ולומד חדש נושא ~0 שורות. | ⛔ **בלי הכרעה, הכפתור הראשי החדש מוביל לחפיסה שנכשלת על כמעט כל מילה.** ‏T-155ⓒ אוסרת `/api/review` (כתיבת SM-2), ו-`/api/practice` ⛔ אינו מכניס שורה — ⇒ ⛔ **אין נתיב כתיבה קביל.** ⇒ **בקשה ל-PM: החלטה על נתיב הכתיבה של «סינון מילים»** (‏`self_marked_known` כמו `levels/scan`? ‏`attempts` עם שורה חדשה? ‏`next_review_at` נשאר NULL?). ⛔ **DEV ⛔ אינו ממציא אותה** — § 0.16 שולח «מכניקת לימוד» ו«כל דבר שנוגע ב-`word_progress`» ל-PM. נרשם כ-**F-140 🔴**, והאריח נמסר **נעול עם המספר** (‏D-046 · § 4.2ו) ⛔ ולא כ-CTA שבור. | ⬜ **⛔ ⛔ אינה נסגרת C-0324, ⛔ ובכוונה מוצהרת — ההכרעה שהיא מבקשת ⛔ עדיין ⛔ אינה כתובה.** זו ⛔ אינה כשל **צורה** של תוכנית אלא **בקשת הכרעה** (F-140, חומרה CRITICAL — מסלול הכתיבה של «סינון מילים»), ולכן ⛔ אין להעלות אותה ל-`27-pm-lessons § A1` שתקרתה 12 — היא ⛔ אינה לקח על **איך** כותבים תוכנית. ⛔ **ו⛔ אין לסמן אותה ✅ רק כדי לרוקן את התור:** `cards` ⛔ אינה `ACTIVE_WORKSTREAM` היום (`arena` הוא), ו-`PM.md` STEP 2 אוסר עלי לפתוח עבודה מחוצה לה. ⇒ **נשארת פתוחה → PM, ותוכרע כשהמוקד יחזור ל-`cards`.** ⚠️ ⛔ **אינה חוסמת לומד:** האריח נמסר **נעול עם המספר** (D-046) ⛔ ולא כ-CTA שבור. |


### מדידת בסיס — 46 התוכניות, 24/08

⛔ **היסטוריה, ⛔ לא רשימת מטלות.** נמדד בהרצת `check:plan` על כל 46:

| ציון | כמה תוכניות |
|---|---|
| 7/9 | 1 |
| 6/9 · 6/7 | 9 |
| 5/9 · 5/7 | 21 |
| 4/9 · 4/7 | 14 |
| 3/9 | 1 |

**הרכיבים החסרים הנפוצים:** `addressed` · `verify` · `files`. ⚠️ `render`/`finish` חסרים
כמעט בכולן מסיבה נכונה — הן נכתבו **לפני** ש-`36 § 14.4` נכתב.
⇒ **הקו הזה הוא הבסיס. השאלה היחידה שמעניינת: האם התוכנית הבאה טובה מ-5/9.**
