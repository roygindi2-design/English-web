<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0035                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: DEV                   # PM | DEV | CRITIC | HUMAN — **C-0035 (DEV) הוא טיק תכנון בלבד: אפס קוד, אפס תלויות, `docs/superpowers/plans/2026-08-07-review-engine.md` נכתב ומכסה שתי משימות — T-005 ו-T-006.** התוכנית של T-029 מוצתה ב-C-0034, ולכן הטיק הזה פתח את השדרה הבאה: 7.1 (`scheduler.ts`) · צבירת ההתקדמות וטלמטריית D-010 (`progress.ts`) · 7.7 (`levelGate.ts`) · ואחריהן שכבת התמדה אחת עם אכיפה בצד השרת (`0005_review_state.sql` + `POST /api/review`). ארבע משימות, כל אחת עם קוד בדיקה אמיתי, מוטציות מוגדרות מראש ופקודת אימות. **ACTIVE_TASK_ID=T-005 והטיק הבא של DEV הוא ביצוע משימה 1.** ⚠️ **חוב סקירת העומק לא זז ואינו שייך לטיק הזה: C-0029 (T-015) · C-0031 · C-0032 · C-0033 · C-0034 (T-029/משימות 1–4).** ⚠️ **T-020 של ה-PM נשארת דחופה** (סוגרת F-021, משחררת T-018). חסמים אנושיים: T-043 (קובצי המקור) · P-001 (תוכן ל-T-034). מצב C-0034 המלא: היומן למטה.
STATE: PLANNING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-005             # **תוכנית `2026-08-07-review-engine.md` נכתבה ב-C-0035 ומכסה T-005 + T-006.** הטיק הבא של DEV מבצע את משימה 1 (‏`lib/core/scheduler.ts`, מנוע 7.1). T-029 הושלמה (C-0031..C-0034) וממתינה לסקירה
CRITIC_ROUNDS_ON_TASK: 0          # T-015 · T-029/משימות 1–4 טרם נסקרו
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-07T16:46:26Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה. F-021 היא הכרעת PM, לא הכרעת רוי, ואינה חוסמת את המדידה (המדיניות היא נתון ולא לוגיקה).
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."

# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: DEV                   # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: "2026-08-07T17:36:29Z"
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "6806e68"  # נסקר C-0031: מהיר. ארבע פקודות עברו טרי. main==dev אחרי קידום #4 — הכל אצל המשתמש

# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T13:09:15Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 4          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: **F-021 🟠 (חדש C-0020 — נגד ה-PM: 1.7.1 חסר מהתמצית + סתירת כללי סינון; חוסמת את T-018)** · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת, T-034) · F-008 🟡 (חסום ב-T-025) · R-010 (אסור להעתיק פריטים ממאל"ו). **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0035 | DEV | DEV | 16:46Z | **טיק תכנון — אפס קוד, אפס תלויות, אפס נגיעה ב-`app/` או ב-`lib/`.** התוכנית של T-029 מוצתה ב-C-0034, ולכן הטיק הזה כתב את `docs/superpowers/plans/2026-08-07-review-engine.md` — **תוכנית אחת שמכסה שתי משימות תור, T-005 ו-T-006**, בארבע משימות מימוש: ⓐ `lib/core/scheduler.ts` (מנוע 7.1) · ⓑ `lib/core/progress.ts` (צבירת ההתקדמות + שני שדות D-010) · ⓒ `lib/core/levelGate.ts` (מנוע 7.7) · ⓓ `0005_review_state.sql` + `POST /api/review` (התמדה ואכיפה בצד השרת, כפי ש-7.7 דורש). **ארבע החלטות הוכרעו בתכנון ולא יישארו לזמן הכתיבה, כל אחת עם נימוק:** ⓐ **מיפוי הדירוג הבינארי ל-`q` של SM-2** — `good→4` ולא 5 (כפתור בינארי אינו יכול לטעון "היזכרות מושלמת בלי היסוס"), `again→2` ולא 0 (עונש ה-EF ב-q=0 הוא ‎−0.80 מול ‎−0.32 ב-q=2, ו"לא ידעתי" בחשיפה ראשונה אינו ראיה לקושי מקסימלי; שניהם <3 ולכן מאפסים את הרצף באופן זהה). ⓑ **מצב הטריאז' נגזר מהנוסחה ולא מקבוע מומצא:** `usableHorizon = daysUntilExam − clamp(0.10×daysUntilExam,1,14)`, והרוחב עצמו הוא `triageMinUsableDays` — **פרמטר מוצר חובה בלי ברירת מחדל**, בדיוק הדפוס של `promoteAfterConsecutiveCorrect` ב-`flashcard.ts`. מספר שאין לו מקור לא ייכתב בקוד וייוצג כאילו נמדד. ⓒ **מבחן שעבר אינו טריאז' אלא היעדר מבחן** — בלי הכלל הזה לומד שהמבחן שלו מאחוריו נתקע בטריאז' לנצח וכל מילה חוזרת כל יום. ⓓ **בעקיפת השער `exposure_pct` ממשיך לדווח את האמת** — אחוז שמזייף 100 כדי להצדיק את הפתיחה מפסיק להיות מדד. **שני לקחים מוכרים נצרבו בתוכנית מראש ולא ייגלו שוב בביצוע:** בדיקת המיגרציה מפשיטה הערות שורה לפני הסריקה, ושתי מגבלות ה-`check` מוצהרות **בשם** בתוך `do $$` — ‏`add column ... check` מדולג כולו כשהעמודה קיימת (שני הלקחים של C-0032). בנוסף, 7.1 דורש שההיוריסטיקה "מילים קשות מקבלות דחיסה אגרסיבית יותר" תסומן בקוד; התוכנית מוסיפה **בדיקה סורקת-מקור** שנופלת אם `// HEURISTIC: unstudied` נעלם — דרישת מפרט שאחרת אינה נאכפת בשום מקום. התוכנית כוללת ~60 בדיקות בקוד מלא, שמונה מוטציות מוגדרות מראש עם הכשל הצפוי המדויק, ומדור מפורש של **מה שהיא אינה מכסה** (אין מסך · אין קריאת תור · המיגרציה לא תרוץ מול Supabase חי בלופ). ⛔ בטיק הזה לא הורצה שום פקודת אימות, ולא נטענה שום טענת הצלחה — אין קוד לאמת. | `docs/superpowers/plans/2026-08-07-review-engine.md` (חדש) · `plan/00-control.md` · `plan/50-tasks.md` |
| C-0034 | DEV | CRITIC | 15:46Z | **טיק ביצוע — משימה 4 מתוך 4, האחרונה בתוכנית `2026-08-07-onboarding-goal.md`. T-029 הושלמה · TD-13 נסגר.** `app/dev/onboarding/` (page + layout, `robots: noindex`) נוסף ל-`ROUTES` של `check:mobile` לפי דפוס `/dev/identity`, ועמו ארבע טענות שנקראות מה-DOM החי: קיום `[data-daily-minutes]` · שלוש אפשרויות · **ברירת המחדל `5` נמדדת בפיקסלים** · כל שורת אפשרות ≥44px · שדה הציון כקלט לטיני מספרי. **ההרצה הראשונה חשפה פגם אמיתי — ולא בטופס אלא בהארנס עצמו.** סריקת יעדי המגע הגנרית נפלה שלוש פעמים בכל רוחב: `too small: input"" 20x20` ×3. `superpowers:systematic-debugging` לפני כל תיקון: השורש אינו שהטופס שגוי, אלא ש**הסריקה מדדה את הפקד ולא את אזור ההפעלה**. הדפדפן מפעיל `input[type=radio]` מכל מקום ב-`<label>` העוטף אותו — זו התנהגות הדפדפן, לא מימוש שלנו — ולכן נקודה בת 20px בתוך שורה בת 44px **היא** יעד מגע של 44px. ⛔ **הכיוון שנפסל: לנפח את הרדיו ל-44×44.** זה היה מעוות עיצוב תקין כדי לרצות מדידה שגויה, והשאיר את אותה מדידה שגויה בעינה לכל טופס עתידי. `tapRect()` מציב את התווית העוטפת (או `label[for]`) **אך ורק** ל-`radio`/`checkbox`. ההגבלה מכוונת: אצל שדה טקסט התווית מכילה גם את טקסט העזרה, והצבתה הייתה מנפחת את היעד הנמדד ומחלישה בשקט את הסריקה **בכל טופס במוצר** — הקלה שמכסה יותר ממה שנועדה לה היא בדיוק צורת F-007. **שלוש מוטציות שרצו, לא שלוש טענות:** ⓐ `DEFAULT_DAILY_MINUTES`→20 הפילה **גם** את בדיקת היחידה (`expected 20 to be 5`) **וגם** את הפיקסלים (`the modest goal is preselected — preselected value was "20"` בשלושת הרוחבים) — שני מחסומים עצמאיים על אותה החלטת מוצר, כפי שהתוכנית דרשה. ⓑ כיווץ שורת האפשרות מתחת ל-44px עדיין מפיל את הסריקה הגנרית (`input"" (its label) 335x22`) — **הראיה שההקלה לא השתיקה את המחסום אלא תיקנה את מה שהוא מודד**. ⓒ ביטול ההצבה בקוד מפיל את בדיקת המקור החדשה. 3 בדיקות ב-`scripts/verify-mobile.test.ts` סורקות את הקוד **אחרי הפשטת הערות שורה** — לקח ישיר מ-C-0032: על טקסט גולמי, ההערה שמסבירה כלל מספקת את הטענה שאמורה להוכיח שהכלל ממומש, ואחת מהן אף אוסרת במפורש להרחיב את ההצבה ל-`text`/`email`/`date`/`number`/`password`. **חמש פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 383 בדיקות ✅ (מ-378, 24 קבצים) · build ✅ `Compiled successfully in 985ms` · check:mobile ✅ `all mobile/PWA guarantees hold (390 checks, widths 320/375/414px)` — מ-351.** ⚠️ **מה שעדיין אינו נמדד ונרשם ב-TD-13 כשארית מפורשת:** המעטפת מוגנת-הסשן של `/onboarding` עצמו. ההארנס רץ בלי משתני סביבה של Supabase, `GET /onboarding` מחזיר `307 → /login?expired=1`, ולכן הפיקסטורה מודדת את **הטופס** ולא את **השומר**. השורה נסגרה עם שארית ולא נמחקה. ⛔ אפס תלויות חדשות · ⛔ אין שינוי בנקודות קצה ולכן `docs/api-contract.md` לא נגע. **התוכנית מוצתה — הטיק הבא של DEV הוא טיק תכנון.** | `app/dev/onboarding/page.tsx` · `app/dev/onboarding/layout.tsx` · `scripts/verify-mobile.mjs` · `scripts/verify-mobile.test.ts` (+3) · `plan/30-architecture.md` (TD-13) · `plan/50-tasks.md` (T-029 ✅) |
| C-0033 | DEV | CRITIC | 14:42Z | **טיק ביצוע — משימה 3 מתוך 4 בתוכנית `2026-08-07-onboarding-goal.md`.** המסך של T-029: `components/OnboardingForm.tsx` מרנדר קבוצת רדיו (`data-daily-minutes`) שבה `DEFAULT_DAILY_MINUTES` נבחר מראש, שדה `<input type="date">` ילידי, ושדה ציון היעד. **שלוש החלטות שנבדקו מול הקובץ ולא מול התוכנית:** ⓐ רצפת בורר התאריכים (`min`) נגזרת מ-`toIsoDateInZone(new Date(), LEARNER_TIME_ZONE)` — אותו חשבון בדיוק שה-route משתמש בו מאז C-0032; רצפת UTC הייתה מציעה בבורר, בשעות הראשונות של כל יום מקומי, בדיוק את היום שהשרת דוחה. ⓑ שדה הציון עובר דרך `<LatinField>`, ולכן `LatinFieldProps` הורחב בשני איברי union (`name: 'target_score'` · `inputMode: 'numeric'`) במקום להוסיף שדה לטיני שני — TD-5 נסגר בכך ש-`LatinField` הוא היחיד, ו-`enterKeyHint` הוא prop חובה שרק הוא אוכף. ⓒ `app/onboarding/page.tsx` איבד את ההבטחה "השלב הזה עוד בבנייה" — טקסט זמני שנשאר אחרי שהוא כבר לא נכון הוא השקר הזול ביותר שמוצר מספר. **שני פגמים נמצאו בתוכנית עצמה, לא בקוד, ותוקנו בה במקום:** ⓐ רשימת ה-imports שהתוכנית נתנה ל-`OnboardingForm.tsx` **השמיטה** את `LEARNER_TIME_ZONE` ו-`toIsoDateInZone`, ששתי שורות אחר כך היא קוראת להם — כפי שנכתבה, הקובץ לא מתקמפל כלל. ⓑ **התוכנית רינדרה את הודעת השגיאה של שדה הציון ב-`text-ink-muted`** — בדיוק צבע טקסט העזרה שהיא מחליפה. הלומד היה רואה "הזן מספר שלם." באותו אפור של "הסולם הוא 50–150." ובלי שום סימן ויזואלי שמשהו נדחה, בזמן ש-`aria-invalid` **כן** דיווח לקורא מסך: הפער אינו בין נגיש ללא-נגיש אלא בין מה שה-DOM מצהיר למה שהעין רואה. הצבע הופרד ל-`text-danger`, וגם המחרוזת עצמה מתחלפת ממילא — צבע אינו הערוץ היחיד. **מוטציית TD-5 הפילה בדיוק את הבדיקה בשמה:** החלפת `<LatinField>` ב-`<input dir="ltr">` שקול → `routes the onboarding target-score input through LatinField, not a bare input`. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 378 בדיקות ✅ (מ-376, 24 קבצים) · build ✅ `Compiled successfully in 283ms`.** בנוסף הורץ `check:mobile` כביטוח רגרסיה על `LatinField` המשותף: `✓ all mobile/PWA guarantees hold (351 checks, widths 320/375/414px)` — **אותו מספר בדיוק כמו לפני הטיק. אפס בדיקות חדשות על מסך חדש לגמרי היא הראיה הישירה ש-TD-13 עדיין פתוח**, ולכן שורת TD-13 ב-30-architecture עודכנה מ"חוב עתידי" ל"חוב שעולה עכשיו". ⛔ אפס תלויות חדשות · ⛔ אין שינוי בנקודות קצה ולכן `docs/api-contract.md` לא נגע — משימה 2 היא זו שנגעה בו. | `components/OnboardingForm.tsx` · `components/LatinField.tsx` · `components/EnWord.test.ts` (+1) · `app/onboarding/page.tsx` · `docs/superpowers/plans/2026-08-07-onboarding-goal.md` |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
