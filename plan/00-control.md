<!--
NEXT_AGENT: QA                         # ▶️ C-0553 (DEV) — `T-308` 🟣: הלשונית `סימולציה` נעשתה קישור והלומד נכנס. נוסף `GET /api/amirnet/simulation` + `simulationQueue()` טהורה (‏23 פריטים בסדר ששת הפרקים, או `null` — ⛔ לעולם לא ריצה מקוצרת). פתחתי `T-310` בעצמי (קליטת פריטי `K-006` למסד). ⬜ פנויות ל-DEV: `T-309`·`T-310`·`T-304`·`T-270` (amirnet) · `T-305`·`T-306` (loop) · `T-302`·`T-284` (base).
STATE: READY                      # ▶️ C-0553 (DEV): `verify` מלא ✅ · הליכה חיה 375×780 על `/world/amirnet/simulation` (מוצר, ⛔ לא `/dev`) — כותרת `סימולציה מלאה` · 504 תווים · 10 לחיצים · **0 מתחת ל-44px** · ⛔ אפס גלילה אופקית · ⛔ אפס שגיאות קונסולה · שלוש הלשוניות `A` עם `href`.
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: []   # ▶️ C-0529 — ריק.
CRITIC_ROUNDS_ON_TASK: 0          # ⛔ התור הישן הועבר ל-`plan/archive/control-log.md` (26/08) — הוא היה מת: QA הופך 🟣⇢✅ בכמות מ-`git log`.
LAST_HANDOFF_AT: "2026-09-12T21:52:00Z"   # C-0553 (DEV) — `T-308` נחתה; `T-310` נפתחה.
HUMAN_DECISION_REQUIRED: false    # ▶️ ⛔ אינו ממתין. **44 · 45 · 46 נסגרו כולם ב-23/08.** נותר **47** — שורה ב-`RULES § 0.1 ב׳` שהיא הקובץ של רוי, ו⛔ **אינה חוסמת את הקידום של היום**.
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                 # 🔒 שוחררה 2026-09-12T21:59Z — C-0553 (DEV) סיים: `T-308` 🟣, `T-310` נפתחה.
LOCK_AT: ""
WORKSTREAM_TICKS:                 # ⚠️ בלם 8 שוכתב 23/08 (רוי): סופר **טיקי עבודה בלבד** — טיק שהסתיים בקומיט. ⛔ טיק שקט/נסיגה/שורה-אחת אינו נספר. תקרה **120 לכל פריט** ב-36-video-spec § 13, ⛔ לא לחזון כולו.
#   story:  13 / 120           # § 13-1 · **מוצתה (⬜=0) · הוזזה→`nav` ב-C-0310 (QA).** הפרוסות A/B/C נמסרו (T-185…T-188 · T-202/203 · T-150). שלוש החותמות (36§13.1) — ראה SEALS למטה.
#   nav:     3 / 120           # § 13-2 · **⬜=0 · חתומה · המוקד הוזז→`cards` ב-C-0316 (QA).** פירוט מלא ⇒ `plan/archive/control-log.md` (הוצא C-0418).
#   cards:  14 / 120           # § 13-3 · **⬜=0 · מוצתה שנית · הוזזה→`arena` ב-C-0500 (QA), ⛔ אינה חתומה** (ⓑⓒ חסומות ב-env). 5 🟣 הפכו ✅ (T-066·T-199·T-228·T-243·T-259). פירוט מלא ⇒ `plan/61-deferred.md` · `plan/archive/control-log.md` (הוצא C-0418).
#   arena:  24 / 120           # § 13-4 · **מוצתה שנית (⬜=0), הוזזה→`msgs` ב-C-0514 (QA).** `T-217`·`T-220`·`T-234`·`T-281`·`T-282`·`T-283` 🟣→✅ מוזגו ל-`dev` בטיק הזה. ⛔ אינה חתומה — ⓑⓒ חסומות ב-env. פירוט ⇒ `plan/61-deferred.md` (מעבר שני) · `plan/archive/control-log.md`.
#   studies: 3 / 120           # § 13-5 · לימודים כמכולת מסלולים. ⬜=0 (`T-144` נסגרה ⛔ · `T-246` 🟣 · `T-247` חסומה עד `dev`). פירוט מלא ⇒ `plan/archive/control-log.md` (הוצא C-0418).
#   amirnet: 13 / 120          # 41 § 8-1..3 · ▶️ C-0553 (DEV, **קריאה קדימה** — `msgs` נשאר ACTIVE_WORKSTREAM, ⬜=1 והיא של CONTENT): `T-308` 🟣 — **הלומד סוף-סוף נכנס לסימולציה מהמוצר.** `AMIRNET_BUILT_TABS` נושא `simulation`, `app/(tabs)/world/amirnet/simulation/page.tsx` קיים, וכרטיס רמה פתוחה מריץ את המנוע דרך `GET /api/amirnet/simulation` החדש. 🔴 **הפער שנמדד עכשיו, והוא ⛔ אינו ב-UI:** המאגר במסד **ריק** — 26 הפריטים של `K-006` יושבים ב-`data/generated/*.jsonl` ו⛔ אין `build:amirnet-items` ⇒ הלחיצה מגיעה למשפט, ⛔ לא לריצה. נפתחה `T-310` (קליטה למסד). ⚠️ ו-26 ⛔ אינם מספיקים לריצה אחת (23 ברמה **אחת**) ⇒ הזמנת תוכן נוספת. קודם ⇒ C-0551 (CONTENT): `K-006` 🟣, 26/26 עברו את השער. קודם ⇒ C-0550 (DEV): `T-307` 🟣.
#   msgs:    4 / 120           # 39-messages-spec § 9 · הודעות — **המוקד.** ▶️ C-0527 (DEV) — `T-289` 🟣: המונה והתווית לקורא-מסך אומרים «נקרא», ⛔ ולא «נענה» (`D-207` · `F-212`). ⬜=1 ונותרה **של CONTENT בלבד**: `T-193`. ⇒ ⛔ אין ל-DEV שורה כשירה כאן. ⛔ פריטים אחרים חסומים ב-R-026
MILESTONE_TICKS: 103           # ⛔ מונה M0 הישן — מוקפא, ⛔ ואינו בלם. הבלם החי הוא WORKSTREAM_TICKS
RELEASE_READY: "be80d74 · 2026-09-12T17:56Z · **C-0548 (QA, מלא).** `verify` ✅ טרי (252/252 קובצי בדיקה · 4230 בדיקות · build✅ · check:mobile 1561/1561) · `loop:health` 22/23 (F-233 החדש על 9, שאר הכשלים הישנים סגורים). smoke ✅ (health endpoint, כל 4 הבדיקות). הלומד מקבל בנוסף על C-0546: מאגר פריטי אמירנט אמיתי + `GET /api/amirnet/practice` (`T-297`), ⛔ ללא רכיב UI חדש."
PAUSED_BY_HUMAN: false           # ⚠️ הבלם בודק `== true` בלבד. **נמדד C-0418 ב-`git log` מ-01/09: DEV 55 קומיטים · PM 20 · QA 13** ⇒ שלוש המשימות דלוקות ורצות. רקע ⇒ `plan/archive/control-log.md`
# (DEPLOYS_THIS_MONTH · LAST_DEPLOYED_AT — הוצאו 08/09, `D-203`ⓔ. קפאו ב-23/08; הבלם נמדד מ-`PROMOTIONS_THIS_MONTH`.)
LAST_REVIEWED_COMMIT: "75bc311"  # main אחרי הקידום של 12/09 (C-0546, PROMOTER). `verify` exit 0 נרץ על אותו SHA לפני הקידום.
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1 ---
WORKING_BRANCH: work/current      # ▶️ **שונה 24/08 · RULES § 0.23 · שלב 2.** DEV ו-CONTENT דוחפים לכאן בלבד. ⛔ שם קבוע, ⛔ לעולם לא שם חדש.
MERGE_TARGET: dev                 # רק QA ממזג לכאן, ורק ב-`merge --ff-only`. ⛔ אף סוכן ⛔ אינו דוחף ל-dev ישירות.
ACTIVE_WORKSTREAM: msgs            # ▶️ הוזז C-0514 (CRITIC, מלא) — `arena` מוצתה שנית (⬜=0, נמדד `docs/plan-open.md` § flag: «הבא ברצף עם עבודה פנויה הוא msgs (4 ⬜)»). `studies` גם היא מוצתה (⬜=0) ⇒ נדלגת, ⛔ לא נבחרה.
PREV_WORKSTREAM: "arena"     # 🆕 C-0514 (CRITIC) — `arena` (מעבר שני): T-217·T-220·T-234·T-281·T-282·T-283 נבנו ומוזגו בטיק הזה. שורת `61-deferred` נכתבה (מעבר שני). (ההיסטוריה הקודמת בגיט.)
WORKSTREAM_ENDING: msgs · 1 ⬜ נותרה · 2026-09-12T19:19:00Z · C-0549   # 🆕 PM כותב · **QA קורא**. ⛔ אינו היתר להזיז את `ACTIVE_WORKSTREAM` (`§ 0.23 ז׳`). ⚠️ **נמדד מחדש C-0549:** ה-⬜ היחידה ב-`msgs` היא `T-193` ו**היא של CONTENT** ⇒ ⛔ אין ל-DEV שורה כשירה כאן, ⛔ ולא בגלל פיגור: `05-departments` אומר ש-`39 § 7` חסום ב-`R-026`. ‏`amirnet` (הבאה ברצף) מוזנת ל-**5 ⬜**: `T-270`·`T-304`·`T-307`·`T-308`·`T-309`.
# (SEALS · BUDGET_NOTE · הערות הכיווץ — הוצאו 08/09 ל-`plan/archive/control-log.md`.
#  ⛔ אפס צרכנים בכל המאגר: ⛔ אף סקריפט, ⛔ אף כלל ו⛔ אף פרומפט ⛔ אינם קוראים אותם. ⛔ אין להחזירם.)
IMPROVE_TARGET: ""              # 🩺 D-146 · **ריק = המצב כבוי.** ▶️ **C-0412: נוסה ו⛔ לא ניתן להדליק — נמדד.** `story` ⇒ בדיקה 14 FAIL (3 ⬜ מראש, תקרה 2) · `nav` ⇒ ⛔ אין לה דבר ב-`61-deferred` · `cards`/`arena` ⛔ אינן חתומות. פירוט ⇒ `D-184`.
MERGE_BLOCKERS: ""                # ⇐ **QA כותב · DEV קורא ראשון.** רק מה שחוסם **מיזוג** ל-`dev` (`§ 0.23 ז׳`). ⛔ לא חסם קידום. ▶️ C-0534 (QA, מלא): `F-219` נסגר — `ops(loop)` איחד את `dev` ל-`work/current` (`65359cb`) לפני הטיק הזה; `rev-list` בשני הכיוונים נמדד **0** על `0851258` לפני הקומיטים של הטיק הזה.
PROMOTION_BLOCKERS: ""            # ✅ **נפתר 2026-09-12T16:29Z — C-0546 (PROMOTER):** הקידום עבר, `main`=`75bc311`, כל 5 תנאי השער ירוקים. היסטוריית C-0521 (הסירוב הלא-דטרמיניסטי, F-229) ⇒ `plan/archive/control-log.md` · פריט 110 ב-`03-for-roy.md`. # ⇐ PROMOTER בלבד.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. 🆕 06/09: **רק PROMOTER מקדם לכאן** (`RULES § 0.29`, `5 0 * * *` UTC — ⚠️ הוזז ב-07/09 בבקשה מפורשת של רוי: `0 23` ⇢ `21 23` ⇢ `5 0`, כי `23:21` השאיר 11 דקות בלבד מטיק DEV של 36 דקות). רוי גובר תמיד.
LAST_PROMOTED_AT: "2026-09-12T16:29:28Z"  # ⚡ קידום אוטומטי של הלופ — C-0546 (PROMOTER), כל 5 תנאי השער ירוקים. `2b8e1b7..75bc311` ff-only, 124 קומיטים. פירוט ⇒ `plan/archive/control-log.md`.
PROMOTIONS_THIS_MONTH: 16         # 16 this month (➕ 12/09 16:29Z, C-0546 PROMOTER — קידום אוטומטי). ⛔ Credit budget is no longer a reason to delay (D-086).
```

> 🧑‍⚖️ שתי ביקורות ידניות של רוי — **הפירוט המלא, כולל מצב כל ממצא, ב-`plan/OPERATOR-LOG.md`.**
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.7). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.

### 0.1 יומן העברות מקל — 2 האחרונים בלבד

> ⚠️ **שורה אחת ביומן, ⛔ ולא שתיים** — התקרה נפרצה פעמיים כך (C-0261 · C-0293). שתי המדידות ⇢ `plan/archive/handoff-log.md`.

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|



| C-0551 | CONTENT | QA (ביקורת) | 2026-09-12T20:48:19Z | ✍️ **K-006, ההזמנה הראשונה שבפועל נכתבה תחתיה** — לא אצוות NGSL. `lib/core/amirnetItemGate.ts`/`amirnetChapterGate.ts` קיימים מ-C-0471 והשורה ישבה פתוחה בלי שאף סוכן חזר אליה מ-08/09 עד הטיק הזה. 26 פריטים מקוריים (8 `sc` + 8 `rs`, 2 לכל רמה 1-4 · 2 פרקי `rc` בני 5 שאלות, רמה 1 ורמה 3) — 26/26 עברו את השער האמיתי (מיובא, לא שוחזר) בסבב שני אחרי 8 פסילות עצמיות (`vocab_above_tier`·`correct_length_outlier`). בקרת שלילה: 9 פריטים פגומים בכוונה, 0/9 עברו. שורת K-006 סומנה 🟣 לביקורת. ⛔ **אין קליטה ל-DB** — אין `build:amirnet-items`, נרשם ל-DEV. | `data/generated/amirnet-items-2026-09-12-{sc,rs,rc}.jsonl`·`amirnet-items-manifest-2026-09-12.json`·`25-content-commissions`·`80-content-lessons`·`00-control` |

| C-0553 | DEV | QA | 2026-09-12T21:52:00Z | 🔨 **טיק בנייה — משימה 2 של `docs/superpowers/plans/2026-09-12-amirnet-levels.md`.** ⓐ `T-308` 🟣: הלשונית `סימולציה` נעשתה קישור **באותו קומיט** שבו נוצר `app/(tabs)/world/amirnet/simulation/page.tsx` — `AmirnetTabs.dom.test.tsx` מודדת שכל מפתח BUILT נפתר לעמוד על הדיסק, ולכן השניים ⛔ אינם ניתנים להפרדה. ⓑ **מה שהתוכנית ⛔ לא צפתה, ונמדד בטיק:** «כרטיס פתוח מתחיל את המנוע» דרש **מקור פריטים לריצה** שלא היה — `GET /api/amirnet/practice` מגיש סוג אחד בכל פעם לפי `41 § 7`, ⛔ לא ריצה בת 23 פריטים. ⇒ נוספו `GET /api/amirnet/simulation` ו-`simulationQueue()` **טהורה** שפורשת את ששת הפרקים, או מחזירה `null`. ⓒ ⛔ **`null` ולא ריצה מקוצרת** — «סימולציה מלאה» בת ארבעה פרקים היא בדיוק הכשל ש-`41 § 2` נכתב נגדו. ⓓ **נמדד: המאגר במסד ריק** ⇒ הלחיצה מגיעה למשפט («⛔ אין מספיק פריטים»), וזו הצהרת עובדה (`D-152 § ב׳`) ⛔ ולא `RULES § 0.31`. פתחתי `T-310` בעצמי לקליטת פריטי `K-006`. | `simulationQueue`(+6 בדיקות)·`app/api/amirnet/simulation`(+בדיקה)·`AmirnetSimulationEntry`(+בדיקה)·`app/(tabs)/world/amirnet/simulation`·`AmirnetTabs`(+2 בדיקות)·`api-contract`·`30-architecture`·`50-tasks`·`00-control`·`plan-open`·`architecture-map` |

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
