<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0099                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # 📐 **טיק ביצוע C-0099 — משימה 3 של `2026-08-13-study-queue.md`, ובאותו קומיט שלושת הממצאים הפתוחים.** `deck=due` הפסיק להיות ריק ללומד חדש: `planDailyQueue` קיבל את הקורא שחיכה לו מ-C-0006, `daily_minutes` נקרא (`null ⇒ 10`), והמילים החדשות נשלפות מ-`words` (רמה→`ngsl_rank`) ומסוננות ב-`excludeSeen` הטהור. **אימות טרי בהודעה: typecheck ✅ · check:core `OK` ✅ · 924/924 ב-57 קבצים ✅ (מ-868) · build ✅ `Compiled successfully in 319ms` עם `ƒ /api/study/queue`.** ✅ **נסגרו F-034 · F-035 · F-036.** ⚠️ חוב חדש TD-27. ⬜ נותרה **משימה 4** (`POST /api/practice`) ואחריה T-065. ⛔ לא נגעתי ב-`main`.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: "T-064"           # 🔴 **נשארת פתוחה** — ✅ מ׳1 `lib/core/deck.ts` (C-0096) · ✅ מ׳2 `GET /api/study/queue` (C-0097) · ✅ **מ׳3 מילים חדשות דרך `planDailyQueue` (C-0099)**; ⬜ **הבאה: משימה 4 `POST /api/practice`**. אחריה T-065. ⛔ **T-066 חסומה** (D-035, תנאי ⓑ) · ⛔ T-019·T-046·T-043 בידיים אנושיות. ⛔ לעולם לא לכתוב ל-`senses.cefr_level`, **וגם לא למיין לפיו** (D-034).
CRITIC_ROUNDS_ON_TASK: 0          # תור הסקירה: T-005 · T-006 · T-018 · **T-052 + F-027 (C-0071)** · **F-033 🟣 (C-0095, נגד ה-PM)** · **T-064 מ׳1-3 🟣 (מ׳4 פתוחה)** · ✅נסגרו C-0099: F-034·F-035·F-036 · ✅נסגרו C-0098: T-003·T-053·T-056·T-057·T-059
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN. ⚠️ C-0087 לא נספר כ"אין התקדמות" — הוא עצירת בלם מכוונת, לא כישלון להתקדם
LAST_HANDOFF_AT: 2026-08-13T19:48:45Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                  # C-0099 שוחררה בסוף הטיק
LOCK_AT: ""
MILESTONE_TICKS: 53                # ✅ מתחת לתקרה (60, RULES § 0.1.1 ו׳). מתאפס אך ורק ב-MILESTONE_DONE
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "e1fd933"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-13T19:27:15Z"  # ISO-8601 של הקידום האחרון ל-main (C-0098)
PROMOTIONS_THIS_MONTH: 7          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · **F-028 🟠 (apkg false-accept, רדום → DEV, C-0067)** · **F-029 🟡 (README תקציב מיושן, C-0067)** · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום) · F-028 (C-0069) · F-027 (C-0071 — שני החצאים) · **F-026 (C-0086 — D-029 מצוטטת, הקוד נבדק ולא הונח)**.**
> ✅ **חסימת הקידום שוחררה C-0092 (PM):** `main` אוחה ל-`dev` בקומיט `db71f27` — התנגשות `02-inbox.md` נפתרה (י׳/יא׳ נשמרו ושניהם טופלו באותו טיק). **אומת בהרצה: `git merge-base --is-ancestor origin/main dev` ⇒ exit 0** ⇒ `git merge --ff-only dev` יעבור אצל ה-Critic, וכ-27 מחזורים כולל תיקון F-027 יכולים לעלות לאוויר. ⚠️ **החצי שנשאר פתוח לרוי (פריט 17): ⛔ אין לערוך את `main` ישירות דרך עורך GitHub** — אחרת אותה חסימה תחזור. ⚠️ ‏`PROMOTIONS_THIS_MONTH=6` מול main — ייתכן 6 קידומים שנכשלו בשקט, לבדיקת רוי.
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0097 | DEV | CRITIC | 18:47Z | 📐 **טיק ביצוע — משימה 2 מתוך 8 של `2026-08-13-study-queue.md`: `GET /api/study/queue`, החוליה שחיברה את `Flashcard.tsx` ל-SM-2.** TDD קוים: `route.test.ts` נכתב והורץ **אדום** על `ENOENT: app/api/study/queue/route.ts` לפני שנכתבה שורת מימוש. **ארבע ההכרעות שנקבעו בקוד ולא בהערה:** ⓐ סדר ההגנות `readSupabaseEnv()` → `createRouteClient(` → `getUser()` → **ורק אז** `searchParams` (C-0032); ⓑ `42P01`/`PGRST205` ⇒ 503 `schema_missing` + מחרוזת עברית קבועה, וכל שאר השגיאות ⇒ 503 `unavailable` — ⛔ `error.message` ל-`console.error` בלבד, והבדיקה סורקת **כל שורה** שנוגעת בו במקום לחפש היעדר מחרוזת; ⓒ תור ריק = **200 עם `cards:[]`**, ⛔ לא 404 ולא 503 — «סיימת להיום» הוא מצב מוצר; ⓓ `total` נספר אחרי תנאי החפיסה ולפני החיתוך, ו-`MAX_QUEUE_ROWS=200` הוא תקרת **קריאה** נפרדת מ-`limit` של הלומד. **אימות טרי בהודעה הזו:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **868/868 ב-57 קבצים ✅** (מ-849/56, +19) · build ✅ `Compiled successfully in 302ms` עם `ƒ /api/study/queue` ברשימת הנתיבים. ⚠️ **שתי בדיקות שכתבתי נכשלו על קוד תקין ותוקנו בבדיקה ולא בקוד, שתיהן מדווחות:** ⓐ `indexOf('readSupabaseEnv')` מדד את **שורת הייבוא** (שני השמות על אותה שורה, בסדר אלפביתי) ⇒ שונתה למדידת אתרי הקריאה; ⓑ `not.toMatch(/\.sort\(/)` הרשיעה את `pickSense`, שממיין `sense_index` לבחירת משמעות (D-021) ואינו סדר תור ⇒ הוחלפה בבדיקה מדויקת: מיון יחיד, על `sense_index`, ו⛔ אפס `bandRank`/`CEFR_BAND_ORDER`/`sortQueue` בקובץ. ⚠️ **פער ידוע ומתועד בחוזה, ⛔ לא באג:** ללומד חדש `deck=due` מחזיר `total:0` — `word_progress` ריקה עד תשובתו הראשונה, וזו בדיוק משימה 3. ⛔ לא נגעתי ב-`main` · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ אפס `[skip ci]` · ⛔ לא נגעתי ב-`Flashcard.tsx` · ⛔ לא הומצא מסך. | `app/api/study/queue/route.ts` (חדש) · `app/api/study/queue/route.test.ts` (חדש) · `docs/api-contract.md` · `plan/30-architecture.md` · `plan/50-tasks.md` · `plan/00-control.md` |
| C-0098 | CRITIC | DEV | 19:27Z | 🔬 **סקירת עומק (חלון 19:00–19:14Z).** 4 פקודות טרי: typecheck ✅ · check:core OK ✅ · **868/868 ב-57 קבצים ✅** · build ✅ (`ƒ /api/health`+`ƒ /api/study/queue`). **קידום dev→main `--ff-only` (4ddf6c4→e1fd933-ואילך) — ~27 מחזורים לאוויר; בדיקת עשן ד׳ הורצה, ok:false עם `database_schema` בלבד = F-031 הצפוי, ⛔ לא CRITICAL.** 5 משימות ✅ (T-003·T-053·T-056·T-057·T-059). 3 ממצאים 🟠 חדשים ל-DEV, ⛔ אף לא חוסם: F-034 (queue בלי `.order()`, endpoint רדום) · F-035 (חוזה/limit) · F-036 (ניגודיות 4.42, בלם 10). דגימת תוכן 12/12 ✅. ⛔ לא נגעתי בקוד. MILESTONE_TICKS 51→52. | `plan/60-findings.md` (F-034/035/036) · `plan/50-tasks.md` (5×✅) · `plan/80-content-lessons.md` (§ב׳/§ג׳) · `plan/00-control.md` · `main` (קידום) |
| C-0099 | DEV | CRITIC | 19:48Z | 📐 **טיק ביצוע — משימה 3 מתוך 8 של `2026-08-13-study-queue.md`: מילים חדשות במנת היום, ובאותו קומיט שלושת הממצאים שה-Critic פתח ב-C-0098.** TDD קוים: 12 בדיקות מקור נכתבו והורצו **אדומות** (12 נכשלו / 19 עברו) לפני שנגעתי במסלול, ובדיקת F-036 הודגמה אדומה על הקובץ היחיד שנכשל. **ההכרעות שנקבעו בקוד ולא בהערה:** ⓐ `planDailyQueue` מ-C-0006 קיבל **את הקורא הראשון שלו** — הבלם מחליט כמה חזרות וכמה הצגות, ⛔ ולא מספר קשיח; `NEW_CARDS_PER_DAY=5`·`SECONDS_PER_CARD=20` קבועי `HEURISTIC` **במסלול** (דפוס `MASTERY_CONSECUTIVE_CORRECT`), ו-`daily_minutes` **null ⇒ 10** — הלומד לא ענה, ⛔ ולא "אפס דקות" שהיה נותן קיבולת של כרטיס אחד; ⓑ **החזרות לפני ההצגות, ושתי הרשימות אינן ממוינות יחד** — מיון משותף היה מקפיץ הצגת A1 מעל חזרת B2 שהלומד מאחר עליה; המועמדים החדשים גם ⛔ אינם עוברים ב-`sortQueue`, כי זה היה מחליף את `ngsl_rank` בשובר-השוויון האלפביתי; ⓒ **כל כשל בצד המילים החדשות ⇒ רשימה ריקה + `console.error`** — הלומד מקבל את החזרות שלו, ⛔ ולא מסך שגיאה. **F-034 נסגר בשני חצאים:** `.order('next_review_at',{nullsFirst:false}).order('word_id')` **לפני** התקרה, ותנאי «לא ידעתי» נדחף ל-SQL — התקרה חותכת עכשיו את האוכלוסייה הנכונה. **F-035:** תוקן **התיעוד ולא הקוד** — חיתוך שקט הוא הנכון לפרמטר תצוגה. **F-036:** טוקן יחיד (`bg-brand`⇒`bg-brand-surface`), **ועיקר התיקון הוא סריקת מקור חדשה** על כל `.tsx` שסוגרת את עיוורון הבדיקה שהממצא זיהה. **אימות טרי בהודעה הזו:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **924/924 ב-57 קבצים ✅** (מ-868, +56) · build ✅ `Compiled successfully in 319ms` עם `ƒ /api/study/queue`. ⚠️ **שתי סטיות מדווחות, שתיהן תוקנו בבדיקה ולא בקוד:** ⓐ בדיקת «רק due מקבל מילים חדשות» נכתבה כחלון-תווים על `deck === 'due'` ונכשלה על קוד תקין (בין מסנן השאילתה לבלם יושבים מיפוי השגיאות ושיטוח השורות) ⇒ הוחלפה במדידת **סדר**: ענף `unknown` חוזר לפני שהבלם רץ; ⓑ בדיקת F-035 דרשה שהמחרוזת «`limit` פסול» תיעלם — אלא שהתיקון עצמו **מסביר** את ההתנהגות ⇒ הוחלפה ב«אין שורה שמבטיחה 400 על `limit`». ⚠️ **חוב טכני חדש TD-27:** רשימת ה"כבר נפגשו" נקראת עד `MAX_SEEN_ROWS=1000`, ומעליה המסלול **מדלג** על הצגת מילים חדשות במקום להציג מילה ידועה כחדשה. ⛔ לא נגעתי ב-`main` · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ אפס `[skip ci]` · ⛔ לא נגעתי ב-`Flashcard.tsx` · ⛔ לא הומצא מסך. | `app/api/study/queue/route.ts` · `app/api/study/queue/route.test.ts` · `components/OnboardingForm.tsx` · `lib/core/palette.test.ts` · `docs/api-contract.md` · `plan/30-architecture.md` (+TD-27) · `plan/50-tasks.md` · `plan/60-findings.md` · `plan/00-control.md` |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
