<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0031                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # PM | DEV | CRITIC | HUMAN — **DEV C-0031 היה טיק ביצוע: משימה 1 מתוך 4 בתוכנית `2026-08-07-onboarding-goal.md`.** `lib/core/onboarding.ts` (טהור) הוא עכשיו המקור היחיד לאפשרויות `[5,10,20]`, לברירת המחדל `5`, לטווח 50–150 ולחשבון הימים למבחן — המיגרציה, ה-API והרדיו של המשימות 2–4 יקראו ממנו ולא יחזרו עליו. **מוטציה א׳ (ברירת מחדל→20) הפילה בדיוק את `defaults to the SMALLEST option, never the largest`. מוטציה ב׳ חשפה פגם בתוכנית עצמה, לא בקוד:** הנוסח שהתוכנית הציעה (`new Date('YYYY-MM-DD')`) כבר חסין-DST — צורת תאריך-בלבד נפרסרת כ-UTC לפי ECMA-262 — **וגם המוטציה הנכונה (בנאי רכיבים מקומי) עברה בירוק**, כי TZ של הקונטיינר הוא `UTC` שאין בו שעון קיץ. זה דפוס F-007: מחסום שקיים על הנייר ואינו מודד. `vitest.config.ts` נועל עכשיו `TZ: 'Asia/Jerusalem'`, ואז אותה מוטציה נופלת `expected 2 to be 3` — **אפס רגרסיות בשאר הסוויטה.** **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 366 בדיקות ✅ (מ-347) · build ✅ `Compiled successfully in 3.1s`.** ⛔ אפס תלויות חדשות · ⛔ אין שינוי ב-API ולכן `docs/api-contract.md` לא נגע (המשימה הבאה היא שתיגע בו). **הטיק הבא של DEV: משימה 2 — `0004_onboarding_answers.sql` + `POST /api/profile`.** ⚠️ **T-020 של ה-PM נשארת דחופה ובלתי-תלויה** — סוגרת את F-021 ומשחררת את T-018. חסמים אנושיים: T-043 (קובצי המקור) · P-001 (תוכן ל-T-034) · TD-4/TD-8/0003 (הרצת המיגרציות בפרויקט חי). **C-0029 (T-015) עדיין לא נסקרה על ידי ה-Critic.**
# ↓ ההקשר של C-0030 (טיק התכנון) — נכתבה `docs/superpowers/plans/2026-08-07-onboarding-goal.md`: ארבע משימות המכסות **T-029** (שאלת הדקות ביום לפי R-012), **פרוסת ההתמדה של T-003** (‏`0004_onboarding_answers.sql` + `POST /api/profile`) ו-**TD-13** (‏`/dev/onboarding` בהארנס, כדי ש-`/onboarding` יפסיק להימדד בטעות כמסך ההתחברות). ⛔ **T-003 המלאה נשארת מחוץ לתוכנית** — שאלת המוסד (A7) ממתינה לתוכנית UX מה-PM. שתי הכרעות המוצר שננעלו שם (אין אפשרות מעל 20 דקות · `today` הוא פרמטר) מומשו כלשונן ב-C-0031.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-029             # **משימה 1 בוצעה C-0031.** הבא ברצף בתוכנית `2026-08-07-onboarding-goal.md`: משימה 2 = `0004_onboarding_answers.sql` + `POST /api/profile` (פרוסת ההתמדה של T-003) · משימה 3 = המסך · משימה 4 = TD-13
CRITIC_ROUNDS_ON_TASK: 0          # T-015 ו-T-029/משימה 1 טרם נסקרו
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-07T12:54:58Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה. F-021 היא הכרעת PM, לא הכרעת רוי, ואינה חוסמת את המדידה (המדיניות היא נתון ולא לוגיקה).
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."

# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: CRITIC                  # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: "2026-08-07T13:04:50Z"
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "4264114"  # נסקר C-0024: מהיר. ארבע פקודות עברו טרי. main==dev אחרי קידום #3 — הכל אצל המשתמש

# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T07:11:00Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 3          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: **F-021 🟠 (חדש C-0020 — נגד ה-PM: 1.7.1 חסר מהתמצית + סתירת כללי סינון; חוסמת את T-018)** · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת, T-034) · F-008 🟡 (חסום ב-T-025) · R-010 (אסור להעתיק פריטים ממאל"ו). **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0031 | DEV | CRITIC | 12:54Z | **טיק ביצוע — משימה 1 מתוך 4 בתוכנית `2026-08-07-onboarding-goal.md`.** `lib/core/onboarding.ts` (טהור) הוא המקור היחיד לאפשרויות `[5,10,20]`, לברירת המחדל `5`, לטווח 50–150 (A5) ולחשבון הימים למבחן; שלוש השכבות הבאות (SQL · API · רדיו) יקראו ממנו ולא יחזרו עליו. **מוטציה א׳ (ברירת מחדל→20) הפילה בדיוק את הבדיקה בשמה** — `defaults to the SMALLEST option, never the largest`. **מוטציה ב׳ מצאה פגם בתוכנית של C-0030, לא בקוד, ובשתי שכבות נפרדות:** ⓐ הנוסח שהתוכנית הציעה, `new Date('YYYY-MM-DD')`, הוא צורת **תאריך-בלבד** שנפרסרת כ-UTC לפי ECMA-262 — כלומר המוטציה שנועדה לדמות באג DST הייתה בעצמה חסינת-DST ועברה בירוק. הסכנה האמיתית היא בנאי הרכיבים המקומי `new Date(y, m-1, d)`, שנמדד 2.958… ימים בין 2026-03-27 ל-2026-03-30 ונחתך ל-2. ⓑ **גם המוטציה הנכונה עברה בירוק**, כי `Intl.DateTimeFormat().resolvedOptions().timeZone` בקונטיינר הוא `UTC` — אזור בלי שעון קיץ. בדיקה שמגינה מפני DST בסביבה שאין בה DST אינה מגינה על כלום; זה בדיוק דפוס F-007, מחסום שקיים על הנייר ואינו מודד. לכן `vitest.config.ts` נועל `TZ: 'Asia/Jerusalem'` — אזור הזמן של הלומדים, לא של הקונטיינר — ואז אותה מוטציה נופלת `expected 2 to be 3`. **הסוויטה כולה עברה תחת האזור החדש: אפס רגרסיות.** הערות התוכנית תוקנו במקום כדי שהטיק הבא לא יחזור על ההוראה השגויה. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 366 בדיקות ✅ (מ-347) · build ✅ `Compiled successfully in 3.1s`.** ⛔ אפס תלויות חדשות · ⛔ אין שינוי ב-API ולכן `docs/api-contract.md` לא נגע — משימה 2 היא שתיגע בו. | `lib/core/onboarding.ts` · `lib/core/onboarding.test.ts` (19 בדיקות) · `vitest.config.ts` |
| C-0030 | DEV | DEV | 11:43Z | **טיק תכנון — אפס שינויי קוד.** נכתבה `docs/superpowers/plans/2026-08-07-onboarding-goal.md`: ארבע משימות שמכסות **T-029** · **פרוסת ההתמדה של T-003** · **TD-13**. שתי הכרעות מוצר ננעלו בתוכנית ושתיהן נגזרות מהראיות שכבר בריפו, לא מטעם: (א) **אין אפשרות מעל 20 דקות** — E3 מדדה ש-~40% מהנוטשים בחרו את היעד האינטנסיבי, כלומר הנזק נבע מ**בחירה** בו, ואפשרות שקיימת תיבחר בדיוק על ידי מי שבסיכון; (ב) **`today` הוא פרמטר ולא `new Date()`** — פונקציה תלוית-שעון היא בדיקה שעוברת עד היום שבו לא, ומנוע 7.1 ירצה ממילא "כמה ימים נשארו **נכון למתי**". הסולם 50–150 נלקח מ-A5 בתמצית ולא הומצא; שאלת המוסד (A7) **נשארה בחוץ** כי T-003 ⛔ ממתינה לתוכנית UX מה-PM. שלוש מוטציות נכתבו לתוכנית עם שם הבדיקה שכל אחת חייבת להפיל — ברירת מחדל 20 (מפילה גם יחידה וגם `check:mobile`) · חשבון ימים על `Date` מקומי מעבר לשעון הקיץ 2026-03-27 · אפשרות רביעית ב-TS בלי מגבלת ה-SQL. **`/dev/onboarding` הוא הליבה של TD-13:** ההארנס רץ בלי משתני סביבה של Supabase, ולכן כל שורה שמדווחת `ok /onboarding` מודדת בפועל את מסך ההתחברות (אומת חי ב-C-0013). ⛔ בלי אימות בטיק הזה — לא נגעתי בקוד ואין מה לאמת. | `docs/superpowers/plans/2026-08-07-onboarding-goal.md` |
| C-0029 | DEV | CRITIC | 10:43Z | **טיק ביצוע — T-015 נסגרה, והתוכנית `2026-08-07-source-registry.md` מוצתה במלואה.** שני דברים זולים היום ובלתי אפשריים רטרואקטיבית נכתבו ב-`0003_provenance_telemetry.sql`: **ייחוס** על שורת תוכן, ו**עלות ההגעה לשליטה** לכל צמד (לומד, מילה). `word_progress` היא **שורה מצטברת אחת לצמד** ומפתחה הראשי `(user_id, word_id)` — לוג אירועים באפליקציית חזרה מרווחת גדל בלי תקרה, וזו בדיוק W4 על השכבה החינמית; `time_to_first_correct` ו-`attempts_to_mastery` (שני השמות של D-010 מילה במילה) נשארות **nullable**, כי עד התשובה הנכונה הראשונה הערך אינו ידוע ו-`0` היה מדידה. `data_sources` **משוקפת ולא סמכותית** — הסמכות נשארת `lib/core/dataSources.ts`, ובדיקה מצליבה משווה את קבוצות המזהים. **שתי מוטציות הפילו בדיוק את הבדיקות שלהן:** ‏`('word2word'→'word2word-typo')` → נפלה `seeds a row for every id, and no extra ids`; החלפת המפתח הראשי ל-`id uuid primary key default gen_random_uuid()` → נפלה בדיקת W4. **שתי סטיות מהתוכנית, שתיהן משום שהתוכנית לא הייתה מריצה:** (א) הרגקס של הזריעה סרק את כל הקובץ ולכן היה קולט גם את `('seed','ngsl','generated')` ממגבלת ה-CHECK ומדווח מקור רפאים בשם `seed` — הסריקה צומצמה לגוף ה-`insert`, ונוספה בדיקה שסריקה ריקה אינה עוברת ריקנית; (ב) חלון ה-120 תווים של בדיקת העמודות אינו מגיע לעמודה השלישית ב-`alter` רב-עמודתי, ולכן כל עמודה קיבלה הצהרה משלה — מה שגם הופך את המיגרציה למאמתת את שלוש העמודות במקום להניח שהשאיר אותן קובץ קודם. מגבלת ה-CHECK של `senses.origin` נוספה בבלוק `do $$` נפרד, כי `add column … check (…)` מדולג כולו כשהעמודה קיימת. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 347 בדיקות ✅ (מ-334) · build ✅ `Compiled successfully in 253ms`.** בנוסף `check:mobile` ✅ `351 checks`. ⛔ אפס תלויות חדשות · ⛔ אין שינוי ב-API ולכן `docs/api-contract.md` לא נגע · ההרצה בפועל היא צעד ידני, נספח ב-1ב ב-`docs/SETUP.md`. | `supabase/migrations/0003_provenance_telemetry.sql` · `lib/supabase/telemetry.test.ts` (13 בדיקות) · `docs/SETUP.md` |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
