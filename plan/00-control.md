<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0061                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # **C-0061 (DEV): טיק ביצוע — T-045 ✅, משימה 4 (האחרונה) של `2026-08-12-content-lands.md`. התוכנית סגורה במלואה. 614 בדיקות ✅ · check:mobile 399 בדיקות ✅. ⛔ לא נגעתי ב-`main`.** תור הסקירה: **T-045 (C-0061)** · T-042 (C-0060) · T-049 · T-047 · T-048 · T-005 · T-006 · T-029 · T-018 + חמש אצוות תוכן.
STATE: REVIEWING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: ""                # **`2026-08-12-content-lands.md` הושלמה כולה (T-042 ב-C-0060 · T-045 ב-C-0061) ואין משימה פתוחה עם תוכנית.** הטיק הבא של DEV הוא **טיק תכנון**. שתי מועמדות שאינן חסומות: **T-050** (‏`sense_examples`/`sense_items`/`sense_distractors`, ~2,400 שורות — טרם נפתחה בידי ה-PM, ראה 3.1.18) ו-**F-008** 🟡. 🔴 `is_function_word` מול `lexical_class` = פריט 8 ב-`03-for-roy.md` · פריט 9 = הרצת `supabase/seed/0001_content_batches.sql` (TD-24) — שניהם אינם חוסמים.
CRITIC_ROUNDS_ON_TASK: 1          # T-029 נסקר ✅ נקי. T-005/משימות 1–2 נסקרו — סבב 1. ממתינים לאימות: משימה 3/T-006 (C-0040) + משימה 4/T-005 (C-0041) + T-047/T-048 (C-0057) + T-049 (C-0058) + T-042 (C-0060) + T-045 (C-0061)
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-12T13:00:29Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: CRITIC              # "" | PM | DEV | CRITIC | CONTENT — נעילת סקירה C-0061
LOCK_AT: "2026-08-12T13:08:02Z"
MILESTONE_TICKS: 15
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "fcccfc0"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-12T01:15:52Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 6          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-026 🟡 (תוקן C-0052; נותרה שאלת סיווג `PSEUDOGAP!` ל-PM) · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום).**
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0061 | DEV | CRITIC | 13:00Z | **טיק ביצוע — T-045, משימה 4 והאחרונה בתוכנית `2026-08-12-content-lands.md`. התוכנית סגורה במלואה.** מחצית המשימה **אומתה ולא נכתבה מחדש**: `0003_low_confidence_is_visible.sql` כבר מחזיק את העמודה, את המילוי, את ה-RLS שפותח `low` ללומד ואת חסימת פריטי הניקוד. נוסף כאן החצי החסר בלבד — הסימן. **שלוש הכרעות שהן התנהגות ולא סגנון:** ⓐ **`unverified` הוא שדה של `CardFace` ו⛔אינו נקרא מהמשמעות בתוך React** — TD-11 הוא הלקח, אבל כאן הכשל חמור יותר: החזית של כרטיס **הפקה** היא ההנחיה בעברית, וסימן עליה אומר ללומד שה**שאלה** אינה אמינה לפני שענה, במקום שה**תרגום** ממתין לאישור; משום שהדגל הוא שדה של הפָּנים, "רק על הגב" נעול בבדיקה **בשני הכיוונים**, ו-`buildCard` מציב אותו במקום יחיד. ⓑ **`needsHumanReview` הוא חובה ב-`CardSense`, ⛔בלי ברירת מחדל** — אופציונלי ש-`= false` מציג משמעות שאיש לא סיווג כ**מאומתת**, בדיוק צורת הפגם של `is_function_word` וההפך מ-D-024; **נמדד:** מחיקת השדה מפיקסצ'ר הפילה typecheck בשם הקובץ, והפיכתו לאופציונלי החזירה **0 שגיאות** — כלומר ה-`required` עושה את העבודה. ⓒ **אפס טוקני צבע חדשים ואין `--warning`** — לפלטה 11 טוקנים ואף אחד אינו "אזהרה", והמצאת טוקן בקובץ קומפוננטה היא החלטה של `35-design-constitution.md` בלבד; הסימן הוא `text-ink-muted` (כבר נאכף ל-4.5:1 מול `--surface-raised`), הגליף `◇` הוא ערוץ שני ו-`aria-hidden` משאיר את קורא המסך על המשפט. **הטענה מפוצלת לשלוש בדיקות, ומה שהיא אינה מוכיחה כתוב בראש הקובץ:** סביבת Vitest היא `node` והסימן נגיש רק אחרי חשיפה (`revealed` הוא state), ולכן `renderToStaticMarkup` היה פולט כרטיס **בלי בלוק גב** וכל טענה הייתה עוברת בריק. במקום jsdom ומנוע לחיצות בשביל פסקה אחת: הליבה מודדת את הבוליאני בשני הכיוונים (4 בדיקות) · סריקת מקור בדפוס `EnWord.test.ts` (6) · ו-**`scripts/verify-mobile.mjs` חושפת את `/dev/card` במנוע אמיתי** ודורשת `[data-card-back] [data-card-unverified]` יחיד עם טקסט אחרי הפשטת הגליף, ו**אפס** תחת `[data-card-front]` — 9 בדיקות חדשות בשלושת הרוחבים. פיקסצ'ר `/dev/card` הוא היחיד בריפו שנבנה ממשמעות לא-מאומתת; אחיותיו נשארו `false` כדי שגם הגב הנקי יימדד. ⚠️ **מוטציה אחת שרדה וחיזקה בדיקה, והלקח רחב מהמשימה:** החלפת השער `{card.back.unverified ? (` ב-`{revealed ? (` השאירה את **כל שש** בדיקות הקומפוננטה ירוקות, כי ההערה המסבירה שמעל הסימן מכילה את המחרוזת `card.back.unverified` ויושבת באותו בלוק — **סריקה שפרוזה יכולה לספק אינה סריקה**. התיקון: הערות מופשטות לפני כל סריקה, השער נמדד כביטוי שלם ולא כמזהה חופשי, ונוספה בדיקה שאף תנאי שני אינו נפתח בין השער לטקסט. **שבע מוטציות רצו, שש הפילו בדיקה בשמה.** **חמש פקודות רצו טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **614 בדיקות ✅ (מ-604, 43 קבצים)** · build ✅ `Compiled successfully in 1188ms` · check:mobile ✅ `all mobile/PWA guarantees hold (399 checks, widths 320/375/414px)`. ⛔ אפס תלויות חדשות · ⛔ אין שינוי בנקודות קצה, ולכן `docs/api-contract.md` לא נגעתי בו · ⛔ לא נטען אף סקיל עיצוב · ⛔ לא נגעתי ב-`main`. | `lib/core/flashcard.ts` · `lib/core/flashcard.test.ts` · `components/Flashcard.tsx` · `components/Flashcard.test.ts` · `app/dev/card/page.tsx` · `app/dev/card/typed/page.tsx` · `app/dev/card/swap/page.tsx` · `scripts/verify-mobile.mjs` · `plan/30-architecture.md` (3.1.19) · `plan/50-tasks.md` (T-045 ✅) · `plan/00-control.md` |
| C-0060 | DEV | CRITIC | 11:49Z | **טיק ביצוע — T-042, משימות 1–3 מתוך 4 בתוכנית `2026-08-12-content-lands.md`. שלוש המשימות נוסעות בקומיט אחד משום שהשלישית אינה ניתנת להרצה בלי שתי הראשונות: הסקריפט מייבא את שתיהן, ופיצול היה משאיר בריפו שני מודולים טהורים שאיש אינו קורא.** נוצרו `lib/core/batchRecord.ts` (10 בדיקות) · `lib/core/spotCheck.ts` (11) · `scripts/build-ingest-sql.mjs` + `scripts/build-ingest-sql.test.ts` (9) · `supabase/seed/0001_content_batches.sql` (537 שורות, טרנזקציה אחת) · `npm run build:ingest`. **הפלט המדוד של הסקריפט: `343 rows read from 6 batch files · 0 rejected by the gate · lot 343 · inspect 343 · accept up to 2 · basis full`.** כל 343 השורות עברו את `gateSense` **פעם שנייה** בזמן הפליטה (R-014) — אפס נדחו, ו⛔אף שורה לא תוקנה כאן; שורה שנדחית מדווחת ונופלת החוצה, ותיקון תוכן הוא טיק של סוכן התוכן. **שלוש הכרעות שהן התנהגות ולא סגנון:** ⓐ **`parseBatchRecord` קיים כדי שהמרה בהשמה תהיה בלתי אפשרית** — שורת ה-jsonl היא snake_case, `relation_type` מאוית אחרת מ-`relationType`, ו-`JSON.parse(l) as GeneratedSense` **עובר typecheck** ואז השער קורא `undefined` לכל מסיח ומפסיק לנקד אותם בשקט. ⓑ **`spotCheckPlan` מממש שורה אחת של ISO 2859-1 ומסרב להשלים את הטבלה** — הלוט 343 נמצא **מתחת** לטווח 501–1,200 ש-R-014 מצטטת, ולכן בדיקה **מלאה** (סירוב לדגום אינו טענה על תקן) וזריקה מעל 1,200. ⓒ **`needs_human_review` נגזר מ-`translation_confidence` ו⛔לא מ-`spot_check`** — הם חלוקים היום: 343 מסומנים לדגימה, **אחד** צריך אישור (`a`/determiner, `כלשהו`, `low`), וזו השורה שתדליק את הסימן במשימה 4. **13 מוטציות רצו: 11 הפילו בדיקה בשמה, ו-2 שרדו ותיקנו את הבדיקות** — spread ל-`sense` (הבדיקה חוזקה לנעול את רשימת שבעת המפתחות) וסדר קבצים לא ממוין (זהות-בייטים בין שתי ריצות אינה מודדת סדר; נוספה בדיקה שקוראת את שש כותרות האצווה). ⚠️ **שני פגמים בתוכנית עצמה נמדדו ותוקנו במקום להתעלם מהם:** דוגמת ה-SQL שבתוכנית מפרה את `words.n_letters not null` (`0002:51`), ולכן `BatchRecord` הורחב ב-`nLetters`/`nSyllables`/`isFunctionWord` המועתקים מהשורה (⛔`is_function_word` לעולם אינו מוסק — פריט 8); ובדיקת הברחת הגרשיים שבתוכנית אינה יודעת לקרוא ליטרל **הפותח** בגרש, ו-18 מהערות ההפרעה בעברית פותחות כך (`'כסף' בעברית הוא גם money`), ולכן היא הוחלפה בסורק תווים. ⚠️ **`spot_check` אינו נכתב ל-SQL כלל** — ל-`senses` אין עמודה כזו, והמצאת עמודה היא עבודת סכמה שהמשימה אינה בעליה; התוכנית מודפסת בכותרת הקובץ. ⚠️ **TD-24 נרשם:** הפלט הוא קובץ SQL ולא כותב service-role (אין Supabase חי, T-019), ו**הקובץ טרם הורץ** — ⛔ בדאטהבייס עדיין 0 שורות תוכן עד שרוי מחיל אותו (פריט 9 ב-`03-for-roy.md`, אינו חוסם את הלופ). ⚠️ **שארית מפורשת:** `sense_examples`/`sense_items`/`sense_distractors` (~2,400 שורות) אינם נכתבים — הצעה ל-PM לפתוח T-050 אחרי סקירה נקייה. **ארבע פקודות רצו טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **604 בדיקות ✅ (מ-574, 42 קבצים)** · build ✅ `Compiled successfully in 390ms`. ⛔ אפס תלויות חדשות · ⛔ אין שינוי בנקודות קצה, ולכן `docs/api-contract.md` לא נגעתי בו · ⛔ לא נטען אף סקיל עיצוב · ⛔ לא נגעתי ב-`main`. | `lib/core/batchRecord.ts` · `lib/core/spotCheck.ts` · `scripts/build-ingest-sql.mjs` · `scripts/build-ingest-sql.test.ts` · `supabase/seed/0001_content_batches.sql` · `package.json` · `plan/30-architecture.md` (3.1.18 · TD-24) · `plan/50-tasks.md` (T-042 ✅) · `plan/03-for-roy.md` (פריט 9) · `plan/00-control.md` |
| C-0059 | DEV | DEV | 10:44Z | **טיק תכנון — `docs/superpowers/plans/2026-08-12-content-lands.md`, ארבע משימות המכסות שתי משימות לופ: T-042 (משימות 1–3) ו-T-045 (משימה 4). ⛔ לא נגעתי בקוד ולא ב-`main`.** **הבירור ש-C-0058 הזמין הסתיים בתשובה הפוכה מהצפוי: מפרט ה-UX של T-045 אינו חסר** — D-024 (חתום בידי רוי) קובע "מוצג ומסומן בממשק כטרם אומת", שורת המשימה קובעת "דיסקרטי, על גב הכרטיסייה", ונמדד ש**מחצית המשימה כבר מומשה**: `0003_low_confidence_is_visible.sql` כבר מוסיף את `needs_human_review`, כבר ממלא אותו, כבר פותח `select` ללומד וכבר חוסם פריטי ניקוד ל-`low`. נמדד גם שהמסך אינו `app/study` (מצב ריק בן 28 שורות) אלא `components/Flashcard.tsx`. **T-042 נבחרה כראשונה ברצף כי המדידה הראתה שהיא נחסמה לשווא:** שורת המשימה עדיין אמרה ⛔ ממתין ל-T-038·T-039 — **שתיהן ✅** — ו-R-014 אסרה לשחררה עד תיקון F-020, ש**נסגרה C-0012**. **המספר שמצדיק את הרצף: 343 שורות תוכן כתובות וגזורות יושבות בשישה קובצי `data/generated/batch-*.jsonl`, ובדאטהבייס 0 שורות.** פילוח שנמדד: 330 `high` · 12 `medium` · **1 `low`** — כלומר בדיוק סימון אחד יידלק במשימה 4, וזה מה שהופך את T-045 לבדיקה אמיתית ולא היפותטית. **שלוש הכרעות שנכתבו כהתנהגות ולא כסגנון:** ⓐ **שורת ה-jsonl אינה `GeneratedSense` ואסור להמיר אותה בהשמה** — היא snake_case, `distractors[].relation_type` מאוית אחרת מ-`relationType`, ו-`JSON.parse(l) as GeneratedSense` **עובר typecheck** ואז השער קורא `undefined` לכל מסיח ומפסיק לנקד אותם בשקט; לכן משימה 1 היא פרסר עם בדיקה שנועלת את רשימת המפתחות. ⓑ **R-014 מתעדת שורה אחת של ISO 2859-1 (501–1,200 ⇐ 80, קבלה עד 2), והלוט שלנו הוא 343** — השלמת השורה החסרה פירושה ציטוט טבלה שלא קראנו, בדיוק המהלך ש-R-014 נכתבה לאסור; לכן `spotCheckPlan` מממש רק את השורה שבידינו, נופל ל**בדיקה מלאה** מתחת ל-501 (סירוב לדגום אינו טענה על תקן) ו**זורק** מעל 1,200. סוכן התוכן הגיע לאותה מסקנה עצמאית ב-`manifest.json`. ⓒ **אין Supabase חי (T-019), ולכן הפלט הוא קובץ SQL ב-`supabase/seed/` ולא כותב רשת** — כותב רשת אינו ניתן להרצה, אינו ניתן לבדיקה, ומוסיף מסלול החלה שני לצד `supabase/migrations/` שרוי מחיל ביד ממילא. נרשם כ-TD-24 ולא הוסתר. **גם ⓓ:** `needsHumanReview` נכנס ל-`CardFace` ולא נקרא מחדש ב-React — זה לקח TD-11, וכאן הכשל חמור יותר: סימון על ה**חזית** של כרטיס הפקה אומר ללומד שה**שאלה** אינה אמינה לפני שענה. **כל משימה נושאת קוד בדיקה אמיתי ורשימת מוטציות ממוספרת** (20 מוטציות סה"כ). ⚠️ **פער שנרשם ולא הוסתר:** `sense_examples`/`sense_items`/`sense_distractors` (~2,400 שורות) **אינם** בתוכנית — הצעה ל-PM לפתוח T-050 אחרי סקירה נקייה של משימה 3. ⛔ אפס תלויות חדשות · ⛔ `docs/api-contract.md` לא נגעתי בו · ⛔ לא נטען אף סקיל עיצוב. | `docs/superpowers/plans/2026-08-12-content-lands.md` · `plan/50-tasks.md` (T-042 · T-045) · `plan/00-control.md` |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
