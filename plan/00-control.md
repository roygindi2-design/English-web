<!--
CYCLE_ID: C-0102                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # 📐 **טיק ביצוע C-0102 — משימה 6 של `2026-08-13-study-queue.md`: `/study` מתחבר לתור (`components/StudyDeckScreen.tsx`).** `/study` נשאר Server Component שקורא `?deck=` דרך `parseDeckName` (ערך לא מוכר ⇒ `due`, ⛔ לא 404); רכיב הלקוח מחזיק חמישה מצבים ואת כל הרשת. **בחירת ה-endpoint לפי החפיסה היא כל D-033** — `unknown`⇒`/api/practice`, `due`⇒`/api/review` — נמדדת בהכלת סוגריים ⛔ ולא במרחק תווים, ואומתה במוטציה. סימון שנכשל **נזרק הלאה** כדי שהכרטיס יישאר בחפיסה (המוטציה חשפה שהבדיקה הראשונה מדדה את הפונקציה הלא נכונה ⇒ הוחלפה). **אימות טרי בהודעה: typecheck ✅ · check:core `OK` ✅ · 980/980 ב-62 קבצים ✅ (מ-951/59) · build ✅ `Compiled successfully in 369ms` · check:mobile 609/609 ✅.** ⚠️ ארבע סטיות: `apiGet` מחזיר גוף במקום לזרוק (אפס קוראים נמדדו) · שומר F-027 הופנה ל-`StudyDeckScreen.tsx` · פעולת המצב הריק תלוית-חפיסה · `EXPECTED_CONSOLE` צר בהארנס (TD-29). ⚠️ חוב חדש TD-28. ⬜ הבאות: **משימות 7–8** של T-065. ⛔ לא נגעתי ב-`main`.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: "T-065"           # 🔵 **בעבודה — משימות 5–6/8 נסגרו (C-0101 · C-0102).** ⬜ נותרו: מ׳7 הבורר בלשונית `כרטיסיות` (`CardsScreen`) · מ׳8 פיקסצ'ר `/dev/deck` ל-`check:mobile`. ✅ T-064 נסגרה במלואה C-0100. ⛔ **T-066 חסומה** (D-035, תנאי ⓑ) · ⛔ T-019·T-046·T-043 בידיים אנושיות. ⛔ לעולם לא לכתוב ל-`senses.cefr_level`, **וגם לא למיין לפיו** (D-034).
CRITIC_ROUNDS_ON_TASK: 0          # תור הסקירה: T-005 · T-006 · T-018 · **T-052 + F-027 (C-0071)** · **F-033 🟣 (C-0095, נגד ה-PM)** · **T-064 מלאה 🟣 (מ׳1-4, C-0096→C-0100)** · **T-065 מ׳5 🟣 (`CardDeck`, C-0101)** · **T-065 מ׳6 🟣 (`StudyDeckScreen`, C-0102)** · ✅נסגרו C-0099: F-034·F-035·F-036
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN. ⚠️ C-0087 לא נספר כ"אין התקדמות" — הוא עצירת בלם מכוונת, לא כישלון להתקדם
LAST_HANDOFF_AT: 2026-08-13T23:01:45Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                  # C-0102 שוחררה בסוף הטיק
LOCK_AT: ""
MILESTONE_TICKS: 56                # ✅ מתחת לתקרה (60, RULES § 0.1.1 ו׳). מתאפס אך ורק ב-MILESTONE_DONE
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "e1fd933"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-13T19:27:15Z"  # ISO-8601 של הקידום האחרון ל-main (C-0098)
PROMOTIONS_THIS_MONTH: 7          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · **F-028 🟠 (apkg false-accept, רדום → DEV, C-0067)** · **F-029 🟡 (README תקציב מיושן, C-0067)** · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום) · F-028 (C-0069) · F-027 (C-0071 — שני החצאים) · **F-026 (C-0086 — D-029 מצוטטת, הקוד נבדק ולא הונח)**.**
> ✅ **חסימת הקידום שוחררה C-0092 (PM):** `main` אוחה ל-`dev` בקומיט `db71f27` — התנגשות `02-inbox.md` נפתרה (י׳/יא׳ נשמרו ושניהם טופלו באותו טיק). **אומת בהרצה: `git merge-base --is-ancestor origin/main dev` ⇒ exit 0** ⇒ `git merge --ff-only dev` יעבור אצל ה-Critic, וכ-27 מחזורים כולל תיקון F-027 יכולים לעלות לאוויר. ⚠️ **החצי שנשאר פתוח לרוי (פריט 17): ⛔ אין לערוך את `main` ישירות דרך עורך GitHub** — אחרת אותה חסימה תחזור. ⚠️ ‏`PROMOTIONS_THIS_MONTH=6` מול main — ייתכן 6 קידומים שנכשלו בשקט, לבדיקת רוי.
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0100 | DEV | CRITIC | 20:46Z | 📐 **טיק ביצוע — משימה 4 מתוך 8 של `2026-08-13-study-queue.md`: `POST /api/practice`, ואיתה T-064 נסגרת במלואה.** TDD קוים: `route.test.ts` נכתב והורץ **אדום** על `ENOENT: app/api/practice/route.ts` לפני שנכתבה שורת מימוש. **ההכרעות שנקבעו בקוד ולא בהערה:** ⓐ **ההבטחה של D-033 נאכפת בהיעדר** — חמשת שדות התזמון (`next_review_at`·`easiness`·`interval_days`·`repetition`·`consecutive_correct_recognition`) **אינם מופיעים בקובץ המסלול**, ובדיקת מקור קוראת אותו כטקסט ונועלת את ההיעדר; הנימוק שנכתב בחוזה ולא רק בקוד: לומד שמתרגל מילה קשה חמש פעמים היה מגיש ל-SM-2 חמש תשובות, מנפח את `interval_days` ודוחף שבועות קדימה **בדיוק את המילה שהוא מתקשה בה**; הרצף מוחרג מהכיוון ההפוך — הוא מקדם לייצור (7.7), ותרגול על מילה מוכרת אינו הוכחת הכרה. ⓑ **שורה חסרה ⇒ 404, ⛔ ולעולם לא `insert`** — מילה בלי שורת התקדמות מעולם לא נענתה ולכן אינה יכולה להיות בחפיסת תרגול; הוספה כאן הייתה ממציאה היסטוריית למידה מ-POST תועה, ועם עמודות התזמון בברירת המחדל, כלומר «לחזרה עכשיו». ⓒ **ה-`update` תחום בשני מפתחות** — `user_id` **וגם** `word_id`; תיחום ב-`word_id` בלבד היה כותב על כל הלומדים במאגר, ולכן זה נמדד בבדיקה ולא נסמך על עין. ⓓ סדר ההגנות `readSupabaseEnv()` → `createRouteClient(` → `getUser()` → **ורק אז** הגוף (דפוס C-0032), נמדד כ**סדר אינדקסים** ולא כנוכחות מחרוזת. **אימות טרי בהודעה הזו:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **935/935 ב-58 קבצים ✅** (מ-924/57, +11) · build ✅ `Compiled successfully in 376ms` עם `ƒ /api/practice` ברשימת הנתיבים. ⚠️ **סטייה אחת מדווחת, תוקנה בבדיקה ולא בקוד:** בדיקת דליפת `error.message` שבתוכנית היא `not.toMatch(/error\.message[\s\S]{0,40}(NextResponse|json\()/)` — **מד מרחק תווים ולא זרימת נתונים** — והיא הרשיעה קוד תקין (המחרוזת יוצאת ל-`console.error` ומיד אחריה שורת `return`, ~20 תווים). נמדד בהרצה שאותו ביטוי עובר על `app/api/study/queue/route.ts` **רק במקרה**, כי שתי שורות שאינן קשורות יושבות באמצע ומרחיקות. ⛔ ריפוד הקוד כדי לספק מד-מרחק הוא שינוי הקוד לפי הבדיקה ⇒ הוחלפה בדרישה עצמה: כל `.message` על שורת `console.error`, ואף `json(` בלי `.message`. **חזקה מהמקורית** (תופסת דליפה גם ב-200 תווים) ו**אומתה במוטציה**: הזרקת `debug: error.message` לגוף התשובה הפילה אותה, השחזור החזיר לירוק. ⛔ לא נגעתי ב-`main` · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ אפס `[skip ci]` · ⛔ אפס תלויות חדשות · ⛔ לא הומצא מסך. | `app/api/practice/route.ts` (חדש) · `app/api/practice/route.test.ts` (חדש) · `docs/api-contract.md` · `plan/30-architecture.md` · `plan/50-tasks.md` · `plan/00-control.md` · `docs/superpowers/plans/2026-08-13-study-queue.md` |
| C-0101 | DEV | CRITIC | 21:46Z | 📐 **טיק ביצוע — משימה 5 מתוך 8 של `2026-08-13-study-queue.md`: `components/CardDeck.tsx`, חפיסת הגלילה של T-065.** TDD קוים: `CardDeck.test.ts` נכתב והורץ **אדום** על `ENOENT: components/CardDeck.tsx` לפני שנכתבה שורת מימוש. **ההכרעות שנקבעו בקוד ולא בהערה:** ⓐ **«אין גלילה אחורה לכרטיס שסומן» ממומש בהסרת הצומת מה-DOM** — מאזין גלילה שמחזיר את הלומד קדימה מתנגש חזיתית עם `scroll-snap` של הדפדפן, כלומר שובר את המחווה היחידה שהמסך הזה בנוי עליה; ההסרה הופכת את הכלל לנכון **מעצם המבנה**, ולכן ⛔ אין `preventDefault` ו⛔ אין `overflow-hidden`, ושתי השלילות נאכפות בבדיקה. ⓑ **הגלילה יושבת ב-`useEffect` ולא במטפל הלחיצה** — ברגע הלחיצה הכרטיס שסומן **עדיין** ב-DOM והבא טרם זז למקום שאליו יגיע, ולכן גלילה שם נוחתת על המיקום הישן; היעד גם אינו «הראשון שנותר» אלא **הבא אחרי זה שסומן**, אחרת לומד שגלל קדימה נזרק חזרה למעלה. ⓒ **`behavior: 'auto'` ⛔ ולעולם לא `'smooth'`** — גלילה חלקה מבוקשת אימפרטיבית, ולכן `prefers-reduced-motion` ב-CSS אינו יכול לכבות אותה. ⓓ **`onGraded` שנדחה ⇒ הכרטיס נשאר במקומו וניתן לסימון שוב** — ⛔ סימון שנבלע הוא תשובה שאבדה; ההודעה ללומד שייכת למסך שמעליו (משימה 6), היחיד שיודע אם זו הרשת או הסשן. ⓔ **סימון אחד בכל רגע** — לחיצה כפולה על אותו כרטיס הייתה שולחת שתי תשובות, ובחפיסת `due` השנייה מתזמנת מילה שנענתה פעם אחת. **בדיקת D-033 מודדת הכלה ולא מרחק תווים** — זה הלקח של C-0100: היא חולצת את ענף `{deck === 'unknown'` **בהתאמת סוגריים** ודורשת שהמחרוזת «לא משנה את מועד החזרה» תהיה בפנים ו⛔ לא בחוץ, ובדיקה נפרדת מודדת את המחלץ עצמו כדי שלא יעבור בריק. **שתי מוטציות אימתו את השומרים בהרצה:** `'auto'`→`'smooth'` הפילה את שומר התנועה, ושתילת המחרוזת בענף `due` הפילה את שומר D-033 — שתיהן חזרו לירוק בשחזור. **אימות טרי בהודעה הזו:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **951/951 ב-59 קבצים ✅** (מ-935/58, +16) · build ✅ `Compiled successfully in 314ms`. ⚠️ **סטייה אחת מדווחת:** `onFinished` **לא נכתב** — בלוק ה-Props בתוכנית מונה שלושה props בדיוק, והשורה על התרוקנות הרשימה מבקשת רביעי; הוכרע לפי החתימה, והחפיסה מרנדרת בעצמה «סיימת» + קישור ל-`/cards` (בדיוק מה שאותה שורה מכתיבה), כי למשימות 6–8 אין קורא ל-`onFinished` והוא היה prop מת שאיש אינו מעביר ואף בדיקה אינה מודדת. ⛔ לא נגעתי ב-`Flashcard.tsx` · ⛔ לא הומצא מסך סיום (T-055 חסומה ב-F-032) · ⛔ לא נגעתי ב-`main` · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ אפס `[skip ci]` · ⛔ אפס תלויות חדשות · ⛔ `docs/api-contract.md` לא נגעתי — הטיק הזה אינו נוגע בנקודות קצה. | `components/CardDeck.tsx` (חדש) · `components/CardDeck.test.ts` (חדש) · `plan/30-architecture.md` · `plan/50-tasks.md` · `plan/00-control.md` · `docs/superpowers/plans/2026-08-13-study-queue.md` |
| C-0102 | DEV | CRITIC | 23:01Z | 📐 **טיק ביצוע — משימה 6 מתוך 8 של `2026-08-13-study-queue.md`: `/study` מתחבר לתור.** TDD קוים: שלושת קובצי הבדיקה נכתבו והורצו **אדומים** (7 נכשלו / 5 עברו) לפני שנכתבה שורת מימוש. **ההכרעות שנקבעו בקוד ולא בהערה:** ⓐ **בחירת ה-endpoint לפי החפיסה היא כל D-033** — `unknown` ⇒ `/api/practice`, `due` ⇒ `/api/review`; החפיסה מציגה ללומד «תרגול — לא משנה את מועד החזרה» כל אותו זמן, והחלפת שני החוטים לא הייתה שגיאת הקלדה אלא הפיכת ההבטחה שעל המסך לשקר. נמדד **בהכלת סוגריים** (לקח C-0100) ⛔ ולא במרחק תווים, ואומת במוטציה: החלפת שני המסלולים ⇒ אדום. ⓑ **סימון שנכשל נזרק הלאה** — `<CardDeck>` משאיר כרטיס בדיוק כאשר `onGraded` נדחה, ולכן בליעת השגיאה כאן הייתה מחליקה את הכרטיס מהמסך כאילו נשמר. **הבדיקה הראשונה שנכתבה לזה מדדה את `sendGrade` — שזורק ממילא — ונשארה ירוקה כשמחקתי את ה-`throw`;** היא הוחלפה במדידת בלוק ה-`catch` עצמו, ואז המוטציה הפילה אותה. ⓒ **`schema_missing` מקבל משפט משלו ⛔ ולעולם לא את המצב הריק** — «אין כרטיסיות» אומר ללומד שסיים להיום, ותקלה אצלנו שמדווחת כסיום שולחת אותו מרוצה ממוצר שבור. ⓓ **`elapsed_ms` = `Math.min(Math.round(…), MAX_ELAPSED_MS)` עם המספר מיובא** — ⛔ לא ערך שהמסלול ידחה ב-400 ו⛔ לא 600000 שנכתב מחדש. **אימות טרי בהודעה הזו:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **980/980 ב-62 קבצים ✅** (מ-951/59, +29) · build ✅ `Compiled successfully in 369ms` · **`check:mobile` 609/609 ✅** בשלושת הרוחבים. ⚠️ **ארבע סטיות מדווחות:** ⓐ **`apiGet` שונה** — הוא זרק `Error` על כל תשובה שאינה 2xx וזרק איתה את הקוד שמבדיל בין שלוש התקלות של התור; אפס קוראים נמדדו לפניו. ⓑ **שומר F-027 ב-`ActionBar.test.ts` הופנה** מ-`app/study/page.tsx` ל-`components/StudyDeckScreen.tsx` — הסרגל עבר עם המצבים שהוא שייך להם; מחיקת השורה הייתה מוציאה את `/study` מהשומר. ⓒ **הפעולה במצב הריק תלוית-חפיסה** — «התחל מילים חדשות» על חפיסת `unknown` הוא קישור למסך שהלומד עומד בו, כלומר המבוי הסתום של F-027. ⓓ **`check:mobile` נפל ותוקן בהארנס אחרי שנמדד שהכשל אינו במסך** — בלי ENV התור עונה 503 לפי חוזהו ו-Chromium רושם זאת; נוסף `EXPECTED_CONSOLE` צר לכתובת אחת ולסטטוס אחד (TD-29), ⛔ ולא פטור למסלול. ⚠️ **חוב חדש TD-28:** `elapsed_ms` נמדד מהסימון הקודם ולא מכניסת הכרטיס לתצוגה. ⛔ לא נגעתי ב-`Flashcard.tsx` ו⛔ לא ב-`CardDeck.tsx` · ⛔ לא נגעתי ב-`main` · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ אפס `[skip ci]` · ⛔ אפס תלויות חדשות · ⛔ `docs/api-contract.md` לא נגעתי — הטיק אינו משנה אף נקודת קצה. | `components/StudyDeckScreen.tsx` (חדש) · `components/StudyDeckScreen.test.ts` (חדש) · `app/study/page.tsx` · `app/study/page.test.ts` (חדש) · `lib/api/client.ts` · `lib/api/client.test.ts` (חדש) · `components/ActionBar.test.ts` · `scripts/verify-mobile.mjs` · `plan/30-architecture.md` · `plan/50-tasks.md` · `plan/00-control.md` · `docs/superpowers/plans/2026-08-13-study-queue.md` |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
