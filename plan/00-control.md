<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0075                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # **C-0076 (DEV): טיק ביצוע (◰) — משימה 5, האחרונה בתוכנית navigation-shell (T-058). התוכנית `2026-08-12-navigation-shell.md` סגורה כולה; T-051 ו-T-058 שניהם 🟣.** ⛔ הבא בתור ל-DEV: **T-003** (יתרת ה-onboarding — שדה המוסד; תוכנית מלאה ב-40-decisions § 4.2ד). ⚠️ סטייה מוצהרת אחת בשורת T-058 (השומר של התוכנית הרשיע `app/page.tsx` על הערה).
STATE: REVIEWING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-003             # ⬜ הבאה בתור — יתרת ה-onboarding (שדה המוסד), תוכנית מלאה ב-40-decisions § 4.2ד. **T-051 + T-058 🟣 — תוכנית navigation-shell נסגרה כולה C-0076.** ⛔ לעולם לא לכתוב ל-`senses.cefr_level`. ⚠️ T-007 (NGSL) עדיין ⛔ חסום ב-T-043. T-059 ⬜ תיעוד בלבד.
CRITIC_ROUNDS_ON_TASK: 0          # תור הסקירה: T-005 · T-006 · T-018 · **T-052 + F-027 (C-0071)** · **T-051 משימות 2–5 (C-0072 · C-0073 · C-0075 · C-0076) + T-058**
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN. C-0076 התקדם: תוכנית navigation-shell נסגרה (774 → 779 בדיקות)
LAST_HANDOFF_AT: 2026-08-13T02:44:59Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                  # "" | PM | DEV | CRITIC | CONTENT — שוחררה בסוף C-0076
LOCK_AT: ""
MILESTONE_TICKS: 30
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "e09bced"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-12T01:15:52Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 6          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-026 🟡 (**שאלת ה-`PSEUDOGAP!` הוכרעה C-0074 — D-029; נותר רק לסמן ✅ ב-T-059**) · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · **F-028 🟠 (apkg false-accept, רדום → DEV, C-0067)** · **F-029 🟡 (README תקציב מיושן, C-0067)** · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום) · F-028 (C-0069) · F-027 (C-0071 — שני החצאים).**
> 🚨 **קידום חסום (Critic C-0074, 01:13Z):** `main` הסתעף מ-`dev` — הקומיט `4ddf6c4` ("Update 02-inbox.md", עריכה ישירה ל-main מ-12/08) אינו ב-`dev`, ולכן `git merge --ff-only dev` נכשל וכ-26 מחזורים כולל תיקון F-027 אינם עולים לאוויר. **⛔ Critic אינו יכול לאחות** (האיחוי מתנגש ב-`02-inbox.md`, קובץ PM). **פעולה נדרשת מ-PM:** `git merge origin/main`→`dev`, פתור את התנגשות `02-inbox.md` (שמור י׳/יא׳), דחוף `dev`. אז ה-Critic יקדם. הרצה נקייה (4 פקודות ✅ · 737 בדיקות) אומתה על `dev@1f88154`. `/api/health` חי=ok על קוד C-0047. ⚠️ ‏PROMOTIONS=6 מול main ב-C-0047 → ייתכן 6 קידומים שנכשלו בשקט, לביקורת PM. פירוט: `03-for-roy` פריט 17.
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0076 | DEV | CRITIC | 02:44Z | **טיק ביצוע (◰) — משימה 5, האחרונה בתוכנית `2026-08-12-navigation-shell.md`: שתי מכולות ה-`justify-center` האחרונות (T-058).** `app/loading.tsx:10` ו-`app/error.tsx:10` עברו `flex flex-1 flex-col justify-center gap-4` → `flex flex-1 flex-col gap-4`. זה F-011, שכבר חזר פעם אחת כ-F-016, ולכן נוסף שומר סטטי. ⚠️ **הפגם המשמעותי של הטיק, והוא בקוד הבדיקה שהתוכנית עצמה כתבה:** הביטוי `/flex-1[^"'`]*justify-center/` הפיל **שלושה** קבצים ולא שניים — `app/page.tsx` נפל על **הערה** שמצטטת את הצמד verbatim (``because `flex-1 justify-center` centred``), ⛔ בלי שיש `justify-center` באף `className` שלו; אותה מלכודת יושבת גם ב-`app/sources/page.tsx` שניצל רק כי אינו ברשימה. **ההכרעה: תוקן השומר ⛔ ולא ההערה** — מחיקת שלוש מילים מהתיעוד ההיסטורי של הפגם כדי לראות ירוק היא בדיוק "התאמת הקוד לבדיקה", והערך שנמחק הוא הראיה. נוספה `markupOnly()` שמסירה הערות בלוק ו-`//` **רק בפתח שורה**, כדי ש-`https://` בתוך מחרוזת ישרוד, ובדיקה חמישית נועלת את שני הכיוונים: הערה עם הצמד ⛔ אינה נתפסת · `className` עם הצמד **כן** · URL אינו נחתך. **שתי מוטציות, כל אחת הרגה את הבדיקה בשמה:** החזרת `justify-center` ל-`loading.tsx` → `app/loading.tsx does not centre its page container` ✗ · אותה החזרה ב-`error.tsx` → הבדיקה שלו ✗. **נמדד:** `heading anchored to top` **36/36** ✅ ואפס שורות כישלון — עיגון המכולות לא הזיז אף כותרת. `check:mobile` נשאר **594** (שני הקבצים אינם מסלולים ב-`ROUTES`). TD-26 נאכף: `pkill -f '[n]ext-server'` + `pgrep -af` + `rm -rf .next` לפני המדידה. **חמש פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **779 בדיקות ✅ (מ-774, 52 קבצים)** · build ✅ · **check:mobile ✅ 594**. ⛔ אפס שינוי ב-`lib/core` · ⛔ אפס שינוי בנקודות קצה ובחוזה ה-API · ⛔ לא נגעתי ב-`main`. **עם זה נסגרה תוכנית navigation-shell כולה.** | `app/loading.tsx` · `app/error.tsx` · `scripts/verify-mobile.test.ts` · `plan/30-architecture.md` (3.1.28) · `plan/50-tasks.md` (T-051 · T-058) · `plan/60-findings.md` (F-011) |
| C-0075 | DEV | CRITIC | 01:56Z | **טיק ביצוע (◰) — משימה 4 בתוכנית `2026-08-12-navigation-shell.md`: ההארנס רואה את מעטפת הלשוניות (T-051).** ‏`TAB_ROUTES` נוסף ליד `FLOW_ROUTES`, ו-D-028 נטען עכשיו **משני הכיוונים**: מסך לשונית נבדק על `no action bar on a tab screen`, ומסך זרימה על `no tab bar on a flow screen` — הכיוון השני הוא זה שתופס את השינוי שנראה תמים בדיף. ⚠️ **סטייה נמדדת מצעד 4.3, והבדיקה החדשה היא שתפסה אותה:** התוכנית קבעה ש-`/cards` נמדד ישירות; **נמדד חי — `/cards` מחזיר 307 → `/login?expired=1`**, כי משימה 3 (C-0073) הכניסה אותו ל-`PROTECTED_SCREENS`. ההארנס היה מדפיס שלוש שורות `ok /cards` על **מסך ההתחברות** — F-027 סיבה 1, שנייה אחרי שהיא נסגרה. לכן **שלוש** פיקסטורות `/dev/tabs/{studies,cards,me}`, ו⛔שלושת המסלולים האמיתיים נשארים מחוץ ל-`ROUTES`. ⚠️ **סטייה שנייה מצעד 4.1:** "אותם רכיבים שהמסך מרנדר" **לא היו קיימים** — שלושת המסכים היו JSX פנימי בשרת-קומפוננטה מוגנת-סשן, והעתקתו לפיקסטורה היא F-027 סיבה 2 מלידה. חולצו `components/{Studies,Cards,Me}Screen.tsx`; המסך והפיקסטורה מרנדרים **את אותו הקובץ**, והפרידה הפכה לבלתי אפשרית ולא ל"ניתנת לגילוי". שומרי C-0073 עברו ל-`MeScreen.test.ts` ⛔ ואף אחד לא נמחק. ⛔ אפס שינוי במסך, בתווית, במסלול או בזרימה. **שתי מוטציות, שתיהן הרגו בדיקות בשמן:** ⓐ `<TabBar />` מ-`app/(tabs)/layout.tsx` ל-`app/layout.tsx` → **15 כשלים** (`/` · `/signup` · `/login` · `/study` · `/dev/onboarding` × 3 רוחבים) · ⓑ מחיקת לשונית `אני` מ-`TABS` → `exactly four tabs` ×9 **וגם** 3 בדיקות יחידה. ⚠️ **הפגם המשמעותי של הטיק — כמעט אימות שקרי:** `pkill -f "next start"` ⛔ **אינו הורג את השרת** (Next משנה שם ל-`next-server`), הוא נשאר על פורט 3000, וההארנס מאמץ שרת חי — כלומר **שלוש הרצות `check:mobile` מדדו בנייה ישנה** והדפיסו כישלונות על קובץ שלא היה בה. נתפס כי המדידה לא הסתדרה, לא כי משהו נכשל. תועד TD-26; כל המספרים נמדדו אחרי `pkill -f next-server` + `rm -rf .next` + בנייה נקייה. **חמש פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **774 בדיקות ✅ (מ-737, 52 קבצים)** · build ✅ · **check:mobile ✅ 594 (מ-489)**. ⛔ אפס שינוי ב-`lib/core` · ⛔ אפס שינוי בנקודות קצה ובחוזה ה-API · ⛔ לא נטען אף סקיל עיצוב · ⛔ לא נגעתי ב-`main`. | `app/dev/tabs/{layout,studies/page,cards/page,me/page}.tsx` · `components/{Studies,Cards,Me}Screen.tsx` · `components/{Me,Studies}Screen.test.ts` · `app/(tabs)/{studies,cards,me}/page.tsx` · `app/(tabs)/me/page.test.ts` · `scripts/verify-mobile.mjs` · `scripts/verify-mobile.test.ts` · `plan/30-architecture.md` (3.1.27 · TD-26) · `plan/50-tasks.md` (T-051) |
| C-0074 | PM | DEV | 01:14Z | **טיק תכנון (📐) — שער הטריאז' החזיר 📐 ולא 🔬: ארבעת ה-BLOCKER הפתוחים אינם מחקר. R-005/R-006 תלויים במדידות T-013/T-016/T-018 שכל הקוד שלהן כבר 🟣 בביקורת, ו-R-010/R-013 הם חסימות רישוי קבועות (מאל"ו · AnkiWeb) שאין להן מה להיפתר.** ⛔ אפס מחקר רשת, אפס סוכני משנה, ⛔ לא נפתח `10-pedagogy`. **שני תוצרים. ⓐ § 4.2ד — תוכנית UX ל-T-003**, שהייתה ⛔ חסומה עליי במפורש. **התחולה צומצמה למה שבאמת נשאר:** דקות/תאריך/ציון נבנו ב-T-029, ומה שנותר מהכותרת הוא **שדה המוסד** לבדו. **ההכרעה היחידה שיש בה שיניים: טקסט חופשי, ⛔ לא רשימה נפתחת** — רשימת מוסדות עם ספי פטור היא טענה פדגוגית, ו-A7 עצמו (‏nite.org.il, דרגה א׳) קובע ש**אין סף ארצי אחיד**, כלומר רשימה שאמציא היא חלק 5 בדיוק. ⛔ ולא `<LatinField>`: שם מוסד נכתב עברית, ו-TD-5 חל על קלט לטיני. שש השאלות רצו, ושתיים מהן נענו **"כלום"** בכוונה — השדה אינו מזיז מדד ואינו נותן רווח, ובדיוק לכן הוא רשות ואחרון. **ⓑ D-029 — סיווג `PSEUDOGAP!`**, השאלה שהמתינה ל-PM מאז C-0052: שורה הנושאת את שני הסימנים אינה נטענת, כי `GAP` מכחיש שיש תוכן ו-`!` רק מתאר תוכן — וטעינתה הייתה מחייבת להחליט מה משמעות הגוף העברי של שורת GAP, ⛔ בלי שום מקור שמגדיר `PSEUDOGAP`. ⚠️ **הפגם המשמעותי של הטיק נתפס אצלי לפני הכתיבה, ורק בגלל `verification-before-completion`:** כתבתי ל-50-tasks משימת קוד T-059 ("יישם את D-029 ב-`lexicon.ts`") **על סמך הנחה שלא בדקתי** — ואז המדידה הראתה ש-`lib/core/lexicon.ts:98–105` כבר מפיל `pseudo_gap` בנפרד, `lexicon.test.ts:99` כבר נועל אותו, וההערה בקוד אומרת מפורשות שההפרדה נשמרה *"for the PM to rule on it"*. **המשימה הייתה טיק Dev שלם שאינו משנה שורה.** נכתבה מחדש כתיעוד בלבד. ⚠️ **חריגות שנרשמות ואינן מוסתרות:** `15-syllabus-digest` עומד על **150/150** שורות — הטיק הבא של PM חייב לגזום לפני שהוא מוסיף · פריט 13 (ביקורת Netlify) עדיין פתוח מ-12/08 ו⛔לא שוכפל · שכבה 2 בחזון נסרקה: שלושת פריטיה מכוסים ב-T-047/T-048/T-049 ואין פער · `02-inbox.md` ריק. ⛔ אפס נגיעה ב-`30-architecture` · `60-findings` · קוד · `main`. | `plan/40-decisions.md` (§ 4.2ד · D-029) · `plan/15-syllabus-digest.md` (D-029) · `plan/50-tasks.md` (T-003 🟢 · T-059) · `plan/03-for-roy.md` (16) · `plan/00-control.md` |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
