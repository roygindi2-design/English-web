<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0037                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # PM | DEV | CRITIC | HUMAN — **C-0037 (DEV) היה טיק ביצוע: משימה 2 מתוך 4 בתוכנית `2026-08-07-review-engine.md` — `lib/core/progress.ts` (צבירת התקדמות + שני שדות D-010) + 14 בדיקות.** ארבע פקודות רצו טרי: typecheck ✅ · check:core ✅ · **424 בדיקות ✅ (מ-410, 26 קבצים)** · build ✅. שתי מוטציות רצו ונפלו כצפוי. **ACTIVE_TASK_ID נשאר T-005 — הטיק הבא של DEV הוא משימה 3 (`lib/core/levelGate.ts`, מנוע 7.7 / T-006).** ⚠️ **חוב סקירת העומק גדל ואינו זז: C-0029 (T-015) · C-0031 · C-0032 · C-0033 · C-0034 (T-029) · C-0036 · C-0037 (T-005/משימות 1–2).** ⚠️ **T-020 של ה-PM נשארת דחופה** (סוגרת F-021, משחררת T-018). חסמים אנושיים: T-043 (קובצי המקור) · P-001 (תוכן ל-T-034). מצב C-0037 המלא: היומן למטה.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-005             # **משימות 1–2 מתוך 4 בתוכנית `2026-08-07-review-engine.md` הושלמו** (`scheduler.ts` ב-C-0036 · `progress.ts` ב-C-0037). הטיק הבא של DEV מבצע את משימה 3 (`lib/core/levelGate.ts` — שער עליית רמה, מנוע 7.7). T-029 הושלמה (C-0031..C-0034) וממתינה לסקירה
CRITIC_ROUNDS_ON_TASK: 0          # T-015 · T-029/משימות 1–4 · T-005/משימות 1–2 טרם נסקרו
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-07T18:40:43Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה. F-021 היא הכרעת PM, לא הכרעת רוי, ואינה חוסמת את המדידה (המדיניות היא נתון ולא לוגיקה).
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."

# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                    # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: ""
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "6806e68"  # נסקר C-0031: מהיר. ארבע פקודות עברו טרי. main==dev אחרי קידום #4 — הכל אצל המשתמש

# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T13:09:15Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 4          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: **F-021 🟠 (חדש C-0020 — נגד ה-PM: 1.7.1 חסר מהתמצית + סתירת כללי סינון; חוסמת את T-018)** · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת, T-034) · F-008 🟡 (חסום ב-T-025) · R-010 (אסור להעתיק פריטים ממאל"ו). **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0037 | DEV | CRITIC | 18:40Z | **טיק ביצוע — משימה 2 מתוך 4 בתוכנית `2026-08-07-review-engine.md`. חצי הצבירה של T-005 סגור.** `lib/core/progress.ts` (טהור) מחזיק `WordProgress` כ**שורה אחת לכל (משתמש, מילה) ולא שורה לכל אירוע חזרה** — ‏`0003_provenance_telemetry.sql` מתעד את W4 (השכבה החינמית של Supabase) כסיבה, וכל מה שהשער 7.7 צריך נגזר מהצבירה. `applyGrade()` מקבל את `answeredAtMs` מהקורא ואינו קורא שעון. **ארבע ההכרעות של התכנון עמדו במבחן המימוש ואף אחת לא נדחפה לזמן הכתיבה:** ⓐ שני שדות D-010 בשמם המדויק (`time_to_first_correct` · `attempts_to_mastery`), שניהם `null` עד שהאירוע קרה — ‏`0` הוא מדידה ולא "לא ידוע", בדיוק מה שהמיגרציה של C-0029 כבר קובעת; ⓑ שניהם נכתבים **פעם אחת ואינם נדרסים**; ⓒ `consecutiveCorrectRecognition` נספר על כרטיסי `recognition` בלבד — זו ההגדרה המילולית של `MasteryState` ב-`flashcard.ts`, ובלעדיה `directionFor` היה מקדם מילה לייצור על סמך תשובות ייצור; ⓓ `masteryConsecutiveCorrect` הוא פרמטר מוצר חובה בלי ברירת מחדל, דפוס `promoteAfterConsecutiveCorrect`. **`isExposed` הוא `attempts >= 1` — "נחשף" אינו "שלט" (7.7):** תשובה שגויה יחידה היא חשיפה, וזו הפונקציה היחידה ששער משימה 3 יצרוך, ולכן ההבחנה חייבת לחיות כאן ולא בשער. **שתי מוטציות רצו, לא שתי טענות:** ⓐ הפיכת `time_to_first_correct` לדריס → `is never overwritten by a later correct answer` נופלת עם `expected 900000 to be 12000` — מדד שנדרס מפסיק להיות "הזמן לנכונה הראשונה" והופך למשהו אחר לגמרי תחת אותו שם של D-010; ⓑ החלפת `attempts` ב-`consecutiveCorrectRecognition` בשדה `attempts_to_mastery` → `records the TOTAL attempts at the moment mastery is reached` נופלת עם `expected 3 to be 6` — ספירת הרצף המנצח בלבד הייתה מדווחת 3 על מילה שלקחה 30 ניסיונות. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 424 בדיקות ✅ (מ-410, 26 קבצים) · build ✅ `Compiled successfully in 286ms`.** ⛔ אפס תלויות חדשות · ⛔ אין שינוי בנקודות קצה ולכן `docs/api-contract.md` לא נגע · ⛔ אין מסך בטיק הזה (התוכנית מצהירה על כך במפורש). נותרו בתוכנית שתי משימות: `levelGate.ts` (7.7) · `0005_review_state.sql` + `POST /api/review`. | `lib/core/progress.ts` (חדש) · `lib/core/progress.test.ts` (חדש, 14 בדיקות) · `plan/30-architecture.md` (§ 3.1.7) · `plan/50-tasks.md` (T-005 🟣) |
| C-0036 | DEV | CRITIC | 17:40Z | **טיק ביצוע — משימה 1 מתוך 4 בתוכנית `2026-08-07-review-engine.md`. מנוע 7.1 סגור.** `lib/core/scheduler.ts` (טהור) מממש SM-2 מילה במילה עם דחיסה לעבר תאריך המבחן: `EF' = EF + (0.1 − (5−q)(0.08 + (5−q)0.02))` ברצפת `1.3` · `I(1)=1` · `I(2)=6` · `I(n)=ceil(I(n−1)×EF)`. **ההכרעות של התכנון עמדו במבחן המימוש ואף אחת לא נדחפה לזמן הכתיבה:** `good→q=4` ו-`again→q=2` כקבועים בשם · נפילה מאפסת את הרצף ל-`I(1)` ו**שומרת** את ה-`EF` שנצבר · `preExamGapDays = clamp(round(0.10×ימים_עד_המבחן),1,14)` נגזר ואינו קבוע (Cepeda 2008) · `triageMinUsableDays` הוא פרמטר מוצר חובה בלי ברירת מחדל, ולכן `usableHorizon = daysUntilExam − preExamGapDays` וטריאז' הוא `usableHorizon < triageMinUsableDays`. **מבחן שעבר חוזר ל-`classic` ואינו טריאז'** — בלי הכלל הזה לומד שהמבחן שלו מאחוריו נתקע בטריאז' לנצח. `addDaysIso` עוגן ל-UTC (לקח `daysUntilExam`): בדיקה חוצה את מעבר שעון הקיץ הישראלי `2026-03-26 +2 → 2026-03-28` ואינה מאבדת יום. **שלוש מוטציות רצו, לא שלוש טענות:** ⓐ איפוס ה-`EF` בנפילה → `expected 2.5 to be close to 2.04`; ⓑ קיבוע `preExamGapDays` ל-`3` → שלוש נפילות (`expected 3 to be 6` · `to be 1` · `to be 14`); ⓒ מחיקת ההערה `// HEURISTIC: unstudied` → `carries the HEURISTIC marker` נופלת. המוטציה השלישית היא המחסום שהתכנון הוסיף במיוחד: 7.1 מתיר את הדחיסה האגרסיבית למילים קשות (`DIFFICULT_EASINESS_CEILING = 1.8` · `DIFFICULT_WORD_COMPRESSION = 0.5`) **אך ורק** בתנאי שהיא מסומנת בקוד כלא-נחקרת, וזו דרישת מפרט שאחרת אינה נאכפת בשום מקום. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 410 בדיקות ✅ (מ-383, 25 קבצים) · build ✅ `Compiled successfully in 333ms`.** ⛔ אפס תלויות חדשות · ⛔ אין שינוי בנקודות קצה ולכן `docs/api-contract.md` לא נגע · ⛔ אין מסך בטיק הזה (התוכנית מצהירה על כך במפורש). נותרו בתוכנית שלוש משימות: `progress.ts` · `levelGate.ts` · `0005_review_state.sql` + `POST /api/review`. | `lib/core/scheduler.ts` (חדש) · `lib/core/scheduler.test.ts` (חדש, 27 בדיקות) · `plan/30-architecture.md` (§ 3.1.6) · `plan/50-tasks.md` (T-005 🟣) |
| C-0035 | DEV | DEV | 16:46Z | **טיק תכנון — אפס קוד, אפס תלויות, אפס נגיעה ב-`app/` או ב-`lib/`.** התוכנית של T-029 מוצתה ב-C-0034, ולכן הטיק הזה כתב את `docs/superpowers/plans/2026-08-07-review-engine.md` — **תוכנית אחת שמכסה שתי משימות תור, T-005 ו-T-006**, בארבע משימות מימוש: ⓐ `lib/core/scheduler.ts` (מנוע 7.1) · ⓑ `lib/core/progress.ts` (צבירת ההתקדמות + שני שדות D-010) · ⓒ `lib/core/levelGate.ts` (מנוע 7.7) · ⓓ `0005_review_state.sql` + `POST /api/review` (התמדה ואכיפה בצד השרת, כפי ש-7.7 דורש). **ארבע החלטות הוכרעו בתכנון ולא יישארו לזמן הכתיבה, כל אחת עם נימוק:** ⓐ **מיפוי הדירוג הבינארי ל-`q` של SM-2** — `good→4` ולא 5 (כפתור בינארי אינו יכול לטעון "היזכרות מושלמת בלי היסוס"), `again→2` ולא 0 (עונש ה-EF ב-q=0 הוא ‎−0.80 מול ‎−0.32 ב-q=2, ו"לא ידעתי" בחשיפה ראשונה אינו ראיה לקושי מקסימלי; שניהם <3 ולכן מאפסים את הרצף באופן זהה). ⓑ **מצב הטריאז' נגזר מהנוסחה ולא מקבוע מומצא:** `usableHorizon = daysUntilExam − clamp(0.10×daysUntilExam,1,14)`, והרוחב עצמו הוא `triageMinUsableDays` — **פרמטר מוצר חובה בלי ברירת מחדל**, בדיוק הדפוס של `promoteAfterConsecutiveCorrect` ב-`flashcard.ts`. מספר שאין לו מקור לא ייכתב בקוד וייוצג כאילו נמדד. ⓒ **מבחן שעבר אינו טריאז' אלא היעדר מבחן** — בלי הכלל הזה לומד שהמבחן שלו מאחוריו נתקע בטריאז' לנצח וכל מילה חוזרת כל יום. ⓓ **בעקיפת השער `exposure_pct` ממשיך לדווח את האמת** — אחוז שמזייף 100 כדי להצדיק את הפתיחה מפסיק להיות מדד. **שני לקחים מוכרים נצרבו בתוכנית מראש ולא ייגלו שוב בביצוע:** בדיקת המיגרציה מפשיטה הערות שורה לפני הסריקה, ושתי מגבלות ה-`check` מוצהרות **בשם** בתוך `do $$` — ‏`add column ... check` מדולג כולו כשהעמודה קיימת (שני הלקחים של C-0032). בנוסף, 7.1 דורש שההיוריסטיקה "מילים קשות מקבלות דחיסה אגרסיבית יותר" תסומן בקוד; התוכנית מוסיפה **בדיקה סורקת-מקור** שנופלת אם `// HEURISTIC: unstudied` נעלם — דרישת מפרט שאחרת אינה נאכפת בשום מקום. התוכנית כוללת ~60 בדיקות בקוד מלא, שמונה מוטציות מוגדרות מראש עם הכשל הצפוי המדויק, ומדור מפורש של **מה שהיא אינה מכסה** (אין מסך · אין קריאת תור · המיגרציה לא תרוץ מול Supabase חי בלופ). ⛔ בטיק הזה לא הורצה שום פקודת אימות, ולא נטענה שום טענת הצלחה — אין קוד לאמת. | `docs/superpowers/plans/2026-08-07-review-engine.md` (חדש) · `plan/00-control.md` · `plan/50-tasks.md` |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
