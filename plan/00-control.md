<!--
NEXT_AGENT: CRITIC                 # ▶️ C-0417 (DEV) — `T-248` (loop · תשתית) נבנתה במלואה: דגל «הזרימה הפעילה מוצתה» מצטמצם ל-`ACTIVE_WORKSTREAM` בפועל, verify ירוק טרי (3318/3318). T-238 (🟣 מ-C-0414) עדיין ממתינה ל-QA, ⛔ לא נגעתי. פירוט ⇒ § 0.1.
STATE: BUILD                      # ▶️ C-0409 (CRITIC) — הביקורת נסגרה, המוקד חוזר לבנייה. פירוט ⇒ § 0.1.
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-238             # ▶️ 03/09 · C-0414 (DEV) — ⓑ נבנתה לפי `D-183`; ⓐ+ⓑ נוחתים יחד, verify ירוק, 🟣 בתור QA. ⛔ DEV לא מפנה את השדה — זה PM/QA.
CRITIC_ROUNDS_ON_TASK: 0          # ⛔ התור הישן הועבר ל-`plan/archive/control-log.md` (26/08) — הוא היה מת: QA הופך 🟣⇢✅ בכמות מ-`git log`.
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-09-04T00:53:55Z
HUMAN_DECISION_REQUIRED: false    # ▶️ ⛔ אינו ממתין. **44 · 45 · 46 נסגרו כולם ב-23/08.** נותר **47** — שורה ב-`RULES § 0.1 ב׳` שהיא הקובץ של רוי, ו⛔ **אינה חוסמת את הקידום של היום**.
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: "PM"                # ננעלה C-0418 (PM) 01:06Z — טיק תכנון.
LOCK_AT: "2026-09-04T01:06:00Z"
WORKSTREAM_TICKS:                 # ⚠️ בלם 8 שוכתב 23/08 (רוי): סופר **טיקי עבודה בלבד** — טיק שהסתיים בקומיט. ⛔ טיק שקט/נסיגה/שורה-אחת אינו נספר. תקרה **120 לכל פריט** ב-36-video-spec § 13, ⛔ לא לחזון כולו.
#   story:  13 / 120           # § 13-1 · **מוצתה (⬜=0) · הוזזה→`nav` ב-C-0310 (QA).** הפרוסות A/B/C נמסרו (T-185…T-188 · T-202/203 · T-150). שלוש החותמות (36§13.1) — ראה SEALS למטה.
#   nav:     3 / 120           # § 13-2 · **⬜ = 0 · נחתמה והמוקד הוזז ל-`cards` ב-C-0316 (QA).** פרוסה A ב-C-0314 (T-204 · T-205 · T-206 · T-174 + T-147 · T-148); **T-146 נסגרה כולה ב-C-0315** (F-137 ✅). כל השורות מוזגו ל-`dev` ✅ ב-C-0316. חותמות בבלוק SEALS.
#   cards:   9 / 120           # § 13-3 · **מוצתה · הוזזה→`arena` ב-C-0322.** ⛔ אינה חתומה. ▶️ **C-0378 (DEV, ‏`ACTIVE_TASK_ID` חריג · D-171): `T-225` נבנתה במלואה** (ⓐ–ⓕ) ⇒ 🟣, `F-140` ו-`F-102` ✅ נסגרו. **נותרו `F-143` (חוסם את `T-199`ⓐ — ⛔ אין מסך שמקבל `deck=sentences`, `lib/core/deck.ts:47`) ו-`F-144` (⛔ אינו חוסם).** פירוט ⇒ `plan/61-deferred.md` · `plan/archive/control-log.md`. ▶️ C-0414 (DEV, חריג): `T-238`ⓑ נבנתה (`D-183`) ⇒ 🟣.
#   arena:  19 / 120           # § 13-4 · **מוזזת→`studies` ב-C-0367 (QA), חסומה-חיצונית.** ⬜ אחת (`T-220`) חסומה ב-F-164/PM ⇒ אפס שורות בנות-בנייה. `T-153`·`T-181`·`T-219` 🟣⇢✅ ב-C-0367. חותמות ⇒ בלוק SEALS · שארית ⇒ `plan/61-deferred.md`. ▶️ **C-0411 (DEV): `T-231` נבנתה תחת חריג ⓐ (🔴 ממצא-מוצר), ⛔ לא דרך הזרימה הפעילה `general`** — נספרת כאן כי תגה `arena`, לא כפריצת חסימה. ▶️ **C-0413 (DEV, חריג ⓐ): `T-239` נבנתה תחת אותו חריג — הקומיט (`46a9c63`) לא נספר בזמנו כי הרגיסטר ננעל אצל PM (C-0412); הספירה תוקנה עכשיו ב-C-0415, ⛔ ולא בוצע קומיט קוד נוסף.**
#   studies: 3 / 120           # § 13-5 · לימודים כמכולת מסלולים. `T-144` תוקנה (`D-176`), הבנייה עברה ל-`T-246`/`T-247`. ▶️ **C-0381: `T-246` נבנתה במלואה ⇒ 🟣** (`verify` ירוק) — `F-178`/`F-179` פתוחים, ⛔ אינם חוסמים. ⬜ פנויה **1** (`T-144` עצמה — גזרה בלבד; `T-247` חסומה ב-`T-246` עד `dev`).
#   msgs:    1 / 120           # 39-messages-spec § 9 · הודעות — T-190…T-193 בתור. ⛔ פריטים 2–5 חסומים ב-R-026
MILESTONE_TICKS: 103           # ⛔ מונה M0 הישן — מוקפא, ⛔ ואינו בלם. הבלם החי הוא WORKSTREAM_TICKS
RELEASE_READY: "8bee299 · 2026-09-03 · 135 commits לפני main (נמדד: ./scripts/g rev-list --count origin/main..origin/dev) · טבעת העולם 8→9 צמתים + אמירנט (T-252) + פעולה ראשית אמיתית ב'אני' (T-145) + תיקון כשל כתיבה שקט ב-word_progress (T-238ⓐ) + משתמש בדיקה ל-dev (T-113) + קו-בסיס מסע (T-227)"  # C-0409 (QA) · verify ירוק טרי (3279/3279 · check:mobile 1208) · הליכה חיה ב-375×780 על שני המסכים שהשתנו, אפס ⬜ פתוח ⛔ הופך אותה לחסימה. ⚠️ 135 מתקרב לבלם הרך (150) — עדיין לא הגיע.
PAUSED_BY_HUMAN: false           # ⚠️ הבלם בודק `== true` בלבד. ⚠️ **ההערה «כל שבע המשימות כבויות מ-31/08» מיושנת** — נמדד ב-`git log` בטיק C-0385: טיקי DEV (`C-0381` 01/09 · `C-0383` 02/09 00:16Z) ו-PM (`C-0384` 00:53Z · `C-0385`) ירו כסדרם. רקע ⇒ `plan/archive/control-log.md`
DEPLOYS_THIS_MONTH: 5            # PR #2 built and deployed; smoke test green.
LAST_DEPLOYED_AT: "2026-08-23T11:03:26Z"
LAST_REVIEWED_COMMIT: "fb808a4"  # main אחרי PR #2. ⚠️ מיזוג דרך ה-UI מפצל ענפים — פירוט ⇒ `plan/archive/control-log.md`
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1 ---
WORKING_BRANCH: work/current      # ▶️ **שונה 24/08 · RULES § 0.23 · שלב 2.** DEV ו-CONTENT דוחפים לכאן בלבד. ⛔ שם קבוע, ⛔ לעולם לא שם חדש.
MERGE_TARGET: dev                 # רק QA ממזג לכאן, ורק ב-`merge --ff-only`. ⛔ אף סוכן ⛔ אינו דוחף ל-dev ישירות.
ACTIVE_WORKSTREAM: general         # 🔴 ▶️ **C-0389 (PM · `D-179` · הרשאת `D-174`): הוזז `studies` ⇢ `general`.** ‏`studies` **מוצתה** — נמדד בטיק: ⬜=0 (`T-144` נסגרה `⛔` ב-C-0387 · `T-246` 🟣 ממתינה ל-QA · `T-247` חסומה ב-`T-246` עד `dev`), ו-`loop:health` **בדיקה 11 FAIL**. ⇒ תחת `general` כשירות **`general` ∪ `loop` ∪ `base`** (`DEV.md` STEP 2) = **29 ⬜** שאף טיק DEV ⛔ לא יכול היה לגעת בהן, ובהן `T-235`, `T-143` 🔴 («⛔ אין דרך לענות על שאלה בשיעור» — נמדד חי 02/09: `/dev/lesson` ⇒ 591 תווים · `taps=1`) ו-`T-145`. ⚠️ **⛔ זו ⛔ אינה חותמת ו⛔ אינה מעבר לזרימת פיצ׳ר** — החזרה ל-`msgs` (4 ⬜, הדגל ב-`docs/plan-open.md` נוקב בה) היא של QA לבדו (§ 0.23ז).
PREV_WORKSTREAM: "studies"     # 🆕 D-174 · נכתב ב-C-0389 באותה עריכה שהזיזה את המוקד ל-`general` — בדיקות 13·14 מודדות ממנו. הזרימה שממנה זזנו: `studies`, אחרי שפרוסה A שלה (`T-246`) נבנתה במלואה ב-C-0381.
IMPROVE_TARGET: ""              # 🩺 D-146 · **ריק = המצב כבוי.** ▶️ **C-0412: נוסה ו⛔ לא ניתן להדליק — נמדד.** `story` ⇒ בדיקה 14 FAIL (3 ⬜ מראש, תקרה 2) · `nav` ⇒ ⛔ אין לה דבר ב-`61-deferred` · `cards`/`arena` ⛔ אינן חתומות. פירוט ⇒ `D-184`.
# (SEALS · nav ו-story — הוצאו ל-`plan/archive/control-log.md` ב-C-0318, אותו דפוס בדיוק שבו story הוצא ב-C-0316 כשהמוקד זז. ⛔ לא נמחקו.)
# (SEALS · arena — הוצאו ל-`plan/archive/control-log.md` ב-C-0368, אותו דפוס שבו nav · story · cards הוצאו ב-C-0318 ו-C-0367. **arena ⛔ אינה חתומה** — שלוש החותמות ⛔ לא ניתנות למדידה מהלופ, `03-for-roy` פריט 77. ⛔ לא נמחקו.)
# (SEALS · cards — הוצאו ל-`plan/archive/control-log.md` ב-C-0367, אותו דפוס שבו nav ו-story הוצאו ב-C-0318. **cards ⛔ אינה חתומה** — הוזזה חסומה-חיצונית · F-142/F-143 · ראה `plan/61-deferred.md`.)
RELEASE_BLOCKERS: ""              # QA כותב כאן כשמיזוג נחסם — **המינימום שנדרש כדי למזג**, ⛔ ולא כל הממצאים. DEV לוקח אותה לפני כל דבר אחר.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-23T11:03:26Z"  # קודם בידי רוי ידנית (PR #2), ⛔ לא בידי ה-Critic. פירוט ⇒ `plan/archive/control-log.md`
PROMOTIONS_THIS_MONTH: 13         # 13 this month. ⛔ Credit budget is no longer a reason to delay (D-086).
```

> 🧑‍⚖️ שתי ביקורות ידניות של רוי — **הפירוט המלא, כולל מצב כל ממצא, ב-`plan/OPERATOR-LOG.md`.**
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.7). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.

### 0.1 יומן העברות מקל — 2 האחרונים בלבד

> ⚠️ **שורה אחת ביומן, ⛔ ולא שתיים** — התקרה נפרצה פעמיים כך (C-0261 · C-0293). שתי המדידות ⇢ `plan/archive/handoff-log.md`.

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.
> 🧹 `C-0409` הועבר 03/09/2026 (C-0411, שתי שורות בלבד) · `C-0412` הועבר 03/09/2026 (C-0415, שתי שורות בלבד) · `C-0414` הועבר 03/09/2026 (C-0416, שתי שורות בלבד) · `C-0415` הועבר 04/09/2026 (C-0417, שתי שורות בלבד) — פירוט המדידה שם.


| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0416 | DEV | CRITIC | 2026-09-03T22:54:17Z | 🧹 **`T-159` (base · מבנה), שני חצאים, שניהם נמדדו מחדש ⛔ ולא הועתקו.** ⓐ כבר נכון בקוד: `AppGrid.tsx` מרנדר אריח מושבת כ-`<button aria-disabled>` בלי `href`, מכוסה ב-`AppGrid.test.ts:60`. ⓑ נוספה בדיקת רגרסיה ב-`worldApps.test.ts` שסורקת `app/**/page.tsx` (מדלגת קבוצות-מסלול) ומוודאת שלכל `WORLD_APP_HREF` יש עמוד. **הנחת הפתיחה מיושנת:** `app/(tabs)/world/story/page.tsx` קיים (T-186), הבדיקה עוברת היום ⛔ ולא נכשלת בשם. `npm run verify` ירוק טרי (`test` 3314/3314 · `check:mobile` 1208 checks) · `generate-map` רץ (נגע ב-`lib/`) ⛔ אפס דלתא · `measure:plan` רץ. ⛔ אינו UI. `ACTIVE_TASK_ID` (`T-238`) לא נגעתי. | `T-159` 🟣 |
| C-0417 | DEV | CRITIC | 2026-09-04T00:44:03Z | 🔨 **טיק בנייה — `T-248` (loop · תשתית, `general` fan-out), נבנתה במלואה, TDD.** דגל «הזרימה הפעילה מוצתה» ב-`scripts/measure-plan-tables.mjs` הטיל את ה-`🔴` המחייב על **כל** זרימה עם `spent > 0` ו-0 פתוחות — **גם `nav` וגם `studies`, אף ששתיהן חתומות ומאחור ו-`ACTIVE_WORKSTREAM` הוא `general`.** נמדד לפני התיקון: שתיהן אכן הטילו את ה-`🔴` ב-`docs/plan-open.md` החי. ⓐ הדגל קורא עכשיו את `ACTIVE_WORKSTREAM` מ-`plan/00-control.md` (רגקס חדש) ומצטמצם ל-`here === ACTIVE_WORKSTREAM`. ⓑ זרימה מוצתה שאינה הפעילה מקבלת `ℹ️` נפרד — «מוצתה ⛔ ואינה הפעילה ⇒ ⛔ אין פעולה» — ⛔ ולא הטלה על QA. ⓒ ארבע בדיקות TDD חדשות (24/24 ירוק), מבודדות דרך `PLAN_CONTROL_FILE` — דריסת סביבה חדשה, אותו דפוס כמו `PLAN_TABLES_OUT`/`PLAN_OPEN_OUT`. `npm run verify` ירוק טרי (`test` 3318/3318 · `check:mobile` 1208 checks · `check:rules` 328/41/0 · build ok, exit 0). `measure:plan` רץ; `docs/plan-tables.md` זהה בייט-לבייט (הדגלים חיים רק ב-`plan-open.md`), `docs/plan-open.md` נדחף באותו קומיט — נמדד אחרי: `nav`/`studies` כעת `ℹ️` בלבד, אפס `🔴` שגוי. ⛔ `generate-map` לא רץ — הטיק לא נגע ב-`app/`/`components/`/`lib/` (נבדק `git status`, אפס דלתא). `ACTIVE_TASK_ID` (`T-238`, כבר 🟣 בתור QA) ⛔ לא נגעתי. | `T-248` 🟣 |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
