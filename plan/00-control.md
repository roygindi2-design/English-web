<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0025                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: PM                    # PM | DEV | CRITIC | HUMAN — CRITIC C-0024 היה **טיק מהיר** (07:05Z, הרצה מתוזמנת): 4 פקודות רצו טרי (typecheck ✅ · check:core ✅ `/lib/core purity: OK` · 313 בדיקות ✅ · build ✅ 267ms). **קידום #3 dev→main בוצע — main==dev, כל הקוד אצל המשתמש.** נסגרו ✅: T-026 (C-0018) · T-030 (C-0005) · T-031 (C-0006) — כולן עברו מבחן שלוש-השאלות. אין ממצא חוסם: F-021/F-006 תיעוד/תהליך · F-012 טענת המרה (לא-חוסם) · F-019 קובץ חיצוני שאינו נקלט · F-008 🟡 · F-020 סגור. **ל-DEV אין עבודה כשירה במסלול הקריטי** (T-013/T-016/T-023 חסומות ב-T-043, פעולת רוי). **ל-PM עבודה דחופה: T-020** (השלמת התמצית — 1.6/1.7/1.7.1 + סנכרון טבלת המחקר) **שסוגרת את F-021 ומשחררת את T-018.** נותרו 🟣 בתור ל-CRITIC בטיק הבא: T-001/T-002/T-027 (מכסת 3/טיק מוצתה). חסמים אנושיים: T-043 (קובצי המקור) · P-001 (תוכן ל-T-034).
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: ""                # ⛔ **אין משימה פעילה ל-DEV.** תוכנית coverage-measurement מוצתה: 3/3 המשימות שנכתבו בה בוצעו. T-013/T-016 נשארות 🟣 בלי מספר עד T-043 (פעולת רוי), ו-T-018 בלי תוכנית עד ש-F-021 נסגרת. הטיק הבא של DEV הוא **טיק תכנון** למשימה אחרת, או טיק שקט
CRITIC_ROUNDS_ON_TASK: 1          # סבב הביקורת הידני של רוי נספר כסבב 1 על T-001+T-002
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-07T07:11:00Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה. F-021 היא הכרעת PM, לא הכרעת רוי, ואינה חוסמת את המדידה (המדיניות היא נתון ולא לוגיקה).
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."

# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                  # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: ""
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "4264114"  # נסקר C-0024: מהיר. ארבע פקודות עברו טרי. main==dev אחרי קידום #3 — הכל אצל המשתמש

# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T07:11:00Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 3          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: **F-021 🟠 (חדש C-0020 — נגד ה-PM: 1.7.1 חסר מהתמצית + סתירת כללי סינון; חוסמת את T-018)** · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת, T-034) · F-008 🟡 (חסום ב-T-025) · R-010 (אסור להעתיק פריטים ממאל"ו). **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0025 | CRITIC | PM | 07:11Z | **טיק מהיר (07:05Z, הרצה מתוזמנת).** ארבע פקודות רצו טרי: typecheck ✅ · check:core ✅ (`/lib/core purity: OK`) · **313 בדיקות ✅** · build ✅ (`Compiled successfully in 267ms`). נסגרו ✅ שלוש משימות דרך מבחן שלוש-השאלות: **T-026** (C-0018 — plan עודכן, `auth.test.ts`+36, core נקי) · **T-030** (C-0005 — plan עודכן, `auth.test.ts`+38) · **T-031** (C-0006 — plan עודכן, `queue.test.ts` 141ש'). אין ממצא חוסם (F-021/F-006 תיעוד · F-012 המרה · F-019 קובץ חיצוני לא-נקלט · F-020 סגור). **קידום #3 dev→main בוצע בהצלחה — main==dev.** PM: T-020 דחוף — סוגר את F-021 ומשחרר את T-018. | קידום #3 · 3 משימות ✅ |
| C-0024 | DEV | CRITIC | 06:45Z | **טיק ביצוע — משימה 3 (האחרונה) בתוכנית coverage-measurement.** שלושה תוצרים: `lib/core/sources.ts` (פרסרים טהורים — TSV דו-עמודי ל-H1/H2/H4, JSONL של Kaikki ל-H3, CSV של NGSL לפי **שם עמודה ולא לפי מיקום**), `scripts/measure-coverage.mjs` (השכבה הבלתי-טהורה היחידה) ו-`data/README.md` (חוזה הקבצים של T-043). **כלל היושרה שהמשימה קיימת בשבילו נאכף בקוד ונמדד בפועל:** קובץ חסר מדווח `unavailable` ולא `0%`, וכל פרסר סופר `skipped` — ההרץ **מסרב להדפיס אחוז** למקור שדילג על יותר מ-0.5%. ארבעת מסלולי הכשל הורצו מול `data/` סינתטי: (א) NGSL חסר → יציאה 1 עם `T-043`; (ב) 1 שורה במקום 2809 → יציאה 1 `expected exactly 2809`; (ג) 2809 שורות + H1 של 3 גלוסות → יציאה 0, **STRICT 1/2809 מול LENIENT 2/2809** — ההפרש הוא בדיוק רשומת ה-`!`, כלומר שתי הקריאות של F-021 באמת מייצרות שני מספרים; (ד) קובץ שהפרסר לא קורא → יציאה 1 `H2 parser skipped 3/3 lines (100.00%)`. **⚠️ סטייה מהתוכנית, ממדידה ולא משיקול:** התוכנית הציעה `tsx` כתלות חדשה. נמדד ש-Node v22.22.2 מפשיט טיפוסים לבדו אך **אינו פותר מפרט חסר-סיומת** (`ERR_MODULE_NOT_FOUND` על `./lexicon`), ולכן נרשם וו-רזולוציה יחיד מ-`node:module` — **אפס תלויות חדשות.** **שלוש מוטציות הופעלו והפילו בדיוק את הבדיקות שלהן:** ביטול מסנן כותרת ה-TSV · הסרת מפתח `code` הישן של Kaikki · ספירת תרגום שאינו עברית כשגיאת פרסור (הפילה 4, בהן "היעדר תרגום אינו skip"). **ארבע פקודות רצו טרי: typecheck ✅ · check:core ✅ (`/lib/core purity: OK`) · 313 בדיקות ✅ (עלייה מ-285) · build ✅ (`Compiled successfully in 288ms`).** ⛔ **עדיין אין מספר כיסוי — הקוד סגור, T-043 פתוח.** | `lib/core/sources.ts` · `scripts/measure-coverage.mjs` · `data/README.md` · 28 בדיקות |
| C-0023 | DEV | CRITIC | 05:41Z | **טיק ביצוע — משימה 2 בתוכנית coverage-measurement (T-013+T-016).** `lib/core/coverage.ts`: `measureCoverage(headwords, sources, policy)` היא **פונקציה אחת לשתי המשימות** — הן דורשות פלט זהה ונבדלות רק במערך המקורות, ולכן שתי פונקציות היו שני עותקים של אותו חשבון. **מדיניות F-021 היא ארגומנט ולא ענף בקוד:** `STRICT_POLICY` מול `LENIENT_POLICY` נכנסות מבחוץ, כך שהכרעת ה-PM לא תדרוש כתיבה מחדש ולא מדידה חוזרת. **שלוש הכרעות שנמדדו:** ⓐ הכיסוי המשולב הוא **איחוד ולא סכום** — מוטציה ל-`reduce((n,s)=>n+s.covered,0)` החזירה 3 במקום 2 על שני מקורות שחולקים מילה. ⓑ `uncovered` מחזיר את **האיות המקורי** של הכותרת ולא את מפתח הנרמול, כי רוי קורא את הרשימה — מוטציה שהחזירה את המפתח הפילה 2 בדיקות (`'  House '`→`'house'`, `'Mango'`→`'mango'`). ⓒ `GAP` נופל דרך `classifyGloss` ולכן **אינו כיסוי**; הסרת המחסום הפילה את הבדיקה. **⚠️ פגם בתוכנית שהתגלה בביצוע:** קוד הבדיקה שבתוכנית אינו מתקמפל בריפו הזה — `noUncheckedIndexedAccess` הופך `r.perSource[0]` ל-`| undefined`, ו-`tsc` נפל על **20 שגיאות TS2532, כולן בקובץ הבדיקה** (המימוש עבר נקי). לא נבחר `!` אלא `source(r, i)` שזורקת שגיאה בעלת שם; שלוש המוטציות הורצו שוב מול הבדיקה המתוקנת והפילו זהה. התוכנית עודכנה כדי שמשימה 3 לא תיתקל בזה שוב. **ארבע פקודות רצו טרי: typecheck ✅ · check:core ✅ (`/lib/core purity: OK`) · 285 בדיקות ✅ (עלייה מ-271) · build ✅ (`Compiled successfully in 327ms`).** ⛔ **אין עדיין מספר כיסוי** — משימה 3 סוגרת את הקוד, T-043 סוגר את הנתונים. | `lib/core/coverage.ts` · 14 בדיקות |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
