<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0018                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # PM | DEV | CRITIC | HUMAN — DEV C-0018: T-026 נבנתה (משימה 2 בתוכנית account-integrity). נותרה משימה 3 (F-015+TD-5) לטיק הבא. ההיסטוריה של CRITIC C-0017 (מהיר): ארבע הפקודות עברו טרי, אין ממצא חוסם פתוח, קידום #2 dev→main בוצע (b5f26ea→1a3775b, כל C-0013..C-0017 אצל המשתמש). סומנו ✅: T-028/T-009/T-041. נותרו 🟣 בתור: T-001/T-002/T-027/T-030/T-031. חסמים פתוחים ל-PM: T-020 חוסמת 4 משימות · P-001 חוסמת T-034 · TD-17 (רשת) חוסמת את אותן 4.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: "F-015"           # התוכנית: docs/superpowers/plans/2026-08-06-account-integrity.md — משימה 1 (F-009) ✅ C-0017, משימה 2 (T-026) ✅ C-0018, נותרה משימה 3: F-015+TD-5
CRITIC_ROUNDS_ON_TASK: 1          # סבב הביקורת הידני של רוי נספר כסבב 1 על T-001+T-002
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-07T01:41:53Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה.
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."

# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                  # "" | PM | DEV | CRITIC
LOCK_AT: ""                                                                                          # ISO-8601. חובה לקרוא שעה אמיתית: date -u +%Y-%m-%dT%H:%M:%SZ
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "1a3775ba06e37ee6b40a56eee63bcbe6032d1d6b"  # נסקר C-0017: מהיר. ארבע פקודות עברו טרי. main==dev אחרי קידום #2 — הכל אצל המשתמש

# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T01:07:00Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 2          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת, T-034) · F-008 🟡 (חסום ב-T-025) · F-015 ⚪ · R-010 (אסור להעתיק פריטים ממאל"ו). **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.


### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0018 | DEV | CRITIC | 01:41Z | **טיק ביצוע — משימה 2 בתוכנית account-integrity (T-026).** כל עוד אימות המייל כבוי (Q-001 ⓑ) שגיאת כתיב בכתובת = חשבון אבוד בשקט, ולכן הכתובת שנרשמה מוצגת עכשיו ב-`/onboarding` דרך `<RegisteredAddress>` עם תיקון בלחיצה אחת. היעד של היציאה הוא **אסימון ולא נתיב** (`destination=fix_address`): הגוף מגיע ב-POST לא מאומת, ולכן קבלת נתיב הייתה הופכת את `/logout` ל-open-redirect — 6 קלטים עוינים (`https://evil.example`, `//evil.example`, `/signup`, `FIX_ADDRESS`, רווח נגרר) נבדקים ומתמוטטים ל-`/`. הכתובת נקראת **לפני** `signOut()`, אחרת אין סשן ואין מה להציג. **מדידה ולא הצהרה:** פיקסטורה `app/dev/identity` (noindex, `fixture@example.com` — RFC 2606) נוספה ל-`check:mobile` כי `/onboarding` מפנה בלי env (TD-13). **מוטציה הוכיחה שהבדיקות עובדות:** החלפת `<EnWord>` ב-`<span>` עירום הפילה 3 בדיקות מובייל + בדיקת TD-14. **חמש פקודות רצו טרי: typecheck ✅ · check:core ✅ (`/lib/core purity: OK`) · 247 בדיקות ✅ · build ✅ · 315 בדיקות מובייל ✅.** | T-026 🟣 |
| C-0017 | CRITIC | PM | 01:08Z | **מוד מהיר (01:04Z). קידום #2 dev→main.** ארבע הפקודות רצו טרי אצלי: typecheck ✅ · check:core ✅ (`/lib/core purity: OK`) · 239 בדיקות ✅ · build ✅. אין ממצא חוסם פתוח — F-012/F-019 הם 🟠 שיפור/תוכן-לא-נקלט, F-008/F-015 מתחת לסף. `merge --ff-only dev` (b5f26ea→1a3775b). סומנו ✅ לפי מבחן שלוש-השאלות: T-028 · T-009 · T-041. | קידום #2 · 3×✅ |
| C-0017 | DEV | CRITIC | 00:41Z | **טיק ביצוע — משימה 1 בתוכנית account-integrity (F-009).** התקרה של bcrypt נספרת עכשיו ב-**בתים** ולא בתווים: `passwordByteLength` דרך `TextEncoder` (טהור — `TextEncoder` אינו ברשימת האסורים של `check:core`), כי סיסמה של 40 אותיות עבריות היא 40 תווים ו-**80 בתים** — ספירת תווים הייתה משאירה את הבאג במקומו רק רחוק יותר. `mapAuthError` הפסיק לנחש שדה: `validation_failed`, הקוד שבו GoTrue משתמש **גם** לכתובת פגומה **וגם** לחריגת האורך, עובר ל-`unavailable` — הודעה ברמת הטופס במקום האשמה מתחת לשדה האימייל, שהוא בדיוק הנזק שהממצא תיאר (לומד מתקן שוב ושוב כתובת תקינה). הרמז "8 תווים לפחות. בלי כללים נוספים." הפסיק לשקר. **10 בדיקות חדשות אומתו במוטציה כפולה:** 8 מהן נכשלו מול הקוד הישן לפני המימוש, ו-שינוי הקבוע ל-`7200` מפיל 3 — קבוע שבדיקה עוברת גם כשהוא שבור אינו נבדק. **חמש פקודות רצו טרי בטיק הזה: typecheck ✅ · check:core ✅ · 239 בדיקות · build ✅ · 288 בדיקות מובייל.** | F-009 ✅ |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
