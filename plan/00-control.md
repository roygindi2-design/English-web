<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0043                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # PM | DEV | CRITIC | HUMAN — **C-0041 (DEV) ביצוע: משימה 4 — האחרונה — בתוכנית `2026-08-07-review-engine.md`. `0005_review_state.sql` + `POST /api/review` + `lib/core/reviewRequest.ts`.** חמש פקודות טרי (typecheck ✅ · check:core ✅ · 469 בדיקות ✅ · build ✅ `ƒ /api/review` · check:mobile ✅). **התוכנית סגורה במלואה — T-005 ו-T-006 ממתינות לסקירה.** שני חובות חדשים: **TD-19** (0005 טרם הורץ בפרויקט חי → 503 עד שיורץ) · **TD-18** (שלושת פרמטרי המוצר ב-route ולא ב-`/lib/core`). ⚠️ T-020 של PM דחופה (F-021 חוסמת את T-018). חסמים אנושיים: T-043 · P-001.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-005             # **כל 4 משימות התוכנית `2026-08-07-review-engine.md` הושלמו (C-0036/37/40/41).** T-005 ו-T-006 ✅ בקוד, ⏳ בסקירה. **אין משימה פתוחה עם תוכנית — הטיק הבא של DEV הוא טיק תכנון**, אחרי סקירת ה-Critic. T-029 הושלמה (C-0031..C-0034) וממתינה לסקירה
CRITIC_ROUNDS_ON_TASK: 1          # T-029 נסקר ✅ נקי. T-005/משימות 1–2 נסקרו — סבב 1. ממתינים לאימות: משימה 3/T-006 (C-0040) + משימה 4/T-005 (C-0041)
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-07T21:45:58Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה. F-021 היא הכרעת PM, לא הכרעת רוי, ואינה חוסמת את המדידה (המדיניות היא נתון ולא לוגיקה).
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: CONTENT               # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: "2026-08-10T04:08:58Z"
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "f0bd4c5"  # נסקר C-0038: עומק, C-0032..C-0037. ארבע פקודות טרי. main==dev אחרי קידום #5 — הכל אצל המשתמש
# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T19:21:15Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 5          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-021 🟠 (חוסמת T-018) · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0043 | CONTENT | CRITIC | 04:17Z | **טיק תוכן — אצווה שלישית. 45 כותרות NGSL בדירוגים 1–45, 60 משמעויות (D-021), 60/60 עברו את `gateSense` אחרי סבב תיקון אחד.** שתי האצוות הקודמות כיסו 75–278, ו**דירוגים 1–75 מעולם לא נגעו בהם** — ולכן הכלל העולה (תדירות עולה, בדילוג על כותרת שכבר יש לה שורה) מצביע בדיוק על 1–45. **כל 45 הכותרות הן מילות דקדוק, וזו תוצאה של הכלל ולא סטייה ממנו**; כל שורה נושאת `is_function_word` כדי שפיצ׳ר עתידי יוכל להחליט אם ללמד אותן. הבלוק שנותר (46–75) הוא הבא בתור. חמש-עשרה כותרות נושאות שתי משמעויות (of · have · for · that · on · with · as · at · by · so · about · know · there · can · get), ולכן 45 כותרות ייצרו 60 שורות. **רשומת `low` אחת (`a`) · שתי `medium` (`will` · `would`)** — שלושתן מקרים שבהם לעברית אין מקבילה מילונית לתפקיד הדקדוקי, וכולן נכתבו עם התרגום הטוב ביותר ולא הושמטו (D-024). ⚠️ **ההתנגשות שדווחה ב-C-0042 עדיין פתוחה ואינה בבעלותי:** מדיניות ה-RLS ב-`0002_content_bank.sql` (שורות 154–175) מסננת `translation_confidence <> 'low'` בכל ארבע ה-select, ולכן `a` לא תגיע ללומד — בסתירה ל-D-024. מסומן ל-T-042. ⛔ קבצים בלבד: לא נכתב ל-DB ולא נגעתי בקוד. | `data/generated/batch-2026-08-09.jsonl` (חדש, 60 שורות) · `data/generated/manifest.json` · `data/generated/manifest-2026-08-08.json` (ארכוב) · `plan/80-content-lessons.md` (§ א׳ שורה 5 + § א׳1 כלל ראשון) |
| C-0042 | CONTENT | CRITIC | 04:05Z | **טיק תוכן — אצווה שנייה. 45 כותרות NGSL בדירוגים 76–125, 55 משמעויות (D-021), 55/55 עברו את `gateSense` אחרי שני תיקונים.** כלל הבחירה נרשם במניפסט: **דירוג תדירות עולה, בדילוג על כותרת שכבר יש לה שורה** — האצווה של 07/08 בררה מילות תוכן מתוך 75–278 והשאירה את מילות הדקדוק, וזו האצווה שסוגרת את הפער. עשר כותרות נושאות שתי משמעויות (then · only · look · first · way · over · back · last · call · show), ולכן 45 כותרות ייצרו 55 שורות. **אפס `low`; שתי רשומות `medium`** (any · such) כי התרגום העברי אינו חד-חד-ערכי. ⚠️ **התנגשות שנמצאה ולא תוקנה על ידי — היא אינה בבעלותי:** מדיניות ה-RLS ב-`0002_content_bank.sql` (שורות 154–175) מסננת `translation_confidence <> 'low'` בכל ארבע ה-select, כלומר רשומת `low` **אינה מגיעה ללומד כלל** — בסתירה ל-D-024 שלפיו היא כן מוצגת עם סימון 'טרם אומת'. ⛔ קבצים בלבד: לא נכתב ל-DB ולא נגעתי בקוד — הקליטה היא T-042. | `data/generated/batch-2026-08-08.jsonl` (חדש, 55 שורות) · `data/generated/manifest.json` · `data/generated/manifest-2026-08-07.json` (ארכוב) · `plan/80-content-lessons.md` (§ א׳ שורות 3–4) |
| C-0041 | DEV | CRITIC | 21:45Z | **טיק ביצוע — משימה 4, האחרונה, בתוכנית `2026-08-07-review-engine.md`. ההתמדה מחווטת והאכיפה עברה לצד השרת; התוכנית סגורה במלואה.** `POST /api/review` הוא הקצה: הוא היחיד שנוגע בשעון, ושלוש הפונקציות הטהורות (`applyGrade` · `scheduleReview`) מקבלות `nowMs` ו-`today` כארגומנטים. **ארבע ההכרעות שהמימוש נשען עליהן:** ⓐ **חמש עמודות ה-SM-2 יושבות על `word_progress` הקיימת ולא בטבלה שנייה** — 0003 קבע שורה אחת לכל (משתמש, מילה) בגלל W4, ויומן חזרות באפליקציית SRS גדל בלי תקרה; `next_review_at` **nullable** כי `default now()` היה מציף את התור הראשון בכל המילון. ⓑ **שתי מגבלות ה-`check` מוצהרות בשם בתוך `do $$`** — `add column ... check` מדולג **כולו** כשהעמודה קיימת (נמדד C-0032), ופרויקט שהריץ טיוטה היה נשאר בלי המגבלה בשקט. ⓒ **`firstSeenAtMs = nowMs - elapsedMs` ולא `nowMs`** — עיגון בזמן התשובה היה כותב `time_to_first_correct = 0` לכל מילה שנלמדה במבט ראשון, ו-0 הוא מדידה ולא "מיידי"; מאותו נימוק `MAX_ELAPSED_MS = 600_000` **דוחה ב-400 ואינו חותך** — ערך חתוך אינו ניתן להבחנה מערך אמיתי. ⓓ **`update`/`insert` ולא `upsert`** (D-016), בדיוק כמו `POST /api/profile`. **הדירוגים והכיוונים מיובאים מ-`lib/core/flashcard.ts` ואינם משוכפלים** — זה המחסום נגד סחיפה בין מה שהכרטיס מציע למה שה-API מקבל. **שתי מוטציות רצו, לא שתי טענות:** `'hard'` נוסף ל-`BINARY_GRADES` בלי לגעת בוולידציה → `rejects a grade outside BINARY_GRADES` נופלת `expected true to be false`; חיתוך במקום דחייה של `elapsed_ms` → `rejects an abandoned card rather than recording an 8-hour answer` נופלת. **חמש פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 469 בדיקות ✅ (מ-445, 30 קבצים) · build ✅ `Compiled successfully in 231ms` עם `ƒ /api/review` · check:mobile ✅ `390 checks, widths 320/375/414px`.** ⛔ אפס תלויות חדשות · ⛔ אין מסך · ✅ `docs/api-contract.md` עודכן **באותו קומיט** (חוק גלובלי). **שני חובות חדשים: TD-19** — `0005_review_state.sql` טרם הורץ בפרויקט חי, ועד אז כל קריאה נופלת ב-503 (אותו מעמד כמו TD-8); **TD-18** — `TRIAGE_MIN_USABLE_DAYS` ו-`MASTERY_CONSECUTIVE_CORRECT` הם פרמטרי מוצר בלי מקור, ולכן הוגדרו ב-route ולא ב-`/lib/core`: קבוע שמיוצא מהשכבה הטהורה מצוטט אחר כך כאילו השכבה גזרה אותו. | `supabase/migrations/0005_review_state.sql` (חדש) · `lib/supabase/reviewState.test.ts` (חדש, 11 בדיקות) · `lib/core/reviewRequest.ts` (חדש) · `lib/core/reviewRequest.test.ts` (חדש, 13 בדיקות) · `app/api/review/route.ts` (חדש) · `docs/api-contract.md` · `plan/30-architecture.md` (§ 3.1.10 · TD-18 · TD-19) · `plan/50-tasks.md` (T-005 · T-006) |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
