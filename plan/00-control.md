<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0047                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: DEV                   # **C-0047 (DEV, טיק תכנון): נכתבה `docs/superpowers/plans/2026-08-12-sense-accuracy.md` — חמש משימות שמכסות את T-018 ואת החצי הטהור של T-010. ⛔ לא נכתב קוד מוצר בטיק הזה. ⚠️ **תור הסקירה של ה-Critic לא זז ואינו מושפע:** T-005 · T-006 · T-029 + חמש אצוות תוכן עדיין ממתינות לסקירה, ו-`NEXT_AGENT` הוא איתות עדיפות ולא נעילה. **C-0046 (PM): T-020 הושלמה — 1.7.1 הועתק לתמצית מילה במילה (אומת ב-`diff`), נוסף § 1.6, וסתירת הסינון הוכרעה ב-D-025 (`GAP` לא נטען · `!` כן, כ-low · ניקוד אינו עילה). **T-018 שוחררה — Dev רשאי לכתוב לה תוכנית.** ⛔ F-021 נשארת פתוחה פורמלית: `60-findings.md` אינו קובץ של ה-PM, סגירתה היא פעולת Critic. תור הסקירה שקדם לי לא זז: T-005 · T-006 · T-029 + שלוש אצוות תוכן. ✅ **רוי חתם על D-025 (23:38Z)** — הכרעת סינון H1 סגורה, ⛔ לא לפתוח מחדש. חסמים אנושיים שנשארו: T-043 · T-046 · R-015 · P-001.
STATE: PLANNING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-018             # **תוכנית `2026-08-12-sense-accuracy.md` נכתבה C-0047 — משימה 1 בה (`lib/core/senseInventory.ts`) היא הצעד הבא של Dev, והיא טיק ביצוע.** T-005/T-006/T-029 סגורות בקוד וממתינות לסקירה בלבד. ישן: **כל 4 משימות התוכנית `2026-08-07-review-engine.md` הושלמו (C-0036/37/40/41).** T-005 ו-T-006 ✅ בקוד, ⏳ בסקירה. **אין משימה פתוחה עם תוכנית — הטיק הבא של DEV הוא טיק תכנון**, אחרי סקירת ה-Critic. T-029 הושלמה (C-0031..C-0034) וממתינה לסקירה
CRITIC_ROUNDS_ON_TASK: 1          # T-029 נסקר ✅ נקי. T-005/משימות 1–2 נסקרו — סבב 1. ממתינים לאימות: משימה 3/T-006 (C-0040) + משימה 4/T-005 (C-0041)
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-12T00:45:36Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                    # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: ""
MILESTONE_TICKS: 2
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 0
LAST_DEPLOYED_AT: ""
LAST_REVIEWED_COMMIT: "f0bd4c5"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T19:21:15Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 5          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-021 🟠 (**התוכן תוקן C-0046 — נותרה סגירה פורמלית בידי Critic**) · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039).**
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0047 | DEV | DEV | 00:45Z | **טיק תכנון — לא נכתב קוד מוצר, וזו הכוונה.** נבחרה T-018 (שוחררה ב-C-0046), לא הייתה לה תוכנית, ולכן נכתבה `docs/superpowers/plans/2026-08-12-sense-accuracy.md`: **חמש משימות** שמכסות את T-018 במלואה ואת החצי הטהור של T-010 (מפת רמות CEFR) — האחרון אינו תוספת שרירותית אלא דרישה של § 1.7.1, שמחייב פילוח דיוק **לפי רמת CEFR**. 🔬 **מה שנמדד בפועל לפני שנכתבה שורת תוכנית אחת** (`awk`/`cut`/`grep` על הקבצים האמיתיים): `data/h1-hebrew-wordnet.tsv` — 17,564 שורות, **כולן בדיוק 2 עמודות**, 10,611 מחרוזות אנגלית ייחודיות, 3,920 מהן עם יותר מגלוסה אחת, 3,301 שורות `!`, 702 שורות `GAP`. CEFR-J — 7,798 שורות, 699 מהן עם פסיק בתוך מרכאות; Octanove — 2,136 שורות, ובהן `remonstrate,vern` (שגיאת כתיב) ו-`batter,` (POS ריק). 🔴 **הממצא שקובע את היקף התוכנית: כלל 3 ו-4 של § 1.7.1 אינם ניתנים למדידה היום.** הייצוא של H1 בריפו הוא `english<TAB>hebrew` בלבד — **אין בו מזהי synset**, ולכן מיפוי synset↔synset אינו ניתן לביטוי; ואין ב-`data/` שום מלאי משמעויות אנגלי (sense number · tag_cnt). לכן התוכנית בונה את כל הקוד מול פיקסצ׳רים היום, והמריץ מדפיס `unavailable` לאחוז עצמו — ⛔ **בשום מצב לא 0%**, שני דברים הפוכים. שני הקבצים החסרים נרשמו ב-`03-for-roy.md` עם שמות ועמודות מדויקים, והלופ ממשיך בלי להמתין. ⚠️ **סתירה שנמצאה ולא תוקנה בכוונה:** `lib/core/lexicon.ts:110` (`displayableGloss`) מחזיר `null` ל-`low` לפי D-013, בעוד D-024/D-025 קובעים ש-`low` **כן** מוצג כמסומן — אותה סתירה בדיוק שקיימת ב-RLS ב-`0002_content_bank.sql:154-175`. שייך ל-T-045, ותיקון מתוך תוכנית מדידה היה דליפת היקף; התוכנית עוקפת אותו בכך שהיא צורכת את `classifyGloss` ישירות. **מומלץ ל-Critic לפתוח ממצא פורמלי** — `60-findings.md` אינו קובץ של Dev. ✅ אומת בהרצה חיה מול הקוד האמיתי (Node 22.22.2, type-stripping): `normalizeEnglish('café')='cafe'` · `normalizeHebrew('בַּנְק')='בנק'` · `classifyGloss('!חוֹף')` מחזיר `low` עם `match='חוף'` · `GAP` נופל כ-`gap_record`. ⛔ לא הורץ `npm install` ולא נטען אף סקיל עיצוב — טיק תכנון אינו נוגע בקוד. | `docs/superpowers/plans/2026-08-12-sense-accuracy.md` (חדש, 5 משימות) · `plan/03-for-roy.md` (פריט 5) · `plan/50-tasks.md` (T-018 · T-010) · `plan/30-architecture.md` · `plan/00-control.md` |
| C-0046 | PM | CRITIC | 23:05Z | **מצב 🔬 — BLOCKER R-006 פתוח, וחוסמו היחיד היה T-020 (משימת PM).** T-020 הושלמה: 1.7.1 הועתק לתמצית מילה במילה (`diff` מול `10-pedagogy.md`: 15 שורות זהות), נוסף § 1.6 (מקורות תרגום מותרים/חסומים/לא-נמדדים), וכללי R-007 נכתבו כלשונם. **D-025 הכריע את סתירת F-021 ⓑ:** `GAP` אינו נטען · רשומת `!` **כן** נטענת כ-`low` · **ניקוד אינו עילה לסינון** — § 2 הפסיד בשניים משלושה, בדיוק כפי ש-F-021 מדד. הסתירה שלא הייתה ב-F-021 (R-007 "אסור כתרגום ראשי" מול D-024 "low כן מוצג") הוכרעה בכלל עדיפות: לא-`!` קודם ל-`!`, ובהיעדר חלופה מציגים מסומן ולא משמיטים. התמצית **147/150 שורות** (`wc -l`), וכל מפרידי ה-`---` הוסרו כי הם הפכו את השורה שמעליהם לכותרת setext. **⛔ לא נעשה מחקר רשת ולא שוגרו סוכני משנה** — כל חומר המשימה כבר היה מאומת ב-`10-pedagogy.md`; חיפוש נוסף היה מוסיף סיכון של תוכן בלי מקור, לא ידע. 🔍 **ביקורת Netlify — לא אומתה, ואני לא מתיימר שכן:** אין לסוכן `NETLIFY_AUTH_TOKEN`, ושתי ההגדרות חיות בלוח המחוונים בלבד. מה שכן נמדד: **0 PR-ים פתוחים** (GitHub API), ולכן Deploy Preview אינו יכול לרוץ כרגע. R-015 + T-046 נפתחו כמנגנון. ⚠️ `MILESTONE_TICKS` היה 0 אחרי 45 מחזורים — כלומר בלם 8 לא היה מתוחזק. הועלה ל-1. | `plan/15-syllabus-digest.md` (נכתב מחדש) · `plan/40-decisions.md` (D-025) · `plan/20-alerts.md` (R-015 + עדכון R-007) · `plan/50-tasks.md` (T-020 ✅ · T-018 שוחררה · T-046) |
| C-0045 | CONTENT | CRITIC | 04:20Z | **טיק תוכן — אצווה חמישית. 45 כותרות NGSL בדירוגים 146–201, 61 משמעויות (D-021), 61/61 עברו את `gateSense` בסבב אחד — 0 פסילות ו-0 תיקונים.** זו האצווה הראשונה שנשלטת ב**מילות תוכן** ולא במילות דקדוק: 36 מתוך 45 הן שם עצם, פועל או שם תואר. 16 כותרות נושאות שתי משמעויות (help · own · point · start · interest · old · off · live · end · while · kind · play · home · case · book · around), ולכן 45 כותרות ייצרו 61 שורות. **אפס `low` · ארבע `medium`** (`own`#1 · `own`#2 · `off`#2 · `while`#2) — ארבעתן מקרים שבהם לעברית אין מקבילה מילונית אחת, וכולן נכתבו עם התרגום הטוב ביותר ולא הושמטו (D-024). בדיקה מקדימה מול `allowed-words` (כלל § א׳1) תפסה **18 פגמים לפני השער** — 14 סחיפות רמה ו-4 מספרים במילים — וזו הסיבה שהשער עצמו לא ראה אף כשל. ⚠️ **ההתנגשות מ-C-0042/C-0043 עדיין פתוחה ואינה בבעלותי:** מדיניות ה-RLS ב-`0002_content_bank.sql` (שורות 154–175) מסננת `translation_confidence <> 'low'` בסתירה ל-D-024. באצווה הזו אין `low`, ולכן היא אינה נפגעת; הפער נשאר מסומן ל-T-042. ⛔ קבצים בלבד: לא נכתב ל-DB ולא נגעתי בקוד. | `data/generated/batch-2026-08-11.jsonl` (חדש, 61 שורות) · `data/generated/manifest.json` · `data/generated/manifest-2026-08-10.json` (ארכוב) · `plan/80-content-lessons.md` (§ א׳ שורה 7 + כלל שלישי ב-§ א׳1) |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
