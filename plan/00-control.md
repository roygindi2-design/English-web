<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0028                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # PM | DEV | CRITIC | HUMAN — DEV C-0028 היה **טיק ביצוע**: **T-012 נסגרה** (משימה 3 בתוכנית `2026-08-07-source-registry.md`). `lib/core/provenance.ts` טהור — `checkNgslRowCount` (2,809 בדיוק, 2,801 מזוהה בשמו) ו-`checkSourceUrl` שקורא את ההוסט המורשה מהרישום של T-011 ולכן מגן על **כל שבעת המקורות**. `NGSL_EXPECTED_ROWS` הוסר מ-`scripts/measure-coverage.mjs` ומנותב דרך השומר. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 334 בדיקות ✅ (מ-323) · build ✅ `Compiled successfully in 250ms`.** בנוסף `check:mobile` ✅ `351 checks`. **הבא ב-DEV: T-015** (משימה 4 והאחרונה באותה תוכנית). ⚠️ **T-020 של ה-PM נשארת דחופה ובלתי-תלויה** — סוגרת את F-021 ומשחררת את T-018. חסמים אנושיים: T-043 (קובצי המקור) · P-001 (תוכן ל-T-034).
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-015             # משימה 4 והאחרונה בתוכנית `2026-08-07-source-registry.md`. T-011 (1+2) ו-T-012 (3) הושלמו. T-015 קוראת את מזהי המקורות מהרישום, ולכן היא בת-ביצוע מיידי
CRITIC_ROUNDS_ON_TASK: 1          # סבב הביקורת הידני של רוי נספר כסבב 1 על T-001+T-002
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-07T09:42:00Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה. F-021 היא הכרעת PM, לא הכרעת רוי, ואינה חוסמת את המדידה (המדיניות היא נתון ולא לוגיקה).
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."

# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                  # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: ""
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "4264114"  # נסקר C-0024: מהיר. ארבע פקודות עברו טרי. main==dev אחרי קידום #3 — הכל אצל המשתמש

# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T07:11:00Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 3          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: **F-021 🟠 (חדש C-0020 — נגד ה-PM: 1.7.1 חסר מהתמצית + סתירת כללי סינון; חוסמת את T-018)** · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת, T-034) · F-008 🟡 (חסום ב-T-025) · R-010 (אסור להעתיק פריטים ממאל"ו). **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0028 | DEV | CRITIC | 09:42Z | **טיק ביצוע — T-012 נסגרה** (משימה 3 בתוכנית `2026-08-07-source-registry.md`). שני דברים בלתי-תלויים יכולים להיות שגויים בקובץ מקור, ורק אחד מהם נראה בנתונים: **מספר השורות** נמדד אחרי המעשה (2,809 = v1.2; **2,801 = הגרסה המשוקפת** מהדומיין שאיננו שולטים בו — נפרסרת בהצלחה ומזיזה כל דירוג תדירות), ו**מקור הבייטים** אינו ניתן למדידה בדיעבד ולכן נבדק בזמן המשיכה. `lib/core/provenance.ts` (טהור) מחזיר `ProvenanceVerdict` ולא זורק — הסיבה נשארת מחרוזת אחת ב-`lib/core`, והרַץ מדפיס אותה כמו שהיא. **ההוסט מגיע מ-`dataSource(id).host`** ולכן `checkSourceUrl` מגן על שבעת המקורות ולא רק על NGSL (`kaikki` מ-`newgeneralservicelist.com` נדחה). הבדיקה היא **גבול תווית ולא תת-מחרוזת**: `endsWith(domain)` העירום מקבל `evil-newgeneralservicelist.com` ו-`includes` מקבל גם `newgeneralservicelist.com.example.net` — שתיהן נבדקות מפורשות, וכך גם `http` ומחרוזת שאינה URL. `NGSL_EXPECTED_ROWS` **הוסר** מהרַץ: עותק שני של המספר סוחף, והעותק שסוחף הוא זה שאיש לא בודק ביחידה — בדיקה ברַץ נכשלת אם ליטרל `\b280\d\b` יחזור לשם. קובץ **חסר** (T-043, פעולה אנושית) וקובץ **שגוי** (R-004) נשארו שני כשלים נפרדים בכוונה. **שתי מוטציות הפילו בדיוק את הבדיקות שלהן:** החלפת בדיקת ההוסט ל-`endsWith(domain)` → נפלה `rejects a host that merely ENDS with the domain`; החלפת השוואת השורות ל-`rows >= 2801` → נפלו שתי בדיקות המנייה. בנוסף **ריצה חיה** של `node scripts/measure-coverage.mjs` מול קובץ סינתטי של 2,801 שורות החזירה את הודעת R-004 — כלומר חיווט ה-import של TS תחת Node מאומת ולא מונח. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 334 בדיקות ✅ (מ-323) · build ✅ `Compiled successfully in 250ms`.** בנוסף `check:mobile` ✅ `351 checks`. ⛔ אפס תלויות חדשות · ⛔ אין שינוי ב-API ולכן `docs/api-contract.md` לא נגע. | `lib/core/provenance.ts` · `lib/core/provenance.test.ts` · `scripts/measure-coverage.mjs` · 11 בדיקות |
| C-0027 | DEV | CRITIC | 08:44Z | **טיק ביצוע — T-011 נסגרה במלואה** (משימות 1+2 בתוכנית `2026-08-07-source-registry.md`). העובדה על מקור חיצוני — שם, רישיון, הוסט מורשה — נכתבת מעכשיו **פעם אחת** ב-`lib/core/dataSources.ts` (שבע רשומות `Object.freeze`, טהור), ושלושה צרכנים קוראים ממנה. **הבדיקה שהמשימה קיימת בשבילה:** `docs/data-licenses.md` נשמר בגיט אך נבדק **בייט-מול-בייט** מול `licencesMarkdown()` — בלעדיה המסמך הופך בשקט לעותק שנערך ביד וסותר את הקוד, בדיוק מחלקת הכשל של F-021. הגנרטור `scripts/write-data-licenses.mjs` **אינו** מחובר ל-CI בכוונה: גנרטור שרץ ב-CI מסתיר סחיפה במקום להיכשל עליה. `/sources` מרנדר מאותו רישום, ולינק אליו יושב בכותרת התחתונה של **כל** מסך — החובה נצמדת למוצר ולא לעמוד. **שתי מוטציות הופעלו והפילו בדיוק את הבדיקות שלהן:** (א) `printf "hand edited" >> docs/data-licenses.md` → נפלה בדיקת הסחיפה עם ההוראה להריץ את הגנרטור; (ב) הסרת `<EnWord>` מסביב ל-`{source.licence}` → נפלה `source.licence is Latin text and must render inside <EnWord>`. אכיפה **חיובית** ל-TD-14 היא היחידה שעובדת כאן — סריקה שלילית אינה רואה markup חסר. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 323 בדיקות ✅ (מ-313) · build ✅ `Compiled successfully`.** בנוסף `check:mobile` ✅ `351 checks` — `/sources` ירוק ב-320/375/414px על `heading anchored to top`, `all tap targets >= 44px`, `no horizontal scroll` ו-`clean console`. ⛔ אפס תלויות חדשות. | `lib/core/dataSources.ts` · `docs/data-licenses.md` · `app/sources/page.tsx` · `app/layout.tsx` · 10 בדיקות |
| C-0026 | DEV | DEV | 07:43Z | **טיק תכנון — בלי קוד.** מסלול הכיסוי מוצה (T-043/F-021 מחוץ לקוד), ולכן נבחר רצף אחר של **שלוש משימות פנויות** שאיש לא תכנן: T-011 (רישיונות + `/sources`) · T-012 (שומר גרסת NGSL ודומיין) · T-015 (עמודות מקור + טלמטריה). **המכפיל: שלושתן חוזרות על אותה עובדה** — שם המקור, רישיונו, וההוסט היחיד שמותר למשוך ממנו — ולכן התוכנית מציבה רישום אחד, `lib/core/dataSources.ts`, ושלושתן קוראות ממנו. `docs/data-licenses.md` **נוצר מהרישום ונבדק מול הבייטים שלו**, וטבלת `data_sources` ב-DB נבדקת שהמזהים בה זהים בדיוק למזהים בקוד — שתי בדיקות שנועדו לתפוס בדיוק את סוג הסחיפה שיצר את F-021. שני פרטים ננעלו בתוכנית מראש מהקוד הקיים ולא מהזיכרון: `noUncheckedIndexedAccess` (מלכודת 20 שגיאות ה-TS2532 של C-0023 — התוכנית מורה על גישה דרך פונקציה בעלת שם ולא `!`), והאכיפה החיובית של TD-14 (`/sources` מרנדר `name`/`licence`/`host` הלטיניים, וסריקה שלילית אינה יכולה לראות markup חסר). כל משימה נושאת קוד בדיקה אמיתי, ולכל אחת נקבע צעד מוטציה שמפיל בדיוק את הבדיקה שלה. ⛔ **אין פלט אימות בטיק הזה — לא נכתב קוד.** | `docs/superpowers/plans/2026-08-07-source-registry.md` |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
