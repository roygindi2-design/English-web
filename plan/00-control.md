<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0085                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # **C-0085 (DEV): טיק ביצוע — משימה 6, האחרונה בתוכנית `2026-08-13-institution-and-finish.md`. T-057 בוצעה → 🟣, והתוכנית נסגרה במלואה.** `MIN_GAP = 8` + סריקת מרווח על `FLOW_ROUTES`: **+15 בדיקות, הארנס 594 → 609.** ⚠️ **כל 13 הכשלים שהסריקה דיווחה היו שקריים — כולם חפיפה (מרווח שלילי) של `<ActionBar>` שהוא `fixed` ושל מתג `הצג` שהוא `absolute` בתוך שדהו.** ⛔ לא שונה אף מסך; התיקון במכשיר, ושתי ההחרגות ננעלו בבדיקות. **חמש פקודות טרי: typecheck ✅ · check:core `OK` ✅ · 817 בדיקות ✅ · build ✅ · check:mobile ✅ 609.** ⛔ לא נגעתי ב-`main`. 🔴 **איחוי `main`→`dev` עדיין ממתין ל-PM** (`4ddf6c4`) — הקידום חסום עד אז.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: ""                # ⬜ **התוכנית `2026-08-13-institution-and-finish.md` נסגרה — אין משימה פעילה, וה-PM נדרש לתעדף את הבאה.** 🟣 ממתינות לביקורת: **T-003 (C-0079..C-0083) · T-056 (C-0084) · T-057 (C-0085)**. ⛔ לעולם לא לכתוב ל-`senses.cefr_level`. ⚠️ T-007 (NGSL) עדיין ⛔ חסום ב-T-043. T-059 ⬜ תיעוד בלבד.
CRITIC_ROUNDS_ON_TASK: 0          # תור הסקירה: T-005 · T-006 · T-018 · **T-052 + F-027 (C-0071)** · **T-003 🟣 (C-0079..C-0083), לסקירה כשלמות** · **T-056 🟣 (C-0084)** · **T-057 🟣 (C-0085)** · ✅נסגרו C-0082: T-051 · T-058
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN. C-0085 התקדם: 810 → 817 בדיקות, הארנס 594 → 609, T-057 נסגרה
LAST_HANDOFF_AT: 2026-08-13T09:49:51Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: DEV                 # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: "2026-08-13T10:41:00Z"
MILESTONE_TICKS: 39
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "9769c3f"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-12T01:15:52Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 6          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-026 🟡 (**שאלת ה-`PSEUDOGAP!` הוכרעה C-0074 — D-029; נותר רק לסמן ✅ ב-T-059**) · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · **F-028 🟠 (apkg false-accept, רדום → DEV, C-0067)** · **F-029 🟡 (README תקציב מיושן, C-0067)** · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום) · F-028 (C-0069) · F-027 (C-0071 — שני החצאים).**
> 🚨 **קידום חסום — עדיין פתוח (אומת שוב Critic C-0082, 07:11Z; ל-PM טרם היה תור מאז שהדגל הוסף):** `main` הסתעף מ-`dev` — הקומיט `4ddf6c4` ("Update 02-inbox.md", עריכה ישירה ל-main מ-12/08) אינו ב-`dev`, ולכן `git merge --ff-only dev` נכשל וכ-26 מחזורים כולל תיקון F-027 אינם עולים לאוויר. **⛔ Critic אינו יכול לאחות** (האיחוי מתנגש ב-`02-inbox.md`, קובץ PM). **פעולה נדרשת מ-PM:** `git merge origin/main`→`dev`, פתור את התנגשות `02-inbox.md` (שמור י׳/יא׳), דחוף `dev`. אז ה-Critic יקדם. הרצה נקייה (4 פקודות ✅ · 737 בדיקות) אומתה על `dev@1f88154`. `/api/health` חי=ok על קוד C-0047. ⚠️ ‏PROMOTIONS=6 מול main ב-C-0047 → ייתכן 6 קידומים שנכשלו בשקט, לביקורת PM. פירוט: `03-for-roy` פריט 17.
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0085 | DEV | CRITIC | 09:49Z | **טיק ביצוע (◰) — משימה 6, האחרונה בתוכנית `2026-08-13-institution-and-finish.md`: מרווח נמדד בין יעדי מגע סמוכים (T-057). התוכנית נסגרה במלואה.** `MIN_GAP = 8` יושב לצד `MIN_TAP = 44` ב-`scripts/verify-mobile.mjs`, וסריקה בתוך בלוק `FLOW_ROUTES` מודדת את המרווח האנכי בין כל שני יעדי מגע שחולקים מקום אופקי — **+15 בדיקות (5 מסלולים × 3 רוחבים), הארנס 594 → 609**, בדיוק כפי שהתוכנית חזתה. עד הטיק הזה ההארנס אכף **גודל בלבד**: שני יעדים תקינים בני 44px שנוגעים זה בזה עברו ירוק, והאגודל שכיוון לשני נחת על הראשון. ⚠️ **כל 13 הכשלים שהסריקה דיווחה כפי שהתוכנית כתבה אותה היו שקריים — נמדד ולא שוער.** כל 13 הזוגות היו **מרווח שלילי** (חפיפה), אף אחד לא מרווח צר, ושניים בלבד בשורשם: `a"מקורות הנתונים ו" ↔ a"בואו נתחיל" -40px` — קישור הרישיון ב-`<footer>` מול `<ActionBar>` שהוא `fixed inset-x-0 bottom-0 z-20`; השוואת מלבן של סרגל `fixed` בגלילה 0 מול המסמך שמתחתיו מודדת את הכיסוי, שהוא כל תכליתו, בעוד שהשאלה האמיתית (האם הסרגל **מפנה** את הקישור) כבר נמדדת בבדיקה נפרדת מעמוד שנגלל עד הסוף — והיא עוברת. ו-`input"" ↔ button"הצג" -54px` — מתג חשיפת הסיסמה, `absolute inset-y-0 right-0`, יושב **בתוך** שדהו בכוונה תחילה (`pr-16` שומר לו מקום, C-0005): יעד מורכב אחד, לא שני שכנים. ⛔ **לאף זוג אין `gap-*` להרחיב, ולכן לא שונה אף מסך בטיק הזה** — הפגם במכשיר. שתי החרגות צרות: `overlayRoot()` משווה רק זוגות באותה שכבה, ו-`encloses()` מחריג רק פקד `position: absolute` שמלבנו **כולו** בתוך מלבן האחר; כל אחת ננעלה בבדיקה שנוקבת בתכונה שהיא מפעילה, כדי שלא תוכל להתרחב ל"דלג על הזוגות שנכשלים". ⚠️ **שני שומרים שהתוכנית עצמה כתבה, שניהם נמדדו:** ⓐ `toMatch(/gap\s*<\s*MIN_GAP/)` **סותר את הקוד שהתוכנית מכתיבה בצעד 4** — הסריקה רצה בתוך העמוד ומקבלת את הרצפה כארגומנט, ולכן במקור כתוב `gap < min`; הבדיקה נכשלה מול המימוש התקין שלה עצמה. ⓑ חמור יותר: `not.toMatch(/Math\.abs\([^)]*gap[^)]*\)\s*>=/)` מכסה רק את שורת ה-`break`, בעוד שהמוטציה שבאמת מסתירה חפיפה היא `Math.abs(gap) < min` בשורת ה**דיווח** — **נמדד: הרגקס של התוכנית מחזיר `false` על המוטנט.** `Math.abs` על המרווח נאסר לחלוטין, לצד נעילת החיסור החתום. **חמש מוטציות ריצות:** `MIN_GAP = 0` → `declares the floor…` · `Math.abs(gap) < min` בדיווח → `compares a signed gap…` (**מול נוסח התוכנית: עובר**) · מחיקת `a.overlay !== b.overlay` → `never compares a fixed overlay…` · **מוטציה חיה על גאומטריה אמיתית** — שני קישורים ב-`gap-0` ב-`app/page.tsx` → ההארנס נקב `a"כבר יש לך חשבון?" ↔ a"MUTATION-PROBE" 0px` בשלושת הרוחבים (המסך הוחזר, ⛔ לא נדחף) · ובדיקת רצפה ב-`MIN_GAP = 200` שהדפיסה את הזוגות האמיתיים: הצפוף במאגר הוא `input ↔ input 46px` ב-`/signup`, כלומר הבדיקה היא **שומר נסיגה** ולא תיקון, וזה נאמר במפורש ולא מוסתר מאחורי ירוק. **חמש פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **817 בדיקות ✅ (מ-810, 54 קבצים)** · build ✅ · **check:mobile ✅ 609 (מ-594)**. TD-26 קוים: `pkill` → `pgrep` ריק → `rm -rf .next` → build. ⛔ אפס סוכני משנה · ⛔ `10-pedagogy.md` לא נפתח · ⛔ לא נטען סקיל עיצוב (הטיק אינו נוגע בצבע, טיפוגרפיה או רדיוס) · ⛔ לא נגעתי ב-`main`. | `scripts/verify-mobile.mjs` · `scripts/verify-mobile.test.ts` · `docs/superpowers/plans/2026-08-13-institution-and-finish.md` (משימה 6 ✅, התוכנית נסגרה) · `plan/30-architecture.md` (3.1.35) · `plan/50-tasks.md` (T-057 🟣) · `plan/00-control.md` |
| C-0084 | DEV | CRITIC | 08:47Z | **טיק ביצוע (◰) — משימה 5 מתוך 6 בתוכנית `2026-08-13-institution-and-finish.md`: משפט כשל עברי אחד (T-056).** `lib/core/failure.ts` הוא מודול קבועים טהור ואפס-ייבואים — `RETRY_HE` · `FAILURE_HE` (‏save/load/offline/crash) · `FAILURE_TITLE_HE` (‏route/app) — וארבעת הצרכנים (`app/error.tsx` · `app/global-error.tsx` · `components/MeScreen.tsx` · `components/OnboardingForm.tsx`) מייבאים ו⛔אינם מנסחים מחדש. לפני הטיק היו **ארבעה נוסחים לאותו אירוע בארבעה קבצים**, ולומד שפגש שניים מהם לא יכול היה לדעת אם נתקל בתקלה אחת או בשתיים. שתי הכותרות נשארו **נפרדות** (‏`משהו נתקע` מול `האפליקציה לא נטענה`) כי שני הגבולות נכשלים משני דברים שונים, אבל המשפט וכפתור הניסיון החוזר משותפים. ⛔ `AUTH_MESSAGES_HE` לא נגעה — היא עונה על "מה הקלדתי לא נכון", וקיפולה לתוך "משהו נתקע" היה מוחק מהלומד בדיוק את המידע שבעזרתו הוא מתקן. ⚠️ **שומר שהתוכנית עצמה כתבה חלול, נמדד ולא שוער:** הסריקה שהיא מכתיבה, ``includes("'נסה שוב'") \|\| includes('>נסה שוב<')``, הורצה על המאגר **לפני** הכתיבה מחדש והדפיסה **קובץ אחד מתוך שלושה** — `components/MeScreen.tsx` בלבד. `app/error.tsx` ו-`app/global-error.tsx` מדפיסים את התווית כטקסט JSX מוזח (`` >\n  נסה שוב\n < ``), והשורה החדשה חולפת על פני `>נסה שוב<`. כלומר **המוטציה שצעד 6 בתוכנית מורה לבצע במפורש ("החזר את המחרוזת ל-`app/error.tsx`, צפה לכשל שנוקב בשמו") הייתה מקבלת ירוק.** השומר הוחלף ב-`restatesRetryLabel()`: התווית אסורה במארקאפ בכל צורה, ו-`markupOnly()` (אותה הפשטה צרה מ-§ T-058) מסירה הערות כדי שקובץ שמתעד את הנוסח שממנו נגמל לא יורשע עליו. בדיקה שישית נועלת את **שני** כיווני הגלאי. **שלוש מוטציות ריצות, כל אחת הרגה בדיקה בשמה:** התווית כטקסט JSX ב-`app/error.tsx` → `⛔ no screen restates the retry label as a literal` נוקב בקובץ (**מול נוסח התוכנית: עובר**) · המשפט הישן ב-`MeScreen` → `shows a Hebrew sentence and a retry when the read failed…` · איחוד הכותרות → `gives the two boundary screens their own titles…`. ⚠️ `components/MeScreen.test.ts` שינה טענה מ-`toContain('לא הצלחנו לטעון את ההתקדמות כרגע.')` לטענה על הייבוא ועל `{FAILURE_HE.load}` — הבעלות על **הנוסח** עברה ל-`lib/core/failure.test.ts`, וטענה על המחרוזת הליטרלית היא בדיוק מה שאיפשר למסך לשאת את השלישי מארבעת הנוסחים. **חמש פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **810 בדיקות ✅ (מ-804, 54 קבצים)** · build ✅ · **check:mobile ✅ 594, 0 חדשות** (הטיק אינו נוגע בגאומטריה). TD-26 קוים: `rm -rf .next` → build. ⛔ אפס סוכני משנה · ⛔ `10-pedagogy.md` לא נפתח · ⛔ לא נגעתי ב-`main`. | `lib/core/failure.ts` (חדש) · `lib/core/failure.test.ts` (חדש) · `app/error.tsx` · `app/global-error.tsx` · `components/MeScreen.tsx` · `components/MeScreen.test.ts` · `components/OnboardingForm.tsx` · `docs/superpowers/plans/2026-08-13-institution-and-finish.md` (משימה 5 ✅) · `plan/30-architecture.md` (3.1.34) · `plan/50-tasks.md` (T-056 🟣) · `plan/00-control.md` |
| C-0083 | DEV | CRITIC | 07:45Z | **טיק ביצוע (◰) — משימה 4 מתוך 4 בתוכנית `2026-08-13-institution-and-finish.md`: בלוק "המטרה שלך" ב-`/me` (T-003). המשימה הושלמה במלואה → 🟣.** `LearnerGoal` (‏`institution` · `targetScore` · `examDate`) מיוצא מ-`components/MeScreen.tsx`, ו-`goal` הוא פרופ **חובה** ולא רשות — פרופ רשות הוא פרופ שקורא שוכח, וזו סיבה 2 של F-027. הבלוק נפתח ב-`goal.institution !== null || goal.targetScore !== null`; ⛔ **תאריך המבחן לבדו אינו פותח אותו** — הכלל ככתבו ב-§ 4.2ד, ולספירה לאחור כבר יש בית ב-`<StudiesScreen>` (סתירה נמדדת, ⛔ לא הורחב בסמכות Dev). שם המוסד `truncate` בשורה אחת עם `title`, כדי שגובה הבלוק לא ייגזר מאורך מה שהלומד הקליד. `app/(tabs)/me/page.tsx` קורא `institution, target_score, exam_date` ב-**`maybeSingle()`** ולא `single()` — קריאה שנכשלה מניבה מטרה ריקה שנראית כאין-בלוק, אותה שתיקה כנה ש-`wordsLearned === null` כבר משתמש בה. ⛔ אפס חישוב על שלושת הערכים (4.4.3): אין סף, אין הערכת מוכנות, אין ציון חזוי. ⚠️ **שני שומרים שהתוכנית עצמה כתבה חלולים, ושניהם נמדדו ולא שוערו:** ⓐ **המוטציה שצעד 7 בתוכנית מבטיח במפורש שתיתפס — אינה נתפסת.** התוכנית מחליפה `||` ב-`&&` ומצהירה "the regex requires the `||` composition"; נמדד שגם `goal\.institution[\s\S]{0,40}goal\.targetScore` וגם `toContain('!== null')` **עוברים על המוטנט**, פשוט כי `` !== null && `` הוא אותם 13 תווים כמו `` !== null || ``. כלומר הבדיקה היחידה שהוקצתה להתנהגות הליבה לא הבחינה בין "מוצג כשיש מוסד **או** ציון" לבין "מוצג רק כששניהם קיימים" — שני מסכים שונים לחלוטין ללומד שמילא שדה אחד. ⓑ **`page.toContain(column)` אינו טענה על קריאה** — נמדד: עם כל בלוק ה-`.from('profiles')` מחוק ורק אובייקט ה-`goal` נותר, שלושת השמות עדיין בקובץ דרך `profile?.institution ?? null`, ולכן שלוש הטענות עוברות **מעל מסך שאינו קורא כלום**. העמודות נטענות עכשיו בתוך ארגומנט ה-`select()` שנלכד אחרי `from('profiles')`. **ארבע מוטציות ריצות, כל אחת הרגה בדיקה בשמה:** `||`→`&&` → `hides itself only when institution and score are BOTH empty` (**מול נוסח התוכנית: 15 עוברות**) · מחיקת קריאת `profiles` → `selects the three goal columns from profiles` (**מול נוסח התוכנית: עוברת**) · `goal?:` → `takes the goal as a required prop…` · הסרת `truncate` → `shows a long institution on one line…`. **חמש פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **804 בדיקות ✅ (מ-796, 53 קבצים)** · build ✅ · **check:mobile ✅ 594 בדיקות, 0 חדשות** — הפיקסטורה קיבלה `SAMPLE_GOAL` עם שם מוסד עברי ארוך (`המכללה האקדמית להנדסה אורט בראודה`) כדי שההרכבה הרחבה ביותר תימדד, ו-`/dev/tabs/me @320px no horizontal scroll` ✅ הוא ההוכחה ש-`truncate` מחזיק. TD-26 קוים: `pkill` → `pgrep` ריק → `rm -rf .next` → build. ⛔ TD-19/TD-24 בעינם — `0009` טרם הורצה על פרויקט חי, ולכן הקריאה תיכשל, `profile` יהיה `null` והבלוק לא יוצג (כשל שקט ומכוון). ⛔ לא נטען סקיל עיצוב (שורת T-003 היא `—`) · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ לא נגעתי ב-`main`. | `components/MeScreen.tsx` · `components/MeScreen.test.ts` · `app/(tabs)/me/page.tsx` · `app/dev/tabs/me/page.tsx` · `docs/superpowers/plans/2026-08-13-institution-and-finish.md` (משימה 4 ✅) · `plan/30-architecture.md` (3.1.33) · `plan/50-tasks.md` (T-003 🟣) · `plan/00-control.md` |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
