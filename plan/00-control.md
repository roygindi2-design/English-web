<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0016                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: DEV                   # PM | DEV | CRITIC | HUMAN — C-0016 היה טיק תכנון; הטיק הבא של Dev הוא **ביצוע** של משימה 1 בתוכנית (F-009). ⚠️ תור הביקורת של ה-Critic לא התקצר: T-001/T-002/T-027/T-030/T-031/T-028/T-009/T-041. T-020 (PM) חוסמת 4 משימות, P-001 חוסמת T-034, ו-TD-17 (רשת) חוסמת את אותן 4 בפעם שנייה.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: "T-026"           # התוכנית: docs/superpowers/plans/2026-08-06-account-integrity.md — משימה 1 היא F-009, משימה 2 היא T-026, משימה 3 היא F-015+TD-5
CRITIC_ROUNDS_ON_TASK: 1          # סבב הביקורת הידני של רוי נספר כסבב 1 על T-001+T-002
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-06T23:47:46Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה.
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."

# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                  # "" | PM | DEV | CRITIC
LOCK_AT: ""                                                                           # ISO-8601. חובה לקרוא שעה אמיתית: date -u +%Y-%m-%dT%H:%M:%SZ
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "b5f26eab55c12d3a024659d2030cdf5855368d43"  # נסקר C-0012: מהיר. ארבע פקודות+check:mobile עברו טרי. main==dev — הכל אצל המשתמש

# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-06T20:14:32Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 1          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת, T-034) · F-008/F-009 🟡 · F-015 ⚪ · R-010 (אסור להעתיק פריטים ממאל"ו). **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.


### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0016 | DEV | DEV | 23:47Z | **טיק תכנון — לא נגעתי בקוד.** תוכנית התצוגה מוצתה, ולכן נכתבה תוכנית חדשה: `docs/superpowers/plans/2026-08-06-account-integrity.md`, שלוש משימות על אותם קבצים — F-009 (תקרת bcrypt נמדדת בבתים, ו-`validation_failed` מפסיק להאשים את שדה האימייל), T-026 (הכתובת שנרשמה מוצגת ב-onboarding עם תיקון בלחיצה אחת, ונמדדת דרך fixture כי `/onboarding` מפנה בלי env — TD-13), ו-F-015+TD-5 (`<LatinField>` יחיד ש-`enterKeyHint` חובה בטיפוס שלו). **המסלול הקריטי של M1 נחסם בפעם השנייה, והפעם בסביבה:** נמדד מהסנדבוקס — `newgeneralservicelist.com`, `kaikki.org` ו-`cefr-j.org` מחזירים `403 host_not_allowed`, ואילו GitHub ו-npm תקינים. לכן T-007/T-010/T-013/T-016/T-018 אינן ניתנות להרצה כאן כלל → TD-17 + T-043 (פעולה של רוי: להוסיף את קובצי המקור תחת `data/`). F-008 נבדקה ונפסלה מהתוכנית במכוון — התיקון המוצע שלה (200 `awaiting_email_confirmation`) היה משקר ללומד קיים כל עוד Q-001 ⓑ בתוקף ולא נשלח מייל כלל; היא חסומה ב-T-025. | תוכנית · TD-17 · T-043 |
| C-0015 | DEV | CRITIC | 23:16Z | **טיק ביצוע — T-041, המשימה האחרונה בתוכנית התצוגה. תוכנית התצוגה מוצתה.** TD-11 נסגר: `locateTarget` בליבה מחזיר את טווח מילת המטרה ו-`exampleSegments` מגיע מוכן מ-`buildCard`; React לא מחליט איזו מילה היא המטרה. **סקירת סוכן משנה תפסה ארבעה פגמים ממשיים, כל אחד עם פלט הרצה** — כיוון ההקלדה היה מבוי סתום (`controls: []` אחרי שליחה, גזר דין שחושב ונזרק), `revealed` לא אופס בהחלפת כרטיס (כרטיס ב׳ הגיע חשוף בלי כפתור), האכיפה החיובית של TD-14 לא נכתבה (`<span>` עירום השאיר הכל ירוק), ו-`mt-auto` היה חסר משמעות בלי `flex-1` (כפתור ב-y=243 מתוך 780 — F-011 הפוך). כל ארבעת התיקונים אומתו במוטציה. חמש פקודות רצו טרי: 229 בדיקות · 288 מובייל. | T-041 🟣 · TD-11 ✅ · TD-14 ✅ · TD-15 · TD-16 |
| C-0014 | DEV | CRITIC | 21:59Z | **טיק ביצוע — T-009 (משימה 2 מתוך 3 בתוכנית התצוגה).** `<EnWord>`/`<EnText>` הם הדלת היחידה לאנגלית, ומחסום סורק־מקור נכשל אם מישהו כותב `lang`/`dir`/`ltr-inline` ביד; כל 6 הבדיקות אומתו במוטציה (6 מוטציות מזיקות הפילו, פרמוט CSS לא). F-017 נסגרה **בשני הכיוונים** — `replaceAll` לבדו הפסיק לתפוס את האיות `A.I` בלי נקודה. סקירת סוכן משנה שינתה קוד בשלוש מדידות עצמאיות: `inline-block`→`inline` (104px→52px), `text-brand` הוסר (4.02–4.42:1 מתחת לרצפה), ו-`preview.pos` היה אנגלית עירומה שהמחסום לא ראה. חמש פקודות רצו טרי: 204 בדיקות · 168 מובייל. | T-009 🟣 · F-017 ✅ · TD-14 |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
