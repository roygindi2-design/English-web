<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0033                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # PM | DEV | CRITIC | HUMAN — **C-0033 (DEV) ביצע את משימה 3 מתוך 4 בתוכנית `2026-08-07-onboarding-goal.md`:** `components/OnboardingForm.tsx` החדש · `components/LatinField.tsx` (הרחבת שני איברי union) · `app/onboarding/page.tsx` (הטקסט הזמני הוסר) · טענה חיובית ב-`EnWord.test.ts`. ארבע הפקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **378 בדיקות ✅ (מ-376, 24 קבצים)** · build ✅ `Compiled successfully in 283ms`. בנוסף `check:mobile` רץ: **351 בדיקות — בדיוק כמו לפני השינוי**, וזו הראיה הישירה שהמסך החדש אינו נמדד (TD-13). **שני פגמים בתוכנית תוקנו בה במקום** (imports חסרים שמנעו קומפילציה · הודעת שגיאה בצבע טקסט העזרה) — הפירוט ביומן. **הטיק הבא של DEV: משימה 4 — `/dev/onboarding` בהארנס = סגירת TD-13.** ⚠️ **T-020 של ה-PM נשארת דחופה** (סוגרת F-021, משחררת T-018). חסמים אנושיים: T-043 (קובצי המקור) · P-001 (תוכן ל-T-034). **חוב סקירת עומק: C-0029 (T-015) · C-0031 (T-029/משימה 1) · C-0032 (T-029/משימה 2) · C-0033 (T-029/משימה 3).**
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-029             # **משימות 1–3 בוצעו (C-0031, C-0032, C-0033).** נותרה משימה 4 בתוכנית `2026-08-07-onboarding-goal.md`: `/dev/onboarding` + `scripts/verify-mobile.mjs` = TD-13
CRITIC_ROUNDS_ON_TASK: 0          # T-015 · T-029/משימות 1–3 טרם נסקרו
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-07T14:42:00Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה. F-021 היא הכרעת PM, לא הכרעת רוי, ואינה חוסמת את המדידה (המדיניות היא נתון ולא לוגיקה).
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."

# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: DEV                   # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: "2026-08-07T15:36:51Z"
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "6806e68"  # נסקר C-0031: מהיר. ארבע פקודות עברו טרי. main==dev אחרי קידום #4 — הכל אצל המשתמש

# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T13:09:15Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 4          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: **F-021 🟠 (חדש C-0020 — נגד ה-PM: 1.7.1 חסר מהתמצית + סתירת כללי סינון; חוסמת את T-018)** · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת, T-034) · F-008 🟡 (חסום ב-T-025) · R-010 (אסור להעתיק פריטים ממאל"ו). **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0033 | DEV | CRITIC | 14:42Z | **טיק ביצוע — משימה 3 מתוך 4 בתוכנית `2026-08-07-onboarding-goal.md`.** המסך של T-029: `components/OnboardingForm.tsx` מרנדר קבוצת רדיו (`data-daily-minutes`) שבה `DEFAULT_DAILY_MINUTES` נבחר מראש, שדה `<input type="date">` ילידי, ושדה ציון היעד. **שלוש החלטות שנבדקו מול הקובץ ולא מול התוכנית:** ⓐ רצפת בורר התאריכים (`min`) נגזרת מ-`toIsoDateInZone(new Date(), LEARNER_TIME_ZONE)` — אותו חשבון בדיוק שה-route משתמש בו מאז C-0032; רצפת UTC הייתה מציעה בבורר, בשעות הראשונות של כל יום מקומי, בדיוק את היום שהשרת דוחה. ⓑ שדה הציון עובר דרך `<LatinField>`, ולכן `LatinFieldProps` הורחב בשני איברי union (`name: 'target_score'` · `inputMode: 'numeric'`) במקום להוסיף שדה לטיני שני — TD-5 נסגר בכך ש-`LatinField` הוא היחיד, ו-`enterKeyHint` הוא prop חובה שרק הוא אוכף. ⓒ `app/onboarding/page.tsx` איבד את ההבטחה "השלב הזה עוד בבנייה" — טקסט זמני שנשאר אחרי שהוא כבר לא נכון הוא השקר הזול ביותר שמוצר מספר. **שני פגמים נמצאו בתוכנית עצמה, לא בקוד, ותוקנו בה במקום:** ⓐ רשימת ה-imports שהתוכנית נתנה ל-`OnboardingForm.tsx` **השמיטה** את `LEARNER_TIME_ZONE` ו-`toIsoDateInZone`, ששתי שורות אחר כך היא קוראת להם — כפי שנכתבה, הקובץ לא מתקמפל כלל. ⓑ **התוכנית רינדרה את הודעת השגיאה של שדה הציון ב-`text-ink-muted`** — בדיוק צבע טקסט העזרה שהיא מחליפה. הלומד היה רואה "הזן מספר שלם." באותו אפור של "הסולם הוא 50–150." ובלי שום סימן ויזואלי שמשהו נדחה, בזמן ש-`aria-invalid` **כן** דיווח לקורא מסך: הפער אינו בין נגיש ללא-נגיש אלא בין מה שה-DOM מצהיר למה שהעין רואה. הצבע הופרד ל-`text-danger`, וגם המחרוזת עצמה מתחלפת ממילא — צבע אינו הערוץ היחיד. **מוטציית TD-5 הפילה בדיוק את הבדיקה בשמה:** החלפת `<LatinField>` ב-`<input dir="ltr">` שקול → `routes the onboarding target-score input through LatinField, not a bare input`. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 378 בדיקות ✅ (מ-376, 24 קבצים) · build ✅ `Compiled successfully in 283ms`.** בנוסף הורץ `check:mobile` כביטוח רגרסיה על `LatinField` המשותף: `✓ all mobile/PWA guarantees hold (351 checks, widths 320/375/414px)` — **אותו מספר בדיוק כמו לפני הטיק. אפס בדיקות חדשות על מסך חדש לגמרי היא הראיה הישירה ש-TD-13 עדיין פתוח**, ולכן שורת TD-13 ב-30-architecture עודכנה מ"חוב עתידי" ל"חוב שעולה עכשיו". ⛔ אפס תלויות חדשות · ⛔ אין שינוי בנקודות קצה ולכן `docs/api-contract.md` לא נגע — משימה 2 היא זו שנגעה בו. | `components/OnboardingForm.tsx` · `components/LatinField.tsx` · `components/EnWord.test.ts` (+1) · `app/onboarding/page.tsx` · `docs/superpowers/plans/2026-08-07-onboarding-goal.md` |
| C-0032 | DEV | CRITIC | 13:53Z | **טיק ביצוע — משימה 2 מתוך 4 בתוכנית `2026-08-07-onboarding-goal.md`.** שכבת ההתמדה של T-029: `0004_onboarding_answers.sql` מוסיף ארבע עמודות **nullable** ל-`profiles` (‏"לא נשאל" ו-"בחר 5 דקות" חייבים להישאר שתי שורות שונות), הצהרה נפרדת לעמודה, ושתי מגבלות `check` **בשם** בתוך `do $$` — כי `add column ... check` מדולג כולו כשהעמודה כבר קיימת, ואז המגבלה הייתה נשלחת חסרה לכל פרויקט שהריץ טיוטה מוקדמת. ‏`POST /api/profile` מאמת דרך `checkOnboarding` **אחרי** בדיקת הסשן (קורא לא מאומת לא ילמד אילו שדות מתקבלים), וכותב ב-`update` ולא `upsert` (D-016). **מוטציית הסחיפה SQL↔TS נפלה כמתוכנן:** אפשרות רביעית ב-TS בלבד → `expected [5,10,20] to deeply equal [5,10,20,30]`. **שני פגמים נמצאו בתוכנית עצמה, לא בקוד, ותוקנו בה במקום:** ⓐ בדיקת המיגרציה סרקה את **טקסט הקובץ הגולמי**, ולכן הערת הכותרת שמסבירה מדוע `daily_minutes smallint not null` אסור **התאימה לרג'קס שאוסר אותו** — הסוויטה האדימה על מיגרציה תקינה. הכיוון המסוכן הוא ההפוך: על טקסט גולמי, הטענה החיובית `alter table ... daily_minutes` מסתפקת בעמודה שקיימת **רק בהערה**. מדד: מוטציה שהופכת את `onboarded_at` להערה עוברת בירוק לפני הפשטת ההערות ומפילה 2 בדיקות אחריה — מחסום שהערה יכולה לספק אינו מחסום (צורת F-007). ⓑ התוכנית גזרה את `today` מ-`new Date().toISOString().slice(0,10)`. ישראל היא UTC+2/+3, ולכן בשעתיים-שלוש הראשונות של כל יום מקומי תאריך ה-UTC הוא עדיין **אתמול** (‏`2026-09-09T21:30Z` = ‏`2026-09-10` בירושלים) — לומד שממלא את הטופס אחרי חצות **ביום המבחן** היה שומר תאריך שכבר עבר, ומנוע 7.1 היה מקבל מספר ימים שלילי. נוספו `LEARNER_TIME_ZONE` ו-`toIsoDateInZone` **טהורים** (השעון נשאר בקצה, ב-route), 4 בדיקות, והמוטציה חזרה-ל-UTC נופלת. אותו תיקון הוחל על נוסח משימה 3 בתוכנית, שם רצפת בורר התאריכים סבלה מאותו פגם. **ארבע פקודות רצו טרי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · 376 בדיקות ✅ (מ-366, 24 קבצים) · build ✅ `Compiled successfully in 3.2s`.** ⛔ אפס תלויות חדשות · `docs/api-contract.md` עודכן **באותו קומיט** (סעיף `POST /api/profile` + שורת `/api/onboarding` המתוכננת סומנה כמומשה, פרט לשאלת המוסד שנשארת ב-T-003). | `supabase/migrations/0004_onboarding_answers.sql` · `lib/supabase/onboarding.test.ts` (6) · `app/api/profile/route.ts` · `lib/core/onboarding.ts` (+2 יצואים, +4 בדיקות) · `docs/api-contract.md` |
| C-0031 | CRITIC | DEV | 13:09Z | **מהיר — 13:07Z, הרצה ידנית של רוי (השעה נפלה ב-13:00–13:14).** ארבע הפקודות רצו טרי אצלי: typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **366 בדיקות ✅ (23 קבצים)** · build ✅ `Compiled successfully in 301ms`. **קידום #4 בוצע** — `git merge --ff-only dev` → `push origin main`, ‏main==dev, כל C-0026..C-0031 אצל המשתמש. **אין ממצא חוסם:** F-021/F-006 בתיעוד (`15-syllabus-digest.md`, לא-חוסם) · F-012 טענת המרה = שיפור · F-019 חפיסה חיצונית שאינה נקלטת · F-008 🟡 · F-020 (הפגם היחיד) כבר תוקן C-0012. **T-011 נסגרה ✅** (שלוש-השאלות: plan/ עודכן ב-C-0027 build 0a997a9 · `dataSources.test.ts` 8 בדיקות · check:core נקי). לא נפתחו ממצאים חדשים (מהיר). חוב סקירת-עומק נצבר ולא-חוסם: C-0029 (T-015) · C-0031 (T-029/משימה 1). | `plan/00-control.md` · `plan/50-tasks.md` (T-011 ✅) |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
