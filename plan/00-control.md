<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0063                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: DEV                   # **C-0063 (DEV): טיק תכנון — לא נגעתי בקוד.** נכתבה `docs/superpowers/plans/2026-08-12-outside-sources.md` — 4 משימות המכסות **T-044** (משימות 1–2, החצי התוכנתי של 🟠 F-019) ו-**T-010** (משימות 3–4, החצי שנשאר פתוח מ-C-0051). הטיק הבא של DEV הוא **טיק ביצוע** עם `superpowers:executing-plans` החל ממשימה 1.
STATE: BUILDING                    # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: "T-044"           # **תוכנית קיימת: `docs/superpowers/plans/2026-08-12-outside-sources.md`.** הסדר: T-044 משימות 1–2 (פרסר `.apkg` טהור + סירוב קליטה, F-019), ואז T-010 משימות 3–4 (`words.cefr_profile_band` + פולט SQL). ⚠️ נמדד בתכנון: **124 מתוך 342 שורות תוכן חלוקות** בין `senses.cefr_level` (רמת משמעות, נכתבה אצלנו) לבין פרופיל CEFR-J/Octanove (רמת מילה) — **זה לא באג**, ולכן T-010 כותב לעמודה חדשה ולעולם לא דורס. ⛔ קובץ ה-`.apkg` של F-019 **אינו בריפו** — פריט 10 ב-`03-for-roy.md`. T-050 עדיין לא נפתחה בידי ה-PM.
CRITIC_ROUNDS_ON_TASK: 0          # C-0063: טיק תכנון, אפס קוד — אין מה לסקור. תור הסקירה שנותר ללא שינוי: T-005 · T-006 · T-018
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-12T13:51:25Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                  # "" | PM | DEV | CRITIC | CONTENT — לא נתפסה: טיק תכנון, אפס קוד (C-0063)
LOCK_AT: ""
MILESTONE_TICKS: 17
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "fcccfc0"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-12T01:15:52Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 6          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-026 🟡 (תוקן C-0052; נותרה שאלת סיווג `PSEUDOGAP!` ל-PM) · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום).**
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0063 | DEV | DEV | 13:51Z | **טיק תכנון (שלב 3) — ⛔ אפס שורות קוד, אפס תלויות, אפס נגיעה ב-`main`.** נבחרה 🟠 **F-019** לפי סדר העדיפויות (אין 🔴 פתוח; F-006 הוא קובץ של ה-PM, F-012 חסום ב-P-001), והתיקון המוצע שלה מפנה ל-**T-044**. ל-T-044 לא הייתה תוכנית, ולכן נכתבה `docs/superpowers/plans/2026-08-12-outside-sources.md` — **4 משימות המכסות שני מזהי משימה**: T-044 (1–2) ו-T-010 (3–4). **למה השתיים באותה תוכנית:** שתיהן קוראות קובץ שמישהו אחר כתב, ושתיהן מוכרעות ב**מקור** ולא באיכות — חפיסת F-019 טובה ובכל זאת נפסלת (כיוון הפוך + היעדר רישיון מסחרי, R-010), והפרופילים מורשים ובכל זאת אסור להם לדרוס. מוסכמה אחת: **טענה חיצונית נוחתת בעמודה משלה, לצד שלנו, לעולם לא מעליה.** **ארבע מדידות שנלקחו בתכנון והן שמעצבות את התוכנית:** ⓐ חיפוש `apkg`/`anki2` בריפו החזיר **0 תוצאות** — החפיסה של F-019 מעולם לא נכנסה לגיט, ולכן הפיקסצ׳רים משחזרים את ה**צורה** המתועדת בממצא, ואסור לטעון בשום מקום שהחפיסה עצמה נקראה; נוסף פריט 10 ל-`03-for-roy.md` והוא **אינו חוסם**. ⓑ ל-`words` **אין עמודת רמה כלל** (`0002_content_bank.sql:35-56`), בעוד `senses.cefr_level` קיימת ומשמעותה אחרת. ⓒ הפרופילים מכסים **287 exact_pos · 18 lemma_only · 1 miss** מתוך 306 זוגות `(headword, pos)` שבתוכן. ⓓ **124 מתוך 342 שורות חלוקות (36%)** בין הרמה שסוכן התוכן כתב לבין רמת הפרופיל — `A2→A1` 62 · `B1→A2` 21 · `B1→A1` 19 ועוד. **זו אינה תקלה, ולכן לא נפתח ממצא:** `senses.cefr_level` היא רמת **משמעות** (`mean` = להתכוון, B1) ו-CEFR-J היא רמת **מילה** (`mean`, A1) — שתיהן נכונות. לכן העמודות החדשות הן `cefr_profile_band`/`cefr_profile_source` ו⛔**לא** `cefr_level`, כדי לא לשחזר את התנגשות `is_function_word`/`lexical_class` (פריט 8). ⚠️ **שתי המדידות האחרונות נלקחו בנרמול גס** (lowercase בלבד, בלי הרחבת `/` ובלי `POS_ALIASES`), כי הרצת `levelOf` דורשת `npm install` וזה טיק תכנון — ולכן הן **רצפה ולא תוצאה**: משימה 3 שלב 1 מודדת מחדש עם המודול האמיתי, מחליפה אותן, ויש לה סף עצירה מפורש. התוכנית כוללת נתיבי קבצים מדויקים, בלוקי `Interfaces` עם חתימות, **קוד בדיקה אמיתי** (68 בדיקות חדשות), 22 מוטציות שכל אחת ממופה לבדיקה שחייבת ליפול בשמה, וסקירה עצמית. ⛔ לא נטען סקיל עיצוב · ⛔ `docs/api-contract.md` לא נגעתי בו (אין שינוי בנקודות קצה) · ⛔ לא נגעתי ב-`main`. | `docs/superpowers/plans/2026-08-12-outside-sources.md` · `plan/50-tasks.md` · `plan/00-control.md` |
| C-0062 | CRITIC | PM | 13:12Z | **מוד ⚡מהיר (השעה שנקראה: 13:06Z — הרצה ידנית שרוי הפעיל, לא חלון העומק 19:00Z).** הרצתי בעצמי את ארבע הפקודות, פלט טרי: `typecheck` ✅ · `check:core` → `/lib/core purity: OK` ✅ · `npm test` → **614 passed (43 files)** ✅ · `build` → Compiled ✅ (פונקציות שרת קיימות: `ƒ /api/review`, `ƒ /onboarding`, middleware). בנוסף `check:mobile` ✅ (399 בדיקות, רוחבים 320/375/414). **אימות חמש המשימות הטריות (T-042 C-0060 · T-045 C-0061 · T-047/T-048/T-049 C-0057–C-0058):** שלוש שאלות השער עוברות — plan/ עודכן, הבדיקות קיימות בפועל, `check:core` עובר; התוצרים קיימים על הדיסק (נבדק `ls`). כולן כבר מסומנות ✅ בידי Dev. **לא נפתחו ממצאים חדשים — מוד מהיר.** **⛔ לא קודם dev→main.** הסיבה היחידה: תקרת הקידום היומית כבר נוצלה **היום** ב-01:15:52Z (`LAST_PROMOTED_AT`), ו-§ 0.1.1 ב׳ קובע עד קידום אחד ליממה — קדם כבר היום, לא מקדם שוב גם בסקירה נקייה. הסקירה עצמה נקייה: אין 🔴/🟠-פגם פתוח חדש. **`LAST_REVIEWED_COMMIT` הושאר `fcccfc0` בכוונה** — מוד מהיר אינו סקירת עומק רב-צירית, וריצת העומק ב-19:00Z צריכה עדיין לכסות את C-0057..C-0061 (בעיקר צינור התוכן T-042). **PM הבא:** פתח T-050 (3.1.18, ~2,400 שורות `sense_*`) והכרע פריט 8 (`is_function_word` מול `lexical_class`). ⛔ לא נגעתי בקוד, ב-`main`, ולא נטען סקיל עיצוב. | `plan/00-control.md` · `plan/archive/handoff-log.md` |
| C-0061 | DEV | CRITIC | 13:00Z | **טיק ביצוע — T-045, משימה 4 והאחרונה בתוכנית `2026-08-12-content-lands.md`. התוכנית סגורה במלואה.** מחצית המשימה **אומתה ולא נכתבה מחדש**: `0003_low_confidence_is_visible.sql` כבר מחזיק את העמודה, את המילוי, את ה-RLS שפותח `low` ללומד ואת חסימת פריטי הניקוד. נוסף כאן החצי החסר בלבד — הסימן. **שלוש הכרעות שהן התנהגות ולא סגנון:** ⓐ **`unverified` הוא שדה של `CardFace` ו⛔אינו נקרא מהמשמעות בתוך React** — TD-11 הוא הלקח, אבל כאן הכשל חמור יותר: החזית של כרטיס **הפקה** היא ההנחיה בעברית, וסימן עליה אומר ללומד שה**שאלה** אינה אמינה לפני שענה, במקום שה**תרגום** ממתין לאישור; משום שהדגל הוא שדה של הפָּנים, "רק על הגב" נעול בבדיקה **בשני הכיוונים**, ו-`buildCard` מציב אותו במקום יחיד. ⓑ **`needsHumanReview` הוא חובה ב-`CardSense`, ⛔בלי ברירת מחדל** — אופציונלי ש-`= false` מציג משמעות שאיש לא סיווג כ**מאומתת**, בדיוק צורת הפגם של `is_function_word` וההפך מ-D-024; **נמדד:** מחיקת השדה מפיקסצ'ר הפילה typecheck בשם הקובץ, והפיכתו לאופציונלי החזירה **0 שגיאות** — כלומר ה-`required` עושה את העבודה. ⓒ **אפס טוקני צבע חדשים ואין `--warning`** — לפלטה 11 טוקנים ואף אחד אינו "אזהרה", והמצאת טוקן בקובץ קומפוננטה היא החלטה של `35-design-constitution.md` בלבד; הסימן הוא `text-ink-muted` (כבר נאכף ל-4.5:1 מול `--surface-raised`), הגליף `◇` הוא ערוץ שני ו-`aria-hidden` משאיר את קורא המסך על המשפט. **הטענה מפוצלת לשלוש בדיקות, ומה שהיא אינה מוכיחה כתוב בראש הקובץ:** סביבת Vitest היא `node` והסימן נגיש רק אחרי חשיפה (`revealed` הוא state), ולכן `renderToStaticMarkup` היה פולט כרטיס **בלי בלוק גב** וכל טענה הייתה עוברת בריק. במקום jsdom ומנוע לחיצות בשביל פסקה אחת: הליבה מודדת את הבוליאני בשני הכיוונים (4 בדיקות) · סריקת מקור בדפוס `EnWord.test.ts` (6) · ו-**`scripts/verify-mobile.mjs` חושפת את `/dev/card` במנוע אמיתי** ודורשת `[data-card-back] [data-card-unverified]` יחיד עם טקסט אחרי הפשטת הגליף, ו**אפס** תחת `[data-card-front]` — 9 בדיקות חדשות בשלושת הרוחבים. פיקסצ'ר `/dev/card` הוא היחיד בריפו שנבנה ממשמעות לא-מאומתת; אחיותיו נשארו `false` כדי שגם הגב הנקי יימדד. ⚠️ **מוטציה אחת שרדה וחיזקה בדיקה, והלקח רחב מהמשימה:** החלפת השער `{card.back.unverified ? (` ב-`{revealed ? (` השאירה את **כל שש** בדיקות הקומפוננטה ירוקות, כי ההערה המסבירה שמעל הסימן מכילה את המחרוזת `card.back.unverified` ויושבת באותו בלוק — **סריקה שפרוזה יכולה לספק אינה סריקה**. התיקון: הערות מופשטות לפני כל סריקה, השער נמדד כביטוי שלם ולא כמזהה חופשי, ונוספה בדיקה שאף תנאי שני אינו נפתח בין השער לטקסט. **שבע מוטציות רצו, שש הפילו בדיקה בשמה.** **חמש פקודות רצו טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **614 בדיקות ✅ (מ-604, 43 קבצים)** · build ✅ `Compiled successfully in 1188ms` · check:mobile ✅ `all mobile/PWA guarantees hold (399 checks, widths 320/375/414px)`. ⛔ אפס תלויות חדשות · ⛔ אין שינוי בנקודות קצה, ולכן `docs/api-contract.md` לא נגעתי בו · ⛔ לא נטען אף סקיל עיצוב · ⛔ לא נגעתי ב-`main`. | `lib/core/flashcard.ts` · `lib/core/flashcard.test.ts` · `components/Flashcard.tsx` · `components/Flashcard.test.ts` · `app/dev/card/page.tsx` · `app/dev/card/typed/page.tsx` · `app/dev/card/swap/page.tsx` · `scripts/verify-mobile.mjs` · `plan/30-architecture.md` (3.1.19) · `plan/50-tasks.md` (T-045 ✅) · `plan/00-control.md` |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
