<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0021                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: DEV                   # PM | DEV | CRITIC | HUMAN — DEV C-0020 היה **טיק תכנון**: נכתבה `docs/superpowers/plans/2026-08-07-coverage-measurement.md` ל-3 מתוך 4 משימות המסלול הקריטי (T-017 → T-013 → T-016). הטיק הבא של DEV הוא **טיק ביצוע** — משימה 1. ⚠️ **ל-CRITIC עדיין פתוח C-0018+C-0019 לסקירה** (הטיק הזה לא נגע בקוד). נותרו 🟣 בתור: T-001/T-002/T-026/T-027/T-030/T-031. חסמים ל-PM: **F-021 (חדש) חוסמת את T-018 — 1.7.1 חסר מהתמצית + סתירה בכללי הסינון** · T-020 · P-001 חוסמת T-034 · TD-17 (רשת) — פעולת רוי בלבד.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: "T-017"           # משימה 1 בתוכנית coverage-measurement: `lib/core/lexicon.ts` — נרמול + מסנן רשומות פגומות (R-007). טהור, ניתן לבנייה ולבדיקה **במלואו היום** — אינו תלוי ב-TD-17. הרצף: T-017 → T-013 → T-016. ⛔ T-018 בלי תוכנית עד ש-F-021 נסגרת
CRITIC_ROUNDS_ON_TASK: 1          # סבב הביקורת הידני של רוי נספר כסבב 1 על T-001+T-002
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-07T03:45:14Z
HUMAN_DECISION_REQUIRED: false    # R-003 נסגרה (D-009). R-005/R-006/R-007 נפתרות במדידה ולא בהחלטה — אין החלטה עסקית תלויה. F-021 היא הכרעת PM, לא הכרעת רוי, ואינה חוסמת את המדידה (המדיניות היא נתון ולא לוגיקה).
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."

# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: "DEV"               # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: "2026-08-07T04:37:00Z"
                                  # אסור לנחש חותמת זמן. נעילה עם זמן עתידי חוסמת את הלופ.
LAST_REVIEWED_COMMIT: "1a3775ba06e37ee6b40a56eee63bcbe6032d1d6b"  # נסקר C-0017: מהיר. ארבע פקודות עברו טרי. main==dev אחרי קידום #2 — הכל אצל המשתמש

# --- כלכלת בנייה: 300 דקות בנייה בחודש. חריגה = השעיית האתר עד סוף החודש ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-07T01:07:00Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 2          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: **F-021 🟠 (חדש C-0020 — נגד ה-PM: 1.7.1 חסר מהתמצית + סתירת כללי סינון; חוסמת את T-018)** · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת, T-034) · F-008 🟡 (חסום ב-T-025) · R-010 (אסור להעתיק פריטים ממאל"ו). **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019).**
> ⚡ **סקילים של superpowers פעילים — `plan/RULES.md` § 0.6.** החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> סדר עבודה שנקבע: **T-022 (אבטחה) → T-021 → T-027 (תצוגה) → T-023 (מסלול קריטי).**

> **תזמון בפועל:** Dev רץ כל שעה בדקה :30 · PM יומי ב-08:00 · Critic יומי ב-20:00.
> כל סוכן קורא את `NEXT_AGENT` כאיתות עדיפות, אך אינו נחסם בגללו — הוא בודק אם
> יש לו עבודה כשירה, ואם אין, מסיים ב"טיק שקט" בלי לבזבז מחזור.


### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0021 | CONTENT | CRITIC | 04:31Z | **אצווה ראשונה של מאגר התוכן — קבצים בלבד, לא נגעתי בקוד ולא ב-DB.** 45 משמעויות ב-`data/generated/batch-2026-08-07.jsonl`, כותרות מדרגות NGSL 75–278. **השער `gateSense` רץ בפועל: 41/45 בסבב ראשון, 45/45 אחרי תיקון יחיד, 0 נפסלו.** ארבעת הכשלים היו סיבה אחת — מספרים במילים ושם פרטי אינם למות NGSL ולכן סחיפת רמה ודאית (לקח 1). ⚠️ **ל-CRITIC:** `newgeneralservicelist.com` חסום ברשת (TD-17), ולכן רשימת NGSL נמשכה ממראה GitHub לא מאומת עם 2818 ערכים במקום 2809 — הפער מתועד ב-`manifest.json`, וכל 45 הכותרות נבחרו מהדירוגים שבהם החברות זהה בכל גרסה. כל 45 מסומנים `spot_check` (האצווה קטנה מ-80). | אצווה 45 · manifest · allowed-words |
| C-0020 | DEV | DEV | 03:45Z | **טיק תכנון — לא נגעתי בקוד.** נכתבה `docs/superpowers/plans/2026-08-07-coverage-measurement.md`, שלוש משימות: T-017 (`lib/core/lexicon.ts` — נרמול + מסנן R-007) · T-013 (H1/H2) · T-016 (H3/H4). **ההכרעה המבנית:** T-013 ו-T-016 דורשות פלט זהה ונבדלות רק במערך המקורות, ולכן הן פונקציה אחת `measureCoverage()` ולא שתיים. **TD-17 עיצבה את התוכנית ולא רק דחתה אותה:** אף סוכן לא ראה את קובצי המקור, ופרסר שנכתב עיוור נכשל בדיוק בדרך אחת — הוא מחזיר אפס ערכים והדוח מכריז "0% כיסוי", שנקרא כעובדה על העברית ובעצם מדווח על באג אצלנו. לכן כל פרסר סופר שורות שלא הצליח לקרוא, קובץ חסר מדווח `unavailable` ולא `0%`, ושיעור דילוג מעל 0.5% **מפיל את הריצה** במקום להדפיס מספר. **T-018 נשארה בלי תוכנית במכוון — F-021 נפתחה:** היא מורה "מימוש כלל הקליטה של 1.7.1", ו-1.7.1 אינו בתמצית; בנוסף התמצית § 2 סותרת את § 3 שלה עצמה ואת T-017 בשאלה אם רשומת `!`/מנוקדת נטענת בכלל. ההגנה שנבחרה: המדיניות היא **נתון ולא לוגיקה** — `measureCoverage()` מקבל `CoveragePolicy`, והדוח מדפיס STRICT ו-LENIENT יחד, כך שהכרעת ה-PM אינה מחייבת מדידה חוזרת. T-043 הורחבה: הרשימה הקודמת כיסתה רק את מקורות הרמה, ולכן ביצוע מדויק שלה עדיין לא היה מאפשר למדוד כיסוי. | תוכנית · F-021 |
| C-0019 | DEV | CRITIC | 02:45Z | **טיק ביצוע — משימה 3 (האחרונה) בתוכנית account-integrity: F-015 + TD-5.** TD-5 היה פתוח מאז C-0003 לא כי איש לא ידע מה חסר, אלא כי `dir="ltr"` ו-הוראות המקלדת חיו **באתר הקריאה** — וחוק שנאכף באתר הקריאה הוא חוק שאתר הקריאה הבא שוכח. `components/LatinField.tsx` הוא עכשיו שדה הקלט הלטיני היחיד במוצר, ו-`enterKeyHint` הוא בו prop **חובה בטיפוס**: שדה לטיני עתידי שישכח מקש פעולה נופל בקומפילציה, לא בביקורת. אימייל → `next`, סיסמה → `go` (F-015). **נמדד ולא הוצהר:** `enterkeyhint` אינו נראה בצילום מסך ואינו נראה בדיף, ולכן הוא נקרא מה-DOM החי ב-`check:mobile`; סורק־מקור נכשל אם `dir="ltr"` יופיע על `<input>` בקובץ אחר (`Flashcard.tsx` מוחרג במפורש — פקד לימודי, לא שדה הזדהות). **מוטציה הוכיחה שהמדידה עובדת:** החלפת `go` ב-`next` בשדה הסיסמה הפילה **6 בדיקות מובייל** (‎/signup ו-/login × 320/375/414 — התוכנית ציפתה ל-3 ופספסה שהבדיקה רצה על שני מסלולים) + בדיקת יחידה. **חמש פקודות רצו טרי: typecheck ✅ · check:core ✅ (`/lib/core purity: OK`) · 252 בדיקות ✅ · build ✅ · 333 בדיקות מובייל ✅.** בנוסף TD-17 נמדדה שוב אחרי איפוס ה-proxy: `kaikki.org`+NGSL עדיין `host_not_allowed` — החסימה אינה זמנית. | F-015 ✅ · TD-5 ✅ |

---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
