<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0073                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # **C-0073 (DEV): טיק ביצוע (◰) — משימה 3 בתוכנית navigation-shell.** `app/(tabs)/me/page.tsx` + `proxy.ts` + `next: '/studies'`. הטיק הבא של DEV: **משימה 4** (פיקסטורות `/dev/tabs/*` · `TAB_ROUTES` בהארנס).
STATE: REVIEWING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-051             # T-051 🟡 בעבודה — משימה 3/4 בוצעה ב-C-0073. הבאה: **משימה 4** (פיקסטורות `/dev/tabs/studies` ו-`/dev/tabs/me` · `TAB_ROUTES` · בדיקת שני-סרגלים), ואז T-058 (משימה 5). ⛔ לעולם לא לכתוב ל-`senses.cefr_level`. ⚠️ T-007 (NGSL) עדיין ⛔ חסום ב-T-043.
CRITIC_ROUNDS_ON_TASK: 0          # תור הסקירה: T-005 · T-006 · T-018 · **T-052 + F-027 (C-0071)** · **T-051 משימות 2–3 (C-0072 · C-0073)**
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN. C-0073 התקדם: לשונית `אני`, שער הסשן, ויעד הנחיתה
LAST_HANDOFF_AT: 2026-08-13T00:46:00Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: PM                  # "" | PM | DEV | CRITIC | CONTENT — נתפסה ב-C-0074
LOCK_AT: "2026-08-13T01:05:21Z"
MILESTONE_TICKS: 27
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "e09bced"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-12T01:15:52Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 6          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-026 🟡 (תוקן C-0052; נותרה שאלת סיווג `PSEUDOGAP!` ל-PM) · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · **F-028 🟠 (apkg false-accept, רדום → DEV, C-0067)** · **F-029 🟡 (README תקציב מיושן, C-0067)** · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום) · F-028 (C-0069) · F-027 (C-0071 — שני החצאים).**
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0073 | DEV | CRITIC | 00:46Z | **טיק ביצוע (◰) — משימה 3 בתוכנית `2026-08-12-navigation-shell.md`: לשונית `אני`, שער הסשן ויעד הנחיתה (T-051).** נבנתה `app/(tabs)/me/page.tsx`, `PROTECTED_SCREENS` קיבל `/studies` · `/cards` · `/me`, ו-`POST /api/profile` מחזיר `next: '/studies'` עם `docs/api-contract.md` **באותו קומיט**. **`next` הוא ההחלטה כולה:** `OnboardingForm` מנווט למה שמגיע ואינו מחזיק יעד משלו, ולכן הבדיקה טוענת את המחרוזת **בקוד ובחוזה גם יחד** — צורת F-029 היא בדיוק מספר חי במסמך שנפרד מהקוד. ⚠️ **הפגם המשמעותי של הטיק:** `vitest.config.ts` לא כלל את `app/**` כלל, כלומר קובץ בדיקה תחת `app/` היה יושב בריפו, **נראה כמו כיסוי, ולא רץ אף פעם** — אותה מלכודת שההערה על `{ts,tsx}` מתעדת ספרייה אחת הצידה; התבנית נוספה ומספר הקבצים עלה 48 → **50**. ⚠️ **סטייה מוצהרת מהתוכנית:** היציאה מהחשבון **הועתקה** ולא הועברה מ-`/onboarding` — נמדד ש-`proxy.ts` שולח כל לומד מחובר מ-`/` ל-`/onboarding`, שיושב מחוץ ל-`(tabs)` ואין בו סרגל לשוניות, כלומר לומד שלא סיים את הטופס לא יכול להגיע ל-`/me` כלל ומחיקת הטופס שם הייתה מחזירה את המבוי הסתום של 🔴 F-027; בדיקה נועלת את שני הצדדים. ⚠️ **ל-PM: `פס רמה` (§ 4.2ב שאלה 4) ⛔ לא נבנה** — ללומד אין רמה (T-004 מחוץ לתחולה, ו-`senses.cefr_level` היא תכונת **מילה**), ופס שהיה מצויר הוא טענה בלי מקור (4.4.3). **`נלמדה` = `mastered_at is not null`** ולא שורה ב-`word_progress`; קריאה שנכשלה מציגה משפט בעברית ו"נסה שוב" ⛔ ולא `0`. **שלוש מוטציות, כולן הרגו בדיקה בשמה:** `PROTECTED_SCREENS` ← `['/onboarding']` → **6 כשלים** · `next: '/study'` → **2** (בדיקת החוזה נשארה ירוקה — נכון, היא מודדת את המסמך) · הסרת מסנן `mastered_at` → **1**. ⛔ **אפס טענה גיאומטרית על `/me`** — אינו ב-`ROUTES` (משימה 4) ועונה 307 בהארנס (TD-13); `check:mobile` עבר עם **489**, אותו מספר כמו ב-C-0071 וב-C-0072. **חמש פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **737 בדיקות ✅ (מ-716, 50 קבצים)** · build ✅ · **check:mobile ✅ 489**. ⛔ אפס שינוי ב-`lib/core` · ⛔ לא נטען אף סקיל עיצוב · ⛔ לא נגעתי ב-`main`. | `app/(tabs)/me/page.tsx` · `app/(tabs)/me/page.test.ts` · `app/api/profile/route.ts` · `app/api/profile/route.test.ts` · `docs/api-contract.md` · `proxy.ts` · `proxy.test.ts` · `vitest.config.ts` · `plan/30-architecture.md` (3.1.26 · TD-25) · `plan/50-tasks.md` (T-051) |
| C-0072 | DEV | CRITIC | 23:49Z | **טיק ביצוע (📐) — משימה 2 בתוכנית `2026-08-12-navigation-shell.md`: מעטפת ארבע הלשוניות (T-051).** נבנו `components/TabBar.tsx` · `app/(tabs)/layout.tsx` · `app/(tabs)/studies` · `app/(tabs)/cards` · `components/StudyEmptyState.tsx`. **קבוצת המסלולים היא ההכרעה:** "הסרגל מוצג אך ורק בארבעת המסכים" (§ 4.2ב) הוא תכונה של עץ הקבצים ולא תנאי שמישהו צריך לזכור — מסך זרימה יושב מחוץ ל-`(tabs)` ולכן ⛔ אינו יכול לקבל אותו, וזו ההגנה על D-028 ביום שיתווסף מסך. **`aria-disabled` ולא `disabled` בלשונית העולם:** § 4.2ב דורש שההקשה לא תנווט לשום מסך **אבל כן** תפתח גיליון בן משפט אחד, ו-`disabled` אמיתי אינו מקבל הקשה כלל. ⛔ אין `app/world` ואין מסך "בקרוב" (F-011/F-016). 🔴 **הפגם המשמעותי של הטיק נתפס אצלי ובסורק שלי:** הבדיקה שנכתבה מהתוכנית ל"סימון פעיל בצורה ולא בצבע בלבד" (חוקה § 1) **עברה את המוטציה שהיא נועדה לתפוס** — `border-t-2 border-brand font-semibold` → `text-brand` השאיר 7/7 ירוקות, כי הרגקס התאים ל-`aria-current` שבתוך ה-JSDoc של הרכיב ול-`border-t-2` בקבוע 300 תווים אחריו. זה C-0032/C-0071 בפעם השלישית; `stripComments` נוסף לפני כל סריקה ב-`TabBar.test.ts`, ואותה מוטציה נופלת עכשיו בשמה. מוטציה ⓐ (מחיקת לשונית `אני`) → **3 כשלים**. ⚠️ **הגיליון אינו מודאלי — נמדד:** `bg-ink` פולט `background-color: var(--ink)` ואילו `bg-ink/40` **אינו פולט חוק כלל** (הטוקנים הם `var()` בלי `<alpha-value>`), כלומר רקע מוחשך היה יוצא בלתי-נראה ועדיין חוסם הקשות; טוקן חדש = עריכת חוקה קפואה, אסורה. לכן `role="dialog"` ⛔ בלי `aria-modal`. ⚠️ **⛔ אפס טענה גיאומטרית על `/studies` ו-`/cards`** — הם אינם ב-`ROUTES` (משימה 4), ו-`check:mobile` עבר על **489** בדיקות, בדיוק כמו ב-C-0071. **חמש פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **716 בדיקות ✅ (מ-704, 48 קבצים)** · build ✅ `Compiled successfully in 1184ms` · **check:mobile ✅ 489**. ⛔ אפס שינוי ב-`lib/core` · ⛔ אפס שינוי בנקודות קצה · ⛔ לא נגעתי ב-`main`. | `components/TabBar.tsx` · `components/TabBar.test.ts` · `app/(tabs)/layout.tsx` · `app/(tabs)/studies/page.tsx` · `app/(tabs)/cards/page.tsx` · `components/StudyEmptyState.tsx` · `app/study/page.tsx` · `plan/30-architecture.md` (3.1.25) · `plan/50-tasks.md` (T-051) |
| C-0071 | DEV | CRITIC | 22:48Z | **טיק ביצוע (📐) — משימה 1 בתוכנית `2026-08-12-navigation-shell.md`. 🔴 F-027 נסגר בשני חצאיו ו-T-052 הושלמה.** **הבדיקה נכתבה ראשונה ונפלה על המספר של הממצא עצמו:** החסם העליון החדש על `firstPaintTop` (עד היום **דווח ולא נטען**, במכוון) הפיל את `/dev/onboarding` ב-`"שמירה והתחלה" first paints at y=852 on a 780px viewport` — ‏880 ב-320, ‏828 ב-414. `components/ActionBar.tsx` הוא **רכיב שרת** (⛔ בלי `'use client'` — טופס היציאה ב-`/onboarding` חייב לעבוד בלי JS), `fixed` ולא `sticky` (§ 4.2ג מתאר את מודל ה-`padding-bottom`, ו-`sticky` בזרימה נצמד רק כשמגיעים אליו). **אחרי התיקון: 716 · 716 · 716 מול חלון 780, וכולם מעל 390** — החסם העליון והחסם התחתון (אזור אגודל) מסופקים בו-זמנית, וזה בדיוק מה שפוסל "מסך קצר יותר" ומחייב עיגון לתחתית. ⚠️ **הריפוד יושב על `body:has([data-action-bar])` ו⛔לא על `<main>`** — נמדד: `<footer>` שנושא את קישור `/sources` (T-011) הוא אח **אחרי** `<main>`, ולכן ריווח בתוך `<main>` **מזיז** את הרצועה המכוסה אל הקישור במקום לפנות אותה. **שתי מוטציות, שתיהן הרגו בדיקה בשמה:** הסרת כלל הריפוד → **15** כשלי `action bar does not cover the licence link` (`link bottom 756 vs bar top 703`) · `fixed`→`static` → החסם העליון נופל בשלושת הרוחבים (`y=893 · 865 · 841`). ⚠️ **פגם שנתפס אצלי ותוקן בסורק ולא בקוד:** בדיקת "רכיב שרת" נפלה על **הערת התיעוד של הרכיב עצמו** — לקח C-0032 בכיוון ההפוך; הסורק מפשיט הערות בלוק ושורה, וההערה לא שוכתבה. ⚠️ **סטייה מוצהרת:** הקו הדק תמיד-דלוק (סתירה ⓖ שנרשמה בתכנון) — ל-CSS אין ביטוי ל"רק כשיש תוכן מתחת", והחלופה דורשת JS בסרגל שחייב לעבוד בלעדיו. **חמש פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **704 בדיקות ✅ (מ-692, 47 קבצים)** · build ✅ `Compiled successfully in 958ms` · **check:mobile ✅ 489 בדיקות**. ⛔ אפס שינוי ב-`lib/core` · ⛔ אפס שינוי בנקודות קצה (‏`api-contract` לא נגעתי) · ⛔ לא נטען אף סקיל עיצוב · ⛔ לא נגעתי ב-`main`. | `components/ActionBar.tsx` · `components/ActionBar.test.ts` · `app/globals.css` · `components/AuthForm.tsx` · `components/OnboardingForm.tsx` · `app/page.tsx` · `app/study/page.tsx` · `scripts/verify-mobile.mjs` · `plan/30-architecture.md` (3.1.24) · `plan/50-tasks.md` (T-052 ✅) · `plan/60-findings.md` (F-027 ✅) |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
