<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0054                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # **C-0054 (DEV): טיק ביצוע — משימה 4 מתוך 5 בתוכנית `2026-08-12-sense-accuracy.md` (`lib/core/senseSelection.ts`, § 1.7.1 כללים 2/3/4/7). 15 בדיקות, **שש מוטציות הופלו, אפס ענפים לא-נמדדים**. סטייה מכוונת אחת מהתוכנית: `no_second_signal` הופרד מ-`two_signal_disagree` (פער כיסוי אינו כישלון דיוק — הלקח של F-026); ההתנהגות מול הלומד זהה. ארבע הפקודות ירוקות בהרצה שלי, **527 בדיקות**. תור הסקירה: T-005 · T-006 · T-029 · T-018/משימות 1–4 + חמש אצוות תוכן. הצעד הבא של Dev: משימה 5 (`senseAccuracy.ts` + הראנר) — היא זו שמפיקה את מספר R-006.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-018             # **משימות 1–4 בתוכנית `2026-08-12-sense-accuracy.md` בוצעו (C-0050 · C-0051 · C-0052 · C-0054). הצעד הבא של Dev: משימה 5 — `lib/core/senseAccuracy.ts` + `scripts/measure-sense-accuracy.mjs`, טיק ביצוע. T-010 נשארת ⬜ — הכתיבה לטבלת `words` לא בוצעה.** T-005/T-006/T-029 סגורות בקוד וממתינות לסקירה בלבד.
CRITIC_ROUNDS_ON_TASK: 1          # T-029 נסקר ✅ נקי. T-005/משימות 1–2 נסקרו — סבב 1. ממתינים לאימות: משימה 3/T-006 (C-0040) + משימה 4/T-005 (C-0041)
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN
LAST_HANDOFF_AT: 2026-08-12T04:42:28Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                    # "" | PM | DEV | CRITIC | CONTENT
LOCK_AT: ""
MILESTONE_TICKS: 8
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "fcccfc0"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-12T01:15:52Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 6          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-026 🟡 (תוקן C-0052; נותרה שאלת סיווג `PSEUDOGAP!` ל-PM) · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום).**
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0054 | DEV | CRITIC | 04:42Z | **טיק ביצוע — משימה 4 מתוך 5 בתוכנית `2026-08-12-sense-accuracy.md` (T-018).** נוצר `lib/core/senseSelection.ts` — המימוש היחיד של כלל הקליטה § 1.7.1, כללים 2/3/4/7, טהור לחלוטין וכולו מוזרק (`H1SynsetIndex` · `SecondSignal`), ולכן הכלל ניתן להוכחה **לפני** שייצוא ה-WordNet נוחת (T-043) במקום להמתין לו. **הדבר החשוב בטיק הזה הוא שסדר הכללים הוא התנהגות ולא סגנון, ושלוש בדיקות מכסות בדיוק את זה:** ⓐ כלל 2 נבדק **לפני** כלל 3, ולכן מילה חד-משמעית שה-H1 במקרה מכסה נשארת `fast_single` — אחרת שיעור הכיסוי של H1 היה מנופח בכל המילים שכלל 2 סגר לבדו. ⓑ כלל 7 (`covered[0]`) נכון **רק** משום ש-`sensesFor` מחזיר ממוין לפי `senseNumber`; הבדיקה מזינה את הרשומות בסדר הפוך (משמעות 2 לפני 1), ובלעדיה `covered[0]` היה "הראשון ברשימה" — בדיוק מה שכלל 7 אוסר. ⓒ שוויון דירוג נשאר על משמעות #1 (`r < bestRank` ולא `<=`): אות שנותן לכולם אותו דירוג אינו הצבעה למשמעות מאוחרת. ⚠️ **סטייה מכוונת אחת מהתוכנית, והיא רשומה בגוף התוכנית ובארכיטקטורה 3.1.14:** התוכנית החזירה `two_signal_disagree` גם כשהאות השני **לא הביע דעה כלל**, וזה זוקף פער **כיסוי** במקור השני לחובת ה**דיוק** של כלל 4 — אותה הטעיה בדיוק שהייתה F-026, שם פער R-005 נספר כפספוס R-006. הוסף `route: 'no_second_signal'`; `confidence:'low'` · `needsHumanReview:true` · `scorable:false` **זהים לחלוטין**, ולכן שום דבר שהלומד רואה לא זז — רק הסיבה המדווחת. משימה 5 אינה מושפעת: `AccuracyReport` מפלח לפי `Ambiguity` ולפי רמת CEFR ולעולם לא לפי `route`. **שבע בדיקות מעבר לשמונה שהתוכנית נתנה**, כולן על ענפים שהתוכנית השאירה ללא מדידה: H1 שקיים אך אינו מכסה מועמד (חייב ליפול לכלל 4 ולא להחזיר `h1_synset` ריק) · קדימות כלל 2 על כלל 3 · סדר קלט הפוך · שוויון דירוג · אות שמביע דעה על משמעות אחת בלבד · נרמול הלמה בצד החיפוש · ההפרדה עצמה. **שש מוטציות רצו וכל אחת הפילה בדיקה בשמה:** `covered[covered.length-1]` → **2 כשלים** · `scorable: chosen != null` → **2 כשלים** · כלל 2 אחרי כלל 3 → כשל הקדימות · `r <= bestRank` → כשל השוויון · קיפול `no_second_signal` ל-`disagree` → כשל ההפרדה · הסרת ענף `no_candidate` → כשל אי-הנפילה ל-POS אחר. **אין בקובץ ענף שאינו נמדד.** ⛔ `!` (non-null assertion) לא נכתב — `finish` הורחב ל-`SenseRecord | null | undefined` כדי **לענות** ל-`noUncheckedIndexedAccess` ולא להשתיק אותו. **ארבע פקודות רצו טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **527 בדיקות ✅ (מ-512, 34 קבצים)** · build ✅ `Compiled successfully in 252ms`. ⛔ אפס תלויות חדשות · ⛔ אין מסך ואין נגיעה בנקודות קצה, ולכן `docs/api-contract.md` לא נגעתי בו · ⛔ לא נטען אף סקיל עיצוב · ⛔ לא נגעתי ב-`main`. | `lib/core/senseSelection.ts` · `lib/core/senseSelection.test.ts` · `docs/superpowers/plans/2026-08-12-sense-accuracy.md` · `plan/30-architecture.md` (3.1.14) · `plan/50-tasks.md` · `plan/00-control.md` |
| C-0053 | CONTENT | — | 04:25Z | **אצוות תוכן — 45 כותרות NGSL (דירוגים 195–260), 58 משמעויות, 58/58 עברו את `gateSense` בסבב אחד, 0 נפסלו, 0 סומנו `low`.** הכותרות נלקחו הפעם מ**הדומיין הקנוני** `newgeneralservicelist.com` ולא מהמראה ב-GitHub — TD-17 אינו תקף עוד, והדבר נרשם לרוי (פריט 7) יחד עם סתירת 2,800/2,809/2,818 שאני **לא** מכריע. 🔴 **הלקח היחיד של הטיק הוא פגם ששער תקין אינו יכול לתפוס:** שני גזעים שהשער אישר כפו ריבוי (`several ____` · `many ____`) בעוד מפתח התשובות הוא הכותרת ביחיד — הלומד היה ממלא `hours` ומסומן כטועה. השער בודק שהגזע אינו **מכיל** את המילה; אין ולא יהיה בו בדיקה שהמילה **מתאימה** לגזע. תוקן, השער הורץ מחדש נקי, והכלל נכנס ל-§ א׳ שורה 8. ⛔ לא נגעתי בקוד, ב-DB, ב-`main` ובקבצים של PM/Critic. | `data/generated/batch-2026-08-12.jsonl` · `data/generated/manifest.json` · `data/generated/manifest-2026-08-11.json` · `plan/80-content-lessons.md` (§ א׳ 8) · `plan/03-for-roy.md` (7) · `plan/00-control.md` |
| C-0052 | DEV | CRITIC | 03:47Z | **טיק ביצוע — משימה 3 מתוך 5 בתוכנית `2026-08-12-sense-accuracy.md` (T-018).** נוצר `lib/core/senseGold.ts` — Hebrew Wordnet כתשובה הידועה של R-006, טהור, והצד העברי עובר דרך `classifyGloss()` **ודרכו בלבד** כדי ש-D-025 יחיה במקום אחד. 🔴 **הדבר החשוב בטיק הזה אינו המודול אלא מה שההרצה עליו חשפה — F-026, ‏CRITICAL:** ‏`GAP` לא סונן מעולם. ‏`GAP_RECORD` היה `/^GAP(\s|$)/` ("‏GAP כטוקן עומד"), אבל **הקובץ האמיתי מאיית `GAP!`** — נמדד: **702** שורות בדיוק כך · **3** שורות `PSEUDOGAP!` · **0** שורות בכתיב `GAP`/`GAP <note>` שהפיקסצ׳ר והבדיקות השתמשו בו, ו-705 השורות האלה הן בדיוק קבוצת השורות **שאין בהן אף תו עברי**. כלומר D-025 קבע "‏GAP אינו נטען" והקוד טען את כל 702 כגלוסות **`confidence:'high'`** — ה-`!` נגרר ולא מוביל, ולכן הן לא סומנו אפילו `low`. **קריאת קוד לא הייתה מגלה את זה:** הפונקציה נראית נכונה מול הפיקסצ׳ר שלה, וההרצה הראשונה על הקובץ האמיתי הדפיסה `gap: 0` במקום 702 — הסטייה מהטבלה הנמדדת שבראש התוכנית היא מה שחשף. **התיקון ב-`lexicon.ts` בלבד:** `/^GAP(!|\s|$)/` + `PSEUDO_GAP_RECORD` עם סיבת נפילה **נפרדת** (‏D-025 נוקב ב-`GAP` בלבד, ולכן 3 אינם מתחברים ל-702; שאלת הסיווג פתוחה ל-PM ב-F-026). ⚠️ **שלוש השלכות, והשלישית היא הסיבה שזה חוסם:** ⓐ `displayableGloss()` היה מחזיר את המחרוזת `"GAP!"` ככרטיסייה (`wash up → GAP!`) · ⓑ `coverage.ts` בודק `kind==='drop'` בלבד, ולכן עד 705 כותרות היו נספרות כמכוסות ב-R-005 · ⓒ **500 למות שכל רשומתן `GAP!` היו מקבלות מ-`judge()` את הפסק `miss` ולעולם לא `no_gold`** — כלומר פער הכיסוי של R-005 נזקף לדיוק של R-006, התקלה המדויקת שהפיצול `miss`/`no_gold` נועד למנוע. ✅ **אומת שלא הגיע ללומד:** אפס `GAP` בכל `data/generated/batch-*.jsonl` ובכל ה-SQL, ו-`ngsl-1.2.csv` עדיין חסר, ולכן שום מספר שגוי לא פורסם. **המדידה על הקובץ האמיתי אחרי התיקון:** `lines 17564 · lemmas 10049 · gap 702 · pseudoGap 3 · empty 0 · low 3301 · malformed 0` (+`acceptedGlosses 13460`) — **`lemmas` ירד 10,549 → 10,049**, כלומר 500 למות יצאו מהמדגם הנמדד כנדרש, והמספרים מכסים עכשיו **בדיוק** את טבלת התוכנית. **שמונה בדיקות מעבר לתוכנית**, כולן על פגמים שהבדיקות שהתוכנית נתנה לא היו תופסות: קדימות `accepted` על `low` (הצורה היחידה שבה שני הענפים נבדלים) · שורה תלת-עמודתית כ-`malformed` ולא חיתוך שקט · המונה `lines` שהתוכנית לא בדקה כלל · נרמול הצד האנגלי **בקובץ** ולא רק בשאילתה · `PSEUDOGAP!` בנפרד. **ארבע מוטציות הופלו:** הסרת ה-`!` → 4 כשלים בשני קבצים · היפוך `accepted`/`low` → כשל הקדימות · `cols.length < 2` → כשל · קיפול `pseudo_gap` ל-`droppedEmpty` → 2 כשלים. ⛔ **מוטציה חמישית נבדקה ואינה מפילה דבר** (סדר שתי התבניות — שתיהן עוגנות ב-`^`), ולכן אינני טוען שבדיקה שומרת עליה. **ארבע פקודות רצו טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **512 בדיקות ✅ (מ-494, 33 קבצים)** · build ✅ `Compiled successfully in 259ms`. ⛔ אפס תלויות חדשות · ⛔ אין מסך ואין נגיעה בנקודות קצה, ולכן `docs/api-contract.md` לא נגעתי בו · ⛔ לא נטען אף סקיל עיצוב. | `lib/core/senseGold.ts` · `lib/core/senseGold.test.ts` · `lib/core/lexicon.ts` (F-026) · `lib/core/lexicon.test.ts` · `docs/superpowers/plans/2026-08-12-sense-accuracy.md` · `plan/30-architecture.md` (3.1.13) · `plan/50-tasks.md` · `plan/60-findings.md` (F-026) · `plan/00-control.md` |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
