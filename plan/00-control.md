<!--
CYCLE_ID: C-0104                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # 📐 **טיק ביצוע C-0104 — משימה 8 של `2026-08-13-study-queue.md`, האחרונה של T-065: פיקסצ'ר `/dev/deck` + בלוק המדידה ב-`scripts/verify-mobile.mjs`.** ההארנס צייר את `<CardDeck>` בפעם הראשונה ו**הפיל אותו פעמיים**: `h-dvh` הוריד את שני כפתורי הסימון 52px מתחת לקו (`y=105..832` במסך 780px), ו-`flex-1` הקריס את הכרטיס ל-215px כי עמודת השורש היא `min-h-dvh` וגובהה בלתי-מוגדר. שני הפגמים תוקנו **באותו טיק**: החפיסה מצהירה `h-[calc(100dvh-10rem)]`, המספר נרשם **TD-30**, וההארנס מודד את התוצאה מול **מכולת ה-snap** ו⛔ לא מול החלון. **שתי מוטציות אימתו בהרצה.** **אימות טרי בהודעה: typecheck ✅ · check:core `OK` ✅ · 1008/1008 ב-63 קבצים ✅ (מ-991) · build ✅ · check:mobile 645 ✅ (מ-609, +36).** ⚠️ סטייה אחת: `components/CardDeck.tsx` ו-`CardDeck.test.ts` נגעו — ברשימת T-065 אך לא של משימה 8. ✅ **T-065 שלמה — כל שמונה המשימות של התוכנית סגורות.** ⛔ לא נגעתי ב-`main`.
STATE: REVIEWING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: ""                # ✅ **T-065 נסגרה במלואה C-0104** — שמונה משימות, C-0096→C-0104. ✅ T-064 נסגרה C-0100. ⛔ **T-066 חסומה** (D-035, תנאי ⓑ) · ⛔ T-019·T-046·T-043 בידיים אנושיות. ⛔ לעולם לא לכתוב ל-`senses.cefr_level`, **וגם לא למיין לפיו** (D-034). ⬜ אין תוכנית מימוש פתוחה — הטיק הבא של Dev הוא טיק תכנון, אלא אם ה-Critic יפתח ממצאים.
CRITIC_ROUNDS_ON_TASK: 0          # תור הסקירה: T-005 · T-006 · T-018 · **T-052 + F-027 (C-0071)** · **F-033 🟣 (C-0095, נגד ה-PM)** · **T-064 מלאה 🟣 (מ׳1-4, C-0096→C-0100)** · **T-065 מ׳5 🟣 (`CardDeck`, C-0101)** · **T-065 מ׳6 🟣 (`StudyDeckScreen`, C-0102)** · ✅נסגרו C-0099: F-034·F-035·F-036 · **T-065 מ׳7 🟣 (`CardsScreen` — הבורר, C-0103)** · **T-065 מ׳8 🟣 (`/dev/deck` + תיקון גובה `CardDeck`, C-0104) — T-065 שלמה**
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN. ⚠️ C-0087 לא נספר כ"אין התקדמות" — הוא עצירת בלם מכוונת, לא כישלון להתקדם
LAST_HANDOFF_AT: 2026-08-14T01:04:06Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: ""                  # C-0104 שוחררה בסוף הטיק
LOCK_AT: ""
MILESTONE_TICKS: 58                # ✅ מתחת לתקרה (60, RULES § 0.1.1 ו׳). מתאפס אך ורק ב-MILESTONE_DONE
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "e1fd933"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-13T19:27:15Z"  # ISO-8601 של הקידום האחרון ל-main (C-0098)
PROMOTIONS_THIS_MONTH: 7          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · **F-028 🟠 (apkg false-accept, רדום → DEV, C-0067)** · **F-029 🟡 (README תקציב מיושן, C-0067)** · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום) · F-028 (C-0069) · F-027 (C-0071 — שני החצאים) · **F-026 (C-0086 — D-029 מצוטטת, הקוד נבדק ולא הונח)**.**
> ✅ **חסימת הקידום שוחררה C-0092 (PM):** `main` אוחה ל-`dev` בקומיט `db71f27` — התנגשות `02-inbox.md` נפתרה (י׳/יא׳ נשמרו ושניהם טופלו באותו טיק). **אומת בהרצה: `git merge-base --is-ancestor origin/main dev` ⇒ exit 0** ⇒ `git merge --ff-only dev` יעבור אצל ה-Critic, וכ-27 מחזורים כולל תיקון F-027 יכולים לעלות לאוויר. ⚠️ **החצי שנשאר פתוח לרוי (פריט 17): ⛔ אין לערוך את `main` ישירות דרך עורך GitHub** — אחרת אותה חסימה תחזור. ⚠️ ‏`PROMOTIONS_THIS_MONTH=6` מול main — ייתכן 6 קידומים שנכשלו בשקט, לבדיקת רוי.
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0102 | DEV | CRITIC | 23:01Z | 📐 **טיק ביצוע — משימה 6 מתוך 8 של `2026-08-13-study-queue.md`: `/study` מתחבר לתור.** TDD קוים: שלושת קובצי הבדיקה נכתבו והורצו **אדומים** (7 נכשלו / 5 עברו) לפני שנכתבה שורת מימוש. **ההכרעות שנקבעו בקוד ולא בהערה:** ⓐ **בחירת ה-endpoint לפי החפיסה היא כל D-033** — `unknown` ⇒ `/api/practice`, `due` ⇒ `/api/review`; החפיסה מציגה ללומד «תרגול — לא משנה את מועד החזרה» כל אותו זמן, והחלפת שני החוטים לא הייתה שגיאת הקלדה אלא הפיכת ההבטחה שעל המסך לשקר. נמדד **בהכלת סוגריים** (לקח C-0100) ⛔ ולא במרחק תווים, ואומת במוטציה: החלפת שני המסלולים ⇒ אדום. ⓑ **סימון שנכשל נזרק הלאה** — `<CardDeck>` משאיר כרטיס בדיוק כאשר `onGraded` נדחה, ולכן בליעת השגיאה כאן הייתה מחליקה את הכרטיס מהמסך כאילו נשמר. **הבדיקה הראשונה שנכתבה לזה מדדה את `sendGrade` — שזורק ממילא — ונשארה ירוקה כשמחקתי את ה-`throw`;** היא הוחלפה במדידת בלוק ה-`catch` עצמו, ואז המוטציה הפילה אותה. ⓒ **`schema_missing` מקבל משפט משלו ⛔ ולעולם לא את המצב הריק** — «אין כרטיסיות» אומר ללומד שסיים להיום, ותקלה אצלנו שמדווחת כסיום שולחת אותו מרוצה ממוצר שבור. ⓓ **`elapsed_ms` = `Math.min(Math.round(…), MAX_ELAPSED_MS)` עם המספר מיובא** — ⛔ לא ערך שהמסלול ידחה ב-400 ו⛔ לא 600000 שנכתב מחדש. **אימות טרי בהודעה הזו:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **980/980 ב-62 קבצים ✅** (מ-951/59, +29) · build ✅ `Compiled successfully in 369ms` · **`check:mobile` 609/609 ✅** בשלושת הרוחבים. ⚠️ **ארבע סטיות מדווחות:** ⓐ **`apiGet` שונה** — הוא זרק `Error` על כל תשובה שאינה 2xx וזרק איתה את הקוד שמבדיל בין שלוש התקלות של התור; אפס קוראים נמדדו לפניו. ⓑ **שומר F-027 ב-`ActionBar.test.ts` הופנה** מ-`app/study/page.tsx` ל-`components/StudyDeckScreen.tsx` — הסרגל עבר עם המצבים שהוא שייך להם; מחיקת השורה הייתה מוציאה את `/study` מהשומר. ⓒ **הפעולה במצב הריק תלוית-חפיסה** — «התחל מילים חדשות» על חפיסת `unknown` הוא קישור למסך שהלומד עומד בו, כלומר המבוי הסתום של F-027. ⓓ **`check:mobile` נפל ותוקן בהארנס אחרי שנמדד שהכשל אינו במסך** — בלי ENV התור עונה 503 לפי חוזהו ו-Chromium רושם זאת; נוסף `EXPECTED_CONSOLE` צר לכתובת אחת ולסטטוס אחד (TD-29), ⛔ ולא פטור למסלול. ⚠️ **חוב חדש TD-28:** `elapsed_ms` נמדד מהסימון הקודם ולא מכניסת הכרטיס לתצוגה. ⛔ לא נגעתי ב-`Flashcard.tsx` ו⛔ לא ב-`CardDeck.tsx` · ⛔ לא נגעתי ב-`main` · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ אפס `[skip ci]` · ⛔ אפס תלויות חדשות · ⛔ `docs/api-contract.md` לא נגעתי — הטיק אינו משנה אף נקודת קצה. | `components/StudyDeckScreen.tsx` (חדש) · `components/StudyDeckScreen.test.ts` (חדש) · `app/study/page.tsx` · `app/study/page.test.ts` (חדש) · `lib/api/client.ts` · `lib/api/client.test.ts` (חדש) · `components/ActionBar.test.ts` · `scripts/verify-mobile.mjs` · `plan/30-architecture.md` · `plan/50-tasks.md` · `plan/00-control.md` · `docs/superpowers/plans/2026-08-13-study-queue.md` |
| C-0103 | DEV | CRITIC | 23:51Z | 📐 **טיק ביצוע — משימה 7 מתוך 8 של `2026-08-13-study-queue.md`: הבורר בלשונית `כרטיסיות`.** TDD קוים: `CardsScreen.test.ts` נכתב והורץ **אדום** (9 נכשלו / 2 עברו) מול המימוש הישן לפני שנכתבה שורת מימוש. **ההכרעות שנקבעו בקוד ולא בהערה:** ⓐ **המונה נמשך ב-`limit=1` ונקרא מ-`total`** — החוזה קובע ש-`total` הוא הספירה **לפני** החיתוך, וזו הסיבה היחידה ששורה אחת מספיקה; `cards.length` **נאסר בבדיקה** כי הוא היה מדפיס «1» לכל חפיסה לא-ריקה, מספר שתמיד שגוי ו**לעולם אינו שגוי בעליל**. ⓑ **שלושה כרטיסים תמיד** — הבדיקה סופרת `key: '` בדיוק שלוש פעמים; בורר שגדל ומצטמצם משאיר לומד בלי יכולת להבחין אם המוצר השתנה או הוא. ⓒ **חפיסה ריקה מושבתת עם המספר** — ממומש ב-`const body` **אחד** שמשמש את שני הענפים, ולכן ענף מושבת **אינו יכול** לאבד את המספר; נמדד בהכלת סוגריים (שני `{body}` בתוך אזור `{entry.enabled ? (`), ו**מוטציה אימתה**: החלפת הגוף בתווית בלבד ⇒ אדום. ⓓ **D-035 נאכפת בקומפיילר** — `DeckEntry` הוא איחוד מובחן שבו `enabled: true` נושא `href: string`, ולכן ענף הניווט אינו יכול לקבל כרטיס בלי יעד; ל-`sentences` יש `href: null` בתוך אובייקט הכניסה, נמדד בהכלת סוגריים ולא במרחק תווים, ו**מוטציה אימתה**: מתן יעד ⇒ אדום. ⓔ **«—» ⛔ אינו `0`** — קריאה שנכשלה וחפיסה ריקה נראות זהות על המסך ורק אחת מהן נכונה (אותו כלל של `<MeScreen>` על `wordsLearned`); כשל ⇒ שלושה מושבתים, נאכף בשלילת `FAILURE_HE` ו-`StudyEmptyState` מהקובץ. ⓕ **המושבת הוא `<button type="button">` בלי מטפל ו⛔ לא `disabled`** — «לא עושה כלום» מבני, והכרטיס נשאר מקבל פוקוס כדי שקורא מסך ימצא את החפיסה וישמע ש-`aria-disabled`. **אימות טרי בהודעה הזו:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **991/991 ב-63 קבצים ✅** (מ-980/62, +11) · build ✅ `Compiled successfully in 303ms` · **`check:mobile` 609/609 ✅** בשלושת הרוחבים. ⚠️ **סטייה אחת מדווחת:** `scripts/verify-mobile.mjs` נגעה למרות שאינה ברשימת הקבצים של המשימה — ברגע שהמסך מבצע רשת, `clean console` נפל על `/dev/tabs/cards` בשלושת הרוחבים בשתי שגיאות 503; הכשל **נמדד ואינו במסך** (ההארנס מריץ `next start` בלי ENV, והתור עונה 503 לפי חוזהו), ולכן נוסף `EXPECTED_CONSOLE` הקשור ל**שתי הכתובות המדויקות ולסטטוס אחד** — 401 או 500 עדיין מפילים. ⛔ לא פטור לנתיב ו⛔ לא הורדת הפיקסצ'ר. TD-29 הורחב בהתאם. ⚠️ **`<StudyEmptyState>` נותר עם קורא יחיד** ותיעודו עודכן, כדי שהקורא הבא לא יחזיר אותו ללשונית. ⛔ לא נגעתי ב-`Flashcard.tsx`, ב-`CardDeck.tsx` ו⛔ לא ב-`StudyDeckScreen.tsx` · ⛔ לא נגעתי ב-`main` · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ אפס `[skip ci]` · ⛔ אפס תלויות חדשות · ⛔ `docs/api-contract.md` לא נגעתי — הטיק אינו משנה אף נקודת קצה. | `components/CardsScreen.tsx` · `components/CardsScreen.test.ts` (חדש) · `components/StudyEmptyState.tsx` (תיעוד) · `app/dev/tabs/cards/page.tsx` (תיעוד) · `scripts/verify-mobile.mjs` · `plan/30-architecture.md` · `plan/50-tasks.md` · `plan/00-control.md` · `docs/superpowers/plans/2026-08-13-study-queue.md` |
| C-0104 | DEV | CRITIC | 01:04Z | 📐 **טיק ביצוע — משימה 8 מתוך 8 של `2026-08-13-study-queue.md`, האחרונה של T-065: המדידה.** TDD קוים: הבדיקות ב-`scripts/verify-mobile.test.ts` נכתבו והורצו **אדומות** על `ENOENT: app/dev/deck/page.tsx` לפני שנכתבה שורת מימוש. **מה שהמשימה הזו קנתה, וזה כל עניינה:** `/study` נמצא ב-`ROUTES` מאז T-041, אבל ההארנס מריץ `next start` בלי ENV והתור עונה 503 לפי חוזהו — ולכן **כל שורת `ok /study` שההארנס אי פעם הדפיס תיארה את מצב הכשל**, ו-`components/CardDeck.tsx` מעולם לא צויר ב-320/375/414. הפיקסצ'ר צייר אותו, ו**הוא נפל פעמיים ברצף — שני פגמים אמיתיים, שניהם בקוד שכבר עבר סקירה:** ⓐ **`h-dvh` ⇒ כרטיס 1 תפס `y=105..832` במסך 780px** — החפיסה אינה בעלת המסך (כותרת ה-layout מעליה, תחתית הרישיונות מתחתיה), ולכן 52px ואיתם **שני כפתורי הסימון** ירדו מתחת לקו הקיפול; זו F-027 בדלת אחורית. ⓑ **`flex-1` ⇒ כרטיס 1 קרס ל-215px וכרטיס 2 נראה מתחתיו** — עמודת השורש היא `min-h-dvh`, כלומר גובה **בלתי-מוגדר**, ולכן שום צאצא אינו נמתח ו-`h-full` על הכרטיס נופל ל-`auto`. **ההכרעה:** חפיסת `scroll-snap` מחייבת גובה מוגדר ולכן היא **מצהירה** עליו — `h-[calc(100dvh-10rem)]`, המסך פחות ה-chrome שה-layout מצייר (52+32+76). ⚠️ זה מספר על קובץ אחר, נרשם **TD-30**, ו⛔ אינו נסמך עליו: ההארנס מודד את התוצאה בשלושת הרוחבים, ולכן שינוי ב-chrome הוא ריצה אדומה ולא 16px שקטים. ⛔ החלופה שנפסלה: `h-dvh` על עמודת השורש + `overflow-y-auto` על `main` (app-shell) — היא מבטלת את `window.scrollTo` שעליו נשענות בדיקות ההישג ומשנה **כל** מסך במוצר, כלומר החלטת ארכיטקטורה שאף משימה לא ביקשה. **המדידה נכתבה מול מכולת ה-snap ו⛔ לא מול החלון** — המכולה חותכת, ולכן כרטיס שחורג מ-`innerHeight` יכול להיות בלתי-נראה וכרטיס שחסרים לו 40px יכול להיות חצי על המסך; שלוש טענות (המכולה על המסך · כרטיס 1 ממלא אותה · כרטיס 2 מתחיל בקצה או אחריו). **יחידת ה-snap היא ה-`<article>` ו⛔ לא `[data-flashcard]` שבתוכו** — הנוסח הראשון דיווח «567 מתוך 583» והרשיע את החפיסה על ה-`pt-4` שהעיצוב מבקש. **שתי מוטציות אימתו בהרצה:** החזרת `h-dvh` ⇒ `the deck fits on screen` אדום בשלושת הרוחבים (`89..832`) · `[MIN_TAP, MIN_GAP]`→`[44, 8]` ⇒ בדיקת הרצפות אדומה — ⚠️ **והנוסח הראשון של אותה בדיקה נשאר ירוק על אותה מוטציה** (הוא דרש שהשמות יופיעו בבלוק, והם מופיעים גם בתוויות), ולכן הוחלף במדידת הארגומנט עצמו. **אימות טרי בהודעה הזו:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **1008/1008 ב-63 קבצים ✅** (מ-991) · build ✅ · **`check:mobile` 645/645 ✅** (מ-609, +36 = 12 בדיקות × שלושה רוחבים). ⚠️ **סטייה אחת מדווחת:** `components/CardDeck.tsx` ו-`CardDeck.test.ts` נגעו — הם ברשימת הקבצים של T-065 אך לא של משימה 8; בדיקת המקור שדרשה את המחרוזת `h-dvh` הוחלפה בבדיקה שדורשת גובה מוגדר, כי המחרוזת שהיא הגנה עליה היא בדיוק הפגם שנמדד. ⛔ הפיקסצ'ר לא הורד מהרשימה ו⛔ לא ניתן לו פטור. ⛔ לא נגעתי ב-`Flashcard.tsx` · ⛔ לא נגעתי ב-`main` · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ אפס `[skip ci]` · ⛔ אפס תלויות חדשות · ⛔ `docs/api-contract.md` לא נגעה — הטיק אינו משנה אף נקודת קצה. | `app/dev/deck/page.tsx` (חדש) · `app/dev/deck/layout.tsx` (חדש) · `scripts/verify-mobile.mjs` · `scripts/verify-mobile.test.ts` · `components/CardDeck.tsx` · `components/CardDeck.test.ts` · `plan/30-architecture.md` · `plan/50-tasks.md` · `plan/00-control.md` · `docs/superpowers/plans/2026-08-13-study-queue.md` |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
