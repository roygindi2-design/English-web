<!--
================================================================================
  plan/00-control.md — מצב בלבד. הקובץ הראשון שכל סוכן קורא, וחייב להישאר זעיר.
  ------------------------------------------------------------------------------
  ⛔ אין לכתוב לכאן חוקים, נימוקים, מסקנות או פרוזה. רק מצב.
     החוקים: plan/RULES.md (ובפרומפט של כל סוכן).
     תקרה קשיחה: 60 שורות. עבר? משהו שאינו מצב נכנס לכאן — הוצא אותו.
================================================================================
-->

# בקרת לופ — מצב

> המדור היחיד שכל שלושת הסוכנים כותבים אליו. חייב להיות תקין תמיד — אם הוא שבור, הלופ עוצר.

```yaml
CYCLE_ID: C-0080                  # מזהה רץ. עולה ב-1 בכל מעבר PM→Dev→Critic→PM
NEXT_AGENT: CRITIC                # **C-0080 (DEV): טיק ביצוע (◰) — משימה 2 מתוך 4 בתוכנית `2026-08-13-institution-and-finish.md`: `institution` עובר `POST /api/profile` אל העמודה, ו-`docs/api-contract.md` עודכן באותו קומיט.** **789 בדיקות ✅ · typecheck ✅ · check:core OK ✅ · build ✅.** ⛔ לא נגעתי ב-`main`.
STATE: BUILDING                   # PLANNING | BUILDING | REVIEWING | BLOCKED | MILESTONE_DONE
ACTIVE_MILESTONE: M0              # M0..M6
ACTIVE_TASK_ID: T-003             # 🔵 **משימות 1–2/4 הושלמו (C-0079 · C-0080).** הבאה: **משימה 3** — שדה המוסד ב-`components/OnboardingForm.tsx` (מעל שדה הציון). אחריה משימה 4, ואז **T-056** (5) · **T-057** (6). ⛔ לעולם לא לכתוב ל-`senses.cefr_level`. ⚠️ T-007 (NGSL) עדיין ⛔ חסום ב-T-043. T-059 ⬜ תיעוד בלבד.
CRITIC_ROUNDS_ON_TASK: 0          # תור הסקירה: T-005 · T-006 · T-018 · **T-052 + F-027 (C-0071)** · **T-051 משימות 2–5 (C-0072 · C-0073 · C-0075 · C-0076) + T-058** · **T-003 משימות 1–2 + F-030 (C-0079 · C-0080)**
CONSECUTIVE_NO_PROGRESS: 0        # תקרה: 2 → מעבר אוטומטי ל-HUMAN. C-0080 התקדם: 785 → 789 בדיקות, משימה 2/4 נסגרה
LAST_HANDOFF_AT: 2026-08-13T05:42:21Z
HUMAN_DECISION_REQUIRED: false    # אין החלטה עסקית תלויה. שלוש פעולות אנושיות ממתינות ואינן עוצרות את הלופ: T-043 (קובצי מקור) · T-046 (טוקן Netlify) · אישור ידני של R-015
BUDGET_NOTE: "כל מקורות התוכן מורשים לשימוש מסחרי בעלות אפס: NGSL (CC BY-SA 4.0) · CEFR-J (מסחרי בציטוט) · Octanove (CC BY-SA 4.0) · Hebrew Wordnet (רישיון פרמיסיבי של אונ׳ חיפה, ללא share-alike — אומת C-0001, H1g) · Kaikki/ויקימילון (CC BY-SA) · word2word (Apache-2.0). ⛔ PanLex ו-MUSE נפסלו ברישיון NC (1.6.3). שני סיכוני תקציב עתידיים תועדו ב-4.3.2: W3 (עלות יצירת תוכן AI) ו-W4 (שכבה חינמית של Supabase)."
# --- נעילה: מונעת שני סוכנים שכותבים לקובץ בו-זמנית ---
LOCK_HELD_BY: DEV                 # "" | PM | DEV | CRITIC | CONTENT — נתפסה ב-C-0081
LOCK_AT: "2026-08-13T06:38:51Z"
MILESTONE_TICKS: 34
PAUSED_BY_HUMAN: false
DEPLOYS_THIS_MONTH: 1
LAST_DEPLOYED_AT: "2026-08-12T01:15:52Z"
LAST_REVIEWED_COMMIT: "e09bced"
# --- כלכלת פריסה: קרדיטים, לא דקות. 15 קרדיטים לפריסה. ראה RULES § 0.1.1 ---
WORKING_BRANCH: dev               # כל הסוכנים דוחפים לכאן. Netlify לא בונה ענף זה.
DEPLOY_BRANCH: main               # Netlify בונה אך ורק את זה. רק ה-Critic מקדם לכאן.
LAST_PROMOTED_AT: "2026-08-12T01:15:52Z"  # ISO-8601 של הקידום האחרון ל-main
PROMOTIONS_THIS_MONTH: 6          # תקרה רכה: 30. מעבר לזה — התראה לרוי
```

> 🧑‍⚖️ **בוצעו שתי ביקורות ידניות של רוי — הפירוט המלא ב-`plan/OPERATOR-LOG.md`.**
> פתוחים כרגע: F-026 🟡 (**שאלת ה-`PSEUDOGAP!` הוכרעה C-0074 — D-029; נותר רק לסמן ✅ ב-T-059**) · F-012 🟠 (חסום בתוכן, P-001) · F-019 🟠 (חפיסה לא נקלטת) · **F-028 🟠 (apkg false-accept, רדום → DEV, C-0067)** · **F-029 🟡 (README תקציב מיושן, C-0067)** · F-008 🟡 · R-010. **נסגרו: F-014/F-016 (C-0013) · F-017 (C-0014) · F-009 (C-0017) · F-015 (C-0019) · F-022/F-023/F-024/F-025 (C-0039) · F-021 (C-0049 — 1.7.1 בתמצית §92 + D-025 חתום) · F-028 (C-0069) · F-027 (C-0071 — שני החצאים).**
> 🚨 **קידום חסום (Critic C-0074, 01:13Z):** `main` הסתעף מ-`dev` — הקומיט `4ddf6c4` ("Update 02-inbox.md", עריכה ישירה ל-main מ-12/08) אינו ב-`dev`, ולכן `git merge --ff-only dev` נכשל וכ-26 מחזורים כולל תיקון F-027 אינם עולים לאוויר. **⛔ Critic אינו יכול לאחות** (האיחוי מתנגש ב-`02-inbox.md`, קובץ PM). **פעולה נדרשת מ-PM:** `git merge origin/main`→`dev`, פתור את התנגשות `02-inbox.md` (שמור י׳/יא׳), דחוף `dev`. אז ה-Critic יקדם. הרצה נקייה (4 פקודות ✅ · 737 בדיקות) אומתה על `dev@1f88154`. `/api/health` חי=ok על קוד C-0047. ⚠️ ‏PROMOTIONS=6 מול main ב-C-0047 → ייתכן 6 קידומים שנכשלו בשקט, לביקורת PM. פירוט: `03-for-roy` פריט 17.
> ⚡ סקילים של superpowers פעילים (`RULES.md` § 0.6). החוק הקשה: אין טענת הצלחה בלי ריצה טרייה.
> **תזמון:** Dev כל שעה ב-:30 · PM יומי 08:00 · Critic יומי 20:00. `NEXT_AGENT` הוא איתות
> עדיפות ולא נעילה — סוכן בלי עבודה כשירה מסיים ב"טיק שקט" בלי לבזבז מחזור.

### 0.1 יומן העברות מקל — 3 האחרונים בלבד

> 2 שורות לרשומה. ישן יותר → `plan/archive/handoff-log.md`. ההיסטוריה המלאה בגיט.

| Cycle | מסוכן | לסוכן | בשעה | סיבת ההעברה (עד 2 שורות) | תוצר |
|---|---|---|---|---|---|
| C-0080 | DEV | CRITIC | 05:42Z | **טיק ביצוע (◰) — משימה 2 מתוך 4 בתוכנית `2026-08-13-institution-and-finish.md`: המוסד עובר את הגבול (T-003).** שתי שורות ב-`app/api/profile/route.ts` — `institution: body.institution` פנימה ל-`checkOnboarding`, `institution: check.answers.institution` החוצה ל-`update`; הכלל של משימה 1 הוא **המסלול היחיד** ביניהן, ו⛔אף ערך גולמי אינו מגיע ל-Postgres. `docs/api-contract.md` עודכן **באותו קומיט** (RULES, Dev § 5): טקסט חופשי רשות · כל צורת ריק ⇒ `NULL` · חיתוך ל-120 ו⛔לא שגיאה · ו-120 נאכפים שוב ב-`profiles_institution_length_check`. ⛔ אפס שינוי בצורות 200/401/422/400/503. ⚠️ **שתי סטיות מהתוכנית, שתיהן חיזוק, ואחת נמדדה ולא הונחה:** ⓐ התוכנית מריצה את הבדיקות על `SRC` — הטקסט הגולמי. נמדד: מוטנט שמוחק את השורה ומשאיר במקומה **הערה** `// § 4.2ד: institution: body.institution` מקבל מהגרסה שבתוכנית **`PASSES`** — כלומר השומר חלול בדיוק במקרה שנזקק לו (זהו בדיוק לקח C-0032). הבדיקות רצות על `CODE`, המקור אחרי הפשטת הערות, ואז אותו מוטנט נופל בשמו. ⓑ התוכנית חותכת את החוזה מהכותרת ועד **סוף הקובץ**, ולכן המילה `institution` בכל אחד מחמשת המדורים שאחריה הייתה מספקת טענה שכל תוכנה "מתועד **כאן**"; החיתוך נחסם ב-`## ` הבא. **שתי מוטציות על הקוד, כל אחת הרגה את הבדיקה בשמה:** מחיקת שורת הכתיבה → `writes the validated value and ⛔ never the raw body` · מוטנט ההערה → `passes the submitted key into checkOnboarding by name`. **ארבע פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **789 בדיקות ✅ (מ-785, 52 קבצים)** · build ✅ `Compiled successfully in 283ms`. ⛔ TD-19/TD-24 בעינם — `0009` טרם הורצה על פרויקט חי, ולכן קריאה אמיתית תיפול ב-503 עד שרוי יחיל אותה. ⛔ לא נטען סקיל עיצוב · ⛔ `10-pedagogy.md` לא נפתח · ⛔ אפס סוכני משנה · ⛔ לא נגעתי ב-`main`. | `app/api/profile/route.ts` · `app/api/profile/route.test.ts` · `docs/api-contract.md` · `plan/30-architecture.md` (3.1.31) · `plan/50-tasks.md` (T-003) · `plan/00-control.md` |
| C-0079 | DEV | CRITIC | 04:48Z | **טיק ביצוע (◰) — משימה 1 מתוך 4 בתוכנית `2026-08-13-institution-and-finish.md`: עמודת המוסד + הכלל הטהור (T-003).** `supabase/migrations/0009_onboarding_institution.sql` — `text` **nullable** + `profiles_institution_length_check` (≤120), ⛔ לא enum ולא מפתח זר (A7: אין רשימה ארצית). ב-`lib/core/onboarding.ts`: `INSTITUTION_MAX_LENGTH=120` + שתי מחרוזות עברית + הכלל trim→ריק⇒`null`→חיתוך. **חיתוך ולא דחייה**, ו-`OnboardingFieldErrors` לא נגעה — אין תשובה שגויה בשדה חופשי. ⚠️ **סתירה בתוך התוכנית עצמה, שהוכרעה במדידה ולא בטעם:** התוכנית מכתיבה `institution: unknown` כשדה **חובה** ובאותה נשימה דורשת typecheck ירוק בסוף משימה 1 בלי לגעת ב-`route.ts` (קובץ של משימה 2) — שני התנאים אינם יכולים להתקיים יחד. נמדד: **5 שגיאות `TS2345/TS2741`** (`route.ts:39` + שלוש קריאות ותיקות של T-029). יושם `institution?: unknown` — `unknown` ממילא כולל `undefined`, מרחב הערכים זהה לכל קורא, ומשימה 2 נשארת שלמה על שלושת חלקיה. ⚠️ **הפגם המשמעותי של הטיק לא היה שלי: החבילה הייתה אדומה על `dev` לפני שנגעתי בה.** נמדד על עץ נקי (stash): `5 failed | 4 passed` ב-`scripts/build-ingest-sql.test.ts`. השורש — אצוות התוכן `batch-2026-08-13.jsonl` של C-0078 (נקלטת **נכון**, 0 נפסלו) מול שומר שמחזיק `343 rows · 6 files · 1 low` כמספרים קשיחים. **ופגם שני, חמור יותר:** ביטחון נמוך זוהה ב-`includes("'low'")`, ו-C-0078 הכניס את **הכותרת** `low` — שורה בביטחון `'high'` נספרה כנמוכה. **התיקון בטענה ולא במספר (F-030):** הספירות נגזרות מהאצוות בזמן ריצה (שוויון מול המקור, חזק ממספר קשיח), המספר נשמר כ**רצפה** בלבד, והתבנית היא `", 'low', "` — כותרת נפתחת מיד אחרי `(` ולכן ⛔ אינה יכולה להתאים. **ארבע מוטציות בטיק:** `slice(MAX+1)` ✗ · `MAX=130` ✗ · דילוג על אצווה → **5 נופלות** · `needs_human_review=false` → `expected [] to have a length of 3`. **ארבע פקודות טרי:** typecheck ✅ · check:core `/lib/core purity: OK` ✅ · **785 בדיקות ✅ (מ-779, 52 קבצים)** · build ✅. ⛔ אפס שינוי בנקודות קצה ובחוזה ה-API (משימה 2) · ⛔ לא נטען סקיל עיצוב · ⛔ `10-pedagogy.md` לא נפתח · ⛔ לא נגעתי ב-`main`. | `supabase/migrations/0009_onboarding_institution.sql` · `lib/core/onboarding.ts` · `lib/core/onboarding.test.ts` · `scripts/build-ingest-sql.test.ts` · `supabase/seed/0001_content_batches.sql` · `supabase/seed/0002_word_cefr_levels.sql` · `plan/30-architecture.md` (3.1.29 · 3.1.30) · `plan/50-tasks.md` (T-003) · `plan/60-findings.md` (F-030) |
| C-0078 | CONTENT | DEV | 04:21Z | **טיק תוכן — אצווה `batch-2026-08-13.jsonl`: 45 כותרות NGSL בדרגות 261–305, 60 משמעויות, כולן עברו את `gateSense`.** ⚠️ **הפגם המשמעותי של הטיק: 18 פסילות בסבב הראשון, הגרוע מאז C-0043, ו-16 מהן על סחיפת רמה במשפט ה-supportive** — כלומר בטקסט שנכתב **אחרי** שסיימתי את בדיקת ה-`grep` המקדימה של כלל א׳1. הבדיקה המקדימה כן עבדה על אוצר המילים של הנושא (פסלה מראש 33 מילים: classroom · english · homework · airport · practise · queue · wool · lever ועוד), אבל `million` · `thousands` · `centre` · `flour` · `oven` · `gloves` ושמות ימים ומספרים נולדו תוך כדי ניסוח ולא נבדקו מעולם. **סבב תיקון אחד, ואז 60/60 · 0 נפסלו · 0 נזרקו.** נרשם לקח 9: הבדיקה המקדימה היא על **הטקסט הסופי** ולא על רשימת מועמדים. ⚠️ **משמעות אחת נזרקה בכוונה לפני הכתיבה** (`level` = קומה): לא נמצא שילוב של שלושה גזעים שבו אף מסיח אינו תשובה תקפה, ולפי לקח 7 זו זריקה ולא ריכוך. **2 רשומות `low`** (`least` · `within`) — ⛔ אף מילה לא הושמטה בגלל אי-ודאות, וכל 60 השורות נושאות `translation_he` לא ריק (נבדק בספירה לפני הכתיבה). כל 60 מסומנות `spot_check` (אצווה קטנה מ-80). ⛔ אפס כתיבה ל-DB · ⛔ אפס נגיעה בקוד (סקריפטי העזר נמחקו ולא נדחפו) · ⛔ לא נגעתי ב-`main`. | `data/generated/batch-2026-08-13.jsonl` · `data/generated/manifest.json` · `data/generated/manifest-2026-08-12.json` · `plan/80-content-lessons.md` (לקח 9) |
---

**החוקים המלאים:** `plan/RULES.md` — פריסה (0.2) · מקביליות (0.3) · סוכני משנה (0.4) · שער טריאז' (0.5)
**מטריצת הקריאה:** `project_plan.md`
