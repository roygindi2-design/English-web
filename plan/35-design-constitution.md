# חוקת העיצוב — קול (Kol) · גרסה 2

**נכתבה מחדש 23/08/2026 בהוראת רוי.** מחליפה את הגרסה מ-12/08/2026 במלואה.
**נגזרת מ-`plan/36-video-spec.md` ומהרנדרים ב-`docs/design/`** — לא מהרצת סקיל.

⚠️ **מה שהשתנה מהותית מגרסה 1:** הגרסה הקודמת נכתבה מהרצה אחת של `ui-ux-pro-max`
ב-12/08, **שבועיים לפני שהחזון של רוי היה קיים**. היא הייתה קפואה, ולכן כל סתירה
מולה נפתרה לטובתה — כולל סתירות מול החזון עצמו. זה נגמר.

**מבנה הגרסה הזאת: שתי שכבות עם מעמד שונה.**
שכבה א׳ **קפואה** — היא נמדדת ומגינה על הלומד. שכבה ב׳ **חיה** — היא כיוון ויזואלי
ומתעדכנת עם החזון. ⛔ הבלבול בין השתיים הוא מה ששבר את גרסה 1.

---

## מדידה שקבעה את כל המסמך הזה

לפני שנקבע ולו כלל אחד, נדגמו הפיקסלים של `docs/design/kol-A-02-deck.png`
והופקו כל ערכי ה-hex מ-`docs/design/render_video_A.py`. התוצאה:

| מה שנמדד ברנדר | האסימון ב-`palette.ts` | התאמה |
|---|---|---|
| `#0f172a` רקע עמוד | `--surface` dark | מדויקת |
| `#1e293b` רקע כרטיס | `--surface-raised` dark | מדויקת |
| `#f8fafc` טקסט גוף | `--ink` dark | מדויקת |
| `#cbd5e1` טקסט משני | `--ink-muted` dark | מדויקת |
| `#334155` מפריד | `--border-subtle` dark | מדויקת |
| `#94a3b8` גבול בקרה | `--border-strong` dark | מדויקת |
| `#3987e5` סימן הדגשה | `--brand` dark | מדויקת |
| `#7dabf8` מילוי כפתור | `--brand-surface` dark | מדויקת |
| `#4ade80` נכון | `--success` dark | מדויקת |
| `#f87171` שגוי | `--danger` dark | מדויקת |

**`render_video_A.py` מכיל בדיוק 10 ערכי hex, וכולם אסימוני `palette.ts`.
אפס צבעים זרים.**

⇒ **הפלטה אינה בסתירה לחזון ואינה זזה.** מי שרנדר את הווידאו עבד מול `palette.ts`.
מה שכן סותר הוא רדיוסים, זוהר, והנחת מצב-בהיר — ואת שלושת אלה הגרסה הזאת פותרת.

---

# שכבה א׳ · הרצפה הקפואה

⛔ **שינוי כאן דרך `NEXT_AGENT=HUMAN` בלבד.** כל סעיף כאן נמדד או תוקן באג אמיתי.
⚠️ הרנדרים של רוי **עומדים בכל הסעיפים האלה כבר עכשיו** — הם אינם מגבילים את החזון.

### א1 · ניגודיות
`lib/core/palette.ts` הוא מקור האמת היחיד לצבע. 11 אסימונים, 13 רצפות ניגודיות
שנאכפות ב-`palette.test.ts` בשני המצבים. ⛔ אין ערך hex גולמי ב-`components/`.
צבע חדש נכנס **רק** ל-`palette.ts` ורק עם בדיקת ניגודיות באותו קומיט.

### א2 · צבע לעולם אינו הערוץ היחיד
נמדד C-0015: `--success` מול `--danger` הם deutan ΔE **4.1** — לומד דויטראנופ
אינו מבחין ביניהם. ⇒ כל מצב נושא **אייקון ותווית**, לא רק צבע.
✅ הרנדר עומד בזה: שלושת המונים ב-`kol-A-02-deck` נושאים ○ · ✕ · ✓.

### א3 · עברית ו-RTL
גופן ממשק חייב כיסוי עברי מלא. ⛔ אסורים: Inter · Roboto · Open Sans · Geist ·
Clash Display · PP Editorial New · Comic Neue · Baloo 2 — לשלושת האחרונים אין
אותיות עבריות כלל, והלומד היה רואה ריבועים.
כל מילה אנגלית בתוך טקסט עברי עוברת דרך `<EnWord>` או `<EnText>` — המקום היחיד
שכותב `lang="en"`.

### א4 · יעדי מגע
44×44px מינימום לכל כפתור, שבב, טאב ושורת רשימה.
**החריג היחיד:** יעד inline בתוך פסקת סיפור רציפה — `36-video-spec § 3`,
ארבעת התנאים שם מחייבים ונמדדים. ⛔ ההחרגה אינה חלה על שום דבר אחר.

### א5 · עיגון למעלה
⛔ `justify-center` על מכולת עמוד ראשית — אסור.
זה F-011 (נמדד: 67% ממסך הבית ריק), והוא חזר כ-F-016.
**חריג מוצהר:** במת הקרב ב-`/arcade` ממורכזת מעצם טבעה — § ב5.

### א6 · Viewport
⛔ `h-screen` אסור. `min-h-[100dvh]` בלבד (באג viewport ב-iOS Safari).
⛔ אפס גלילה אופקית ב-320 · 375 · 414px.

### א7 · נגישות — **אות אחד: `prefers-reduced-motion`**  *(צומצם 31/08/2026 בהכרעת רוי · `D-173`)*
⚠️ **רוי הוא היחיד שרשאי לפתוח שכבה א׳ (‏D-102), והוא פתח את הסעיף הזה פעמיים:**
ב-30/08 23:16Z כדי להרחיב לשלושה אותות (‏`D-160`), וב-31/08 כדי **להחזיר אותו לאות אחד**
(‏`D-173`) — «מחק ממנה לחלוטין את שני האותות המורכבים». ⇒ זו **הכרעה של רוי, ⛔ ולא
פרשנות של סוכן**, והיא סוגרת את הסתירה שנרשמה במפורש ב-`03-for-roy` פריט 80.

| האות | מה הלומד ביקש | מה המוצר חייב לעשות |
|---|---|---|
| `prefers-reduced-motion` | פחות תנועה | ⛔ **ללא חריג, כולל בזירה** — האנימציה נעצרת, הזוהר נשאר סטטי (ב3 § 5) |

⛔ אנימציה לעולם אינה מעכבת חשיפת תוכן (עקרון הקוהרנטיות של Mayer, T-041).

🔴 **מה ⛔ נמחק כאן, ו⛔ לא הוסתר:** `prefers-reduced-transparency` ו-`prefers-contrast`
יצאו מהחוקה. ⛔ **הם ⛔ לא נעלמו — הם ירדו מדרגת «כלל» לדרגת «מודע-ונדחה»**, ורשומים
ב-`plan/61-deferred.md § 61.2` עם התאריך והנימוק. ‏`T-242` נשארת 🚫 ו⛔ לא נמחקה.
**נמדד בטיק הזה (‏C-0376), ⛔ ולא שוער:** `grep -rn "prefers-reduced-transparency\|prefers-contrast"`
על `app` · `components` · `lib` · `scripts` ⇒ **0 תוצאות** ⇒ ⛔ **אפס בעיה נגררת** —
המחיקה מהחוקה ⛔ אינה מפילה שורת קוד או בדיקה אחת, מפני שהמימוש מעולם לא נכתב.
▶️ **החזרה היא מילה אחת מרוי**, והיא היפוך תא אחד ב-`61-deferred`.

### א8 · אייקונים
⛔ אמוג'י כאייקון. SVG בלבד.

### א9 · רצפת גודל־טקסט — **12px בכל המוצר**  *(נוסף 31/08/2026 בהכרעת רוי · `03-for-roy` פריט 68 · `D-161`)*
⛔ **אין טקסט מתחת ל-12px בשום מסך, בשום מצב, כולל בזירה.**
הרקע נמדד ⛔ ולא שוער (F-162): `docs/design/render_video_B.py:175` צייר תווית משבצת ב-**9**,
במרחב `375×812` ⇒ 9 CSS px אצל הלומד. סריקת `app/` ו-`components/` ב-28/08:
`text-xs` (12px) ×11 · `text-[11px]` ×**1** ⇒ 9px היה **18% מתחת** לצומת הקטן ביותר שקיים.
⚠️ **12px הוא המדרגה הקטנה שהמוצר משתמש בה בפועל, ⛔ ולא מספר שנבחר בטעם.**
‏D-137 (רצפת 12px **בזירה**) נבלעת בכלל הזה ו⛔ אינה סותרת אותו.
▶️ **האכיפה היא שורת DEV, ⛔ לא הבטחה:** `T-236` — כלל ב-`npm run verify` שנופל על
`text-[<11 או פחות>px]` ועל כל `font-size` מתחת ל-12px ב-`app/globals.css`.

---

# שכבה ב׳ · הכיוון הוויזואלי

**מקור: `36-video-spec.md` · `37-arena-spec.md` · `38-character-base.md` ·
הרנדרים ב-`docs/design/`.**
⚠️ **הקובץ הזה אינו קפוא.** החזון מתעדכן — השכבה הזאת מתעדכנת איתו.
סתירה בין שכבה ב׳ לבין `36-video-spec` → **36 גובר**, והפער מתוקן כאן באותו טיק.

### ב1 · כהה קודם
המוצר מעוצב **כהה**. `--surface` `#0f172a` הוא ברירת המחדל הוויזואלית.
המצב הבהיר נשאר קיים ונתמך — אך ⛔ **אינו** התצוגה שמולה נשפטת התאמה לחזון.
Dev משווה מסך לרנדר **במצב כהה**.

### ב2 · רדיוסים — הסולם החדש, נמדד מהרנדר
⛔ **סולם שלושת הערכים של גרסה 1 בוטל.** הוא לא תיאר את הרנדר: נספרו בו
20 שימושים ב-16, ועוד 10 · 11 · 13 · 14 · 18 · 20 · 22 · 26.

| אסימון | ערך | שימוש |
|---|---|---|
| `rounded-md` | 6px | שדות קלט, תגיות |
| `rounded-lg` | 8px | כפתורים |
| `rounded-xl` | 12px | בקרות משניות, אריחי מונה |
| `rounded-2xl` | 16px | **ברירת המחדל לכרטיסייה** — הערך השכיח ברנדר |
| `rounded-full` | `9999px` | **כפתור ראשי · שבב · פס התקדמות · טאב העולם** |

⚠️ **תוקן 23/08 באותו טיק שבו נסגר T-168:** הטבלה נקבה קודם ב-`rounded-sm` 4px —
ערך שאינו תואם לא ל-Tailwind (שם `sm` הוא 2px) ולא לקוד, שמשתמש ב-`rounded-md`
**18 פעמים**. הערך הנכון הוא `md` 6px, וכך גם ב-`scripts/radius-hygiene.test.ts`.
⛔ הסולם מדורג — ערך שישי הוא ממצא, ⛔ לא תיקון.

⇒ **`rounded-full` הותר במפורש.** ההנמקה הישנה ("ממשק למבוגרים אינו אפליקציית
ממתקים") נדחית: הכפתור `סינון מילים`, השבב `שינוי רמה`, פס ההתקדמות ועיגול
העולם **כולם פילים ברנדר שרוי אישר.**

### ב3 · זוהר — הותר, עם תקציב
⛔ **האיסור הגורף על blur וצללים רב-שכבתיים בוטל.**
`render_video_A.py` בונה זוהר ב-`glow()`/`rglow()` — ערימת עיגולים שקופים —
ומשתמש בו על טאב העולם, על כרטיסים נבחרים ועל צומת פעיל בטבעת.

**התקציב, כדי שזוהר לא יזלוג לכל מקום:**
1. זוהר צובע **רק** ב-`--brand` / `--brand-surface`, או בצבעי הזירה בתוך הזירה.
2. **עד שני** אלמנטים זוהרים **בחלון הנראה בו-זמנית** — ⛔ ולא «במסלול».  
   *(הובהר 31/08 · C-0376 · ביקורת מובייל)* 🔎 **למה זה היה חסם סתמי:** רשימה נגללת של
   כרטיסים היא **מסך אחד** לפי הנוסח הישן ⇒ כרטיס נבחר אחד בכל מסך היה מוציא כל רשימה
   מחוץ לחוק, גם כשהלומד רואה שני כרטיסים בלבד. **התקציב נמדד על מה שהעין רואה**
   ב-375×812, ⛔ לא על אורך הגלילה. ⛔ **המספר ⛔ לא השתנה: שניים.**
3. זוהר הוא **קישוט בלבד** — ⛔ לעולם אינו הערוץ היחיד שמסמן מצב או מיקוד.
4. ⛔ זוהר על טקסט גוף. כותרות וסמלים בלבד.
5. `prefers-reduced-motion` → הפעימה נעצרת, הזוהר נשאר סטטי.

⛔ **Claymorphism נשאר אסור.** זוהר אינו קלימורפיזם.

### ב4 · טיפוגרפיה
Heebo לכותרות · Assistant לגוף · Noto Sans Hebrew כגיבוי. ⛔ הרשימה השחורה
ב-א3 בתוקף.
ספרות במוני מצב ובשעון הקרב: **טבולריות** (`font-variant-numeric: tabular-nums`)
— מונה שקופץ ברוחב בזמן ספירה לאחור קורא כתקלה.

### ב5 · הזירה — מרחב מגודר
`37-arena-spec.md` שולט על `/arcade` בלבד.

**מותר בזירה, ואסור מחוצה לה:**
- פלטת זירה עצמאית — זהב · כתום · ציאן · סגול. ⛔ **אינה נכנסת ל-`palette.ts`**
  (`36-video-spec` אינווריאנט 6). חיה ב-`app/arcade/arcade-tokens.css`.
- ⛔ **האיסור על סגול בוטל בתוך הזירה בלבד.** הקוסם `rgb(168,85,247)` וההכרזה
  הסגולה של ההתחמקות הם החזון. **מחוץ לזירה — סגול ואינדיגו אסורים כמקודם.**
- מרכוז אנכי (חריג ל-א5) · זוהר מעל תקציב ב3 · מוטיון מעל תקרת ב6.

**לולאת המתנה חיה — החריג שרוי אישר 19/08, ⛔ ועד C-0324 חי בשורת רגיסטר בלבד**
(‏D-128 · F-085). שלוש גדרות, וכולן מחייבות:
- **במת הזירה בלבד.** ⛔ לולאה מתמשכת אסורה בכל מסך אחר במוצר.
- **משרעת ≤2px.** ⛔ נשימה, ⛔ לא תנועה. חריגה מזה היא הפרה, ⛔ לא טעם.
- **`prefers-reduced-motion` עוצר אותה לחלוטין** — שכבה א׳ · א7, ⛔ ואין לה חריג.

⛔ שכבה א׳ **חלה במלואה גם בזירה**: ניגודיות · אייקון+תווית · 44px · reduced-motion.
הזירה חופשייה בטעם, לא בנגישות.

### ב6 · מוטיון
**מעבר בעל משך** (‏פתיחה, סגירה, החלפת מסך, הדגשה): **150–300ms, easing אחד**.
🆕 **חריג יחיד ומדוד — משטח מוצג שחוצה את גובה המסך** (גיליון תחתון · מודאל מסך מלא):
**עד 400ms**.  *(נוסף 31/08 · C-0376 · ביקורת מובייל)* 🔎 **למה זה ⛔ אינו ריכוך:**
התקרה של 300ms נכתבה למעבר של **רכיב**; משטח שנוסע ~800px ב-300ms קורא כקפיצה,
⛔ לא כתנועה, וזה בדיוק מה שסקילי המובייל קוראים «לא גמור». ⛔ **החריג חל אך ורק על
משטח שנוסע את גובה המסך, ⛔ ולא על כפתור, שבב, כרטיס, טאב או הדגשה.**
⛔ ומשטח שנגרר באצבע ⛔ אינו כאן כלל — הוא **קפיץ**, לפי הפסקה הבאה.
🆕 **תנועה שמשתחררת ממחווה** (‏גרירת כרטיסייה, גרירת קלף בזירה, גרירת דמות): **קפיץ —
`damping 1.0` · `response 0.3–0.4`**, ⛔ ולא מעבר בעל משך.  *(הורחב 31/08/2026 באישור רוי · `D-158 § ב׳` · `D-159`)*
⚠️ **למה זה ⛔ אינו טעם:** מעבר CSS בעל משך קבוע ⛔ אי אפשר לתפוס באמצע התנועה ולהפוך
אותה, והמהירות של האצבע בשחרור נחתכת לאפס ⇒ תפר גלוי בין הגרירה לתנועה. לקפיץ
⛔ **אין משך כלל**, ולכן ⛔ אי אפשר לכתוב אותו בשפה של «150–300ms» — וזו הייתה הסתירה.
**שלושת המקומות שנמדדו C-0371 והם היעד:** `app/globals.css:155` (`[data-arena-card]`) ·
`:187` (`[data-arena-stage] [data-arena-figure]`) · `:238` (`[data-flashcard]`).
🆕 **ואילו מאפיינים מותר להנפיש בכלל (‏הפער של `D-158 § ג׳`, שנסגר כאן):** **`transform`
ו-`opacity` בלבד.** ⛔ `box-shadow` · `width` · `height` · `top/left` · `color` ⛔ אינם
מאפייני קומפוזיטור ⇒ ציור או פריסה מלאים בכל פריים. הזוהר של ב3 מונפש על אלמנט נפרד
שנושא `box-shadow` **סטטי**, ומה שזז עליו הוא `opacity` + `transform: scale()`.
⚠️ **האכיפה כבר קיימת ו⛔ אינה הבטחה:** `npm run check:motion` (‏`scripts/check-motion.mjs`,
נכנס ל-`verify` ב-C-0372) עם קו בסיס קפוא ב-`scripts/motion-baseline.md`.
**מוטיון הזירה חורג במכוון** — טעינה 5.3 ש׳, חלון התחמקות 0.4 ש׳, גלגול, פגיעה
קריטית. התזמונים ב-`37-arena-spec § 6` הם **מפרט מדיד**, לא קישוט, ⛔ ואין לכווץ
אותם לתקרת 300ms.

### ב7 · הרשימה השחורה שנשארה
⛔ גרדיאנט סגול או אינדיגו **מחוץ לזירה** · ⛔ hex גולמי ב-`components/` ·
⛔ אמוג'י כאייקון · ⛔ Claymorphism · ⛔ תבנית App Store Landing (אין אפליקציה
בחנויות) · ⛔ "AI" ו"אדפטיבי" כטקסט שיווקי (R-011).

### ב8 · אזורים בטוחים ואזור האגודל — **מובייל אמיתי, ⛔ לא פוסטר**  *(נוסף 31/08 · C-0376)*
⚠️ **הסעיף הזה ⛔ אינו כלל חדש — הוא כלל שכבר נאכף בקוד ו⛔ לא היה כתוב בשום מקום.**
נמדד בטיק: `app/globals.css:88·89·100` · `components/ActionBar.tsx:56` · `components/TabBar.tsx:143`
כולם כבר משתמשים ב-`env(safe-area-inset-bottom)`, ו-`ActionBar.test.ts` כבר נופל בלעדיו.
⇒ **הכלל היה חי בבדיקה ו⛔ לא בחוקה** — בדיוק צורת הכשל של F-085 (גדר בשורת רגיסטר שאיש ⛔ לא אכף).

1. כל סרגל דבוק לתחתית (‏`data-action-bar` · `data-tab-bar` · גיליון) נושא
   `pb-[max(<n>,env(safe-area-inset-bottom))]`, והגוף נושא ריפוד תואם. ⛔ אין בקרה מתחת למחוון הבית.
2. **הפעולה הראשית של מסך יושבת באזור האגודל** — שליש התחתון ב-375×812 — או בסרגל דבוק.
   ⛔ פעולה ראשית בראש מסך גליל ⛔ אינה נגישה באגודל, וזו ⛔ אינה שאלת טעם.
3. ⛔ אין תוכן קריטי מתחת לשורת המצב או מעבר לקצה הבטוח.

### ב9 · עומק משטחים — **שתי שכבות, ⛔ ולא חמש**  *(נוסף 31/08 · C-0376)*
⛔ **⛔ אין כרטיס בתוך כרטיס בתוך כרטיס.** מ-`--surface` מותר לרדת ל-`--surface-raised`
פעם אחת; שכבה שלישית של משטח (רקע · כרטיס · כרטיס-פנימי · שבב-בתוך-כרטיס-פנימי) היא **ממצא**.
🔎 **המקור, ו⛔ אינו טעם:** `skills/imagegen-frontend-mobile/SKILL.md § 15` («⛔ אל תיפול
ל-box-in-box-in-box · ערימות כרטיסים מקוננות · חמש רמות מסגור») ו-`skills/taste-skill/SKILL.md`
נוקבים באותה מחלקת כשל בדיוק. ⇒ **קיבוץ נעשה במרווח ובטיפוגרפיה, ⛔ ולא במסגרת נוספת.**
⚠️ **גדר, כדי שזה ⛔ לא ייקרא כאיסור על היררכיה:** מפריד (`--border-subtle`), רקע שקוף
או שינוי ריווח ⛔ **אינם** «משטח» ⛔ ואינם נספרים.

---

## מה קורה בסתירה

| הסתירה | מי מנצח |
|---|---|
| שכבה א׳ מול כל דבר | **שכבה א׳.** מסמכים אחרים כלולים |
| שכבה ב׳ מול `36-video-spec` | **36.** ושכבה ב׳ מתוקנת כאן באותו טיק |
| שכבה ב׳ מול סקיל עיצוב | **שכבה ב׳** |
| שכבה ב׳ מול `RULES § 0.9` | **שכבה ב׳** — ראה למטה |
| שכבה ב׳ מול קוד קיים | **שכבה ב׳** — נפתחת משימה, ⛔ לא ממצא |

⛔ סקיל עיצוב אינו רץ מחדש כדי "לרענן" את הקובץ הזה. המקור הוא החזון של רוי.


## הכרעה מפורשת מול `RULES § 0.9`

`RULES § 0.9` מצטט כלל אנטי-"AI slop" מ-`web-artifacts-builder`:
*"⛔ פריסות ממורכזות בעודף · גרדיאנטים סגולים · פינות מעוגלות אחידות בכל אלמנט · Inter"*.

| הפריט | המעמד אחרי גרסה 2 |
|---|---|
| פריסות ממורכזות בעודף | **בתוקף** — א5. חריג יחיד: במת הקרב |
| גרדיאנטים סגולים | **בתוקף מחוץ לזירה.** בתוך הזירה — הותר, ב5 |
| פינות אחידות **בכל אלמנט** | **בתוקף** — ב2 הוא סולם מדורג ולא ערך אחד. `rounded-full` מותר לכפתור ראשי ולשבב, ⛔ לא לכל אלמנט |
| Inter | **בתוקף** — א3 |

⇒ אין סתירה אמיתית. ⛔ `RULES § 0.9` אינו מקור לרדיוסים או לזוהר — הקובץ הזה הוא.
`RULES.md` הוא קובץ של רוי ולא נגעתי בו.

---

## 🔎 ביקורת מובייל-פירסט — 31/08/2026 · C-0376 · בהוראת רוי

**מה נבדק מול מה:** כל סעיף בשתי השכבות הועמד מול
`skills/imagegen-frontend-mobile/SKILL.md` **§ 13** (אזורים בטוחים) · **§ 14** (ניווט) ·
**§ 15** (פריסה נקייה · ⛔ קופסה-בתוך-קופסה) · **§ 29** (גודל טקסט וקריאוּת) ·
**§ 30** (טיפוגרפיה) · **§ 31** (ריווח וצפיפות), ומול `skills/taste-skill/SKILL.md`.
**שני עקרונות הברזל שרוי נתן:** ⓐ התאמה לאפליקציית מובייל גוברת על עיקרון ישן;
ⓑ ⛔ אין הסרה שגוררת בעיה בקוד שכבר נכתב ונבדק.

| מה | ההכרעה | הראיה שנמדדה בטיק |
|---|---|---|
| **ב3 §2** — «עד שני זוהרים במסך» | ✏️ **הובהר, ⛔ לא רוכך** — התקציב נמדד על **החלון הנראה**, ⛔ לא על המסלול | הנוסח הישן הוציא **כל רשימה נגללת** מחוץ לחוק. **המספר ⛔ לא השתנה: שניים** |
| **ב6** — תקרת 150–300ms | ✏️ **חריג יחיד ומגודר: עד 400ms למשטח שחוצה את גובה המסך** | ⛔ **אפס בעיה נגררת:** `scripts/check-motion.mjs` אוכף **מאפיינים** (`transform`/`opacity`), ⛔ **ולא משכים** ⇒ ⛔ אף שורת בסיס ⛔ לא זזה |
| **ב8** — אזורים בטוחים ואזור האגודל | 🆕 **נוסף** | ⛔ **אפס בעיה נגררת — הכלל כבר מיושם ונאכף:** `app/globals.css:88·89·100` · `ActionBar.tsx:56` · `TabBar.tsx:143` · ו-`ActionBar.test.ts` כבר נופל בלי `env(safe-area-inset-bottom)`. ⇒ **החוקה נגררה אחרי הקוד, ⛔ ולא להפך** |
| **ב9** — עומק משטחים ≤2 | 🆕 **נוסף** | `imagegen-frontend-mobile § 15` ו-`taste-skill` נוקבים באותה מחלקת כשל. ⚠️ **⛔ אינו שער אוטומטי** — ⛔ אין סקריפט שסופר עומק, ולכן הוא **עיקרון סקירה ל-QA**, ⛔ ולא בדיקה שנופלת |
| **א1…א9 (שכבה א׳)** | ⛔ **אפס עריכה** — למעט **א7** שרוי הורה עליה במפורש (`D-173`) | `D-102`: שכבה א׳ נפתחת בהוראת רוי בלבד. ⛔ **⛔ לא שיערתי ו⛔ לא ריככתי** |
| **א4** — 44×44px | ❔ **שאלה לרוי, ⛔ ולא עריכה** — Material מבקש **48dp** | `03-for-roy` פריט **84**. ⚠️ שינוי כאן נוגע ב-`scripts/verify-mobile.mjs` (‏`MIN_TAP = 44`) וב-**1,214 בדיקות** ⇒ ⛔ **⛔ אינו עריכת פרוזה** |
| **א9** — רצפת 12px | ❔ **שאלה לרוי, ⛔ ולא עריכה** — `§ 29` אומר «אם הטקסט מרגיש קטן, העיצוב ⛔ אינו גמור» | `03-for-roy` פריט **85**. ⚠️ **הרצפה ⛔ אינה היעד** — השאלה היא האם לגוף הטקסט יש **יעד** של 16px, ⛔ ולא האם הרצפה תזוז |

🔴 **ומה ⛔ לא נמצא, ו⛔ לא המצאתי כדי שיהיה מה לדווח:** ⛔ **אף סעיף בחוקה ⛔ אינו מצוטט
כחוסם של שורת משימה או של ממצא.** ‏`grep -o "35 § [א-ת][0-9]"` על `plan/50-tasks.md` ·
`plan/60-findings.md` · `plan/03-for-roy.md` ⇒ **0 תוצאות**. ⇒ **החוקה ⛔ אינה חונקת את
הפיתוח היום** — הפער שנמדד הוא **מה שהיא ⛔ אינה אומרת** (ב8 · ב9), ⛔ ולא מה שהיא אוסרת.
