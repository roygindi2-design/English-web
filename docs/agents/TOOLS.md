# ‏TOOLS — חוזה השימוש בכלים  ⟦נולד 17/09 · `C-0692` · הוראת רוי⟧

> ⛔ **קובץ אחד, ⛔ ולא עותק בכל פרומפט.** הפרומפט של כל סוכן מפנה לכאן בשש שורות.
> 🔬 **כל מספר כאן נמדד**, ומופיע עם התאריך והטיק שבו נמדד. ⛔ אין כאן ולו הנחיה משוערת.

---

## 🔴 § 0 — שלוש הוראות שקודמות לכל הטבלאות

### ⓐ ⛔ הכלי החי, ⛔ ולא טבלה, הוא האמת

**בפתיחת הטיק — רשימת ה-`mcp__*` שאתה באמת רואה היא הערכה שלך.**
⛔ כלי שאינו ברשימה ⛔ **אינו קיים בטיק שלך** ⇒ ⛔ אל תנסה אותו, ו⛔ אל תדווח «נכשל».
⛔ כלי שכן ברשימה ו⛔ אינו מוזכר כאן — ⛔ **אל תמציא לו שימוש**; כתוב שורה ב-`plan/03-for-roy.md`.

🔬 **ולמה זה ⛔ אינו ניסוח יפה אלא כלל שנולד מכישלון.** ‏`docs/skills-registry.md:30-37`
נשא **טבלת מחברים לכל סוכן**, שנקראה 14/09. נמדד מולה 17/09: PM איבד `Descript` וקיבל
`Kernel` · DEV קיבל `Three-js` · PROMOTER קיבל **`Mem0`** — בעוד שורה 36 באותה טבלה מכריזה
«ומה ש⛔ אין לאיש: … `Mem0`». ⇒ **סוכן קרא טענה שגויה במסמך המוסמך שלו עצמו.** זהו `F-268`
בדיוק, קובץ אחד הלאה. **הטבלה ההיא נמחקה, וכאן ⛔ לא נכתבת אחת חדשה במקומה.**

### ⓑ מקומי למסך מקומי · `Kernel` לאתר החי

🔬 **נמדד:** כרומיום המקומי של הארגז **⛔ אינו רואה את האינטרנט** (`QA.md:406`; `F-200` —
‏403 `CONNECT tunnel failed` ב-**14 ניסיונות**, מדיניות ⛔ ולא תקלה חולפת).
⇒ ‏`scripts/verify-mobile.mjs` · `walk-screens` · `diff:render` רצים **מקומית** מול
`next start`. **האתר החי נמדד ב-`Kernel` בלבד** — הוא מריץ כרומיום **בענן**, מחוץ למסלול
היציאה של הארגז. ⛔ **ואין ביניהם תחליפיות לשום כיוון.**

### ⓒ ראיה, ⛔ ולא «הרצתי»

קריאת כלי שלא נכתב ממנה **מספר או צילום** לרגיסטר ⛔ **לא קרתה.**
⛔ «בדקתי ב-Kernel» ⛔ אינה ראיה. ‏«`/world/amirnet` ב-393×852 ⇒ `scrollHeight==clientHeight`» היא.

---

## § 1 — `Supabase`  ⟨סכימה · מיגרציה · ספירה אמיתית⟩

| | |
|---|---|
| **מתי** | כשהשאלה היא «מה **באמת** יש במסד» — מבנה טבלה · מספר שורות · מפתח פרסום |
| **הקריאה** | `list_tables` · `list_projects` · `apply_migration` · `get_publishable_keys` |
| **הראיה שחייבת לחזור** | 🔴 `success: true` **ואז קריאה חוזרת** — `list_tables` שמראה את מה שנוצר. ⛔ `success` לבדו ⛔ אינו ראיה |
| **מה זה ⛔ אינו מוכיח** | ⛔ **שהמוצר עונה.** טבלה תקינה ו-API שמחזיר 500 חיים יחד בשלווה |

🔬 **עובד — נמדד:** ‏`apply_migration` החיל חי את `0021`·`0023`·`0024`·`0026`·`0027`
(‏DEV ‏C-0466 · C-0518 · C-0547 · C-0579 · C-0633), כל אחד נקרא בחזרה באותו טיק.
‏`get_publishable_keys` החזיר מפתח anon אמיתי (QA `C-0677`).

🔴 **ושתי מלכודות מדודות, ⛔ ולא אזהרות כלליות:**
- **הסירוב ⛔ אינו באג ו⛔ אינו מוזמן לעקיפה.** ‏`apply_migration` נדחה ב-`C-0646`:
  «Permission for this action was denied by the Claude Code auto mode classifier.
  **Reason: [Production Deploy]**», ו-`execute_sql` על `auth.users` נדחה ב-`C-0677`:
  «**Reason: [PII Data Handling]**». ⇒ **כתוב את הסירוב כלשונו ברגיסטר ועצור.**
  ⛔ **אל תנסח מחדש · ⛔ אל תפצל · ⛔ אל תחפש כלי אחר שיעשה את זה.**
  📎 הסיבה היא `auto_mode_allow: []` ברמת המשימה — **שדה של רוי**, ⛔ לא שלך.
- **`execute_sql` ⛔ אינו ערוץ טעינה.** ‏`F-262`: הוא דורש שה-SQL יעבור **בגוף הקריאה**
  ⇒ 6,715 שורות = **585,246 בתים** בפרומפט. ⇒ טעינה המונית היא seed/JSONL, ⛔ לא הכלי הזה.

---

## § 2 — `Kernel`  ⟨⛔ כל מדידה על האתר החי⟩

| | |
|---|---|
| **מתי** | ⛔ **כל** טענה על `https://<the live site>` — מסך · סטטוס · זמן |
| **הקריאה** | `manage_browsers create (headless:true)` ⇒ `execute_playwright_code` ⇒ **`manage_browsers delete`** |
| **הראיה שחייבת לחזור** | קוד סטטוס · **המידה שנמדדה** (‏`scrollHeight` · רוחב · ms) · צילום |
| **מה זה ⛔ אינו מוכיח** | ⛔ ש-375 עובד אם מדדת 393. **הרוחב שמדדת הוא הרוחב שהוכחת, ⛔ ולא אחר** |
| 🔴 **ניקוי** | ⛔ **`manage_browsers delete` תמיד — גם כשהבדיקה נכשלה. דפדפן ענן דולף עולה כסף** |

🔬 **מחובר ומאומת 17/09** (`get_connection_context` ⇒ principal + project scope).
🔴 **ו⛔ מעולם ⛔ לא הופעל על ידי סוכן מתוזמן** — ⛔ אפס קריאות ברגיסטרים מאז שרוי חיבר
אותו ב-14/09, למרות ש-`QA.md:386` מחייב אותו. ⇒ **אם הטיק שלך נוגע באתר החי, זה הכלי.**

⚠️ **דגימה אחת ⛔ אינה ראיה על נקודת קצה משתנה.** ‏`/api/health` מדד `ok:false` ב-4 מתוך 6
משיכות ב-`C-0607` 13:32Z, ו-`ok:true` ב-6 מתוך 6 ב-18:29Z. ⇒ **משוך לפחות 3 פעמים ודווח
את התבנית.** והמשיכה הראשונה מדדה **5,543ms** מול ~800ms לחמש הבאות — זו התנעה קרה
(`T-327`), ⛔ ולא כשל.

---

## § 3 — `Netlify`  ⟨רק אחרי דחיפה ל-`main`⟩

| | |
|---|---|
| **מתי** | ⛔ **רק** אחרי שדחפת ל-`main`. ⛔ טיק בנייה ⛔ אינו נוגע בכלי הזה |
| **הקריאה** | `get-projects` ⇒ `siteId` · `get-deploy-for-site` |
| **הראיה שחייבת לחזור** | `state=="ready"` · `context=="production"` · `branch=="main"` · 🔴 **`commit_ref` == ה-SHA שדחפת** |
| **מה זה ⛔ אינו מוכיח** | 🔴 ⛔ **`ready` ⛔ אינו `ok:true`.** פריסה יכולה להיות `ready` וכל פונקציה בה 500 ⇒ § 2 ⛔ אינו מדולג |

🔬 **נמדד 17/09:** `silly-medovik-b304e5` ⇒ `currentDeploy.state: ready`.
⚠️ **`commit_ref` הוא כל העניין** — פריסה `ready` של ה-SHA של **אתמול** היא אור ירוק
לקידום ש⛔ מעולם לא יצא. **השווה ל-SHA שדחפת, ⛔ ולא ל«main».**
📎 ‏`NETLIFY_AUTH_TOKEN` ⛔ **אינו נדרש** לזה.

---

## § 4 — `Figma`  ⟨מקור העיצוב **החי**⟩

```
fileKey   v216k02v3L0azhfOw3y2ur          ⟵ הקובץ של רוי
```

| | |
|---|---|
| **מתי** | **DEV · QA:** ⛔ **רק** כשהשורה ב-`50-tasks.md` נוקבת `nodeId` מפורש · 🎨 **PM:** מצייר מסגרת לכל **מסך חדש** לפני שהשורה נכתבת ⟨`PM.md` STEP 5.2 · `D-295`⟩ — `use_figma`, בעמוד `PM · מסכים` |
| **הקריאה** | `get_metadata` (מבנה) ⇒ `get_design_context` (קוד + **תמונה**) |
| **הראיה שחייבת לחזור** | הקוד **והתמונה** של המסגרת. ⇒ ההשוואה היא מול מה שראית, ⛔ לא מול תיאור |
| **מה זה ⛔ אינו מוכיח** | ⛔ שזה נראה כך **במכשיר**. ‏`Kernel` ב-393×852 מוכיח את זה, ⛔ ו-Figma לא |

🔬 **נמדד 17/09, שתי הבדיקות:** ‏`get_design_context` על `1:2` החזיר React+Tailwind עם
צבעים, גופנים, מרווחים ו-`data-node-id` — **ותמונה מרונדרת**. ‏`use_figma` יצר צומת
(`3314:2`), קרא בחזרה ומחק ⇒ `wrote:true`. ⇒ **קריאה וכתיבה, שתיהן חיות.**
📎 ‏`whoami` מחזיר `seat: View` — **והשדה הזה ⛔ אינו נכון.** הכתיבה החיה מפריכה אותו.

🔴 **שלוש מלכודות מדודות:**
- ⛔ **`get_variable_defs` ⛔ אינו שמיש לך.** הוא החזיר «You currently have **nothing
  selected**. You need to **select a layer** first» ⇒ הוא תלוי **בחירה ב-UI חי**.
  ⇒ ⛔ **אל תנסה אותו בטיק מתוזמן.** הטוקנים חיים ב-`lib/core/palette.ts`.
- ⚠️ **נכסי התמונה פגים אחרי 7 ימים** («stored on a remote server for 7 days»)
  ⇒ **משוך ויישם באותו טיק.** ⛔ אל תשמור URL לשורה הבאה.
- ⚠️ **`use_figma` (כתיבה) דורש את `figma-use` לפניו, והוא ~10,000 טוקנים.**
  ⇒ ⛔ **אסור לטעון אותו בטיק שאינו כותב ל-Figma.** **קריאה ⛔ אינה דורשת אותו.**
  ⛔ **ו-`claude plugin install` ⛔ אינו נדרש ו⛔ אינו עובד** — הסקיל מגיע כמשאב של המחבר
  (`get_figma_skill`), ומשימה מתוזמנת נושאת `enabled_plugins: []`.

⛔ **ו-`diff:render` ⛔ אינו מוחלף.** הוא מול 42 הרנדרים הקפואים ב-`docs/design/`;
‏Figma הוא המקור שרוי **יכול לשנות**. 🔬 ‏`render_kol.py` ו-`render_world.py` — שבעה
מסקריפטי הרנדר מייבאים אותם — ⛔ **אינם קיימים, לא בעץ ולא בהיסטוריית git** ⇒ הרנדרים
⛔ אינם ניתנים לייצור מחדש. **זו הסיבה שיש Figma.**

---

## § 5 — מה ש⛔ אינו כלי, ו⛔ אין לחפש לו מחבר

| | |
|---|---|
| **מצב הלופ** | ‏`plan/**` ב-git, ⛔ ותמיד. `pre-push` קורא `LOCK_HELD_BY` **מהקובץ** ⇒ מצב מחוץ ל-git ⛔ אינו ניתן לאימות על ידי השער ו⛔ אינו ב-`git log`, והלופ משוחזר משכפול |
| **תור · מסמכים · זיכרון חיצוני** | ⛔ אין. ‏`50-tasks.md` הוא התור, `plan/` הוא התיעוד |
