# הוראת ייצור — תור אמירנט, Tier 1 ואחריו Tier 2 (K-005 · D-140ⓑ · `plan/41-amirnet-spec.md`)

**נכתב 2026-08-28 (C-0348 · PM).** ⛔ הזמנה נלווית: `plan/25-content-commissions.md` שורת **K-005**.

---

## ✅ ראשית — הקובץ **כן** בריפו. ⛔ אל תגזור אותו.

⚠️ **הסעיף הזה הוחלף 28/08 23:3xZ (עריכת אופרטור), וההחלפה חוסכת לך את רוב הטיק.**

ה-PM כתב כאן, **ובצדק גמור באותו רגע**, ש-`data/amirnet-vocab.csv` ⛔ אינו בריפו.
הוא מדד זאת ⛔ ולא שיער: הקובץ נחסם בשקט על ידי `.gitignore` שורה 12 (`data/*.csv`),
האופרטור הריץ `git add -A`, וגיט **דילג עליו בלי מילה**.

🔴 **זו הייתה טעות של האופרטור, ⛔ ולא של ה-PM — וה-PM הוא שתפס אותה.**

**תוקן:** `.gitignore` נושא עכשיו **החרגה מפורשת** (`!data/amirnet-vocab.csv`),
והקובץ **מעקב**. נמדד אחרי התיקון:

| מה נבדק | התוצאה |
|---|---|
| `git ls-files data/amirnet-vocab.csv` | ✅ **מוחזר** |
| שורות | **6,713** |
| העמודה הראשונה | `headword` (‏BOM הוסר בקליטה) |
| Tier 1 · 2 · 3 · 4 | **1,243 · 2,139 · 2,417 · 914** |

⇒ ⛔ **אל תבצע את הגזירה שבסעיף 1. פשוט קרא את הקובץ.**

```
data/amirnet-vocab.csv
headword,pos,cefr,tier,tier_name,amirnet_level,is_connector,source
```

⚠️ **סעיף 1 נשאר למטה כ**מסלול נסיגה** בלבד** — אם ומתי הקובץ ⛔ אינו שם, גזור לפי
הצעדים שם משני קבצי המקור. ⛔ בכל מקרה אחר, ⛔ אל תגזור: תוצאה זהה, עלות של טיק.

---


## 1 · הגזירה — חמישה צעדים, כולם דטרמיניסטיים

**הקלט (שניהם בריפו · נבדקו ב-`test -f` בטיק הזה):**
`data/cefrj-vocabulary-profile-1.5.csv` · `data/octanove-vocabulary-profile-c1c2-1.0.csv`

שניהם `headword,pos,CEFR,…`. ⚠️ **קרא ב-`utf-8-sig`** — יש BOM.

1. אחד את שני הקבצים לפי `headword`.
2. במילה שמופיעה בכמה רמות — **קח את הנמוכה ביותר** בסדר `A1 < A2 < B1 < B2 < C1 < C2`.
   זו הנקודה שבה הלומד פוגש אותה לראשונה.
3. **זרוק `A1`** — נחשבות ידועות ו⛔ אינן מצדיקות תרגול ייעודי.
4. **זרוק `C2`** — מעל טווח הבחינה.
5. מפה: `A2 ⇒ Tier 1` · `B1 ⇒ Tier 2` · `B2 ⇒ Tier 3` · `C1 ⇒ Tier 4`.

**מה שיצא כשה-PM הריץ את חמשת הצעדים האלה בטיק הזה:**

| Tier | CEFR | נמדד עכשיו | ה-README אומר | פער |
|---|---|---|---|---|
| 1 | A2 | **1,244** | 1,243 | +1 |
| 2 | B1 | **2,140** | 2,139 | +1 |
| 3 | B2 | **2,417** | 2,417 | 0 |
| 4 | C1 | **914** | 914 | 0 |
| | **סה״כ** | **6,715** | 6,713 | **+2** |

⇒ **השיטה משוחזרת.** פער שתי המילים ⛔ **אינו מוסבר כאן ואיני מנחש אותו** — סביר שהוא
נורמליזציה של `headword` באגף האופרטור. הוא **0.03%** ו⛔ אינו משנה שום החלטה בהזמנה הזאת.
⚠️ ⛔ **אל תצטט את מספרי ה-README כאילו מדדת אותם.** גזור, וכתוב במניפסט מה **אתה** קיבלת.

⛔ **ומה ש⛔ אינו ניתן לשחזור, ולכן ⛔ אינו חלק מההזמנה:** העמודה `is_connector`
(‏38 מילים) קיימת **רק** בקובץ החסר. ⛔ אל תמציא אותה מחדש. במקומה — סעיף 3 למטה.

---

## 2 · המספר שההזמנה קיימת כדי להזיז (D-120)

**נמדד בטיק הזה** מול `data/generated/batch-*.jsonl` (651 כותרות נבדלות):

| Tier | מילים בטווח | כבר יש להן שורת משמעות | % |
|---|---|---|---|
| **1 (A2)** | 1,244 | **125** | **10.0%** |
| **2 (B1)** | 2,140 | **103** | **4.8%** |
| 3 (B2) | 2,417 | 93 | 3.8% |
| 4 (C1) | 914 | **0** | **0.0%** |

⇒ **טווח הבחינה המוצהר (‏1,500–3,000 מילים = Tier 1+2 = 3,384) מכוסה היום ב-228 מילים,
כלומר 6.7%. חסרות 3,156.**

**זה, ו⛔ לא «עוד אצווה», הוא הצוואר:** מסכי הכרטיסיות בנויים, ו-`word_progress` מחזיק
**שורה אחת**. ⛔ אין ללומד מה לסנן כי ⛔ אין מה להציג לו.

---

## 3 · סדר העבודה — ⛔ ולא «מה שנוח»

**ראש התור, ⛔ ואינו נתון לשיקול דעת:**

**ⓐ 31 מילות הקישור והיחס של Tier 1+2 שאין להן שורה.** נגזרות דטרמיניסטית:
`pos ∈ {conjunction, preposition}` בתוך Tier 1+2. **נמדד: 42 בטווח · 31 בלי שורת משמעות.**
בין השאר `despite` · `because of` · `instead of` · `provided` · `except` · `besides` ·
`throughout` · `nor` — כלומר **בדיוק המילים ש-`41 § 6.2` מעמיד במרכז השלמת המשפטים**,
שם «חלק ניכר מהשאלות נפתר מזיהוי היחס הלוגי ⛔ ולא מהכרת המילה החסרה».
⇒ **31 מילים שקונות יותר מ-31.**

> ⚠️ ⛔ **זו ⛔ אינה העמודה `is_connector` ואיני טוען שהיא.** זו גזירה לפי `pos` מהמקור
> שכן בריפו. היא **רחבה יותר** (42 מול 38) ו**משוחזרת בפקודה**.

**ⓑ אחר כך Tier 1 (A2) לפי סדר אלפביתי**, עד שהוא גמור. **ⓒ ורק אז Tier 2.**
⛔ **אל תיגע ב-Tier 3 ו-Tier 4** בהזמנה הזאת.

⚠️ **מבחן כשירות הכותרת קודם להכל** (לקח 15 · אותו מבחן של K-004): כותרת שהיא נטייה,
צירוף רב-מילים או ערך עם לוכסן — `according to` · `onto/on to` · `toward/towards` ·
`toward` — ⛔ **אינה כותרת ו⛔ אין ליצור לה שורה.** דלג, ורשום כמה דילגת.

⛔ **כיסוי לפני ליטוש:** מילה בלי שום משמעות עברית שווה יותר ממסיח חמישי למילה שכבר יש
לה ארבעה. ⛔ אל תחזור למילים של K-004 כדי לשפר אותן.

---

## 4 · מה נכתב — הצורה, ⛔ בלי שינוי

**⛔ אותה שורה בדיוק** של `data/generated/batch-*.jsonl` היום. ⛔ אל תמציא סכמה:
`headword` · `pos` · `sense_index` · `definition_en` · `translation_he` · `cefr_level` ·
`translation_confidence` · `examples{supportive,neutral}` · `items[]` · `distractors[]` ·
`he_one_to_many_group` · `he_interference_note`.

* `cefr_level` הוא ה-**CEFR** של המילה (‏`A2`/`B1`), ⛔ **לא** ה-Tier ו⛔ לא רמת אמירנט 1–4.
  שתי הסקאלות אינן זהות והמיפוי ביניהן הוא **הצעה מנומקת**, ⛔ לא עובדה רשמית.
* **הפלט:** `data/generated/batch-<YYYY-MM-DD>.jsonl` + `manifest-<YYYY-MM-DD>.json`,
  ובמניפסט: מספרי הגזירה שקיבלת · `selection_rule` · `quota_rule` · בקרת השלילה.
* ⚠️ **`quota_rule` — כמה בהרצה:** ⛔ **אין מספר מוצהר, ו-`41 § 9.4` («כמות פריטים לרמה»)
  היא הכרעה של רוי ש⛔ טרם נפלה.** ⇒ **קח את `BATCH_MIN_WORDS` (‏50, `lib/core/levelGate.ts`)
  כתקדים**, בדיוק כפי שעשית ב-K-004, ו**כתוב במפורש ב-`quota_rule` שזה תקדים ⛔ ולא ציטוט.**
  ⛔ אל תעצור בגלל זה. הרצת יותר מ-50? מצוין — כתוב כמה ולמה.

---

## 5 · השער — ✅ **קיים, בריפו, וירוק**

```
npm run measure:gate          # scripts/measure-gate.mjs · lib/core/levelGate.ts
```
נמדד ב-C-0339: **787 שורות · 1,574 דוגמאות · 2,361 גזעים · 0 rows rejected.**
⛔ **זה ⛔ אינו שער חדש שמישהו צריך לכתוב** — הוא זה שאתה כבר מריץ בכל אצווה.

⚠️ **וכל מה שכתוב ב-`CONTENT.md` על `data/generated/` בתוקף במלואו:**
```
npm run build:ingest && npm run build:levels && npm run measure:gate
```
ואז, ⛔ ורק ואז, `npm run verify` — **הפעולה האחרונה לפני הקומיט.** ערכת משהו אחריה,
⛔ כולל `.md`? הרץ שוב.

### ⚠️ הצירים שאין להם שער — ואיני טוען שיש

1. **נכונות התרגום.** ‏`measure:gate` אוכף רמה, בנק מילים וצורה. הוא ⛔ **אינו** יודע
   אם `translation_he` היא המשמעות הנכונה. ⇒ **R-013 בתוקף מלא: משמעות בלי מקור מורשה
   היא BLOCKER, ⛔ לא ניחוש.** עינך ועינו של ה-Critic הן הבקרה היחידה.
2. **מילות יחס וקישור הן הקשה ביותר לתרגם נכון** — `despite` ⇒ «למרות» ⛔ אינה
   «בגלל», והמרחק בין השתיים הוא בדיוק מה ש-`41 § 6.2` רמה 2 בונה עליו מסיח.
   ⇒ ב-31 המילים של ראש התור, **`translation_confidence` נמוך הוא תשובה לגיטימית**,
   ו⛔ שורה שאינך בטוח בה ⛔ אינה נכתבת.
3. **המיפוי CEFR ⇒ רמת אמירנט** הוא הצעה, ⛔ לא מיפוי רשמי, ו⛔ אין להציגו למשתמש כעובדה.

---

## 6 · חמשת האיסורים

1. ⛔ **אל תמציא משמעות.** R-010 · R-013 בתוקף. ⛔ הרפיית R-010 של `41 § 6.1` חלה על
   **פריטי תרגול אמירנט בלבד** (‏K-006), ⛔ **ולא על שורת משמעות של מילה.**
2. ⛔ **אל תיגע בקוד ואל תכתוב לדאטהבייס.** קבצים בלבד.
3. ⛔ **אל תתקן פריט שנפל בשער — ייצר אותו מחדש** (R-014).
4. ⛔ **אל תיצור `data/amirnet-vocab.csv`.** ‏`.gitignore:12` יבלע אותו. זו **T-222**.
5. ⛔ **אל תצטט מספר שלא מדדת.** מספרי ה-README הם ייחוס; המניפסט נושא את **שלך**.

---

## 7 · איך אני יודע שההזמנה הזאת עשתה משהו

במניפסט, שתי שורות שנמדדות **לפני ואחרי**, בפקודה, ⛔ ולא בהערכה:

| מה | לפני (נמדד 28/08) |
|---|---|
| מילות Tier 1 עם שורת משמעות | **125 / 1,244 = 10.0%** |
| מילות Tier 1+2 עם שורת משמעות | **228 / 3,384 = 6.7%** |
| מזה — מילות קישור/יחס | **11 / 42** |

⇒ **הרצה של 50 מילים מראש התור אמורה להזיז את שורת הקישור מ-11 ל-42 ולסגור את הראש
כולו בהרצה אחת.** אם היא ⛔ לא — כתוב מה מנע, ⛔ ואל תעגל.
