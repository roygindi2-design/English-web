# השערים מודדים את מה שהם מצהירים — המפצל · הרגיסטר · המזהה הכפול · הפריפץ׳ — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:executing-plans`.
> כל צעד הוא `- [ ]` ונסגר **רק אחרי הרצה שהפלט שלה נכנס לדיווח**.

**נכתב:** C-0219 (DEV, 📝 טיק תכנון) · 2026-08-20T05:39:16Z (`date -u`)

**Goal:** לסגור את ארבע משימות ה-M0 הפתוחות ברצף אחד — **T-101 → T-129 → T-128 → T-131**.
כולן אותה מחלקה: **שער שמדווח מספר שאינו מודד את מה שהשם שלו מבטיח.** המפצל סופר
צינור בתוך קוד כמפריד עמודות (T-101); 19 שורות ממצא בלתי-נראות למכונה והרצ׳ט ננעל
עליהן (T-129); ממצא שלם בלתי-נראה מפני שמזהה הוטבע פעמיים (T-128); ושלוש שגיאות
קונסולה ש⛔ **לא נמדדו** — רק הכתובת שלהן נרשמה, ⛔ ולא סיבת הכשל (T-131).

**Architecture:** `lib/core/planTable.ts` הוא **הקורא** של הרגיסטרים — טהור, ⛔ אפס
React/DOM/רשת/`process.env`; ידיות הקבצים חיות ב-`scripts/`. `scripts/measure-plan-tables.mjs`
הוא ידית הקובץ ומדפיס את המספרים; `scripts/measure-plan-tables.test.ts` הוא **רצ׳ט**
(תקרה שיורדת בלבד); `scripts/plan-hygiene.test.ts` הוא **אסרציה** על ייחוד מזהים.
`scripts/verify-mobile.mjs` הוא הרנס playwright נפרד ואינו נוגע בשלושת הראשונים.

**Tech Stack:** Node 22 (`node --experimental-strip-types` דרך `registerHooks` ב-`.mjs`) ·
TypeScript (`noUncheckedIndexedAccess`) · vitest 2 (⛔ **אין RTL ואין jsdom**) ·
playwright ל-`check:mobile` בלבד.

**Spec:** `plan/50-tasks.md` שורות T-101 · T-128 · T-129 · T-131 ·
`plan/60-findings.md` F-059 · F-062ⓒ · F-063 · F-078 · F-082 ·
`lib/core/planTable.ts:1-12` (הנימוק לכתיבת מפצל ביד) · בקשת רוי 18/08.

**⛔ מה ⛔ אינו בתוכנית הזאת, במכוון:**
- **מחלקה ⓐ של T-129** — 11 שורות ממצא בנות **9 עמודות** (`F-002·003·004·007·009·010·011·012·013·014·016`).
  T-129 כותבת מפורשות «לתקן מחלקה ⓑ ו-ⓒ». זו סחיפת סכמה בת שני תאי סטטוס, והכרעה
  אם הכותרת גדלה ל-9 או שהתאים מתאחדים היא **הכרעת רגיסטר של ה-Critic** ⇒ ⛔ לא שלך.
  ⚠️ **ובנוסף — T-129 מונה עשר שורות במחלקה ⓐ ו⛔ אינה מזכירה את `F-016`, שנמדדה כאן
  ‏9 תאים.** ⇒ הרצ׳ט הסופי יעצור על **11** ולא על 10, וזה ⛔ אינו כשל בביצוע.
- **T-103** (סיבוב השטף) — משימת מוצר, תוכנית משלה.
- **F-084 🟡** — הכרעת מסך של ה-PM (`featuredAppId`), ⛔ אל תיגע.
- ⛔ **`main`** — בשום צעד. הקידום הוא של ה-Critic בלבד.

---

## Global Constraints — חלים על ארבע המשימות

- **TDD.** הבדיקה נכתבת ראשונה, **מורצת ונמדדת שהיא נופלת** (⛔ לא מונח שתיפול), ואז
  המימוש. פלט שתי ההרצות — האדומה והירוקה — נכנס לדיווח.
- **`/lib/core` טהור.** ⛔ אפס React · window · document · localStorage · fetch ·
  `process.env`. השער: `npm run check:core`.
- **TypeScript ללא `any`.** `noUncheckedIndexedAccess` פעיל ⇒ `arr[i]` הוא `T | undefined`
  ותמיד נבדק, ⛔ לעולם לא `!`.
- ⚠️ **חוק הסדר של T-101, חל על כל ארבעת הקומיטים:** הרצת השער היא **הפעולה האחרונה
  לפני הקומיט**, אחרי כל עריכה **כולל קובצי `plan/`**. הנימוק נמדד: C-0172 הצהיר 1385
  והרצה טרייה נתנה 1383, כי העריכה באה אחרי ההצהרה.
- ⚠️ **`docs/plan-tables.md` הוא ראיה מחויבת** — `measure-plan-tables.test.ts:79-83` מאמת
  שהקובץ המחויב **זהה בית-בבית** להרצה טרייה. ⇒ **כל** עריכה ב-`plan/50-tasks.md` או
  ב-`plan/60-findings.md`, כולל עדכוני הסטטוס של שלב הסגירה, מחייבת
  `npm run measure:plan` **אחריה** ולפני הקומיט. זו מחלקת F-059/F-061/F-062/F-063 —
  ארבע נפילות רצ׳ט מאותו שורש בדיוק.
- **שער האימות בכל קומיט:**
  `npm run typecheck && npm run check:core && npm test && npm run build`
  ⛔ טענת «עובר» בלי הרצה **בהודעה עצמה** אסורה (`superpowers:verification-before-completion`).
- **`check:mobile` נמדד היום 3 כשלים** (F-082ⓑ = T-131). הוא ⛔ **אינו** בשער של
  משימות 1–3; הוא הנושא של משימה 4 בלבד.
- ⛔ אפס נגיעה ב-`plan/10-pedagogy.md` · `plan/35-design-constitution.md` · `main`.

---

## מדידת הבסיס — הורצה בטיק התכנון, ⛔ ואינה ניחוש

```
$ node --version
v22.22.2
$ PLAN_TABLES_OUT=/tmp/pt.md node scripts/measure-plan-tables.mjs
tasks: 131 rows, 0 malformed
findings: 86 rows, 19 malformed
eligible: T-082 … T-131 (22)
stale blockers: none
stale task blockers: none
```

19 השורות הפגומות, עם ספירת התאים שנמדדה לכל אחת:

| מחלקה | שורות | תאים | השורש |
|---|---|---|---|
| ⓐ סחיפת סכמה | F-002·003·004·007·009·010·011·012·013·014·**016** | 9 | תא סטטוס שני (`✅ טופל`) מול כותרת בת 8 — ⛔ **מחוץ לתוכנית** |
| ⓑ צינור בתוך code span | F-017 (11) · F-026 (15) · F-048 (12) · F-053 (10) | >8 | `` `string \| undefined` `` · `/^GAP(!\|\s\|$)/` · `SEED_OUT_DIR \|\| join(…)` · `term === 'AI' \|\| …` |
| ⓑ׳ צינור בפרוזה | F-044 (9) · F-048 (גם) | >8 | «3 failed \| 2 passed» · «(1 failed \| 10 passed)» |
| ⓒ תאים חסרים | F-019 (7) · F-046 (5) · F-047 (5) | <8 | תאים שלא נכתבו מעולם |

⚠️ **ההפרדה בין ⓑ ל-ⓑ׳ היא הציר של התוכנית:** מפצל מודע-code-span (משימה 1) פותר את
ⓑ **מחלקתית**, ⛔ **ואינו נוגע** ב-ⓑ׳ — צינור בפרוזה הוא באמת מפריד עמודות בכל רנדרר
markdown, ולכן חייב להיכתב `\|` (משימה 2). ⛔ אל תנסה ללמד את המפצל לנחש פרוזה.

⚠️ **וסיכון שנמדד מראש:** ב-`plan/50-tasks.md` יש **3** רצפי ` `` ` וב-`plan/60-findings.md`
‏**1**, ובתוכם כבר יושבים `\|` מוברחים. מפצל שסופר גרש אחורי בודד בלבד **ישבור שורות
שהיום תקינות**. נמדד: **0 שורות רגיסטר בעלות מספר אי-זוגי של גרשיים אחוריים** בשני
הקבצים ⇒ הבסיס נקי, והמפצל חייב לשמור עליו כך (משימה 1, צעד 3).

---

## Interfaces — החתימות המדויקות שהתוכנית מוסיפה

```ts
// lib/core/planTable.ts — יצוא חדש יחיד. ⛔ אין שינוי בחתימה של splitRow/rowShape.
/**
 * מיקומי כל הגרשיים האחוריים שפותחים/סוגרים code span בשורה, לפי CommonMark:
 * רצף באורך N נסגר ברצף באורך N בדיוק. רצף שאינו נסגר — ⛔ אינו פותח span.
 * מוחזר כמערך גבולות ממוין [openStart, closeEnd) לשימוש פנימי של splitRow.
 */
export function codeSpans(line: string): ReadonlyArray<readonly [number, number]>;
```

```ts
// scripts/plan-hygiene.test.ts — אין יצוא, רק describe חדש.
// דפוס מזהה שורת ממצא, זהה בצורתו ל-ROW_ID הקיים:
const FINDING_ROW_ID = /^\|\s*(F-\d{3})\s*\|/gm;
```

```js
// scripts/verify-mobile.mjs — שינוי בגוף המאזין הקיים בשורה 473 בלבד.
// לפני:  page.on('requestfailed', (r) => consoleErrors.push(`request failed: ${r.url()}`));
// אחרי:  השורה נושאת גם את errorText, כי בלעדיו ⛔ אי-אפשר להכריע פיקסצ׳ר מול פגם.
page.on('requestfailed', (r) =>
  consoleErrors.push(`request failed: ${r.url()} — ${r.failure()?.errorText ?? 'unknown'}`),
);
```

---

## Task 1 — T-101 · המפצל מזהה צינור בתוך code span

**הקובץ:** `lib/core/planTable.ts` · `lib/core/planTable.test.ts`
**⛔ מה ⛔ משתנה:** החתימה של `splitRow`/`rowShape` · טיפול ב-`\|` · `TASK_COLUMNS`/`FINDING_COLUMNS`.

- [ ] **1.1 · הבדיקה האדומה קודם.** הוסף ל-`lib/core/planTable.test.ts`, בתוך
      `describe('splitRow', …)` הקיים, את שלוש הבדיקות האלה **מילה במילה**:

```ts
  it('does not break a column on a pipe inside a code span', () => {
    // מחלקת F-059 · F-062ⓒ · F-063 — שלוש נפילות רצ׳ט מאותו שורש, ובכל פעם התיקון
    // היה להבריח צינור בשורה אחת. השורש הוא כאן: מפצל שאינו מכיר code span.
    expect(splitRow('| F-053 | `string | undefined` | fix |')).toEqual([
      'F-053',
      '`string | undefined`',
      'fix',
    ]);
  });

  it('honours a doubled backtick run and only closes on a run of the same length', () => {
    // נמדד: 3 רצפי `` ב-50-tasks ו-1 ב-60-findings, ובתוכם כבר יושבים `\|` מוברחים.
    // מפצל שסופר גרש בודד היה שובר את השורות האלה, שהיום תקינות.
    expect(splitRow('| T-001 | ``a ` b | c`` | d |')).toEqual(['T-001', '``a ` b | c``', 'd']);
  });

  it('treats an unterminated backtick run as literal, not as an open span', () => {
    // ⛔ הכשל השקט המסוכן: רצף שאינו נסגר בולע את שארית השורה ומאחד עמודות, כלומר
    // שורה תקינה הופכת פגומה. CommonMark מסכים: רצף בלי סוגר הוא טקסט.
    expect(splitRow('| T-002 | a ` b | c |')).toEqual(['T-002', 'a ` b', 'c']);
  });
```

- [ ] **1.2 · מדוד שהן נופלות.** `npx vitest run lib/core/planTable.test.ts`
      ⚠️ **צפוי:** הראשונה נופלת (4 תאים במקום 3). השנייה והשלישית **עשויות לעבור
      כבר עכשיו** — הן נטו-רגרסיה על ההתנהגות הקיימת. ⛔ **אל תשנה אותן כדי «להאדים»
      אותן.** רשום בדיווח בדיוק אילו נפלו ואילו לא — זו המדידה.
- [ ] **1.3 · מימוש `codeSpans`.** הוסף ל-`lib/core/planTable.ts`, מעל `splitRow`:
      סרוק את השורה, אסוף רצפי `` ` `` (עם האורך שלהם), וזווג כל רצף פותח עם הרצף הבא
      **באורך זהה בדיוק**. רצף שלא נמצא לו זוג — ⛔ אינו פותח span, והסריקה ממשיכה
      מהתו שאחריו. החזר גבולות ממוינים ובלתי-חופפים.
      ⚠️ גרש אחורי אחרי `\` הוא **תוכן** ואינו פותח רצף — `\|` ו-`\\` הם ההברחות
      היחידות (התיעוד ב-`planTable.ts:34-35`), ולכן הסריקה חייבת לדלג על זוג ההברחה
      **באותו סדר** שבו `splitRow` מדלג עליו, אחרת שני הפרסרים נחלקים על אותה שורה.
- [ ] **1.4 · חיווט ל-`splitRow`.** בתוך הלולאה הקיימת, לפני `if (ch === '|')`:
      אם `i` נמצא בתוך אחד מגבולות `codeSpans(line)` — `current += ch; continue;`
      ⛔ **אל תיגע** בענף ההברחה שמעליו ו⛔ אל תשנה את `cells.slice(1, -1)`.
- [ ] **1.5 · ירוק.** `npx vitest run lib/core/planTable.test.ts` — כל הקובץ ירוק,
      **כולל שלוש הבדיקות הקיימות** (`\|` · `\\` · `rowShape`).
- [ ] **1.6 · מדוד את ההשפעה על הרגיסטר.** `PLAN_TABLES_OUT=/tmp/pt1.md node scripts/measure-plan-tables.mjs`
      ⚠️ **הצפי מהמדידה:** `findings` יורד מ-**19** ל-**16 או 15** (‏F-017 · F-026 · F-053
      נסגרות; F-048 ⛔ אינה נסגרת, כי יש בה גם צינור פרוזה). **`tasks` חייב להישאר 0.**
      ⛔ `tasks` שעלה מאפס ⇒ המפצל שבר שורה תקינה ⇒ **חזור ל-1.3**, ⛔ אל תוריד רצ׳ט.
- [ ] **1.7 · הורד את הרצ׳ט למספר שנמדד.** `scripts/measure-plan-tables.test.ts:30` —
      `MALFORMED_FINDINGS_CEILING = <המספר מ-1.6>`, ובהערה מעליו שורה: מה ירד, ולמה
      זה תיקון מחלקה ולא תיקון מופע. ⛔ אל תיגע ב-`MALFORMED_TASKS_CEILING = 0`.
- [ ] **1.8 · רענן את הראיה.** `npm run measure:plan` (כותב ל-`docs/plan-tables.md`).
- [ ] **1.9 · השער, כפעולה אחרונה.** אחרי כל עריכה כולל `plan/`:
      `npm run typecheck && npm run check:core && npm test && npm run build`
- [ ] **1.10 · קומיט.** `loop(DEV): C-XXXX T-101 — המפצל מזהה code span, רצ׳ט 19→N`

**בדיקה עצמית של משימה 1 (⛔ אל תעבור למשימה 2 לפני שהתשובה כתובה):**
`tasks` נשאר 0 · `findings` ירד · הרצ׳ט שווה למספר שנמדד ולא נמוך ממנו · הבדיקה
החדשה נופלת אם מוחקים את קריאת `codeSpans` מ-`splitRow` (הרץ את המוטציה ותעד).

---

## Task 2 — T-129 · שמונה שורות ממצא נעשות קריאות למכונה

**הקובץ:** `plan/60-findings.md` · `scripts/measure-plan-tables.test.ts`
**⛔ מה ⛔ משתנה:** ⛔ **אף מילה של תוכן ממצא.** ⛔ אף שורה במחלקה ⓐ. ⛔ אף מזהה.

- [ ] **2.1 · מחלקה ⓑ׳ — הברח צינור בפרוזה.** בשתי שורות בלבד:
      `F-044` («3 failed | 2 passed») ו-`F-048` («(1 failed | 10 passed)») —
      החלף את הצינור הגולמי ב-`\|`. ⛔ **אל תשנה את הניסוח**, רק את התו.
      ⚠️ אם `F-048` עדיין פגומה אחרי זה — נותר בה צינור פרוזה נוסף; מצא אותו בהרצה
      של 2.4, ⛔ אל תנחש.
- [ ] **2.2 · מחלקה ⓒ — מלא את התאים החסרים.** שלוש שורות, מול הכותרת בת 8
      (`# | חומרה | קובץ:שורה | הממצא | תרחיש הכשל המדויק | תיקון מוצע | סטטוס | סבב`):
      - `F-019` — 7 תאים. חסר תא **אחד**. השווה מול הכותרת ומקם את התוכן הקיים
        בעמודה הנכונה; התא שאין לו תוכן מקבל `—`.
      - `F-046` — 5 תאים. חסרים **שלושה**.
      - `F-047` — 5 תאים. חסרים **שלושה**.
      ⛔ **הכלל:** תא ריק נכתב `—`. ⛔ אין להמציא «תרחיש כשל» או «תיקון מוצע» שלא
      נכתבו — הממצאים האלה סגורים, והמצאה תשקר על מה שהביקורת מצאה.
      ⛔ **אל תזיז את `סטטוס`** — `classifyStatus` קורא אותו לפי מיקום (`FINDING_STATUS_INDEX = 6`),
      וסטטוס שיזוז בטעות משנה את `staleBlocks` בשקט.
- [ ] **2.3 · מדוד.** `PLAN_TABLES_OUT=/tmp/pt2.md node scripts/measure-plan-tables.mjs`
      **הצפי: `findings: 86 rows, 11 malformed`** — בדיוק 11 שורות מחלקה ⓐ.
      ⛔ מספר אחר ⇒ שורה שלא זוהתה; פתח את `/tmp/pt2.md` וקרא אילו שורות נותרו.
- [ ] **2.4 · אמת שהתוכן שרד.** `git diff --stat plan/60-findings.md` — חייב להראות
      **8 שורות בלבד** שהשתנו (2 מ-2.1 · 3 מ-2.2 · והשאר 0). יותר מזה ⇒ עריכה שגויה.
      ובנוסף: `git diff -U0 plan/60-findings.md | grep -c '^+'` מול `'^-'` — שווים.
- [ ] **2.5 · הורד את הרצ׳ט ל-11**, ובהערה: הנותרות הן **מחלקה ⓐ בלבד**, היא הכרעת
      סכמה של ה-Critic ו⛔ אינה של Dev, והמספר הוא 11 ולא 10 כי `F-016` נמדדה 9 תאים
      ו-T-129 ⛔ לא מנתה אותה.
- [ ] **2.6 · `npm run measure:plan`** ואז **השער** (`typecheck && check:core && test && build`).
- [ ] **2.7 · קומיט.** `loop(DEV): C-XXXX T-129 — 8 שורות ממצא קריאות למכונה, רצ׳ט N→11`

**בדיקה עצמית:** האם החזרת צינור גולמי אחד ל-`F-044` מעלה את המספר ל-12? הרץ, ראה
אדום, החזר. ⛔ בלי המוטציה הזאת אין ראיה שהרצ׳ט באמת נועל.

---

## Task 3 — T-128 · מזהה `F-076` הוטבע פעמיים

**הקובץ:** `plan/60-findings.md` · `scripts/plan-hygiene.test.ts` (+ ציטוטים)

⚠️ **ההכרעה, ומדוע היא לא הפוכה:** שורה **97** נשארת `F-076` — `T-098` מצטטת אותה,
והיא הוותיקה (C-0200). שורה **99** (C-0202, `ARENA_IDLE_LOOP` מול חוקת העיצוב § 5)
מקבלת מזהה **חדש בסוף הרצף**. המזהה הגבוה ביותר שנמדד היום הוא **`F-084`** ⇒ החדש
הוא **`F-085`**. ⛔ **מזהה ⛔ אינו ממוחזר ו⛔ אינו נדחף לתוך פער** — פריט שנכתב לפני
שבועיים עדיין מצביע על הישן.

- [ ] **3.1 · הבדיקה האדומה קודם.** הוסף ל-`scripts/plan-hygiene.test.ts` `describe`
      חדש, **מילה במילה**:

```ts
/**
 * F-078 · T-128 — אותו חוק, רגיסטר אחד הלאה.
 *
 * `F-076` הוטבע פעמיים (שורות 97 ו-99) על שני ממצאים שונים. זה ⛔ אינו קוסמטי:
 * `measure-plan-tables.mjs:56-61` בונה `findingStates` כ-`Map`, כלומר השני דורס את
 * הראשון בשקט — ממצא שלם נעלם מהמכונה, ו-`staleBlocks` מכריעה עליו לפי הסטטוס של
 * ממצא אחר לגמרי. זו בדיוק מחלקת `TD-28`, שלישית באותה משפחה.
 */
const FINDINGS = readFileSync('plan/60-findings.md', 'utf8');

/** Only the ID cell of a real table row — `| F-076 | 🟡 MEDIUM | …`. */
const FINDING_ROW_ID = /^\|\s*(F-\d{3})\s*\|/gm;

function findingIds(): string[] {
  return [...FINDINGS.matchAll(FINDING_ROW_ID)]
    .map((m) => m[1])
    .filter((id): id is string => id !== undefined);
}

describe('plan/60-findings.md — the findings register', () => {
  it('has rows at all (guards the regex, not just the file)', () => {
    expect(findingIds().length).toBeGreaterThan(20);
  });

  it('mints every finding ID exactly once', () => {
    const seen = new Map<string, number>();
    for (const id of findingIds()) seen.set(id, (seen.get(id) ?? 0) + 1);
    const duplicates = [...seen.entries()].filter(([, n]) => n > 1).map(([id, n]) => `${id}×${n}`);
    expect(duplicates, 'a duplicate finding ID silently drops one finding from findingStates').toEqual([]);
  });

  it('leaves no gap in the ID sequence — a gap means an ID was lost, not freed', () => {
    const numbers = findingIds()
      .map((id) => Number(id.slice(2)))
      .sort((a, b) => a - b);
    const expected = Array.from({ length: numbers.length }, (_, i) => i + 1);
    expect(numbers).toEqual(expected);
  });
});
```

- [ ] **3.2 · מדוד שהיא נופלת.** `npx vitest run scripts/plan-hygiene.test.ts`
      **הצפי: `mints every finding ID exactly once` נופלת עם `["F-076×2"]`.**
      ⚠️ **וגם `leaves no gap` עשויה ליפול** — נמדד היום **86 שורות ממצא** מול מזהה
      מרבי `F-084`, כלומר הרצף ⛔ אינו רציף (יש מזהי `F-0xxⓑ` או שורות המשך).
      ⛔ **אם היא נופלת מסיבה שאינה הכפילות — הסר את הבדיקה השלישית בלבד** והשאר
      הערה בת שורה שמסבירה מה נמדד; ⛔ אל תמציא מזהים כדי לסגור פער היסטורי, וזה
      ⛔ אינו כשל בביצוע אלא מדידה.
- [ ] **3.3 · הטבע `F-085` בשורה 99.** החלף את תא המזהה בלבד. ⛔ אל תזיז את השורה
      ממקומה בקובץ ו⛔ אל תיגע בתוכן.
- [ ] **3.4 · עדכן את הציטוטים החיים, ⛔ ורק אותם.** נמדד — הציטוטים לממצא של שורה 99:
      - `plan/50-tasks.md` שורת **T-119** (`ARENA_IDLE_LOOP`)
      - `plan/03-for-roy.md` פריט **39** (החריג לחוקת העיצוב)
      - `plan/00-control.md` — האזכור בטקסט של `F-079` («מזהה `F-076` כפול טרם אוחד»)
        נכתב מחדש: הכפילות **אוחדה** ב-C-XXXX, השני הוא `F-085`.
      - `plan/60-findings.md` — הערת `F-079` שמדברת על הכפילות.
      ⛔ **`plan/archive/handoff-log.md` ⛔ אינו מתעדכן** — הוא היסטוריה, ורישום היסטורי
      שמשוכתב מפסיק להיות ראיה. ⛔ `plan/50-tasks.md` שורת **T-098** ⛔ אינה משתנה —
      היא מצטטת את הראשון, שנשאר `F-076`.
      **אמת:** `grep -rn "F-076" plan/ docs/ scripts/ lib/` — כל התוצאה מצביעה על
      ממצא שורה 97, או יושבת ב-`plan/archive/`.
- [ ] **3.5 · ירוק.** `npx vitest run scripts/plan-hygiene.test.ts`
- [ ] **3.6 · `npm run measure:plan`** (הרגיסטר השתנה) ואז **השער**.
- [ ] **3.7 · קומיט.** `loop(DEV): C-XXXX T-128 — F-076 הכפול הופרד ל-F-085 + אסרציית ייחוד`

**בדיקה עצמית:** האם החזרת המזהה ל-`F-076` מפילה את הבדיקה עם `F-076×2`? הרץ את
המוטציה ותעד. ובנוסף: `findings: 86 rows` נשאר 86 — ⛔ שורה לא נעלמה ולא נוספה.

---

## Task 4 — T-131 · שלוש שגיאות הקונסולה על `/dev/tabs/cards` — **מדידה, ⛔ לא השתקה**

**הקובץ:** `scripts/verify-mobile.mjs` (+ `app/dev/tabs/cards` **רק אם** המדידה תורה)

⛔ **הכלל שגובר על כל צעד כאן:** אין להוסיף רשומה ל-`EXPECTED_CONSOLE` לפני שהתשובה
נמדדה. התיעוד בקובץ עצמו (שורות 213-215) כותב זאת: «An entry is only correct when the
harness itself is the reason the request cannot succeed.»

⚠️ **מה שנמדד בטיק התכנון, ומצמצם את המשימה:** החתימה `request failed: /study?_rsc=…`
מגיעה מהמאזין בשורה **473**, שרושם **רק את הכתובת** ו⛔ **זורק את `r.failure()`**.
במקביל, כל רשומות `EXPECTED_CONSOLE` בנויות בצורת `status of NNN` ⇒ שורת
`request failed:` ⛔ **אינה יכולה להתאים לאף רשומה קיימת**, בכל מצב. ⇒ **ההכרעה
חסומה על חוסר מידע שההרנס עצמו מוחק.** לכן הצעד הראשון הוא לשחזר את המידע.

- [ ] **4.1 · שחזר את סיבת הכשל.** `scripts/verify-mobile.mjs:473` → הגרסה בבלוק
      `Interfaces` למעלה (מוסיף `— ${r.failure()?.errorText}`). ⛔ זהו שינוי **מדידה**,
      ⛔ ולא תיקון ו⛔ לא השתקה.
- [ ] **4.2 · הרץ ומדוד.** `npm run check:mobile 2>&1 | tee /tmp/mobile.txt`
      חלץ את שלוש השורות: `grep -n "request failed" /tmp/mobile.txt`
      **תעד את `errorText` המדויק בדיווח.** ⛔ בלי המחרוזת הזאת אין הכרעה.
- [ ] **4.3 · הכרע לפי מה שנמדד, ⛔ ולא לפי מה שנוח.** שני ענפים, ו**רק** שניים:

      **ענף ⓐ — `net::ERR_ABORTED` (או כשל ברמת הרשת).** ⇒ **פיקסצ׳ר.** זהו פריפץ׳
      RSC ש-`<Link href="/study">` יורה, וש-Chromium מבטל כשהעמוד/הניווט מתחלף —
      ⛔ אין כאן תשובת שרת בכלל ולכן ⛔ אין פגם מוצר למדוד. התיקון הוא בהרנס:
      המאזין ⛔ **אינו רושם בקשה שבוטלה**, כי בקשה שבוטלה ⛔ אינה כשל.
      ```js
      page.on('requestfailed', (r) => {
        const why = r.failure()?.errorText ?? 'unknown';
        // ⛔ ביטול ⛔ אינו כשל: פריפץ׳ RSC ש-Chromium מבטל בניווט מייצר ERR_ABORTED
        // בלי שום תשובת שרת. רישום שלו כשגיאת קונסולה מודד את מנוע הפריפץ׳ של Next,
        // ⛔ לא את המסך. כל שאר ה-errorText ממשיכים להיכשל בדיוק כמו קודם.
        if (why === 'net::ERR_ABORTED') return;
        consoleErrors.push(`request failed: ${r.url()} — ${why}`);
      });
      ```
      ⛔ **הסינון הוא על `errorText` בלבד ⛔ ולא על הכתובת** — סינון לפי `/study`
      היה משתיק גם כשל אמיתי באותו נתיב.

      **ענף ⓑ — כל `errorText` אחר, או קוד סטטוס אמיתי.** ⇒ **פגם מוצר.**
      ⛔ אל תיגע ב-`EXPECTED_CONSOLE`. פתח ממצא 🟠 ב-`plan/60-findings.md` עם
      ה-`errorText` המדויק ועם שלוש הכתובות, קבע NEXT_AGENT=CRITIC, ⛔ **וסיים את
      המשימה כאן** — תיקון נתיב `/study` הוא משימת מוצר, ⛔ לא P2 של שער.

- [ ] **4.4 · אמת שהשער עדיין תופס.** מוטציה חובה: הוסף ל-`app/dev/tabs/cards/page.tsx`
      שורת `<img src="/does-not-exist.png" alt="" />` והרץ `npm run check:mobile`.
      **חייב להיכשל** עם `clean console`. הסר את המוטציה והרץ שוב.
      ⛔ בלי המוטציה הזאת אין ראיה שלא השתקת את הבדיקה כולה.
- [ ] **4.5 · המספר.** `npm run check:mobile` — תעד **בדיוק** כמה בדיקות וכמה כשלים,
      מול הבסיס שנמדד ב-C-0218: **888 בדיקות · 3 כשלים.**
      בענף ⓐ הצפי הוא **0 כשלים**. בענף ⓑ הצפי הוא **3** ⛔ והוא בסדר.
- [ ] **4.6 · השער** (`typecheck && check:core && test && build`) ואז `npm run measure:plan`
      אם נגעת ב-`plan/`.
- [ ] **4.7 · קומיט.** `loop(DEV): C-XXXX T-131 — סיבת כשל הפריפץ׳ נמדדה, <ⓐ/ⓑ>`

**בדיקה עצמית:** האם `EXPECTED_CONSOLE` נותר ללא שינוי? (בשני הענפים — כן.)
האם המוטציה של 4.4 הצליחה להאדים? האם `errorText` המדויק נכתב בדיווח?

---

## סגירה — אחרי ארבע המשימות

- [ ] `plan/50-tasks.md` — ארבע השורות ל-🟣, עם המספרים שנמדדו בתא הסטטוס.
- [ ] `plan/30-architecture.md` — רשומה אחת: מפצל מודע-code-span סוגר את מחלקת
      F-059/F-062ⓒ/F-063; מחלקה ⓐ (11 שורות, 9 עמודות) נשארת פתוחה **בכוונה**
      כהכרעת סכמה של ה-Critic.
- [ ] `plan/60-findings.md` — `F-082ⓑ` נסגר או מוחלף בממצא ⓑ החדש, לפי ההכרעה ב-4.3.
- [ ] `plan/00-control.md` — `CYCLE_ID` (`git pull` ואז מקסימום+1) · `ACTIVE_TASK_ID: ""`
      · `NEXT_AGENT: CRITIC` · שחרור `LOCK` · `MILESTONE_TICKS` +1 · שורת יומן עד 2 שורות.
- [ ] ⚠️ **`npm run measure:plan` שוב, כפעולה האחרונה לפני הקומיט האחרון** — עדכוני
      הסטטוס למעלה שינו את `plan/50-tasks.md`, ולכן `docs/plan-tables.md` המחויב
      התיישן ברגע זה בדיוק. זו מחלקת F-059/F-061/F-062/F-063, והיא הפילה את `dev`
      לאדום ב-C-0218. ⛔ אל תדלג.
- [ ] `git push origin dev` · ⛔ **בלי `[skip ci]`** · ⛔ **אפס נגיעה ב-`main`**.
