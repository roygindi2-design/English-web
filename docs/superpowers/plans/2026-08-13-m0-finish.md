# M0 Finish — הבריאות נוגעת בטבלה, ושני מצבי מסך הלימוד

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:executing-plans`. כל צעד הוא `- [ ]` בן 2–5 דקות.
> ⛔ אין `[skip ci]`. דוחפים ל-`dev` בלבד. אימות לפני כל טענת סיום:
> `npm run typecheck && npm run check:core && npm test && npm run build`.

**Loop tasks covered:** **T-053** (משימות 1–3) · **T-054** (משימה 4) · **T-055** (משימה 5).
**נכתב:** C-0091, 2026-08-13T15:0xZ · Dev · מצב 📝 תכנון (⛔ אפס שינוי קוד בטיק הכתיבה).

**Goal:** לסגור את שלוש המשימות שנותרו ב-M0 שאינן ממתינות לידיים אנושיות. השתיים
האחרונות ב-M0 (T-019 · T-046) חסומות ברוי ואינן חלק מהתוכנית הזו.

---

## מה נמדד, ולא הונח

הורץ בריפו הזה ב-2026-08-13 (`cat`, `grep -n`, `ls`, `wc -l`):

| עובדה | ערך שנמדד |
|---|---|
| `/api/health` בודק היום | שלושה דגלי ENV בלבד — `app/api/health/route.ts` מעביר שלושה בוליאנים ל-`buildHealthReport` |
| `lib/core/health.ts` | טהור, 3 בדיקות, `HealthInput` בן 3 שדות בוליאניים |
| טסטים קיימים | `lib/core/health.test.ts` (יחידה) · ⛔ **אין** `app/api/health/route.test.ts` |
| דפוס גישה לדאטהבייס במסלול | `createRouteClient(env, cookieStore)` + `readSupabaseEnv()` מ-`lib/supabase/auth.ts` (`app/api/review/route.ts:1-8`) |
| דפוס טסט למסלול | קריאת **מקור** עם `readFileSync` + `withoutComments()` — `app/api/profile/route.test.ts` |
| `app/study/page.tsx` | 37 שורות, סטטי לחלוטין: מרנדר `StudyEmptyState` ותו לא |
| תור חזרות בצד הלקוח | ⛔ **אינו קיים** — אין endpoint של תור ואין `useState` בעמוד |
| `docs/api-contract.md` § `GET /api/health` | מתעד בדיוק שלוש בדיקות (שורות 8–26) |

⚠️ **הממצא שמעצב את משימות 4–5:** `/study` הוא מסך סטטי בלי מצב. "טעינה" ו"סיום"
אינם קיימים בו כמצבים — ולכן משימה 4 ומשימה 5 **אינן** מוסיפות ויזואל למצב קיים,
אלא מכניסות מכונת מצבים מינימלית לעמוד. זה גדול יותר מ"ליטוש" כפי ש-50-tasks מתאר.

---

## Interfaces — חתימות מדויקות

```ts
// lib/core/health.ts — נשאר טהור. אין fetch, אין process.env, אין supabase.
export type DbProbe =
  | { readonly status: 'ok' }
  | { readonly status: 'unreachable' }
  | { readonly status: 'schema_missing' };

export interface HealthInput {
  readonly hasSupabaseUrl: boolean;
  readonly hasSupabaseAnonKey: boolean;
  readonly allowPlaceholderContent: boolean;
  /** null = הבדיקה לא נוסתה כלל (ENV חסר, אין למה להתחבר). */
  readonly db: DbProbe | null;
}
// HealthReport ללא שינוי מבני. מתווספת בדיקה רביעית בשם 'database_schema'.
```

```ts
// app/api/health/route.ts — הצד הלא-טהור. הוא היחיד שרואה שגיאה גולמית.
async function probeDatabase(env: SupabaseEnv): Promise<DbProbe>;
```

**חוזה המיפוי — זו כל המשימה, ולכן הוא נכתב כאן ולא מומצא בזמן הקוד:**

| מה קורה בפועל | `DbProbe` | `ok` | `detail` שנחשף |
|---|---|---|---|
| `select` הצליח (0 שורות תחת RLS זה **הצלחה**) | `ok` | ✅ | `word_progress reachable` |
| שגיאה עם `code === '42P01'` או `PGRST205` | `schema_missing` | ❌ | `word_progress missing — migrations not applied` |
| כל שגיאה אחרת / חריגה / timeout | `unreachable` | ❌ | `database unreachable` |
| `readSupabaseEnv()` החזיר `null` | `null` | ❌ | `not probed — supabase env missing` |

⛔ **הדרישה הקשה של T-053:** מחרוזת השגיאה של Supabase **לעולם אינה** נכנסת ל-JSON.
שלוש המחרוזות בטבלה הן היחידות שיוצאות. הטסט במשימה 3 אוכף את זה על המקור.

---

## משימה 1 — השכבה הטהורה יודעת על הדאטהבייס (T-053, חלק א׳)

**קבצים:** `lib/core/health.ts` · `lib/core/health.test.ts`
⚠️ TDD: הטסטים נכתבים ורצים **ונכשלים** לפני המימוש.

- [ ] הוסף ל-`lib/core/health.test.ts` את הבלוק הבא **כפי שהוא** והרץ `npx vitest run lib/core/health.test.ts` — חייב להיכשל על טיפוס/בדיקה חסרה:

```ts
describe('buildHealthReport — בדיקת הדאטהבייס (T-053)', () => {
  const envOk = { hasSupabaseUrl: true, hasSupabaseAnonKey: true, allowPlaceholderContent: false };

  it('ok:true רק כששלוש בדיקות ה-ENV והדאטהבייס כולן עוברות', () => {
    const r = buildHealthReport({ ...envOk, db: { status: 'ok' } });
    expect(r.ok).toBe(true);
    expect(r.checks).toHaveLength(4);
    expect(r.checks.map((c) => c.name)).toContain('database_schema');
  });

  it('ENV תקין + מיגרציות לא הורצו = ok:false — זהו הכשל ש-T-053 קיימת בשבילו', () => {
    const r = buildHealthReport({ ...envOk, db: { status: 'schema_missing' } });
    expect(r.ok).toBe(false);
    const db = r.checks.find((c) => c.name === 'database_schema');
    expect(db?.ok).toBe(false);
    expect(db?.detail).toBe('word_progress missing — migrations not applied');
  });

  it('דאטהבייס לא מגיב אינו מתחזה לסכמה חסרה', () => {
    const r = buildHealthReport({ ...envOk, db: { status: 'unreachable' } });
    expect(r.checks.find((c) => c.name === 'database_schema')?.detail).toBe('database unreachable');
  });

  it('db:null נספר ככישלון ולא כהצלחה שקטה', () => {
    const r = buildHealthReport({ ...envOk, db: null });
    expect(r.ok).toBe(false);
    expect(r.checks.find((c) => c.name === 'database_schema')?.detail).toBe(
      'not probed — supabase env missing',
    );
  });

  it('אף detail אינו נושא טקסט שגיאה גולמי', () => {
    for (const status of ['ok', 'unreachable', 'schema_missing'] as const) {
      for (const c of buildHealthReport({ ...envOk, db: { status } }).checks) {
        expect(c.detail).not.toMatch(/PGRST|42P01|stack|at Object/i);
      }
    }
  });
});
```

- [ ] הוסף `DbProbe` ואת השדה `db` ל-`HealthInput`, והוסף בדיקה רביעית `database_schema` לפי טבלת המיפוי. `ok` נשאר `checks.every(c => c.ok)`.
- [ ] הרץ `npx vitest run lib/core/health.test.ts` — 5 הבדיקות החדשות ירוקות.
- [ ] הרץ `npm run check:core` — חייב להישאר `OK`. אם נכשל, ייבאת משהו לא טהור לתוך `/lib/core`.

## משימה 2 — המסלול באמת נוגע בטבלה (T-053, חלק ב׳)

**קבצים:** `app/api/health/route.ts`

- [ ] הוסף `probeDatabase(env)`: `createRouteClient` דורש `cookieStore`, ולבדיקת בריאות **אין** משתמש — השתמש ב-`createClient(env.url, env.anonKey, { auth: { persistSession: false } })` מ-`@supabase/supabase-js` (כבר תלות של `@supabase/ssr`). ⛔ אל תייבא `SUPABASE_SERVICE_ROLE_KEY`.
- [ ] השאילתה: `await supabase.from('word_progress').select('user_id', { head: true, count: 'exact' }).limit(1)`. `head:true` אינו מחזיר שורות — הוא בודק שהטבלה קיימת וש-PostgREST עונה, בלי לגעת בנתוני אף לומד.
- [ ] עטוף ב-`try/catch` וב-`AbortSignal.timeout(3000)` דרך אופציית `fetch` של הלקוח, כדי ש-`/api/health` לא ייתקע 30 שניות כשהדאטהבייס שותק. `catch` → `{ status: 'unreachable' }`.
- [ ] מפה `error.code` לפי טבלת המיפוי בלבד. ⛔ אל תעביר `error.message` לשום מקום מלבד `console.error`.
- [ ] `readSupabaseEnv()` החזיר `null` → אל תנסה לחקור, העבר `db: null`.
- [ ] הרץ `npm run typecheck`.

## משימה 3 — החוזה והשומר (T-053, חלק ג׳ — סוגר את המשימה)

**קבצים:** `docs/api-contract.md` · `app/api/health/route.test.ts` (חדש)

- [ ] עדכן את `docs/api-contract.md` § `GET /api/health`: הוסף את השורה הרביעית ל-JSON לדוגמה ופסקה אחת — "ENV תקין אינו מספיק: `ok:true` מחייב שהמיגרציות הורצו". **באותו קומיט של הקוד** (RULES, Dev § 5).
- [ ] צור `app/api/health/route.test.ts` בדפוס של `app/api/profile/route.test.ts` (קריאת מקור + `withoutComments`) עם ארבעת השומרים האלה:

```ts
const ROUTE = readFileSync('app/api/health/route.ts', 'utf8');
const CONTRACT = readFileSync('docs/api-contract.md', 'utf8');
const CODE = withoutComments(ROUTE);

it('נוגע בטבלה אמיתית ולא רק ב-ENV', () => {
  expect(CODE).toMatch(/from\('word_progress'\)/);
});

it('לעולם אינו מחזיר טקסט שגיאה גולמי ללקוח', () => {
  expect(CODE).not.toMatch(/error\.message[\s\S]{0,80}(NextResponse|detail:)/);
});

it('אינו נוגע במפתח service-role', () => {
  expect(CODE).not.toContain('SUPABASE_SERVICE_ROLE_KEY');
});

it('החוזה מתעד את הבדיקה הרביעית שהקוד מייצר', () => {
  expect(CONTRACT).toContain('database_schema');
});
```

- [ ] הרץ את ארבע פקודות האימות. ⛔ אין טענת סיום בלי שהדבקת את הפלט בדיווח.
- [ ] סמן T-053 כ-🟣 ב-`plan/50-tasks.md`. ⛔ Dev אינו מסמן ✅ — זו סמכות ה-Critic.

⚠️ **מה שהמשימה הזו משנה מעבר לקוד:** בדיקת העשן של `RULES § 0.1.1 ד׳` נעשית אמיתית.
עד היום `/api/health` החי החזיר `ok:true` בזמן ש-**0 שורות תוכן** בדאטהבייס. אחרי המשימה
הוא יחזיר `ok:false` עד שרוי יריץ את המיגרציות (`03-for-roy` פריטים 4 · 9 · 12 · 18).
**זה הפיצ׳ר, לא רגרסיה** — אבל ⚠️ הוא ישבור את בדיקת העשן של ה-Critic בקידום הבא,
ולכן משימה 3 חייבת לכלול שורה ב-`plan/60-findings.md` שמזהירה את ה-Critic מראש.

## משימה 4 — שלד טעינה לכרטיסייה (T-054)

⛔ **שער לפני שנכתבת שורה:** ל-`/study` אין היום מצב טעינה כי אין תור. אם בזמן הביצוע
עדיין אין endpoint של תור — **אל תמציא אחד ואל תמציא השהיה מלאכותית.** רשום ב-`50-tasks`
ש-T-054 חסומה בתלות אמיתית ועבור למשימה 5.

**קבצים:** `components/CardSkeleton.tsx` (חדש) · `components/CardSkeleton.test.ts` (חדש) · `app/study/page.tsx`

- [ ] בנה `CardSkeleton` שמשכפל את **המידות** של `components/Flashcard.tsx` (אותו רדיוס, אותו גובה) — שלד שקופץ במידה אחרת גרוע ממסך לבן.
- [ ] אנימציה: 150–300ms, easing אחד, `@media (prefers-reduced-motion: reduce)` מבטל אותה לחלוטין (חוקה § 5). ⛔ בלי hex גולמי — אסימונים בלבד (חוקה § 6).
- [ ] טסט מקור: השלד מכיל `prefers-reduced-motion`, ⛔ אינו מכיל `#` hex, ומשך האנימציה בטווח 150–300ms.
- [ ] ארבע פקודות אימות + סמן 🟣.

## משימה 5 — סיום מחזור אינו "אין כרטיסיות" (T-055)

⛔ **שער UX:** `plan/40-decisions.md` **אינו** מכיל היום תוכנית למסך הסיום. אם בזמן הביצוע
עדיין אין — ⛔ **אל תמציא מסך.** רשום ב-`plan/50-tasks.md` ש-T-055 ממתינה להכרעת PM,
פתח שורה ב-`plan/60-findings.md` נגד ה-PM, ועצור. **זו התוצאה הנכונה, לא כישלון.**

- [ ] קרא `plan/45-product-questions.md` ש׳2 ו-`plan/02-inbox.md` — האם ההכרעה נחתה?
- [ ] אם כן: הפרד ב-`app/study/page.tsx` בין `empty` (לא היה מה ללמוד) ל-`done` (סיימת את המנה) — שתי מחרוזות עברית קיימות, **פעולה אחת** בכל אחד. ⛔ בלי מספרים חדשים, בלי הבטחת תאריך, בלי "כל הכבוד!" מומצא.
- [ ] טסט: שני המצבים קיימים ואינם חולקים את אותה מחרוזת.
- [ ] ארבע פקודות אימות + סמן 🟣.

---

## בדיקה עצמית — הרץ לפני שאתה טוען שהתוכנית בוצעה

- [ ] `npm run typecheck && npm run check:core && npm test && npm run build` — ארבעתן ירוקות, הפלט מודבק בדיווח.
- [ ] `grep -n "database_schema" docs/api-contract.md app/api/health/route.ts lib/core/health.ts` — שלושתם עונים.
- [ ] `grep -rn "SUPABASE_SERVICE_ROLE_KEY" app/api/health/` — ⛔ אפס תוצאות.
- [ ] `git diff --stat` מול הרשימה כאן — קובץ שנגעת בו ואינו ברשימה הוא זליגת היקף.
- [ ] `plan/50-tasks.md` · `plan/00-control.md` עודכנו באותו קומיט.
