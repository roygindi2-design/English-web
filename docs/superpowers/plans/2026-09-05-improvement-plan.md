# תוכנית שיפורים — טיק תכנון PM, 2026-09-05

> **סוג המסמך: תוכנית לאישור רוי, ⛔ לא תוכנית ביצוע.** הסוכן שכתב אותו (Cowork, בתפקיד
> Tech Lead) ⛔ לא נגע בקוד. לאחר אישור, כל סעיף הופך לשורת/שורות `T-xxx` אמיתיות ב-
> `plan/50-tasks.md` (מספור סופי ייקבע ע"י PM ברגע ההוספה, `max+1` על `dev` החי אז) ומבוצע
> כטיקי בנייה רגילים — **⛔ לא כהרצה של המסמך הזה עצמו** דרך `superpowers:executing-plans`.
>
> **נמדד חי, בשיבוט טרי, 2026-09-04T21:50:49Z** (‏`date -u`, לפני כל שינוי של הטיק הזה):
> `HEAD` = `eccaa26a` על `work/current` (‏`C-0429`, DEV). כל מספר במסמך הזה נושא את המקור
> שממנו נמדד; מספר בלי מקור מסומן ⛔ **לא מאומת**.

---

## 0. תמצית למי שקורא רק פסקה אחת

שלושה ממצאים תפעוליים נמדדו: **(א)** התור בין הבנייה לשער עדיין פתוח, ותוסף השער הזול
שנוסף היום (05:00Z/23:00Z) עדיין ⛔ לא נבדק במחזור שלם. **(ב)** ה"פרוסות" לא נעלמו כתקלה —
הן נגמרו: כל חמש זרימות הפיצ'ר (`story`/`nav`/`cards`/`arena`/`studies`) מדדו ⬜=0 בו-זמנית,
ו-`general` (‏D-174, 31/08) עשה בדיוק את מה שתוכנן לו לעשות. הבעיה האמיתית היא ש-PM לא חתך
פרוסת פיצ'ר חדשה בשום טיק מהשבוע האחרון שנמדד. **(ג)** בדיקה 14 נכשלת על "story" כי היא
סופרת *כל* שורה פתוחה בזרימה, ⛔ לא רק שורות שנפתחו תחת מצב שיפור — Roy כבר אישר את התיקון.
בנוסף: מנגנון "כרטיסיות + Swipe" **כבר קיים ועובד בקוד היום**, ב-`components/Flashcard.tsx`,
ללא שום ספריית מחוות חיצונית — וזה מייצר קונפליקט אמיתי מול הבקשה החדשה שדורש הכרעה
של רוי לפני שנפתחת שורת בנייה (סעיף 4).

---

## 1. כשלים — 48 השעות האחרונות

### 1.1 מה נמדד

`./scripts/g log --all --since="48 hours ago" --oneline | wc -l` ⇒ **58 קומיטים**
(‏`2026-09-03T02:45` עד `2026-09-04T21:03`, `git log` מלא בנספח א׳). מתוכם **DEV 43 · QA 8 ·
PM 4 · CONTENT 0 מדוד ישירות בענף הזה · roy-cowork-qa 1 (סגירת קידום `main` הידנית)** —
נספרו לפי שם המחבר בקומיט, `./scripts/g log --all --since="48 hours ago" --format='%an'
| sort | uniq -c`.

`plan/60-findings.md` (‏229 שורות, נמדד `wc -l`) **⛔ לא נפתחה בו אף שורת F-xxx חדשה
ב-48 השעות האחרונות** — כל ה-F-xxx הפתוחות שנסרקו (F-175, F-176, F-180) נפתחו לפני
2026-09-02 או כבר נסגרו/הועברו לארכיון. ⇒ **48 השעות האחרונות לא ייצרו ממצא מוצר חדש**,
אבל כן ייצרו שלוש תקלות *תפעוליות* שנמדדות ישירות מ-`git log` ומ-`plan/00-control.md`,
ומפורטות למטה.

### 1.2 שלוש התקלות שנמדדו, ופתרון מוצע לכל אחת

| # | התקלה, כפי שנמדדה | ראיה | פתרון מוצע |
|---|---|---|---|
| כ-1 | **התנגשות מספור מחזור (Cycle-ID collision).** קומיט `bf4c785` (16:41:27Z) נעל בתור `C-0426`; קומיט הבא באותו רצף (`e94a4ae`, 16:53:22Z) נאלץ לתקן את עצמו: *"fix cycle-id collision — previous commit wrongly called itself C-0426"*. | `./scripts/g log --format='%h %cI %s'` — שתי השורות מוצגות בנספח א׳ | `DEV.md` STEP 7 כבר אומר "New id: `./scripts/g pull` then max+1 **over what is on `dev` right now**" — אבל המדידה כאן הייתה מול `work/current`, ⛔ לא מול `dev`, ושני סוכנים שהריצו את אותה נוסחה בשני clone-ים שונים תוך דקות קיבלו אותו מספר. **T-254** (סעיף 5) מוסיף בדיקת ייחודיות אוטומטית: לפני commit, `grep` על `NEXT_C_ID` הצפוי מול **גם** `dev` **וגם** `work/current` המרוחק, ⛔ לא רק אחד מהם. |
| כ-2 | **`ACTIVE_TASK_ID` מצביע על שורה שכבר נמסרה — קרה פעמיים ב-8 ימים.** נמדד עכשיו: `plan/00-control.md:5` מציין ש-`T-253` (השורה שה-DEV-agent פתח את הטיק הנוכחי מולה) הייתה כבר `✅ C-0428 (QA): על dev` **לפני** שהטיק הנוכחי (`C-0429`) התחיל. `claude/for-roy.md` (בפרויקט) מתעד מקרה זהה קודם (`T-238` תקוע מ-`C-0414`, פונה ידנית ב-`C-0389`/04/09). | `plan/00-control.md:5`, `git log` C-0429 | הכרעת רוי כבר קיימת (סעיף 3, אופציה ⓑ — תור עד 3 מזהים). זו בדיוק אותה תקלה שהתור בא לפתור. |
| כ-3 | **קצב הבנייה מול קצב המיזוג עדיין לא נבדק אחרי השינוי.** `claude/for-roy.md` (בפרויקט, בלוק שני) מדד היום מוקדם יותר: `work/current` גדל **מ-15 ל-21 קומיטים לפני `dev`** תוך חצי יום, ותור הביקורת עלה **מ-11 ל-12**. השער הזול (`trig_01MLjWVqKN5LZgscGWpVcHuP`) עודכן **היום, 19:10:16Z** (‏`updated_at`, נמדד ב-`list_triggers`) — כלומר טרם עבר מחזור מלא. | `claude/for-roy.md` בלוק 2 · `list_triggers` (נמדד בטיק הזה) | ⛔ **לא לגעת שוב בתזמון עכשיו** — ראה סעיף 2.3. יש למדוד את היחס שוב אחרי 48–72 שעות עם השער הזול פעיל, ⛔ לא לפני. |

### 1.3 לולאה חוזרת שנמדדה, ⛔ אך אינה "כשל" במובן הרגיל

`IMPROVE_TARGET` (‏`plan/00-control.md:32`) נושא הערה: *"נוסה ו⛔ לא ניתן להדליק — נמדד. `story`
⇒ בדיקה 14 FAIL (3 ⬜ מראש, תקרה 2)"*. זו **אותה תקלה** שסעיף 3 בשולחן העבודה כבר מכיל
(רוי כבר הכריע אופציה ⓐ) — לא ממצא חדש, אלא אישור שהתקלה עדיין קיימת נכון להיום ומחכה
לתיקון (‏T-255, סעיף 5).

---

## 2. תזמונים (Cron) ו-Lock Collisions

### 2.1 מה רץ בפועל היום — נמדד מ-`list_triggers` ב-2026-09-04T21:46Z

| סוכן | cron (UTC) | מודל | תפקיד |
|---|---|---|---|
| DEV | `30 */2 * * *` (12×/יום, כל שעה זוגית ב-:30) | Sonnet 5 | בנייה, דוחף ל-`work/current` |
| PM | `0 1,9,17 * * *` (3×/יום) | Opus 5 | תכנון, חותך פרוסות |
| QA-מלא | `0 11,19 * * *` (2×/יום) | Sonnet 5 | שער מיזוג, `--ff-only` ל-`dev` |
| QA-שער (זול) | `0 5,23 * * *` (2×/יום, **נוסף/עודכן 2026-09-04T19:10:16Z**) | Haiku 4.5 | "בר-משלוח כן/לא" בלבד, אפס כתיבה |
| CONTENT | `48 */6 * * *` (4×/יום) | Sonnet 5 | בנק תוכן |

### 2.2 מפת המרווחים — נמדדה בטיק הזה, ⛔ לא מוערכת

ציר שעות UTC אחד, כל הטריגרים יחד:

```
00:30 DEV        00:48 CONTENT  (18 דק' רק!)
01:00 PM         (30 דק' אחרי DEV 00:30)
02:30 DEV
04:30 DEV        05:00 QA-שער   (30 דק' אחרי DEV)
06:30 DEV        06:48 CONTENT  (18 דק' רק!)
08:30 DEV        09:00 PM       (30 דק' אחרי DEV)
10:30 DEV        11:00 QA-מלא   (30 דק' אחרי DEV)
12:30 DEV        12:48 CONTENT  (18 דק' רק!)
14:30 DEV
16:30 DEV        17:00 PM       (30 דק' אחרי DEV)
18:30 DEV        18:48 CONTENT  (18 דק' רק!)   19:00 QA-מלא (30 דק' אחרי DEV)
20:30 DEV
22:30 DEV        23:00 QA-שער   (30 דק' אחרי DEV)
```

**הממצא המדויק:** התזמון הקיים **כבר** בנוי סביב חיץ של **30 דקות** בין DEV לכל אחד
מ-PM/QA-מלא/QA-שער — עקבי בשש מתוך שש ההתנגשויות הפוטנציאליות שנבדקו. **החריג היחיד
הוא CONTENT**, שיושב **18 דקות בלבד** אחרי ארבע מתוך שתים-עשרה ריצות DEV (‏00:30/06:30/
12:30/18:30), חצי מהחיץ שכל שאר הלופ מקבל. זו **לא** השערה — זה חישוב ישיר על שני
ביטויי ה-cron שנמדדו הרגע.

**מה זה עולה בפועל:** נעילת DEV (‏`LOCK_HELD_BY` ב-`plan/00-control.md`) נמדדה בטיק הזה
כמחזיקה **24–40 דקות** ב-4 המחזורים האחרונים (‏`C-0429`: 20:39:38→20:57:19 = 18 דק' עד
שחרור הנעילה, 24 דק' עד commit סופי · `C-0428`: 19:12:31→19:49:55 = 37 דק' · `C-0427`:
16:41:27→17:05:24 = 24 דק'). כלומר ב-18 הדקות בין DEV ל-CONTENT יש **סיכוי אמיתי, לא
תיאורטי**, ש-CONTENT יפגוש נעילת DEV חיה ויֵצא ריק (‏`docs/agents/DEV.md` STEP 1 מגדיר
את אותו כלל בכיוון ההפוך — DEV יוצא בשקט כשנעילת CONTENT צעירה מ-90 דקות; לא אומת כאן
מהו כלל ה-wait של CONTENT עצמו כלפי DEV, כי `docs/agents/CONTENT.md` לא נקרא בטיק הזה —
מסומן ⛔ לא מאומת).

### 2.3 המלצה — ⛔ לא לגעת בתזמון עכשיו, לתקן רק את הפער שנמדד

1. ⛔ **אל תשנה שוב את תדירות QA.** ההחלטה הקודמת (העלאת שער זול) בוצעה **היום** ולא
   עברה מחזור מלא — שינוי נוסף לפני שיש נתון הוא כיוונון פרמטרים מהר יותר משאפשר למדוד
   אותם (‏`RULES` § "מספר לא מאומת").
2. ✅ **הזז את CONTENT מ-`:48` ל-`:15`.** `48 */6 * * *` → `15 1-19/6 * * *` (או פשוט
   `15 */6 * * *` אם שעת העיגון לא קריטית) — נותן ל-CONTENT **45 דקות** אחרי DEV במקום
   18, עקבי עם שאר הלופ. זהו שינוי תזמון בעל תלות ישירה בממצא 2.2, ⛔ לא ניחוש.
3. ✅ **מדוד שוב בעוד 48–72 שעות** את יחס `work/current` מול `dev` (‏`./scripts/g
   rev-list --count origin/dev..origin/work/current`) ואת תור ה-🟣 (‏`docs/plan-open.md
   § 🟣`), **אחרי** שהשער הזול עבר מחזור מלא — ורק אז להחליט אם QA-מלא צריך תדירות
   שלישית.

⚠️ **שני השינויים ב-§ 0.17ז / RULES § 7 (סעיף 2.7 בפרויקט) הם החלטת רוי בלבד**, ולכן שני
הסעיפים למעלה הם **המלצה**, ⛔ לא שינוי מבוצע. שורת המשימה בסעיף 5 (T-256) מייצגת רק
את **הביצוע הטכני** (עריכת ה-trigger + עדכון `claude/LOOP-ARCHITECTURE.md`) לאחר אישור.

---

## 3. היעלמות ה"פרוסות" (Slices)

### 3.1 המדידה — לא תקלה, תוצאה של רצף שהושלם

`plan/00-control.md:14-20` (‏`WORKSTREAM_TICKS`, בלוק ההערות) מראה **חמש** זרימות פיצ'ר
וכולן באותו מצב: `story` ⬜=0 חתומה · `nav` ⬜=0 חתומה · `cards` ⬜=0 (לא חתומה, שני
ממצאים ב-`61-deferred`) · `arena` ⬜=0 (לא חתומה, חסומה חיצונית) · `studies` ⬜=0
(‏`T-144` נסגרה, `T-246` בביקורת, `T-247` חסום עד `dev`). `ACTIVE_WORKSTREAM: general`
(‏`plan/00-control.md:30`) עומד כך מאז **`C-0389` (‏31/08)**, ⇒ **חמישה ימים** על-פני
שני מחזורי C ליום בממוצע.

`plan/RULES.md § 0.26` (נקרא בשלמותו בטיק הזה) מסביר בדיוק למה: `general` **הוא** תור
פרוסות לגיטימי מ-31/08 (‏`D-174`), נפתח כי `loop` נשא 10 שורות ⬜ ו-`base` נשא 15 — **25
שורות** ש-DEV מעולם לא הגיע אליהן. `DEV.md` STEP 2 מיישם את זה נכון: כש-`ACTIVE_WORKSTREAM
= general`, קבוצת הכשירות היא `general ∪ loop ∪ base` — **בדיוק** מה ש-`C-0429` עשה
(‏`T-226`, תיוג `amirnet` ב-`WORKSTREAMS`, שורת `loop`·`תשתית`).

**⇒ המסקנה המדויקת: DEV לא "נתקע" על משימות כלליות — הוא עושה בדיוק את מה שהתור מורה
לו, כי אין שום שורת ⬜ בשום זרימת פיצ'ר.** התקלה האמיתית היא **במעלה הזרם**: PM לא פתח
פרוסת פיצ'ר חדשה. `RULES § 0.6` (נקרא) מגדיר תמהיל יעד **4/1/1 לכל 6 טיקי PM** — 4 פרוסות
חדשות · 1 שיפור · 1 הכרעה/היגיינה — ומעיד על עצמו: **"⚠️ ⛔ אינו בלם ו⛔ אינו ציון —
`loop:health` מדפיס את המספר, ⛔ וזהו"** (‏`plan/RULES.md:362`). כלומר: **אין שום מנגנון
שאוכף את התמהיל** — הוא דיווח רך בלבד.

בדיקת קומיטי ה-PM האחרונים (‏`git log`, נספח א׳) — `C-0422` (D-186/T-253, פרוסת נוחות
קטנה בזירה) · `C-0418` (D-185, החלטת תזמון) · `C-0412` (D-183/D-184, הכרעות) · `C-0407`
(D-182 + T-252, פרוסת עולם) — **מציגה הכרעות והתאמות, ⛔ לא פתיחת פרוסת פיצ'ר חדשה
מלאה** באף אחד מהם. זה תואם את המדידה שהזרימות הפעילות ריקות: **אין מנוע שמייצר שורות
⬜ חדשות בקצב שבו DEV צורך אותן.**

### 3.2 מה גרם לשינוי, בפועל

⛔ **זה לא שינוי בהנחיות שקרה מתחת לרדאר** — `RULES § 0.26` מתועד, מודע לעצמו, ומצוטט
בגלוי מ-`DEV.md`. השינוי האמיתי הוא **תזמוני**: PM רץ 3×/יום מול DEV שרץ 12×/יום (יחס
1:4), וכל טיק PM שבוחר "💤 שקט" או "🔬 הכרעה" (‏`RULES § 0.6`, ארבעת המצבים) **לא** מוסיף
אף שורת ⬜ לזרימת פיצ'ר. עם חמש זרימות ריקות בו-זמנית, כל טיק שאינו "פתיחת פרוסה" משאיר
את DEV **ללא ברירה** מלבד `general`.

### 3.3 עדכון מוצע ל-`docs/agents/PM.md` / `plan/RULES.md`

**אין המלצה לבטל את `general`** — היא פתרה תקלה אמיתית ומדודה (‏`D-174`). ההמלצה היא
לחזק את האכיפה **סביב** התמהיל 4/1/1 בלי להפוך אותו לבלם קשיח (כפי ש-`RULES § 0.6`
מזהיר במפורש שלא לעשות):

1. **T-257** — `loop:health` מקבל בדיקה חדשה (**16**, לא לגעת במספור 1–15 הקיים) שמדווחת
   *(⛔ ולא נכשלת — בדיוק כמו `workTypeMix` הקיימת, `scripts/loop-health.mjs:675`)*:
   "כמה טיקי PM רצופים מאז שנפתחה פרוסת פיצ'ר אחרונה". דיווח רך תואם את התקדים הקיים.
2. **T-258** — `docs/agents/PM.md`: כשכל חמש זרימות הפיצ'ר מדדות ⬜=0 בו-זמנית (בדיוק
   המצב הנוכחי), מצב "📐 תכנון" (‏`RULES § 0.6`) הופך **לחובה** בטיק הבא של PM, ⛔ לא
   רק כשלמשימה הבאה אין תוכנית UX. זו תוספת תנאי-הפעלה אחד למצב שכבר קיים, ⛔ לא מצב
   חדש.

---

## 4. ⚠️ קונפליקט שנמדד: Swipe על "כרטיסיות" מול אינווריאנט קיים ב-`36 § 5`

### 4.1 מה קיים כבר בקוד — נמדד, ⛔ לא הונח

**מנגנון swipe-to-grade מלא כבר רץ היום ב-`components/Flashcard.tsx` (376 שורות, נמדד
`wc -l`), ואפס תלות חיצונית.** ההיגיון הטהור יושב ב-`lib/core/swipeGrade.ts`
(‏`resolveSwipe`, `dragOffset`) עם שלושה ספים מדודים:

```
SWIPE_EDGE_PX          = 20    // אזור קצה שהדפדפן חוטף (רצועת back-swipe של iOS)
SWIPE_MIN_DISTANCE_PX  = 64
SWIPE_MAX_ANGLE_DEG    = 30
SWIPE_FEEDBACK_MAX_MS  = 200   // השתקעות בשחרור בלבד — הגרירה עצמה 1:1, ⛔ 0ms
```

הרכיב עוקב אחרי `onPointerDown/Move/Up/Cancel` ישירות, גורר את הכרטיס `1:1` עם האצבע
(‏D-090ⓑ), מכבד `prefers-reduced-motion` (מאפס תנועה **ולא** ממעיט בה), ומדרג `dx > 0 ⇒
'good'` בפיזי — עם תיעוד מפורש (‏`swipeGrade.ts:11-13`) ש-**F-102** כבר נפתח ונסגר על
הפער בין "ימין פיזי" לכפתור "ידעתי" שיושב מימין ב-RTL. **אין `framer-motion`, אין
`@use-gesture`, ואין שום ספריית מחוות ב-`package.json`** (נמדד: `grep -i
"framer\|gesture\|swipe\|embla" package.json` ⇒ אפס תוצאות; שתי התוכניות הקיימות
`docs/superpowers/plans/2026-08-20-flashcard-interaction.md` ו-
`2026-08-21-flashcard-swipe-and-decay.md` בונות את אותה תבנית: **"המתמטיקה טהורה, הרכיב
רק מצייר"**).

### 4.2 הקונפליקט — "כרטיסיות" ו-Flashcard הם שני מסכים שונים

לשונית **"כרטיסיות"** (‏`app/(tabs)/cards/page.tsx`) מרנדרת **`LevelMapScreen`**, ⛔ **לא**
`Flashcard` — מאז **T-081** היא "מסך הבית" של `36 § 5` (מפת רמות: כרטיס רמה · פס התקדמות ·
שלושה מונים · חפיסות · הערה קבועה), ⛔ **לא** בורר חפיסות וגם לא מסך הדירוג. הקובץ נושא
קבוע מפורש (‏`components/LevelMapScreen.tsx:36`, מצוטט מילה-במילה):

> `INVARIANT_NOTE_HE = 'הסימון של מילים מתבצע בכרטיסיות בלבד'`

וההערה מעליו בקוד: **"אין דרך לסמן ידעתי/לא ידעתי מחוץ לכרטיסייה. המסך מציג מצב ופותח
חפיסות — הוא ⛔ אינו עורך מצב."** זהו אינווריאנט **מוצהר** של `36 § 5` (מסמך העוגן),
⛔ לא ניחוש של DEV.

⇒ **הבקשה "לאפשר במסך הכרטיסיות סינון בעזרת החלקה ימינה/שמאלה (ידעתי/לא ידעתי)" סותרת,
כפי שהיא כתובה, אינווריאנט חתום בקוד ובמסמך העוגן.** שתי קריאות אפשריות, ושתיהן דורשות
הכרעת רוי **לפני** שנפתחת שורת בנייה — `35-design-constitution.md` וה-anchor הם מעל
כל סקיל וכל טעימת PM:

| קריאה | מה זה אומר בפועל | עלות ארכיטקטונית |
|---|---|---|
| **ⓐ הכוונה היא ל-`Flashcard` (חפיסת "מנת היום")** — "כרטיסיות" בפי רוי = חוויית הלימוד עצמה, ⛔ לא מסך `LevelMapScreen` הטכני | **המנגנון קיים היום.** אין שורת בנייה כלל, אולי ליטוש קטן (למשל אנימציית ה-swipe עצמה, ראה 4.3) | אפס — האינווריאנט לא נוגע |
| **ⓑ הכוונה היא ל-`LevelMapScreen` ממש** — רוי רוצה לדרג מילים ישירות מרשימת/מפת הרמה, בלי לפתוח כרטיס | דורש **ביטול או תיקון מפורש** של האינווריאנט ב-`36 § 5` וב-`components/LevelMapScreen.tsx` — שינוי מסמך עוגן, לא רק קוד | גבוה — נוגע בחוזה שנבדק ב-`Flashcard.test.ts` (שומר מקור) ובהערת התיעוד של המסך |

### 4.3 המלצת ארכיטקטורה — בהנחת קריאה ⓐ, ואם נדרש הרחבה

אם רוי מאשר קריאה ⓐ (הסביר יותר: אין תיעוד לשום כוונה לשבור את `36 § 5`), וההרחבה
הנדרשת היא שיפור/הרחבה של ה-swipe הקיים (ולא רק אישור שהוא כבר שם):

**⛔ לא להכניס Framer Motion (או כל ספריית מחוות אחרת) כברירת מחדל.** שלוש סיבות
מדודות: **(1)** `RULES § "🧰 CONDITIONAL — CHOOSING AN IMPLEMENTATION"` (‏`DEV.md:240`)
מחייב `pick-ui-library` **רק** כשתלות UI חסרה בפועל — וכאן המנגנון כבר קיים ועובר בדיקות.
**(2)** שני התקדימים הכתובים (‏`swipeGrade.ts`, `arenaGesture.ts` — קיים גם הוא, נמדד
`find`) בונים **מתמטיקה טהורה בבדיקת יחידה + רכיב מצייר**, בדיוק המבנה שהתוכנית
`2026-08-21-flashcard-swipe-and-decay.md` מנמקת מפורשות ("⛔ style={{}} בקוד מוצר — נמדד
אפס שורות"). ספריית אנימציה חיצונית מכניסה `style={{}}`/inline transforms משלה ומפרה
את התקדים בלי שהיא צריכה. **(3)** `docs/skills-registry.md` שורה 47 מגביל את `animate`
לזרימת `arena` **בלבד** ("שורה שנושאת גם `arena` וגם `שכבה ב׳`") — הכנסת ספריית אנימציה
לשורת `cards` תדרוש חריגה מהשער שגם היא הכרעת PM/רוי, ⛔ לא ברירת מחדל של DEV.

**המלצה קונקרטית:** הרחבת `lib/core/swipeGrade.ts` (או מודול אח באותה תבנית) לפי
הצורך הספציפי שרוי יגדיר — למשל דירוג ישיר מתוך תצוגת רשימה/חפיסה בלי מעבר מסך — **תוך
שמירה על אותם שלושה ספים ואותה טהרה**, ⛔ ספריה חדשה.

---

## 5. שורות משימה מוצעות — ל-`plan/50-tasks.md`, בכפוף לאישור סעיף 6

מספור זמני (`T-254`+), על בסיס `T-253` המדוד כ-`max` כרגע. PM יקצה מספור סופי בזמן
ההוספה בפועל (‏`DEV.md` STEP 7: `max+1` על `dev` החי אז).

| id | אבן דרך | המשימה | סוכן | תלות |
|---|---|---|---|---|
| **T-254** | `M0 · loop · תשתית` | **מניעת התנגשות מספור מחזור (Cycle-ID).** לפני `commit`, `NEXT_C_ID` נבדק מול `./scripts/g ls-remote` על **גם** `dev` **וגם** `work/current` המרוחקים (⛔ לא רק `pull` מקומי) — ראה כ-1, סעיף 1.2. תוספת בדיקה ל-`scripts/loop-health.mjs`, ⛔ לא בדיקה חדשה במספור 1–16, אלא שיפור לוגיקת ה-STEP הקיים. | DEV | סעיף 1.2 |
| **T-255** | `M0 · loop · תשתית` | **בדיקה 14 ב-`loop:health` תספור רק שורות שנפתחו תחת מצב שיפור (🩺), ⛔ כל שורה פתוחה בזרימת היעד.** אופציה ⓐ שרוי אישר. יישום: כל שורת `נוחות` שנפתחת תחת `IMPROVE_TARGET` מסומנת בתא `אבן דרך` בתג נוסף (למשל `שיפור`, לצד `M<n> · <flow> · נוחות`, באותו דפוס בדיוק כמו `שכבה ב׳` — ⛔ נכתב ע"י PM/רוי בלבד, ⛔ ע"י DEV) — ו-`scripts/loop-health.mjs:661` (`there.open`) נספר מחדש כ-`there.open` **המסונן** לתג הזה בלבד. שינוי אחד ב-`plan/RULES.md § IMPROVE` (הוספת סעיף 6 לחמש הגדרות הקיימות) + שינוי ב-`scripts/loop-health.mjs` + `scripts/loop-health.test.ts`. | PM (התיוג) + DEV (הקוד) | סעיף 1.3 · `plan/00-control.md:32` |
| **T-256** | `M0 · loop · תשתית` | **הזזת cron של CONTENT מ-`48 */6` ל-`15 */6` (או `15 1-19/6`).** עדכון ה-trigger (`trig_01HbGgT2AYx2ti9Com9xUupK`) + שורת יומן שינויים ב-`claude/LOOP-ARCHITECTURE.md` § 1/§ 10, **רק אחרי אישור רוי** (סעיף 6, שאלה 1) — לפי מדיניות הפרויקט § 7: שינוי תזמון רק בבקשה מפורשת. | PM (בקשת האישור) → מי שמפעיל את השינוי בפועל (סוכן עם גישת `update_trigger`) | סעיף 2.3 |
| **T-257** | `M0 · loop · תשתית` | **`loop:health` בדיקה 16 (חדשה, דיווח רך — לא נכשלת): כמה טיקי PM רצופים מאז פתיחת פרוסת פיצ'ר אחרונה.** תבנית זהה ל-`workTypeMix` הקיימת (`scripts/loop-health.mjs:675`, מדפיסה ולא נכשלת). | DEV | סעיף 3.3 |
| **T-258** | `M0 · loop · תשתית` | **`docs/agents/PM.md`: מצב "📐 תכנון" הופך לחובה כש-5/5 זרימות הפיצ'ר מדדות ⬜=0 בו-זמנית** (בדיוק המצב הנוכחי, נמדד סעיף 3.1). תוספת תנאי-הפעלה אחד לטבלת ארבעת המצבים הקיימת (`RULES § 0.6`), ⛔ לא מצב חדש. | PM | סעיף 3.3 |
| **T-259** | `M2 · cards · נוחות` | **Swipe ב-"כרטיסיות" — היקף לפי הכרעת רוי בסעיף 6 שאלה 3.** אם קריאה ⓐ (`Flashcard`): שורת ליטוש/הרחבה קטנה על `lib/core/swipeGrade.ts` הקיים, ⛔ ספריה חדשה. אם קריאה ⓑ (`LevelMapScreen`): **שורה חסומה** עד תיקון מפורש של האינווריאנט ב-`36 § 5` — לא נפתחת בלי זה. | DEV | סעיף 4 — ⛔ **חסומה עד הכרעת רוי** |
| **T-260** | `M0 · loop · תשתית` | **`ACTIVE_TASK_ID` → תור מנוהל של עד 3 מזהים.** `plan/00-control.md`: השדה עובר משדה בודד לרשימה (`ACTIVE_TASK_ID: [T-xxx, T-yyy]`, תקרה 3). `plan/RULES.md § 0.26`-בסמוך (או סעיף חדש תואם-מספור): DEV מנקז לפי סדר; PM/QA כותבים, DEV לעולם לא. עדכון `scripts/loop-health.mjs` + `scripts/agent-prompts.test.ts` (כיום בודקים שדה יחיד) + `docs/agents/DEV.md` STEP 2 (הפילטר הנוכחי, `plan/RULES.md:154, 164-165`). אופציה ⓑ שרוי אישר. | PM (עיצוב השדה) + DEV (המימוש) | סעיף 1.2 (כ-2) |

⚠️ **T-259 היא היחידה שמסומנת חסומה מראש** — כל היתר יכולות להיפתח ברגע שהמסמך הזה
מאושר, כי אין בהן קונפליקט לא-פתור מול מסמך עוגן.

---

## 6. שאלות פתוחות — דורשות הכרעת רוי לפני יציאה לדרך

1. **תזמון CONTENT (`T-256`):** לאשר הזזה מ-`:48` ל-`:15` (חיץ 45 דק' מ-DEV במקום 18)?
   ⛔ שינוי תזמוני נוסף — ⛔ מעלה/מוריד תדירות QA — ⛔ מומלץ עכשיו (סעיף 2.3).
2. **מדידה חוזרת של יחס בנייה/מיזוג:** לאשר מדידה חוזרת בעוד 48–72 שעות (אחרי שהשער הזול
   עבר מחזור מלא), ⛔ לפני, כתנאי לכל שינוי תדירות נוסף ב-QA-מלא?
3. **הקריאה הנכונה ל"Swipe בכרטיסיות" (`T-259`, סעיף 4.2):**
   ⓐ הכוונה היא לחפיסת הלימוד (`Flashcard` — **כבר קיים ועובד**, נדרש רק ליטוש/הרחבה)?
   ⓑ הכוונה היא ל-`LevelMapScreen` ממש (מחייב תיקון אינווריאנט ב-`36 § 5`, שינוי מסמך
   עוגן)? **בלי תשובה — T-259 נשארת חסומה.**
4. **T-255 (בדיקה 14):** לאשר את תבנית התיוג המוצעת (תג `שיפור` בתא `אבן דרך`, בדיוק
   כמו `שכבה ב׳` הקיים) — או שיש תבנית אחרת מועדפת?

---

## נספח א׳ — `git log` מלא, 48 השעות האחרונות

```
$ ./scripts/g log --all --since="48 hours ago" --format='%h %cI %an %s'
(58 קומיטים · נמדד 2026-09-04T21:46:42Z · טווח 2026-09-03T02:45 עד 2026-09-04T21:03)
```
הרשימה המלאה זמינה בטרמינל הטיק הזה; שלוש השורות הרלוונטיות ל-כ-1 (סעיף 1.2) הן:

```
bf4c785 2026-09-04T16:41:27+00:00 dev-agent loop(DEV): C-0426 lock before T-229 (loop · תשתית)
e94a4ae 2026-09-04T16:53:22+00:00 dev-agent loop(DEV): C-0427 fix cycle-id collision — previous commit wrongly called itself C-0426
c1c12ec 2026-09-04T17:05:24+00:00 dev-agent loop(DEV): C-0427 T-229 — cancelled-in-prose task rows with a stale status cell, flagged not failed
```
